# Plan fix — round 1

Source review: `20260905-115017-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 6 NIT).

## Fixed

- `[BLOCKING:scope]` "Card 28 cannot reach the identifiers it must call" — added `pack.go`,
  `prompt.go`, `config.go` and `gates.go` to card 28's `Context:`, so the extractor, the card loader,
  the four-parameter render function, the ladder struct and the blinding check are all reachable.
- `[BLOCKING:scope]` "No check ties the cards' `Uses:` list to `pack_targets`" — added
  `TestPreMatrix_KickstartUsesListMatchesPackTargets` to card 28, asserting each card's `Uses:`
  entries equal the ladder file's glyph list element for element in order, and stating why it is a
  separate assertion from the cards-agree-with-each-other one. Card 29's substitution procedure now
  names both checks as the backstop against a half-applied substitution.
- `[NIT:consistency]` "Card 12's pairing assertion cannot hold" — the fixture is now three tool-less
  configs under one letter (one control, two rungs), and the assertion is on the comparison set's
  membership — the distinct rung ids present, each naming the control — rather than on a row count,
  which is a function of the metric list and not of the pairing.
- `[NIT:consistency]` "`RenderPrompt` call-site count is wrong three ways" — dropped both counts and
  enumerated the callers: `run.go`, three in `prompt_test.go`, and one each in `gates_test.go`,
  `prematrix_test.go` and `live_test.go`.
- `[NIT:consistency]` "`go build ./...` is credited with catching test-file breaks" — reattributed
  the compile-break net to each batch's own `go test` in both batch 2's and batch 3's `## Batch
  Tests`, and amended the overview decision to say `go build ./...` covers non-test code only.
- `[NIT:consistency]` "Card 5's \"exactly the eleven\" does not match its second grep" — reworded the
  confirmation step: the first grep is the enumeration the batch is built on, the second is read for
  an inline spelling the first cannot see, and only branches are classified.
- `[NIT:decision]` "Batch 5's `verify:` departs from its own Shared Decision" — amended the decision
  to admit a scoped `go build` prefix for the batch that touches the command-line layer, with the
  reason.
- `[NIT:scope]` "Card 17 names a helper from a file it may not read" — added `provenance.go` to card
  17's `Context:` and named `sha256Hex` explicitly.

## Pushed Back

_None. Every finding verified accurate against the code, and no fix breaks existing functionality,
conflicts with a documented decision, or destabilises work outside this task._
