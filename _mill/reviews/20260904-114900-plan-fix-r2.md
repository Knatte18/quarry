# Plan fix — round 2

Source review: `_mill/reviews/20260904-114637-plan-review-r2.md` (holistic, REQUEST_CHANGES,
2 BLOCKING / 4 NIT, 2 of them demoted from BLOCKING).

## Fixed

- r2 `[BLOCKING:scope]` "Card 6 quotes sources it is not given" — verified: the summary's `Invalid`
  is a cell-id list with no per-repetition reason, and the run-log/scratch facts had no fixed paths
  at all. Cards 3 and 4 now name `.scratch/ladder-live-test.log` and `.scratch/ladder-toc-run.log`
  (appended, never truncated, so all three invocations share one file); card 5 now names
  `.scratch/hand-verify.md`. All three, plus the two cells' raw directories holding the
  per-repetition state files, are in card 6's `Context:`, and card 6 now carries an explicit
  source-for-each-fact paragraph closing with "do not report a fact none of them states".
- r2 `[BLOCKING:design]` "Card 5's hand check compares a repetition against a median" — verified
  against `CellRecord.Metrics` holding `MetricStats` (median/min/max/n) over every counted
  repetition. Card 5 now splits the two comparands: the repetition's own recorded usage is the only
  value-for-value test; the summary side is range containment only, with the reason the median test
  would manufacture a false finding stated in the card.
- r2 `[NIT:decision]` "The `-r2` restart path strands cards 5–8" — the `results-root-name` decision
  now carries one substitution rule covering all three renames (date, `-r2`, `-r3`), stated to apply
  to batch 3's `Context:`/`Creates:` paths as written and not only to prose, plus the two
  consequences: card 4 copies `probe.md` into the fresh root so it satisfies `committed-artifacts`
  on its own, and `ABANDONED.md` is the one file that stays behind. Card 4 carries the copy
  instruction.
- r2 `[NIT:consistency]` "Card 8 puts prior and current cost figures in one HANDOFF row" — card 8
  step (1) now requires the prior row to be left untouched and a **second, separately-labelled** row
  added naming this results root and its reps, and forbids merging this run's figures into the prior
  row, citing the discussion's own rejection of a merged table with a version column.
- r2 `[NIT:consistency]` "Card 1 miscounts the §9a properties" — reworded to "the `system`-record
  half of the first property", matching the discussion's grouping instead of inventing a third
  property the card's own list does not have.
- r2 `[NIT:consistency]` "HANDOFF §4's wave-4 paragraph is left contradicting card 8" — card 8 step
  (2) now retires the "Wave 4 is spawned, workers not yet started" opening and the worker-start
  instructions that follow it, in the same edit and for the same reason step (0) exists.

## Pushed Back

_None. Every round-2 finding was accurate and fixed as suggested._
