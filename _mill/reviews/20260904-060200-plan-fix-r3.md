# Plan fix — round 3

Source review: `20260904-055033-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 2 NIT).

## Fixed

- `[BLOCKING:scope] Card 17 has no function fixture in Context` — verified: the card's `Context:` held
  `decls.go` (no func), `inits.go` (three `init` declarations, used for the separate multipart row) and
  the two tags files, whose `Dup` matches twice and answers `ambiguous` before the kind gate is
  reached. No single-match package-level function was reachable. Added
  `internal/engine/testdata/glyphs/iface.go` to the `Context:` and named both glyphs the row must use —
  `AnonParam` for the function and `UngroupedConst` for the const, both single-match declarations in
  the glyphs fixture package — with a sentence stating why neither tags-fixture name can serve.
- `[NIT:consistency] Stale "later resolve verb" comments left in resolve.go` — verified all three
  named comments: `unitDirs`'s "the later resolve verb promotes collision ... a status type that is not
  its to design", `SpansOf`'s "unlike the later resolve verb's", and `Symbol.File`'s "the later resolve
  and expand verbs". All describe their own file wrongly once the verbs land, which is the exact
  condition cards 2, 9 and 23 already invoke. Card 9 now owns all three `resolve.go` comments as an
  enumerated list rather than the header alone, and card 2 now owns all three `answer.go` comments
  rather than two. Card 7's "do not change `SpansOf` at all" is narrowed to its code and behaviour, so
  it no longer contradicts card 9's one-clause doc re-tensing; the function keeps its inline filter,
  its signature and every line of its body. No scope exception is needed for either file: both are
  files the task edits as its own work, which is why `golang.go` — a file the task otherwise does not
  touch — needed its own named exception and these do not.
- `[NIT:consistency] Loomyard cards open the checkout with Open, not openRepo` — verified:
  `openRepo(t *testing.T, root string)` in `toc_test.go` is what `roundtrip_test.go` and the goldens
  use, and it is already in all three cards' `Context:`. Cards 20 and 22 now name `openRepo`. Card 21
  keeps `Open`, and now says why: `openRepo` takes a `*testing.T` that a benchmark does not have, and
  widening a second T3 test helper is not among this task's two named scope exceptions — so the
  benchmark calls `Open` and fails on its error with `b.Fatalf`.

## Pushed Back

_None. All three findings verified accurate against the code; every fix stayed inside files this task
already edits as its own work._
