# Discussion fix — round 1

Source review: `_mill/reviews/20260905-111409-discussion-review-r1.md` (reviewer: orchestrator,
verdict APPROVE after the blocking-class ceiling demoted the one `scope` finding to NIT).

## Fixed

- `20260905-111409-discussion-review-r1.md` — "D2's IsControl-to-GrantsTools switch misses the
  --allowedTools argv site". Verified accurate against `run.go:664`. D2 now carries the complete
  ten-site `IsControl()` sweep as a table classifying every call site as *server-attached*
  (`GrantsTools()`) or *comparison baseline* (`IsControl()`, unchanged), with the grep that produced
  it and an instruction to re-run and re-confirm it before editing. The sweep turned up a second
  missed site the review did not name — `mcp.go:47`, where `MCPConfigDocument` gates the
  empty-servers document on `IsControl()`; under D2 the two non-control cells would fall through to
  the granted-cell branch and die on `l.Server == nil`, since this ladder file declares no server
  block. Both sites are called out in bold in D2. Testing gained two regression bullets: a
  tool-less non-control cell dispatches with no `--allowedTools` flag (argv byte-identical to the
  control apart from the prompt), and `MCPConfigDocument` returns the empty-servers document for a
  tool-less non-control cell with `l.Server == nil` without erroring.

## Pushed Back

None.
