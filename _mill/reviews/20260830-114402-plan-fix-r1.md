# Fixer report: plan-fix round 1

Source review: `20260830-114027-plan-review-r1.md` (holistic, opushigh).

## Fixed

- `20260830-114027-plan-review-r1.md` — "[BLOCKING:scope] test_run_ladder.py survives the Python deletion": added `bench/loomyard-eval/ladder/tests/test_run_ladder.py` to card 73's `Deletes:`. The file exists on disk and is cited as `Context:` by six cards, so the omission would have stranded a live Python file importing deleted modules in a tree whose grandfather clause card 72 removes in the same batch.
- `20260830-114027-plan-review-r1.md` — "[NIT:decision] ingest never enforces the pin set": card 58's `Requirements:` now states that `ingest` enforces the full pin set before running the gates, with the reason — the turn ceiling ships blank and the gate would otherwise compare against nothing.
- `20260830-114027-plan-review-r1.md` — "[BLOCKING:design] transcript gates ported as single-finding returns": verified against `gates.py` — `gate_denied_tools_not_used`, `gate_no_target_override`, `gate_model_pinned`, `gate_cold_before`, `gate_worktree_neutralised`, and `gate_run_complete_artifacts` all return lists. Cards 14, 15, 17, 21, 22, and 26 now declare `[]GateFinding`, with an explicit note that an empty slice is the passing result and no zero-value sentinel exists. `ObserveWorktreeDirtied` stays singular, and card 22 now says why.
- `20260830-114027-plan-review-r1.md` — "[BLOCKING:scope] session-input paths and skill roots never named": batch 10's `## Batch Scope` now fixes every load-bearing literal in one place — the scratch directory's server declaration, settings document, run agent definition, and scorer definition paths; the installed skill path; and the two skill-scan roots — with a statement that no card may invent a different one. Cards 46, 48, 49, and 50 now refer to that fixed set instead of describing the files generically, and card 48 gains a `SessionInputs` requirement so no later caller re-derives a filename.
- `20260830-114027-plan-review-r1.md` — "[NIT:decision] the launch flag combination has no stated disposition": batch 10's `## Batch Scope` now fixes the shipped flag set (`--setting-sources user,project` plus a server-config flag only when the session has a declaration) and names the definition-relocation fallback. Card 50 requires the doc comment to record that the combination ships unverified and to name that fallback; card 52 states that `prepare-session` assembles no flags of its own. Card 70 now has a decided disposition to document.
- `20260830-114027-plan-review-r1.md` — "[BLOCKING:scope] result-tree artifact names left to inference": card 26 now enumerates the seven unconditional artifacts `GateRunCompleteArtifacts` requires by name and records why the copied launch inputs are excluded (the server declaration among them is conditional and the gate's signature carries no config). Card 62 now names `probe.json` and its `allowlist_blocks`, `denylist_blocks`, and `denial_shape_observed` keys, plus the extend-rather-than-replace behaviour.
- `20260830-114027-plan-review-r1.md` — "[NIT:consistency] two types named Usage in one package": card 8 now names the transcript-side type `MessageUsage` and states the collision it avoids.
- `20260830-114027-plan-review-r1.md` — "[NIT:scope] the usage `transcript` field falls out of the partition": card 10 now names `Transcript` in its survives-unchanged enumeration, recording that it names the in-run-directory copy.

## Pushed Back

_None._
