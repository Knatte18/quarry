# Discussion fix — round 1

Source review: `_mill/reviews/20260827-104619-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, reviewer `orchestrator`).

## Fixed

- `20260827-104619-discussion-review-r1.md` — "[NIT:consistency] \"15 exported identifiers\" contradicts the document's own enumerated list": verified against `quarry/*.go` non-test files — 29 exported top-level declarations (8 funcs, 14 types, 7 vars), matching the Facade-surface enumeration. The stale "15" is the count of distinct engine identifiers `internal/cli` *uses*, which is a different figure already stated correctly under Technical context. Corrected all five occurrences of "15" (Problem ¶3, Facade surface decision, facade-test Rejected clause, Q&A log entry) to 29, wording them "29 exported top-level identifiers" so the two counts can no longer be confused.
- `20260827-104619-discussion-review-r1.md` — "[NIT:consistency] scoutengine: prefix count off by one": verified — `errors.go` has 13 lines containing `scoutengine`, but line 22 is a doc comment referencing the package-qualified `scoutengine.ErrServerNotFoundSentinel`, not a `scoutengine: ` message prefix. Message prefixes: `errors.go` 12, total 59. Corrected 60 → 59 and `errors.go` (13) → (12) at all three sites (Scope bullet, the `scoutengine:` Decision, the Q&A log entry), and added a sentence to that Decision recording the `errors.go:22` doc-comment reference as a separate rename to `quarryengine.ErrServerNotFoundSentinel` in the same pass — otherwise the Testing section's acceptance grep (`grep -rn 'scoutengine' --include='*.go' .` returns only the historical comment) would have been unsatisfiable.

## Pushed Back

None.
