# Plan fix report — round 1

Source review: `_mill/reviews/20260827-180344-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 7 NIT).

## Fixed

- r1 holistic, "[BLOCKING:scope] Card 9 leaves batch 1's spike test uncompilable" — added `internal/quarryengine/query/implementation_spike_lsp_test.go` to card 11's `Edits:` with a requirement to pass a nil map there, and a note that the file is invisible to the hermetic tier so it must be updated deliberately rather than discovered from the batch's leading tagged vet. Sited on card 11 rather than card 9 because card 11 is the `query`-package card and already updates that package's other `Initialize` call sites.
- r1 holistic, "[BLOCKING:design] Supervised tag partitioning has no disposition at the engine layer" — took the first of the two dispositions the finding offers. Card 11 now requires documenting, on both `Options.BuildTags` and `Options.StateDir`, that the supervised daemon is partitioned by `StateDir` alone, that a caller varying `BuildTags` against a fixed `StateDir` gets another tag set's daemon, and that a non-CLI caller must supply a distinct `StateDir` per tag set itself. The finding's second disposition — keying the segment inside `acquireConnection`/`EnsureServer` — was not taken because `discussion.md`'s `daemon-keyed-by-tag-set` decision sites the segment in `internal/cli` deliberately, over the resolved leaf at all three precedence tiers including the two that bypass `workspaceKey`; the finding presents the two as alternatives, so this is a choice between them, not a partial fix.
- r1 holistic, "[NIT:consistency] Card 25 asserts a count card 4 does not record" — card 4 now records three separately labelled figures, `references-unfiltered:`, `references-verified:` and `callers-verified:`, with the last defined as the declaration-excluded count, and states why the three differ. Card 25 now asserts against `callers-verified:` by name and explicitly rules out the other two.
- r1 holistic, "[NIT:consistency] Card 24 offers a tag-keying option the query layer does not have" — dropped the alternative. Card 24 now requires two explicitly distinct `t.TempDir()` state directories and states that the `tags-<hex>` segment lives in `resolveStateDir` and never applies to a direct `References` call.
- r1 holistic, "[NIT:consistency] lsp package doc contradicts card 1" — card 1 now requires updating the package doc's enumerated request surface to include `textDocument/implementation` and removing the "no implementation" clause while keeping the "no callHierarchy" half and its deferral paragraph.
- r1 holistic, "[NIT:consistency] Stale assert-no-callers comments survive cards 20–21" — card 20 now names both: the `filterWithin` call-site comment is rewritten to describe `--within` as scoping before `--except` and nothing more, and `emitAmbiguousOrError`'s doc comment gains `Callers` alongside References/Definition.
- r1 holistic, "[NIT:scope] buildOptions call-site count is wrong" — card 18 now says every call site, and names seven.
- r1 holistic, "[NIT:scope] Two cards name identifiers from files absent from Context" — added `internal/quarryengine/query/definition.go` to card 12's `Context:`, and `internal/quarryengine/query/refs.go` plus `internal/quarryengine/daemon/ensureserver.go` to card 16's.
- r1 holistic, "[NIT:design] Card 29 is not re-runnable" — card 29 now requires a `gh issue list --state all --search <title>` check before each create, skipping and reporting the existing URL on a hit, with the reason stated: the card produces no commit and no file, so nothing else in the repository could detect the duplicate.

## Pushed Back

_None._

## Re-validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` pass, and `_plan_validate.run` returns zero findings with an empty `skip_checks`.
