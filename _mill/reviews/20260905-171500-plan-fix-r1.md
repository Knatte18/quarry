# Plan fix — round 1

Source review: `20260905-170601-plan-review-r1.md` (holistic, opushigh, REQUEST_CHANGES,
1 BLOCKING + 6 NIT). Every finding was verified against the code it cites and fixed; nothing was
pushed back.

## Fixed

- `[BLOCKING:scope]` "runTOC's glyphs branch has no machine-independent test" — verified: card 12
  and all five batch-3 goldens are gated on `loomyardRepo`, which skips when
  `LADDER_LOOMYARD_REPO` is unset, and the done gate sets no such variable. Card 10 now also edits
  `internal/cli/cli_test.go` and carries four groups of `newPipelineFixture`-based `Run` cases:
  `glyphs` and the explicit `toc --view glyphs --depth all --symbols` spelling for a file and a
  directory target in both formats; `toc --view glyphs <dir>` with no symbols flag asserting a
  non-empty symbol list; and `--view full` rendering byte-identically to a viewless `toc`, the
  `view-vocabulary` promise the reviewer correctly noted no card asserted. `internal/cli/cli_test.go`
  added to the overview's All Files Touched.
- `[NIT:scope]` "The glyphs pre-scan's `--root`-with-no-value case is unspecified" — verified
  against `internal/cli/flags.go:146-149`. Card 9's pre-scan bullet now states the disposition
  (reject with the existing `"%s requires a value"` message) with its rationale, and
  `TestParseArgs_Glyphs` gains the row.
- `[NIT:consistency]` "`TestParseArgs_FourVerbGate` is left stale by the fifth verb" — verified at
  `internal/cli/flags_test.go:229-231`. Card 9 now names the rename to
  `TestParseArgs_FiveVerbGate`, its doc-comment count, and the `glyphs` row — including that that
  row's expected `req.verb` is `"toc"`, unlike every other row, and that the asymmetry must be
  stated in the test so a reader does not read it as a bug.
- `[NIT:consistency]` "Card 18 rests on a roadmap state that is not in the file" — verified:
  `docs/roadmap.md` lines 47-73 carry no status marker on 2b or 2c. The false premise is gone;
  card 18 now says only point 2a is removed, that 2b and 2c are left byte-for-byte alone with no
  status marker added or inferred, and that the point-2 intro's "three" and "Any order among the
  three" are adjusted to the two that remain.
- `[NIT:scope]` "The engine half of `viewless-output-unchanged` is never checked" — verified:
  `internal/engine/testdata/loomyard/` exists and is gated on the same variable. Widened rather
  than narrowed: the overview's restatement of the decision now names both golden sets, card 15's
  `git status` gate covers `internal/engine/testdata/` too and requires the engine package's
  Loomyard cases to execute, and batch 3's `verify:` is now
  `go test ./internal/cli/ ./internal/engine/` under the pinned checkout — the only place in the
  plan those cases ever run.
- `[NIT:scope]` "Card 6 confirms the goldens run, not that they pass" — verified. Card 6 now
  requires the pre-existing checkout-gated cases in both `./internal/cli/` and `./internal/engine/`
  to pass before any code card lands, and names the disposition explicitly: a pre-existing failure
  stops the run and is reported as a pre-existing condition, never `-update`-ed and never carried
  into card 7, precisely so card 15's "any pre-existing golden change is this task's defect" rule
  cannot misattribute it.
- `[NIT:decision]` "The hub's `PYTHONPATH=` verify-prefix rule has no stated disposition" —
  verified against `mill-config.yaml:226-236` and the sibling `wts/diff-to-symbols` plan. Added the
  Shared Decision `go-verify-commands-carry-no-pythonpath-prefix` with its rationale: `go test`
  reads no `PYTHONPATH`, and `verify-not-isolated` is language-conditional and does not fire here
  (confirmed by running the validator against this plan).

## Pushed Back

_(none)_
