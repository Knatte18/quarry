# Fixer report: plan-fix round 3

Source review: `20260830-115810-plan-review-r3.md` (holistic, opushigh).

## Fixed

- `20260830-115810-plan-review-r3.md` — "[BLOCKING:scope] No card writes answer.json or usage.json": accurate — the plan fixed both filenames in the complete-artifacts gate and read one of them in the redaction step, but no card wrote either. Card 58 now names both write sites explicitly and records that `ingest` is their only write site anywhere in the plan.
- `20260830-115810-plan-review-r3.md` — "[NIT:decision] probe.json's denied_tools_advertised key has no disposition": verified — `build_summary` sets its meta flag from that key, which the new probe record does not carry. Card 34 now drops the flag rather than retargeting it, and records why: its source was derived from the client's advertised-tool list, which has no counterpart under agent dispatch — the same reason the transcript-sourced advertised-tools field became a definition-sourced granted-tools field. The deny-list probe's definition grants the tool by construction, so the question is answered by the definition rather than observed, and the observed-denial-shape key is the real successor signal.
- `20260830-115810-plan-review-r3.md` — "[BLOCKING:design] Cold preparation abort never advances the attempt index": accurate, and a defect in the round-2 fix rather than in the discussion. The separate `<n>.cold-abort-<k>.json` namespace is dropped entirely. Card 53 now aborts by creating the run directory, writing a cold-abort record inside it, and routing through the same invalidation the run session uses — so the attempt index advances, the ceiling bounds repeated aborts, and each occurrence leaves its own record instead of overwriting the last. Card 63 reads the cause from those invalidated siblings.
- `20260830-115810-plan-review-r3.md` — "[NIT:consistency] Card 7 asserts parity ExtractFencedJSON does not have": verified — the Python returns nil on no block so each caller words its own error, and silently treats any non-`first` selector as `last`. Card 7 now presents both as deliberate changes: a sentinel error for the no-block case that call sites still wrap with their own contextual message, and a plain error for an unrecognised selector, because a silent fallthrough on a typo would pick the wrong block with nothing reporting it.
- `20260830-115810-plan-review-r3.md` — "[NIT:consistency] Skill-leak scan is narrower than the channel it covers": accurate — built-in and managed skills appear in a session's skill listing from outside both scanned roots. Card 46's doc comment must now state that a clean report bounds only the skills this harness or the operator installed, never the whole channel, with the blinding gate remaining the detector for the rest; card 70 adds the same point to the README's residual-channel section.

## Pushed Back

_None._
