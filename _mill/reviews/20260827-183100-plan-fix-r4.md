# Plan fix report — round 4

Source review: `_mill/reviews/20260827-182801-plan-review-r4.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 2 NIT).

## Fixed

- r4 holistic, "[BLOCKING:scope] Card 10 misses 12 ensureSupervised/ensureNative call sites" — verified by grepping `ensureSupervised(` and `ensureNative(` across `internal/quarryengine/daemon/`: `supervised_test.go` has 3 (untagged, so it breaks the hermetic `go test`), `supervised_lsp_test.go` 2 and `supervised_integration_test.go` 3 (both `//go:build lsp`), and `ensureserver_integration_test.go` calls `ensureNative` at two sites the card never named. My round-1 survey searched only for `EnsureServer(`, `finalizeConnection`, `nativeArgv` and `.Initialize(`, which is why the unexported strategy functions were missed. Added the three supervised test files to card 10's `Edits:` and replaced the `EnsureServer`-only requirement with one covering every call site of all four changed functions, naming each file and instructing the implementer to search for the four names rather than trust the list. Added a note distinguishing where a missed site surfaces — hermetic `go test` for the untagged file, the leading tagged vet for the other three.
- r4 holistic, "[BLOCKING:design] documentSymbol classification phase has no deadline" — verified: card 15 bracketed the definition, implementation, references and per-reference phases but not the directional-mode classification calls, while simultaneously treating a `documentSymbol` error as a skip trigger. The card now requires the classification phase to run under its own `context.WithTimeout(ctx, timeout)`, one deadline for the phase rather than one per file, and adds a deadline hit to the enumerated skip-verification list with the timed-out flag set as any other phase timeout would.
- r4 holistic, "[BLOCKING:scope] Context omits registry.go (card 12) and paths.go (card 24)" — verified both. Added `internal/quarryengine/registry/registry.go` to card 12's `Context:` and `internal/cli/paths.go` to card 24's.
- r4 holistic, "[NIT:consistency] Card 17 leaves three stale count comments in facade_test.go" — verified all three counts at `quarry/facade_test.go` lines 56, 103 and 117. Card 17 now requires updating fourteen to fifteen, eight to ten, and seven to eight in the same edit, on the rationale it already applies to `facade.go`'s own count.
- r4 holistic, "[NIT:consistency] Card 18 justifies paths.go siting by an import it does not add" — verified: `internal/cli/paths.go` imports only stdlib, and `resolveBuildTags` calls `quarry.NormalizeBuildTags`. The card now states the new `github.com/Knatte18/quarry/quarry` import explicitly and corrects the siting rationale, which rests on the shared flag-then-environment resolution shape rather than on the file staying stdlib-only.

## Pushed Back

_None._

## Re-validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` pass, and `_plan_validate.run` returns zero findings with an empty `skip_checks`.
