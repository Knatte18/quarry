MILL_REVIEW_BEGIN
# Review: Port the capability-ladder bench harness to Go — holistic

```yaml
duration_s: 222.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-08-30
```

## Findings

### [NIT:consistency] go-cmp listed indirect despite direct test imports
**Location:** `go.mod:20`
**Issue:** Card 12 (`02-transcript-usage.md`) requires `go-cmp` land "as a direct dependency," but `go.mod` still marks `github.com/google/go-cmp v0.7.0 // indirect`, even though `usage_test.go` and `e2e_test.go` import `github.com/google/go-cmp/cmp` directly. A `go mod tidy` would drop the `// indirect` marker.
**Fix:** Run `go mod tidy` (or hand-edit) so `go-cmp` is listed as a direct requirement, matching the card's stated intent.

### [NIT:consistency] Undocumented panic in ObserveWorktreeDirtied
**Location:** `bench/loomyard-eval/ladder/internal/ladder/worktree.go:207-218`
**Issue:** `ObserveWorktreeDirtied` panics on a `RunGit` error with no doc-comment rationale, unlike `daemon.go`'s `readDaemonState`, which explicitly documents that its panic mirrors the Python port's own uncaught-exception propagation (the port-fidelity Shared Decision requires stating what changed and why).
**Fix:** Add a one-line doc comment noting the panic is deliberate and why (mirroring the Python's own uncaught `CalledProcessError`), for consistency with `readDaemonState`'s documented convention.

## Verdict

APPROVE
End-to-end plan alignment, shared decisions, cross-batch contracts, and file inventory all check out; only two non-blocking nits found.
MILL_REVIEW_END
