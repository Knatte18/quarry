# Discussion fix — round 3

Source review: `_mill/reviews/20260828-084619-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 4 NIT — 2 demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- `20260828-084619-discussion-review-r3.md` — "[NIT:consistency] omitempty list contradicts degradation shapes": replaced the partial omitempty enumeration in `json-key-set` with an exhaustive per-key disposition covering the envelope, the identity fields, `definition`, the caller entry, and `enclosing_range`. `kind` and `name` are now explicitly omitempty (without which a file-scope caller entry could not be emitted), `definition.error`/`start_line`/`end_line` are declared, and a file-scope entry is stated to keep `package` since the file parsed.
- `20260828-084619-discussion-review-r3.md` — "[NIT:consistency] Reference-typed CLI helpers cannot be reused": dropped `filterWithin` and `filterUnexpectedCallers` from the "reuse, do not reimplement" list and added an explicit split — declaration exclusion runs engine-side inside `impact` (re-implementing the set-membership rule, forced by the seam ban on engine→cli imports), `--within` runs CLI-side over `impact`'s own entry type via the type-agnostic `isWithinDir`. Both SDK consequences for `quarry.Impact` are now stated: callers already exclude declaration sites, and the result is not `--within`-filtered.
- `20260828-084619-discussion-review-r3.md` — "[NIT:consistency] \"every one of the silent-skip cases\" overstates": narrowed the claim to the two definition-side cases (`defErr != nil`, `len(defLocs) == 0`), noting that `declaration` is assigned before the implementation-side checks so the other two silent-skip cases leave it non-empty and degrade verification only.
- `20260828-084619-discussion-review-r3.md` — "[NIT:decision] `Impact`'s options type has no stated disposition": named the parameter type in the signature (`query.Options`, re-exported as the existing `quarry.Options`), stated that no new options type is introduced and that nothing is added to `query.Options` (no `Within` field), and corrected the facade re-export bullet to say no options alias is added.

## Pushed Back

_None._
