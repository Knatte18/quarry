# Plan fix — round 2

Source review: `20260909-063433-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 3 NIT).

## Fixed

- `20260909-063433-plan-review-r2.md` — "[BLOCKING:design] Card 9's cross-reference warning rests on
  a false premise". Verified against `internal/cli/cli.go`: the phrase "continuing from step 4
  above" occurs four times (lines 242, 264, 284, 307 — `runTOC`, `runResolve`, `runExpand`,
  `runDelta`) and all four cite `Run`'s own shared step 4, "Resolve the repository root by calling
  internal/repopath.ResolveRoot" (lines 230–234), not any step of `runResolve`'s inner list. The
  round-1 instruction was a false premise and would have invited four wrong "corrections". Card 9's
  cross-reference paragraph is replaced with the fact: renumbering `runResolve`'s list invalidates
  no cross-reference, all four openers cite `Run`'s shared step 4, and they are to be left unchanged.

- `20260909-063433-plan-review-r2.md` — "[NIT:consistency] Shared Decision says \"one package var\";
  the plan adds two". The overview's `additive-only, semver-minor` decision now reads "one package
  var per package — `engine.Statuses`, re-exported through the facade as `quarry.Statuses`", so the
  decision and cards 1 and 3 agree.

- `20260909-063433-plan-review-r2.md` — "[NIT:consistency] Card 2 misstates answer_test.go's
  existing test style". Verified: `internal/engine/answer_test.go` holds no `tests := []struct{...}`
  table (its subtests are inline `t.Run` calls), and `TestAnswerExtensionlessFileHeaderRule` and
  `TestAnswerIgnoreSetFreshness` carry no underscore. Card 2 now cites
  `internal/engine/name_test.go` — already in the card's `Context:` — as the table-test precedent,
  says explicitly that the style is being brought in from the sibling file rather than copied from
  this one, and drops the claim about `answer_test.go`'s own naming while keeping the three
  prescribed test names.

- `20260909-063433-plan-review-r2.md` — "[NIT:consistency] Card 10 puts the `name` verb into a doc
  that never names it". Verified against `docs/glyph.md`: the verb `name` appears nowhere; the one
  occurrence of the word is §3's Python spelling rule, where it is an identifier. Fixed by the
  finding's first option — card 10 now requires the coverage paragraph to open with one
  introductory clause defining the `name` verb (unit plus declaration head in, predicted id and
  kind out, no repository read) before any claim is made about it, capped at a single clause. The
  finding's second option — scoping §5 to `resolve` and leaving `name` to godoc — was not taken:
  `_mill/discussion.md`'s "`docs/glyph.md` §5 carries both statements, and covers both batch verbs"
  Decision explicitly rejects it as leaving the published contract half-stated for the exact defect
  family this task closes.

## Pushed Back

_None._
