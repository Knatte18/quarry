# Fixer report: plan-fix round 1

Source review: `20260905-143850-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING / 4 NIT).

## Fixed

- r1 holistic, "[BLOCKING:scope] Stale all-verbs flag test missed by the inventory" — verified against
  `internal/cli/flags_test.go:256-277`: `TestParseArgs_TextAndRootValidForAllVerbs`'s name, doc comment
  and six-row table all state the property card 8 falsifies for `--root`. Card 11 now requires the
  rename, the doc-comment correction, and a `name --text` row, and states why the correction belongs
  to batch 3 rather than the docs batch.
- r1 holistic, "[BLOCKING:design] Internal-reason CLI bytes cannot be reached as specified" — accurate:
  `runName` per card 9 calls the facade directly with no seam, so the prescribed
  "construct the result and drive the renderer and mapper" route reaches neither the branch nor its
  bytes. Took the reviewer's second option: card 12 now requires the mapper assertion plus an explicit
  untested-branch disposition recorded in the test file, and states why adding a maker seam to
  production code for an unreachable branch is the worse trade.
- r1 holistic, "[BLOCKING:design] The `var B` test row has no stated, and no plausible, expectation" —
  accurate: a Go var spec always carries a type or an expression list, so a bare `var B` is not a
  shape `goGroupedConstOrVarSymbols` emits, and the discussion's measured table covers `const B` only.
  Card 2 drops the row and states why, keeping the grouped-var member case that uses a fragment the
  extractor genuinely produces.
- r1 holistic, "[NIT:consistency] runName's internal branch duplicates the exit-code mapping" —
  accurate; `runExpand` passes `codeForExpandError(err)` into `fail` on every error branch for exactly
  this reason. Card 9 step 2 now passes `codeForNameResult(result)` as `fail`'s code argument.
- r1 holistic, "[NIT:design] Nobody is assigned the missing-counts-golden message" — accurate:
  `compareGolden` fatals on its own failed read, and card 15 changes only the parameter type. Card 16
  now owns the mechanism — an update-flag-gated existence check before the `compareGolden` call — and
  says why the flag gate is needed.
- r1 holistic, "[NIT:consistency] \"newlines intact\" in JSON is an escaped sequence, not a byte" —
  accurate: the shared encoder disables HTML escaping only, so a newline is emitted as a two-character
  escape. Cards 7 and 12 now require the JSON half to be asserted after unmarshalling, with the reason
  stated.
- r1 holistic, "[NIT:scope] No partition disposition for symbols from a lossy parse" — accurate;
  verified at `internal/engine/walk.go:270,286`, where `fileEntry` sets `Lossy` and still fills the
  entry's symbols. Card 14 adds a `lossy` field to the shared harvest struct and card 16 asserts, as a
  premise check before partitioning, that the harvest carries no lossy symbol, failing with the
  offending files named. Recorded as a premise rather than an exclusion, because the checkout is
  pinned at a commit whose sources compile, so a lossy file there is itself the finding.

## Pushed Back

_(none — every finding verified accurate, and no fix broke existing behaviour, contradicted a
documented decision, or destabilised work outside this plan.)_
