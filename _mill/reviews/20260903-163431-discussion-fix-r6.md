# Discussion fix — round 6

Source review: `_mill/reviews/20260903-163348-discussion-review-r6.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, 0 demoted, reviewer `opushigh`).

## Fixed

- r6 `[NIT:consistency]` "Q&A log re-bans `#` in the unit segment" — the Q&A answer on unit characters carried the `#` and `/` bans that round 4 removed from the Decision "The Go unit alphabet". The answer now lists only `\`, control characters and whitespace, and states why `/` and `#` need no ban (separator by construction; consumed by the first-`#` split), pointing at the Decision as authoritative.
- r6 `[NIT:design]` "String's owner-join rule is never stated" — the Decision "No constructor, and String is a pure printer" now writes the print form out: `Unit + "#" + strings.Join(append(Owner, Name), ".") + params`, with the nil/empty `Params` rule and the §3 justification for the `.`-join being outermost-first. Recorded as language-free so the Python and C# tasks inherit it rather than inventing a second join. `string_test.go`'s over-deep-`Owner` case is upgraded from "asserts only that it returns" to asserting the exact printed string, which is what pins the rule.

## Pushed Back

None. Under `--orch` no operator is present, so PUSH BACK is unavailable; both findings were verified accurate and neither met a harm category.
