# Plan fix — round 4

Source review: `20260905-173533-plan-review-r4.md` (holistic, opushigh, APPROVE, 0 BLOCKING +
5 NIT). All five verified and fixed; nothing pushed back.

## Fixed

- `[NIT:consistency]` "Two --root doc claims go stale, unnamed by any card" — verified at
  `internal/cli/flags.go:50-51` and `internal/cli/flags_test.go:271`. Card 9 now names both: the
  `parseArgs` doc comment's "valid for the three repository verbs only" and
  `TestParseArgs_TextOnEveryVerbRootOnRepositoryVerbs`'s own doc comment, both corrected to four,
  with a note that the test's name may stay since it names a property rather than a number.
- `[NIT:consistency]` "Batch 2's Batch Tests contradicts card 10 and omits card 11" — verified
  against cards 10 and 11's own field lists. `internal/cli/cli_test.go` moved to the changed list,
  `internal/cli/usage_test.go` added to it, and `internal/cli/name_golden_test.go` added to the
  unchanged regression list, which had also omitted it.
- `[NIT:consistency]` "Card 3 states the zero-symbol incomplete block two ways" — verified: the
  rule bullet unconditionally emitted the separator blank line, which for a zero-symbol answer is a
  leading newline, while the test bullet forbade exactly that. The rule bullet is now conditional —
  the blank line is emitted only when `a.Symbols` is also non-empty — with the reason stated: it is
  a separator between two blocks, and with nothing above it there is nothing to separate.
- `[NIT:scope]` "Card 14 never gives `verbArgs` for the five new golden rows" — verified against
  `internal/cli/after_test.go:169`, where `TestAfterGoldens` builds Run's argv from `verbArgs` while
  `invocation` only reaches the golden's first line. Card 14 now requires `verbArgs` spelled per row
  as a literal `[]string`, states that it is the same invocation the `invocation` string records,
  and forbids deriving one from the other — deriving is what would let a divergence between the
  command run and the command recorded go unnoticed.
- `[NIT:consistency]` "Shared Decision 1's absolutism is not scoped to the two overrides" —
  verified: `the-discussion-decisions-are-binding` as written forbade `glyphs-options-is-exported`
  and `the-preset-is-a-var-not-a-const`, two of its own siblings. It now names exactly those two as
  the deliberate supersessions, points at their entries, and states that both supersede a spelling
  rather than a substance — in one case because Go cannot express a constant slice.

## Pushed Back

_(none)_
