# Discussion fix — round 3 (round cap)

Source review: `_mill/reviews/20260904-060740-discussion-review-r3.md` (verdict REQUEST_CHANGES, 2 blocking, 4 nits; 1 demoted from BLOCKING by the stage's blocking-class ceiling).

Round 3 is the configured cap (`roles.*.holistic.rounds: 3`, operator decision 2026-09-04). All six findings are resolved below and the loop approves implicitly at the cap rather than opening a round 4.

## Fixed

- r3 `[BLOCKING:design]` "`targetIsFile` has no stated source" — verified: `internal/engine/toc.go:41` returns `(DirAnswer, error)` only, and the CLI held nothing but a path string, so the fact the text view's file form depends on had no origin. Added a new decision, `target-kind-and-the-cli-stat`, fixing the whole request pipeline in six ordered steps: flags → root → path arithmetic (escape → exit 1, no filesystem touched) → `os.Lstat` (`IsNotExist` → exit 1, other error → exit 3, else `targetIsFile = !info.IsDir()`) → `TOC` (its sentinels still mapped, to cover a target removed between stat and walk) → render. `Lstat` not `Stat`, matching the engine's never-follow rule for a symlink named as the target. Rejected alternatives recorded: inferring from answer shape, widening `TOC`'s return, statting only under `--text`, and `os.Stat`. The `text-view-grammar` file-form paragraph now points at this decision. A request-pipeline-ordering test case was added.
- r3 `[BLOCKING:design]` "The failure envelope's `error` value is unspecified" — verified: the payload was fixed at two fields with no rule for what the second holds, while the Testing block asserted stdout is "exactly" the envelope. Added a new decision, `error-message-derivation`: `error` is CLI-authored and single-line, byte-identical to the first stderr line, with a per-exit-code table (exit 1 and 2 spelled by quarry; exit 3 carries `internal error: <err.Error()>`). Rationale recorded for why exit 3 is the one pass-through and why the engine's wrapped chain is never exposed for 1 and 2. An error-derivation test case was added, including that the value never contains `internal/engine`.
- r3 `[NIT:consistency]` "Q&A contradicts the after/ golden byte layout" (demoted from BLOCKING) — verified: the Q&A entry still carried the `(exit code: N)` trailer that round 2 removed from `after-outputs-are-the-goldens`. Rewrote the Q&A answer to the settled layout and marked it as superseding the earlier auto-pick, with a pointer to where the trailer is argued. No occurrence of the stale claim remains in the file.
- r3 `[NIT:consistency]` "`--help` breaks the exit-0 stdout rule" — verified: the exit-code table's row 0 says stdout carries the directory answer, which `--help` contradicts. Scoped the table to query invocations and named `--help` as the one stated exception, cross-referenced to `flag-semantics`.
- r3 `[NIT:design]` "Render failure has no exit code" — added the render-failure case to the exit-3 row and stated why it is 3 rather than a new code: the answer holds only plain values, so `RenderJSON`'s error can only be a write failure on the output stream, which is what 3 already means. Recorded that `RenderText` cannot fail and returns no error.
- r3 `[NIT:scope]` "Scope's test list omits JSON rendering" — the Scope In bullet now names target conversion and both renderers, with the JSON view's specific assertions (key order, absent defaults, unescaped `<`/`&`, trailing newline) spelled out to match the Testing section.

## Pushed Back

None. All six findings verified accurate — the two blocking ones against `internal/engine/toc.go:41` and against the discussion text's own Testing assertions. No fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task.
