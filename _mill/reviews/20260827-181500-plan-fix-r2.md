# Plan fix report — round 2

Source review: `_mill/reviews/20260827-181208-plan-review-r2.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 3 NIT).

## Fixed

- r2 holistic, "[BLOCKING:scope] Card 16 ConnKind test violates the query layering row" — verified against `internal/quarryengine/layering_test.go`: the `{pkgDir: "query", isTestRow: true}` row allows root, `registry`, `lsp` and `daemontest` only, and `daemontest.go` re-exports no `ConnKind`. Took the finding's first disposition: card 16 now edits `internal/quarryengine/daemon/daemontest/daemontest.go` to add `ConnKindNative`, `ConnKindSupervised` and `ConnKindLegacy` re-export constants, with `daemontest.go` in its `Edits:` and `layering_test.go` in its `Context:`, and the teardown assertion names those instead of `daemon.`-qualified constants. Chose the re-export over dropping the assertion because the discussion requires the per-`ConnKind` assertion explicitly, and `daemontest`'s existing `StateFile` sets the precedent for exactly this kind of re-export.
- r2 holistic, "[BLOCKING:scope] Layering/seam guards never re-run after batch 2" — verified: `layering_test.go` and `seam_enforcement_test.go` are in the `internal/quarryengine` root package, which only batch 2's verify ran. Took the finding's second disposition: the overview's module-wide `verify:` is now `go vet ./... && go vet -tags lsp ./... && go test ./internal/quarryengine/`, so both guards re-run at every batch boundary. Added an `Enforcement:` clause to the `layering-is-non-negotiable` Shared Decision recording why a `go vet` pass cannot substitute for it.
- r2 holistic, "[BLOCKING:design] Live-tier cards leak detached supervised gopls daemons" — verified: Go's entry has `HasNativeDaemon: true`, the supervised teardown branch never kills, and the idle timeout is ten minutes. Card 24 now pairs each of its two state directories with `daemontest.StateFile` plus a `t.Cleanup` calling `daemontest.KillRecordedDaemon`, matching the three existing spawn sites in `internal/quarryengine/query/refs_integration_test.go`, and lists `daemontest.go` in its `Context:`. Card 25's disposition is stated explicitly and differs because `internal/cli` may not import an engine-internal package under the `layering-is-non-negotiable` decision: it resolves the state file through `quarry.DaemonStateFile`, decodes the `pid` field, and kills it with `proc.KillPID` from a `t.Cleanup`, with both of its runs sharing one `--state-dir` so there is exactly one daemon to reap.
- r2 holistic, "[NIT:consistency] Shared Decision `Applies to:` omits the batch that implements it" — added `registry-build-tag-template` to the `rendered-options-non-nil-means-tagged` decision's `Applies to:` line.
- r2 holistic, "[NIT:consistency] Card 29 contradicts card-scoped-commits" — the `card-scoped-commits` decision now scopes its rule to cards that change a file and states the file-less exception, naming card 29 as the sole instance.
- r2 holistic, "[NIT:scope] Context omits files whose identifiers the Requirements name" — added `internal/quarryengine/errors.go` and `internal/quarryengine/lsp/lspclient.go` to card 13's `Context:`, and `internal/quarryengine/query/refs.go` to card 20's.

## Pushed Back

_None._

## Re-validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` pass, and `_plan_validate.run` returns zero findings with an empty `skip_checks`.
