# Plan fix — round 1

Source review: `_mill/reviews/20260827-113533-plan-review-r1.md` (verdict REQUEST_CHANGES, 3 BLOCKING, 2 NIT, reviewer sonnetmax).

## Fixed

- `20260827-113533-plan-review-r1.md` — "[BLOCKING:scope] Card 3 needs ensureserver.go's doc comment but it isn't in Context": accurate — card 3 instructs carrying over `quarry/ensureserver.go`'s lines 26–31 doc comment onto the new `Logger` var, and `Context:` is an allowlist the implementer may not read past. Added `quarry/ensureserver.go` to card 3's `Context:`.
- `20260827-113533-plan-review-r1.md` — "[BLOCKING:scope] Card 7 edits a file absent from its own Edits: list": accurate — `toolchain_integration_test.go:28` assigns `userCacheDir` directly and card 7's Requirements rewrites it, but the file appeared in batch 2 only as a card 6 Move target. Added `internal/quarryengine/daemon/toolchain_integration_test.go` to card 7's `Edits:`.
- `20260827-113533-plan-review-r1.md` — "[BLOCKING:scope] Cards 8/9 retarget `DetectLanguage`/`LoadRegistry` without their files in Context": accurate — `LoadRegistry` is declared in `registry/load.go` and `DetectLanguage` in `registry/detect.go`, neither of which was listed; card 9 cannot write byte-for-byte delegating wrappers without their signatures, and card 8 retargets `DetectLanguage` call sites. Added both files to `Context:` on cards 8 and 9.
- `20260827-113533-plan-review-r1.md` — "[NIT:consistency] Layering guard spec contradicts itself on `query` test imports": accurate — a single per-package allowed set cannot both permit `query` -> `daemon` and fail a `query` test making that import. Split `query` into two table rows: production files may import root/`registry`/`lsp`/`daemon`; `_test.go` files may import root/`registry`/`lsp`/`daemontest` but not `daemon` directly. Verified this is implementable before adopting it — `refs_test.go`'s `TestReferences_HasNativeDaemonRoutesThroughEnsureServer` reaches `daemon` only through the two toolchain seams, and every other symbol it names (`References`, `Options`, `Query`, `Registry`, `ErrServerNotFoundSentinel`) comes from `query`, `registry`, or the root, so the narrower test row is satisfiable. Rewrote the carve-out paragraph to match: `daemontest` is the one unconditionally-allowed path, everything else is checked against the row for that file's package **and kind**.
- `20260827-113533-plan-review-r1.md` — "[NIT:consistency] Card 7's \"three call sites\" undercounts its own line list": accurate — six line numbers were listed. Reworded to "six call sites across three tests" and named the pairing (one `withTempUserCacheDir` plus one `withFakeInstaller` per test).

## Pushed Back

None.
