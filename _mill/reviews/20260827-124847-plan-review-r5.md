MILL_REVIEW_BEGIN
# Review: Thin quarry/ facade over internal/quarryengine — holistic

```yaml
duration_s: 269.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-27
```

## Findings

None. Verified against source: the 59 `scoutengine: ` occurrence count and its per-file
breakdown (batch 1 card 1), the `errors.go:160` `scout: ` outlier, `errors.go:22`'s doc-comment
target, `docs/port-equivalence.md:104-111`'s claim text, all `defaultLogHandler` call-site line
numbers in `ensureserver.go` (447, 543) and `lspclient.go` (574/577/582/605/608),
`position_test.go`'s two bare-`Position{...}` literals at lines 31/51, `toolchain.go`'s three
exported-seam identifiers, `refs_test.go`'s `withTempUserCacheDir`/`withFakeInstaller` call sites
at lines 391/394, and `refs_integration_test.go`'s full set of line references (`builtins()` at
71/93/188/207/245 plus the line-180 doc mention, `DaemonStateFile` at 86/200/238,
`killRecordedDaemon` at 87/201/239, the `quarry/detect.go` fixture path at 75/193, and the
historical `internal/scoutengine` comment at line 55) — every one matches the plan's claims
exactly, including the ten `builtins()`/doc-comment retargets in the three `daemon`
integration-test files (7+1 in `ensureserver_integration_test.go`, 1 in
`toolchain_integration_test.go`, 1 in `supervised_integration_test.go`). The Batch Index DAG is
acyclic and every `file:`/`depends-on` entry resolves. `All Files Touched` is exactly the union
of `Edits:`/`Creates:`/Move-targets across all 14 cards, with no Move-source-only or Deletes-only
path incorrectly included. `## Rename mechanic` is present in batch 2 (the only batch with
non-empty `Moves:`) and absent, correctly, from batches 1/3/4. Card 12's `facade_test.go` Context
omits `registry/load.go`, `registry/detect.go`, `daemon/daemonstate.go`, and `query/definition.go`
— but `quarry/facade.go` (in Context) already carries the byte-for-byte signatures for all eight
delegating functions per card 9, so the implementer never needs those four files; not a finding.
Global step numbering (cards 1-14) is sequential with no gaps.

## Verdict

APPROVE
Every checked line-number, count, and cross-batch identifier claim verified exactly against source; no violations found.
MILL_REVIEW_END
