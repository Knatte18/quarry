# Plan fix — round 1

Source review: `20260904-053104-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 6 NIT).

## Fixed

- `[BLOCKING:scope] repo.go identifiers named without repo.go in Context` — added
  `internal/engine/repo.go` to the `Context:` of batch 3 card 12, batch 4 card 14, and batch 5's
  cards 20, 21 and 22 (the successors of the old card 20). Verified accurate: card 12 names both
  target sentinels, card 20 names `Open`, and card 14 argues from the sentinel split, all declared in
  that file.
- `[NIT:decision] Card 15 never calls statusForMatches` — card 15's disposition is now written as a
  call to `statusForMatches(g, matches, dirs.collision)` with an explicit mapping from its four return
  values onto `expand`'s kind gate, rather than five inline rows duplicating the shared function's
  first two. The observable behaviour and the row order are unchanged; the kind gate is stated as
  sitting *between* the shared function's zero-and-collision rows and its remaining ones, which is
  what makes the reuse exact rather than approximate.
- `[NIT:consistency] Three doc citations point at the wrong file` — verified against both documents:
  `docs/glyph.md` contains the word "expand" once, at line 181, and none of the three clauses;
  all three are `docs/rewrite-plan.md`'s three-queries section. Re-attributed in batch 1 card 2 and
  batch 4 cards 14 and 15, added `docs/rewrite-plan.md` to those three cards' `Context:`, and added a
  sentence to card 2 stating which document owns which rule so the shipped doc comments cite correctly.
- `[NIT:consistency] Batch 2's regression premise omits the whole-repo round trip` — verified:
  `TestRoundTrip_QuarryItself` opens the module root and calls `TOC(".", {Depth: DepthAll, Symbols:
  true})`, so it does enumerate both new packages. Batch 2's Batch Scope and Batch Tests now name it
  and `tupleSetDiff`'s multiset semantics as the reason the build-tag pair round-trips, and state that
  the argument is a prediction `verify:` checks, with instructions to report a round-trip failure
  rather than delete the fixture.
- `[NIT:consistency] answer.go's own stale comments are ruled out of scope` — card 1's blanket "change
  no existing comment" is narrowed to declarations and JSON tags, with the header comment explicitly
  handed to card 2; card 2 now requires extending the header comment's enumeration to name the three
  new types and rewording `Symbol.HeadStart`'s "consumed by the later expand verb" into the present
  tense, on the same footing cards 9 and 23 give their own files.
- `[NIT:scope] Card 20 carries five deliverables and one host-provisioning step` — split into three
  cards, one artefact and one commit each: card 20 (glyph list plus the timing assertion), card 21
  (the benchmark), card 22 (the expand spot check); the golang.go card renumbered 21 to 23, and every
  cross-reference to those numbers updated in batches 1 and 5. The checkout resolution moved out of
  the card into a stated batch prerequisite in batch 5's Batch Scope, where the halt condition belongs:
  it blocks all three cards, not one.
- `[NIT:consistency] Timing test gates in the opposite order to its sibling` — verified:
  `TestRoundTrip_Loomyard` checks `testing.Short()` before `loomyardRepo`. Cards 20 and 22 now specify
  the same order, with the reason stated — `loomyardRepo` fails rather than skips on a wrong pin, so
  gating on the checkout first would fail under `-short` where the established test skips. Batch 5's
  Batch Tests adds a `-short` run to the required verification.

## Pushed Back

_None. All seven findings verified accurate against the code and the documents; no fix broke existing
functionality, conflicted with a documented decision, or reached outside this task's scope._
