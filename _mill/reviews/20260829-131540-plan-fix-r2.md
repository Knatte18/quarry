# Plan fix — round 2

Source review: `20260829-131145-plan-review-r2.md` (holistic, REQUEST_CHANGES, 3 BLOCKING / 5 NIT).

## Fixed

- `20260829-131145-plan-review-r2.md` — "[NIT:consistency] `gate_artifacts_present` can never pass where it runs". Accurate and fatal as written: two of the four required files are the scorer's output and `run_gates` ran before the scorer, so every run would have failed a fatal gate and the matrix would have halted on attempt 3 of run 1. Split it out as `gate_run_complete_artifacts(run_dir)`, invoked after scoring and immediately before `write_run_json`, and gave `run_gates` an explicit signature that asserts on no scoring artifact (batch 3 card 10, batch 6 card 19), with a test asserting each passes on exactly the directory state that exists when it runs.
- `20260829-131145-plan-review-r2.md` — "[BLOCKING:scope] `task_text_for` has no stated section boundary". Verified the headings in both task files: task 04's task text is immediately followed by its output schema and then `## Notes for whoever scores this (ground truth — do not reveal to A/B/C)`, which names the three real callers and the `burler.go:373` decoy; task 01's is followed by its fasit leads. An unbounded extractor would have pasted the answer key into all 45 prompts. Stated the exact rule in batch 6 card 19 — start at the task-text heading, stop before the next line beginning `## `, strip the `> ` blockquote prefix, raise when absent or empty — with the leak named as the reason, plus a card 22 test asserting neither task's extracted text contains `burler.go:373` or the word `fasit`.
- `20260829-131145-plan-review-r2.md` — "[BLOCKING:scope] cold-cell state-dir resolution ignores `QUARRY_BUILD_TAGS` and an unnamed cache dir". Verified `internal/mcpserver/callcontext.go` resolves its tag set through `cli.ResolveBuildTags`, which falls back to `$QUARRY_BUILD_TAGS`, and that `ResolveStateDir` appends a `tags-<hex>` segment at every tier. Added `QUARRY_BUILD_TAGS` to `run_env`'s scrub list and to `resolve_state_dir`'s raise condition (batch 3 card 9, batch 6 card 17), added `user_cache_dir()` reproducing `os.UserCacheDir` semantics as the single derivation of the production `cache_dir`, threaded `cache_dir` through the signatures that need it, and stated the `QUARRY_CONFIG` disposition explicitly: not scrubbed, because it selects the `servers.yaml` overlay naming the language-server command and clearing it could stop the server starting, but recorded in the probe record so a non-default environment is visible.
- `20260829-131145-plan-review-r2.md` — "[NIT:consistency] batch 6's tests require injection seams the implementation cards never declare". Added the named seams to the implementation signatures: `run_git` with a `git=run_git` parameter on the four worktree functions, `launch_run` with an `executor=launch_run` parameter on `execute_run`, `run_matrix`, and `run_cold_cell`, and spelled out `run_matrix`'s full parameter list in place of `(...)` (batch 6 cards 16, 19, 20, 21).
- `20260829-131145-plan-review-r2.md` — "[NIT:consistency] `worktree_dirtied` is observed after the restore that erases it". Moved the observation into `run_gates` inside `execute_run`, before any restore, and restated card 20 as carrying the already-recorded value — an observation taken after `reset --hard` plus `clean -fdx` would have been `False` for all 45 runs, making card 15's aggregate a column of zeros (batch 3 card 9, batch 6 cards 19 and 20).
- `20260829-131145-plan-review-r2.md` — "[NIT:decision] `--max-turns` is named as depended-upon but never dispositioned". Dispositioned rather than dropped: added `max_turns` to `ladder.yaml` and to the `Ladder` dataclass, and `--max-turns` to `build_argv` and to the probe, identical across all 45 runs and the probe so it bounds cost without biasing a comparison (batch 1 cards 2 and 3, batch 6 cards 18, 19, 22).
- `20260829-131145-plan-review-r2.md` — "[BLOCKING:scope] card 17 names `EnsureServer` without listing its file". Added `internal/mcpserver/tools_toc.go` and `internal/quarryengine/daemon/ensureserver.go` to card 17's `Context:`, so the implementer can check the load-bearing warm-up claim without cold-start exploration, as cards 9 and 21 already allow.
- `20260829-131145-plan-review-r2.md` — "[NIT:consistency] `B_PREAMBLE_BODY` as specified still contains `<TASK TEXT>`". Confirmed the committed body carries the placeholder. Stated that `B_PREAMBLE_BODY` ends at the `<TARGET_DIR>` paragraph and excludes the placeholder line, since `preamble_for` appends the task text itself (batch 1 card 5).

## Pushed Back

_(none — every finding verified as accurate against the committed task files, the benchmark README, and quarry's own source, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task's scope.)_
