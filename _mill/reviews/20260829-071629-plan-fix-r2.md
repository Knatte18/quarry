# Plan fix — round 2

Source review: `_mill/reviews/20260829-071456-plan-review-r2.md`

## Fixed

- r2 "Card 9 Context omits `internal/mcpserver/translate.go`" — verified: card 9 names
  `toZeroBased`, created by card 8 in that file. Added to card 9's `Context:`, matching cards 10
  and 11's own intra-batch convention.
- r2 "Tier-3 stdout-purity mechanism left as an either/or" — verified against the SDK:
  `mcp.CommandTransport` owns the child's stdout pipe and its framed reader consumes it, so the
  "capture stdout independently of the transport" option is not implementable. Batch 7 card 27 now
  prescribes one mechanism — a second, separately-spawned child driven over explicit
  `StdinPipe`/`StdoutPipe`/`StderrPipe` with a hand-built `initialize` plus `initialized`, with the
  `CommandTransport` session reserved for handshake, `tools/list`, and the two `tools/call`
  assertions — and states why `CommandTransport` cannot serve it.
- r2 "`.mcp.json` top-level shape never specified" — verified that this repository has no
  `.claude-plugin/` directory, so the project-scope form applies. Card 28 now pins the top-level
  `mcpServers` object with the exact `command` and `args` values and states why the bare-map form
  is wrong here. Cards 29 and 30 now word the auto-connect claim as taking effect after the
  one-time project-server trust prompt.
- r2 "Batch 3 scope claims three `register*` functions" — verified: batch 3 declares
  `registerLSPTools` and `registerSymbolTool`. Scope reworded to two functions covering three tools.
- r2 "`callContext.Timeout` sourcing unstated" — card 11 now names `cfg.Timeout` as the source and
  states that it is a per-entry deadline rather than a whole-call budget, matching the CLI's
  per-invocation `Options.Timeout`.

## Pushed Back

_(none)_
