# Discussion fix — round 3

Source review: `_mill/reviews/20260827-110740-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, reviewer sonnetmax).

## Fixed

- "[NIT:consistency] doc.go rationale overclaims uniform section→package mapping": verified — `quarry/doc.go`'s "The generalized LSP client" section does embed the `workspace/symbol` resolver, the `--in-file`/`documentSymbol` resolver, and the zero/one/many ambiguity mapping, all of which describe `resolvePosition` in `refs.go` and land in `query`, not `lsp`. Replaced the "every section maps cleanly onto exactly one package" claim with "most sections do", and named the one exception explicitly with its line range and the paragraph-level split it needs.
- "[NIT:design] Package-doc host file unnamed for multi-file subpackages": accurate — "primary file" was undefined for `daemon` (4 files) and `query` (3 files). Named the host file for all four subpackages (`lsp/lspclient.go`, `registry/registry.go`, `daemon/ensureserver.go`, `query/refs.go`) and stated that a dedicated per-package `doc.go` is an acceptable substitute where the comment would dwarf its host, flagging `daemon` as the likely case since it inherits three sections.
- "[NIT:design] Two divergent names left open for the relocated log handler": accurate — the parenthetical read as an unresolved choice for a symbol with 7 call sites. Fixed the name to `quarryengine.Logger` (a package-level `*slog.Logger`) and removed the alternative.

## Pushed Back

None.
