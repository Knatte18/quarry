# Plan fix — round 4

Source review: `_mill/reviews/20260904-120827-plan-review-r4.md` (holistic, REQUEST_CHANGES,
4 BLOCKING / 2 NIT, none demoted). First review of the `harness-mcp-init-fix` batch.

## Fixed

- r4 `[BLOCKING:scope]` "Retype breaks an existing string-array fixture" — verified:
  `testdata/transcripts/tool-bytes.jsonl` is the one fixture carrying `"mcp_servers":["quarry"]`;
  `fakeclaude/main.go` hardcodes `[]` and every other transcript fixture carries `[]`. Card 4 now
  owns converting that line to the object shape, byte-identical otherwise, with an instruction to
  re-check for further string-array init lines rather than trusting the survey. The fixture is in
  card 4's `Edits:` and in All Files Touched.
- r4 `[BLOCKING:design]` "Card 5's finding has nowhere to be recorded" — verified, and worse than
  the review states: `run.go` has no logger and writes nothing to standard error at all, so there
  was no log to fall back on either. The finding is now persisted as a file written into the
  attempt directory immediately before `InvalidateRep`, which renames rather than deletes, so it
  survives at `<cell>/<rep>.invalid-<k>/` — the directories the matrix card already enumerates.
  `runstate.go` joins card 5's `Edits:` for the file-name constant. Card 9's source clause is
  rewritten to distinguish the two reason locations: blinding failures in the completed
  repetition's state file, server-not-connected failures in the invalidated attempt's reason file.
- r4 `[BLOCKING:scope]` "'The required pin' is an unresolvable identifier" — card 6 now spells
  `72c23d9` literally, and states explicitly that it is a different thing from the ladder file's own
  `pinned_sha`, which was the one SHA the card's context did carry.
- r4 `[BLOCKING:scope]` "Named symbols from files absent from Context" — `runstate.go` is now in card
  5 (as an `Edits:` entry, per the finding above, which subsumes the `Context:` remedy), and `mcp.go`
  is in card 7's `Context:`.
- r4 `[NIT:consistency]` "Live test is not 'the first pre-matrix gate'" — the
  `the-done-gate-runs-offline` decision now places it accurately as step (4) of the matrix batch's
  gate card, and says why it is deliberately not in the batch named `pre-matrix-gates`: that batch is
  offline and cheap, and a gate costing a repetition belongs immediately before the matrix.
- r4 `[NIT:consistency]` "Fixture-capture turn omits the mandated `env -u` prefix" — route (b) now
  carries the prefix, with the reason: the fixture pins the init record the matrix's own children
  emit, and a capture under markers the matrix strips could differ in the session-configuration
  fields that record reports.

## Pushed Back

_None. Every round-4 finding was accurate and fixed as suggested._

## Noted, not fixed — an accepted coverage limit now stated in the plan

Card 5's *check* is unit-tested, but its *wiring* in `run.go` is unreachable from the offline
end-to-end suite, because `fakeclaude/main.go` hardcodes an empty `mcp_servers` array and so can
never present a non-connected server. Parameterising the fake would widen the batch past the defect
it exists to fix. The batch's `## Batch Tests` now states this as an accepted gap and names what
covers it instead — the pre-matrix live test, then the matrix's first granted repetition, where a
wiring error surfaces as a rung cell invalidating on a connected server and the `.invalid-*`
accounting makes it visible within one invocation.
