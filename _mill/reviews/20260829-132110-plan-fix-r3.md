# Plan fix — round 3

Source review: `20260829-131745-plan-review-r3.md` (holistic, REQUEST_CHANGES, 6 BLOCKING / 3 NIT).

## Fixed

- `20260829-131745-plan-review-r3.md` — "[BLOCKING:design] Cold configs execute through the main-matrix driver". Accurate and silent: the cold pairs would have run through the main pipeline on the shared worktree against a possibly-warm daemon with none of the cold gates applied, then been `is_complete` by the time `run_cold_cell` looked, so warm numbers would have been reported as cold. Split `plan_runs` into the disjoint `main_runs` / `cold_runs` partitions, made `pending_runs` take a pair list so each driver filters its own, and pointed `run_matrix` at `main_runs` and `run_cold_cell` at `cold_runs` (batch 6 cards 20, 21), with a card 22 test asserting no cold pair appears in `main_runs`.
- `20260829-131745-plan-review-r3.md` — "[BLOCKING:scope] `warm_daemon` is defined but never called". Named the call site — `run_matrix`, immediately before each main-matrix dispatch, per run rather than once per worktree, since the daemon self-expires after its idle timeout and a gap between runs would leave a nominally warm cell cold. Named the payload as `{"targets": [{"query": "Run"}]}`, verified against `symbolInput`/`symbolEntry` in `internal/mcpserver/tools_symbol.go`, and added a post-condition asserting `daemon.json` now exists at the resolved state directory: `workspace_symbol`'s handler calls `resolveCall` once for the whole call before resolving any target, so the daemon starts whether or not the query matches, which makes the state file the right post-condition and not the result payload. Without it a malformed warm-up would have left every "pre-warmed" run cold and silent (batch 6 cards 17, 20, 22).
- `20260829-131745-plan-review-r3.md` — "[BLOCKING:scope] Batch 5 consumes batch 2 and batch 4 contracts it does not depend on". Changed batch 5's `depends-on` to `[1, 2, 3, 4]` in both the frontmatter and the overview Batch Index, added `extract_usage.py` and `score_run.py` to card 13's `Context:`, and stated the `tokens.*` flattening explicitly — the extractor nests the four token classes under `tokens` while `METRICS` names them flat, which the card previously covered with the single word "flattened".
- `20260829-131745-plan-review-r3.md` — "[BLOCKING:scope] `probe.json` key contract is undefined for its reader". Pinned the contract on the reader's side: card 15 now names `<results_root>/probe.json` — correcting "run directory", which was wrong — and names `denied_tools_advertised` as the key it reads, which is the field name card 18's probe writer uses.
- `20260829-131745-plan-review-r3.md` — "[BLOCKING:design] Redaction of the bare tool name `impact` destroys Ladder-B answer prose". Accurate and would have corrupted all 24 Ladder-B answers: `impact` is the one canonical name that is also an ordinary English word, on a task that is literally an impact analysis, so a case-insensitive bare-name pass would have turned every "the impact of this change" in the `evidence` and `reason` fields into `<tool>` — destroying the field the impact scoring rule reads. Excluded `impact` from the bare-name pass, leaving its `mcp__quarry__impact` and CLI forms redacted, and added a test asserting a common-noun use survives while the prefixed form does not (batch 4 card 11).
- `20260829-131745-plan-review-r3.md` — "[BLOCKING:scope] Cold-cell retry semantics are unspecified and collide with `build_worktree`". Gave the cold cell its own retry loop reusing `MAX_ATTEMPTS`, with per-attempt worktree removal before the rebuild — `build_worktree` raises when a directory already exists, so a retry that skipped the teardown would have failed on the rebuild rather than retrying the run — and stated the final teardown whatever the outcome (batch 6 card 21).
- `20260829-131745-plan-review-r3.md` — "[NIT:consistency] Card 19's task-04 section-order rationale is wrong". Confirmed the actual order: task 04's task text is followed by `## Output schema (impact-analysis tasks)` and only then by the scoring notes. Corrected the rationale to the real order while keeping the stop-at-next-`## ` mechanic, which was already safe (batch 6 card 19).
- `20260829-131745-plan-review-r3.md` — "[NIT:consistency] README run-environment omits `QUARRY_BUILD_TAGS`". Added both cleared variables and the deliberate `QUARRY_CONFIG` non-scrub to the README's Run environment section, with the reason each matters to the cold cell's per-path key (batch 7 card 23).
- `20260829-131745-plan-review-r3.md` — "[NIT:consistency] Fasit `_meta` block has no stated disposition in the scorer prompt". Added `strip_fasit_meta` and routed the fasit through it before the prompt, with the asymmetry named: `_meta`'s `role` string contains the word the answer is deliberately scrubbed of, and while it leaks no config it is scoring-irrelevant, so it is dropped rather than left unexplained. Added the assertion to card 12's tests.

## Pushed Back

_(none — every finding verified as accurate against the committed task files, the fasit, and quarry's own source, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task's scope.)_
