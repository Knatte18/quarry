# Discussion fix — round 1

Source review: `_mill/reviews/20260830-104236-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- r1 `[NIT:consistency]` "workspaceKey/buildTagsSegment mischaracterized as one hash" — Technical context / "Daemon isolation" reworded: `workspaceKey` (`internal/cli/paths.go:76-86`) hashes the cleaned absolute target path only; `buildTagsSegment` (`paths.go:88-99`) is an independent segment `ResolveStateDir` (`paths.go:118-140`) appends when tags are non-empty. Stale citation `paths.go:76-97` corrected. Added a note that the `--state-dir`/`$QUARRY_STATE_DIR` tiers bypass `workspaceKey`, and that removing the per-call override makes that tier strictly safer rather than being a bug to re-derive later.
- r1 `[NIT:decision]` "No stated ordering against #009's planned deletion of the same file" — added an ordering bullet to Decisions/`bench-ladder-update` naming #009 `port-ladder-bench-to-go`, the expected merge order, and an explicit delete-wins conflict resolution that carries the prompt change into the Go port instead.

## Pushed Back

None.
