# Plan fix — round 2

Source review: `20260904-063441-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 6 NIT).
All seven findings verified as accurate; none met a harm-check escape. All seven fixed.

## Fixed

- `20260904-063441-plan-review-r2.md` — "[BLOCKING:design] `--symbols` pass-through test rests on a
  false premise". Verified in `internal/engine/walk.go`: `unitFor` returns `""` when `dirRel` is
  `"."`, and `unitSpellable`'s own doc names "the repository root's empty unit" among the rejections
  for which "no file entry in it carries Symbols". A root-level Go file therefore never carries
  symbols, whatever the flag says. Added the overview Shared Decision
  `a-symbols-key-is-never-guaranteed-by---symbols`, recording discussion gotcha 1 plan-wide with all
  three nil-`Symbols` causes; card 18's `--symbols` assertion now requires a named subdirectory of
  the fixture root rather than the root itself.
- `20260904-063441-plan-review-r2.md` — "[NIT:consistency] Copied `writeScratchTree` resolves the
  wrong module root in `quarry/`". Verified: the engine's copy sits two directories below the module
  root and takes three `filepath.Dir` steps; `quarry/` sits one down and needs two. Copying three
  would have resolved the module root to the repository's parent and written the fixture tree
  outside the repository, passing silently. The step count and its reason are now stated in the
  overview decision, in card 4 (two steps), and in card 14 (three steps, same depth as the engine).
- `20260904-063441-plan-review-r2.md` — "[NIT:consistency] Golden invocation line may embed the
  `--root` machine path". Accurate and the most consequential of the NITs: every golden case passes
  `--root <absolute Loomyard path>`, so "the same flags the case passed" would have written a
  machine path into a tracked file. Card 20 now builds the invocation line from the table's "args
  after the verb" column only, with each row carrying its own literal suffix, so the path cannot
  leak by construction rather than being caught by card 21's post-hoc read.
- `20260904-063441-plan-review-r2.md` — "[NIT:consistency] Golden test function name is assumed but
  never fixed". Card 20 now names the function exactly `TestAfterGoldens` and records that cards 21
  and 23's `-run TestAfter` prefix-matches it, so a different name would make the `-update` run a
  silent no-op producing no files.
- `20260904-063441-plan-review-r2.md` — "[NIT:scope] Step-8 error branches have no test path". Took
  the reviewer's first offered fix rather than the "record as untested" alternative, because
  `discussion.md`'s Testing block asks for the error-to-code table outright. Card 16 now declares
  `codeForTOCError(err error) int` as a pure helper that step 8 maps through; card 18 table-tests it
  for wrapped `ErrTargetNotFound`, wrapped `ErrTargetOutsideRepo`, an arbitrary error, and `nil`,
  and records that the branches are otherwise race-only and are covered through the helper rather
  than by provoking a race no test can trigger deterministically.
- `20260904-063441-plan-review-r2.md` — "[NIT:consistency] \"Every token not beginning with `-` is a
  target\" contradicts value consumption". Accurate: the rule as written counted `3` in `--depth 3`
  as a second target and would have rejected a valid invocation. Card 10's bullet now excludes
  tokens consumed as a preceding flag's value, and states the failure the naive rule produces.
- `20260904-063441-plan-review-r2.md` — "[NIT:scope] Card 23's `Context:` omits files its
  Requirements tell it to read". Added the two remaining `after/` outputs and both before-side
  compact files to card 23's `Context:`.

## Pushed Back

None.
