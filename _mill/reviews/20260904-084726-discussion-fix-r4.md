# Discussion fix — round 4

Source review: `_mill/reviews/20260904-084550-discussion-review-r4.md` (APPROVE, 0 BLOCKING, 5 NIT, 0 demoted).

## Fixed

- `20260904-084550-discussion-review-r4.md` — "[NIT:consistency] D5 and Technical context disagree on renderer file placement": decided rather than delegated. The two JSON renderers and the shared `renderJSON` go into `quarry/render.go`, the two text renderers into `quarry/text.go` beside the grammar and `writeSymbolLine` they reuse; no new file is added to `quarry/`. The Technical-context line's "or a new `resolve.go`/`expand.go` pair, mill-plan's call" is gone, so the two sections now say the same thing.
- `20260904-084550-discussion-review-r4.md` — "[NIT:consistency] D2's \"four payload-free paths\" is five in D3": D2 now names the fifth (`repoRelPath` failure, exit 1), states that its list is a summary rather than a closed set, and points at D3's table as the authority where the two differ.
- `20260904-084550-discussion-review-r4.md` — "[NIT:consistency] D15's exit-code column is not total over after-side files": `resolve-glyph-text.txt` and `expand-type-text.txt` get their own `*(none)*` rows instead of being folded into their siblings' cells as "(+ `-text`)", matching the T5a precedent the same paragraph cites. All eight new goldens now have one row each, so the exit-code column is genuinely total.
- `20260904-084550-discussion-review-r4.md` — "[NIT:scope] D13 leaves usageText's `flags:` block unspecified": D13 rewritten as "`usageText`, spelled in full" — the complete replacement text is now given verbatim, the way D7 spells the text grammar. The `flags:` block stays one list with the three toc-only flags marked `toc only:` in their descriptions; rationale and rejected alternatives (per-verb flag sections; a bare `[flags]` usage line) recorded.
- `20260904-084550-discussion-review-r4.md` — "[NIT:design] CLI and engine can disagree on glyph-vs-path after rebasing": new **D8a**. The case is real — `quarry resolve foo.go` inside a directory named `we#ird` reaches the engine as `we#ird/foo.go` and is re-classified there as a glyph. Disposition: accepted and documented as a contract gap in `internal/cli/doc.go`, in the style `internal/engine/resolve.go` records its own three. Closing it needs the target's class to travel with the target, i.e. a new facade and engine signature, and `internal/engine/` is out of scope; both CLI-only workarounds are worse (rejecting the path makes a legitimate repository path unaddressable; escaping invents an encoding no other surface knows). Deliberately not pinned by a test, since a test would fix behaviour this task considers wrong-but-unfixable.

## Pushed Back

None. All five findings verified accurate against the discussion text and the merged code (`internal/engine/resolve.go`'s `isGlyphTarget`, `internal/cli/usage.go`'s current `flags:` block, `after/INDEX.md`'s row shape). No fix breaks existing functionality, conflicts with a documented decision, or destabilises work outside this task.
