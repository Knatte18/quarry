# Plan fix — round 2

Source review: `_mill/reviews/20260906-094312-plan-review-r2.md` (holistic, REQUEST_CHANGES,
2 BLOCKING / 2 NIT).

## Fixed

- r2 holistic, `[BLOCKING:design] Taint-case rep deletion rests on a false premise` — verified against
  `runstate.go:224-230` (`RepIsComplete` returns `s.State == "complete" && !s.BlindingFailed`) and
  `run.go:212` (the resume loop skips only a repetition `RepIsComplete` accepts). A repetition the
  taint path wrote complete-with-the-blinding-failed-flag is therefore already re-attempted in place,
  and deleting it would destroy the transcript and finding the harness preserved deliberately. Card
  30's taint branch now says "Delete nothing", states the completeness predicate as the reason, and
  card 30 closes with an explicit statement that no repetition directory is deleted on any branch.
  Card 30's `Deletes: none` note is rewritten to be literal rather than carving out the raw tree.
  Card 31's coverage requirement no longer asks for a deletion report; it asks for the resume, the
  finding text, and the same explicit "No permitted repetition deletion" assertion the reference root
  makes.

  **Divergence from `_mill/discussion.md`, recorded deliberately.** The discussion's
  `live-matrix-runs-detached` Decision mandates deleting the tainted repetition's directory "so the
  resume actually re-runs it rather than skipping a repetition that is present-but-not-counted-
  complete", and mandates reporting the deletion in the conclusion. That rationale is factually wrong
  against `RepIsComplete`: the repetition is already not counted complete. The plan keeps the
  Decision's stated *intent* — the resume genuinely re-runs the tainted repetition — and drops the
  deletion, which the harness makes unnecessary and which would lose evidence. The conclusion
  requirement is kept in the inverted form (assert no deletion occurred), so the coverage section is
  no less explicit than the reference root's.

- r2 holistic, `[BLOCKING:design] LADDER_EXIT=1 branch omits the hard-error case` — verified against
  `main.go:100` (`runCommand` wraps and returns any `ladder.Run` error, so `summarizeAndReport` at
  `main.go:98` is never reached) — a lock conflict, a `reps_effective` mismatch, a
  `verifyCardsAndPack` hash mismatch, a resume-time memory-path finding or a mid-matrix repetition
  error all exit 1 with no table written. The card's classification is restructured into two tests
  and three outcomes: test 1 is whether this invocation's own log carries a rendered table (the log
  is truncated per launch, so an on-disk `summary.json` from an earlier invocation cannot be used as
  the discriminator); no table means a hard error, with a per-fault disposition list — and the
  pack-hash mismatch case is a stop, not a resume. Test 2 (memory-path gate) now runs only when a
  table was rendered.

- r2 holistic, `[NIT:consistency] Separating-result branch leaves the renumbering unstated` — card
  32's edit 3 separating branch now states the resulting order explicitly: 1 Loomyard adopts glyphs,
  2 M4b, 3 T8, 4 More languages.

- r2 holistic, `[NIT:consistency] New results root's spelling in the roadmap is unspecified` — card
  32's edit 1 now says to spell the root `results/<RUN_DATE>-kickstart`, matching the paragraph's own
  ladder-relative convention, explicitly not the repository-relative spelling the plan uses.

## Pushed Back

_None._
