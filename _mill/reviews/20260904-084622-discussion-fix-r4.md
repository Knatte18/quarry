# Discussion fix — round 4 (extension round)

Source review: `_mill/reviews/20260904-084511-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 5 NIT, 1 of them demoted from BLOCKING by the stage's `blocking_classes: [design]` ceiling).

Round 4 was the non-progress extension round granted after round 3 (round 3's single BLOCKING title was disjoint from round 2's four). It is the last allowed round: `extension_used` is now spent, and the convergence gate's demoted-predicate is not satisfied, so this round is taken as an implicit approval at the round cap.

## Fixed

- r4 `[NIT:consistency]` "`%w: …` wrapping cannot preserve CLI message bytes" (demoted from BLOCKING) — D11 now states that `internal/cli` owns its two literals outright and re-formats them from values it already holds after an `errors.Is` match, and that passing `repopath`'s `err.Error()` through to `usageError` is forbidden. Adds a small exact-string table test in `internal/cli` for both sentences, since today's assertions are substring-only and `docs/research/output-formats/` carries no error golden.
- r4 `[NIT:decision]` "Server `Implementation.Version` has no disposition" — D4 now fixes `Version: "0.1.0"` and states explicitly that it is not part of any contract (nothing in the harness reads it), unlike the server name and tool name.
- r4 `[NIT:scope]` "`--root` ships with no test and no probe" — Testing now requires root resolution to sit behind a small exported function in `internal/mcpserver` with a table test (flag absent / relative / absolute / not-a-directory), rather than leaving the one path that rescues a falsified D3 unverified.
- r4 `[NIT:consistency]` "Moved tests both 'move' and 'stay unchanged'" — Testing now names the two files deleted from `internal/cli`, and states that the moved cases switch from the `err.(usageError)` type assertion to `errors.Is` on the new sentinels.
- r4 `[NIT:design]` "No disposition on `depth: -1` payload size on the measured surface" — D5a now records the no-cap position explicitly, with its grounds (the CLI, plan §10's compact-by-default non-goal, §4's completeness rule, and V1's `toc_dir` having the same reach so the baseline comparison is unaffected).

## Pushed Back

None.
