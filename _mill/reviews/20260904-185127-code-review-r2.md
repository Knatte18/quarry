MILL_REVIEW_BEGIN
# Review: Ladder breadth (M1) — holistic

```yaml
duration_s: 228.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-04
```

## Findings

### [NIT:consistency] task 06 fasit's `_meta.scope` is a string, siblings use arrays
**Location:** `bench/loomyard-eval/tasks/06-loomyard-cold-start-orientation.fasit.json:6`
**Issue:** Tasks 01/02's `_meta.scope` is an array of package paths; task 06's is the string `"whole repository -- no package named in the prompt"`. Card 9 does not mandate a shape, and `StripFasitMeta` drops `_meta` before scoring, so this is cosmetic only.
**Fix:** None required; note for a future fasit-authoring card to pick one shape convention if `_meta.scope` is ever machine-read.

## Verdict

APPROVE
All six batches realise their cards faithfully; cross-batch contracts (InvalidReason, ladder-toc.yaml, prematrix gates, conclusion, docs) are consistent and traced to source.
MILL_REVIEW_END
