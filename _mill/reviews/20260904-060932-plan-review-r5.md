MILL_REVIEW_BEGIN
# Review: resolve + expand (T4) — holistic

```yaml
duration_s: 368.0
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude Opus 5 (1M-context variant), Anthropic
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [BLOCKING:scope] ExpandAnswer's JSON key set is never asserted
**Location:** batch 1 Batch Tests; batch 4 cards 16–18; batch 5 card 22
**Issue:** Batch 1's Batch Tests claims the new types' "JSON tags specifically" are pinned by card 11, but card 11 marshals a `ResolveResult` only; no card anywhere marshals an `ExpandAnswer`, so its six tags (`id`, `status`, `unit,omitempty`, `head,omitempty`, `members,omitempty`, `candidates,omitempty`) ship with zero serialization coverage — `go vet` catches tag *syntax*, not a wrong key spelling or a missing `omitempty`.
**Fix:** Add an `encoding/json` marshal assertion for `ExpandAnswer` to a batch 4 card (a `found` answer plus a `not_found` one, pinning key spellings and every absent key), and correct batch 1's Batch Tests sentence to name it.

### [NIT:consistency] Batch 1's verify contradicts the verify Shared Decision
**Location:** `00-overview.md` Shared Decision "verify commands are cgo-enabled and scoped to the engine package"; `01-answer-types.md` batch yaml
**Issue:** The Decision states every batch's `verify:` is "scoped to `./internal/engine/`", but batch 1's verify is `CGO_ENABLED=1 go build ./... && CGO_ENABLED=1 go vet ./internal/engine/` — the build half is module-wide.
**Fix:** Either scope the build half or amend the Decision to name batch 1's module-wide build as its one deliberate exception.

### [NIT:consistency] Gap 1's recording site differs between Decision and card
**Location:** `00-overview.md` Decision "contract gaps are recorded in code comments"; `03-resolve.md` cards 6 and 7
**Issue:** The Decision says the external-test-unit-versus-real-directory gap is "recorded where the collision is promoted to `ambiguous`" — that is `statusForMatches` (card 6), which carries no such instruction — while card 7 records it where the collision is *read* from `dirsOf`.
**Fix:** Align the two: either move the recording instruction into card 6's `statusForMatches` doc requirements, or reword the Decision to say "where the collision flag is read".

## Verdict

REQUEST_CHANGES
One wire-contract type ships with no serialization coverage; two minor self-contradictions.
MILL_REVIEW_END
