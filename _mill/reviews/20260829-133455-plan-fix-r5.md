# Plan fix — round 5

Source review: `20260829-133128-plan-review-r5.md` (holistic, REQUEST_CHANGES, 3 BLOCKING / 5 NIT).

## Fixed

- `20260829-133128-plan-review-r5.md` — "[NIT:consistency] `run_gates` cannot derive its own deny-list". Accurate: the signature carried no `ladder`, and `deny_list_for(ladder, config)` is the suite's only deny-list source. Added `ladder` to `run_gates`' stated signature — rather than a precomputed `denied_names`, which would invite a second derivation site and defeat the single-source deny-list decision — along with the `env` argument `resolve_state_dir` now needs, and stated that `execute_run` passes the `ladder` it already holds and the scrubbed `run_env()` mapping (batch 3 card 10, batch 6 card 19).
- `20260829-133128-plan-review-r5.md` — "[NIT:consistency] `denied_tool_attempts` is both dropped and kept". Accurate contradiction across four cards. Settled on one disposition and wrote it in both places that said otherwise: the metric is **always** summarised, qualified by the probe's `denied_tools_advertised` flag. What the flag decides is whether a column of zeros may be read as evidence — when denied tools are hidden it may not, and the conclusion says so — rather than whether the column exists. Deleted the "dropped from the reported metrics" wording from cards 18 and 24.
- `20260829-133128-plan-review-r5.md` — "[BLOCKING:design] `--stage` selector is specified only inside test bullets". Accurate, and the cause was mine: a round-4 edit to card 22's CLI requirement did not match its target and landed only the test bullet, so cards 24-26 were each defined by a flag the implementation requirement never mentioned. Moved `--stage {probe,main,cold,all}`, its default, its per-stage behaviour, and the reason it exists into card 22's `Requirements:` paragraph.
- `20260829-133128-plan-review-r5.md` — "[BLOCKING:design] No disposition for a partially-confirmed cold cell". Accurate: all three dispositions assumed uniformity across the repetitions, so a cell with one confirmed-cold run and two native-fallback exhaustions fit none, was `complete: false`, and made card 27's mandatory zero exit unreachable. Added a fourth disposition, `partial`, carrying its confirmed count: recorded, excluded from the summariser's `incomplete` list and from its non-zero exit as a legitimate terminal state, and yielding no warm-versus-cold comparison, since a disjoint-range claim at `n = 3` cannot be made from fewer than three cold runs (batch 5 cards 13 and 15, batch 6 card 21, batch 8 cards 26 and 27, batch 9 card 28).
- `20260829-133128-plan-review-r5.md` — "[BLOCKING:design] Turn-ceiling truncation has no gate or disposition". Accurate and expensive as written: a `none` control that legitimately needed more than 60 turns would have looked like an unparseable answer, been retried twice into the same deterministic ceiling, and halted the whole paid matrix as a gate fault. Added `result_subtype` and `result_is_error` to the extractor so the case is detectable, gave `execute_run` a distinct `truncated` outcome, and made `run_matrix` halt on its **first** occurrence rather than after three — retrying is deterministic waste, and a truncation means the matrix-wide ceiling is wrong, so every completed run was measured under a bar one of its peers could not meet. Stated the operator's remedy as raising `max_turns` and re-running into a new dated directory, since a matrix spanning two ceilings is not a matrix (batch 2 card 7, batch 6 cards 19 and 20, batch 8 card 25).
- `20260829-133128-plan-review-r5.md` — "[NIT:consistency] Main-matrix daemons are not drained before the cold cell". Accurate — the same argument the between-repetition wait rests on, one boundary earlier and with more force, since both task daemons are certainly resident when the main matrix has just finished. Added a drain of each shared task worktree's state directory before the first cold repetition, using the same `DAEMON_EXIT_TIMEOUT_S` bound (batch 6 card 21).
- `20260829-133128-plan-review-r5.md` — "[NIT:consistency] Drift guard asserts a ladder state `load_ladder` rejects". Accurate: `load_ladder` raises on a `quarry_tools` list that is not exactly the canonical seven, so the guard's scenario is unreachable through the file path. Stated that the mutation is post-load and deliberate, that adding a genuine eighth tool would also require relaxing the canonical-seven check, and what the guard actually proves — the derivation, not the loader's tolerance (batch 1 card 4).
- `20260829-133128-plan-review-r5.md` — "[NIT:scope] `internal/cli/paths.go` missing from card 17's `Context:`". Added it — `run_env()`'s requirement reasons about `workspaceKey` precedence and the `tags-<hex>` segment, both defined there.

## Pushed Back

_(none — every finding verified as accurate, including one that traced to a failed edit of my own in round 4, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task's scope.)_
