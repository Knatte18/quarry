# Plan fix — round 4

Source review: `20260904-093521-plan-review-r4.md` (holistic, opushigh, REQUEST_CHANGES, 1 BLOCKING + 3 NIT).

## Fixed

- r4 holistic, `[BLOCKING:design] Card 11's subdirectory-relative target case is unreachable` — verified against `Run`: every test in this file passes an explicit root flag, which sets the base directory to the root, so a relative target's repository-relative form is the argument and the asymmetry assertion would have held vacuously. Changing the process working directory to make it bite is forbidden by the overview's own scratch-tree decision. The case is respelled to use an absolute target under the root, whose repository-relative echo is observably different from the argument, with the reason stated so it cannot be silently reverted to the relative form.
- r4 holistic, `[NIT:consistency] RenderExpandText found branch has no nil-Head guard` — verified: the head is a pointer field of the same shape as the directory answer the previous round guarded, so a caller-built found answer with no head would panic instead of honouring the renderer's one-trailing-newline promise. The found branch now carries the same guard with the same rationale, and card 5's test table now names all three totality guards explicitly.
- r4 holistic, `[NIT:consistency] Card 2's claim about the existing TOC fixtures is wrong` — verified: two of the four fixtures in that file already nest their Go files under a subdirectory; only the two sentinel fixtures put one at the root. The sentence now names the two it is actually true of and points at the two nested ones as the pattern to copy. The instruction it justifies is unchanged, because it was correct either way.
- r4 holistic, `[NIT:scope] Card 17 names before-side files its Context does not list` — the seven before-side outputs the new index rows reference are now listed in card 17's `Context:` and named in its requirements, so the card no longer depends on deriving them from the parent index.

## Pushed Back

_(none — every finding was factually accurate and no fix caused harm under the decision tree.)_
