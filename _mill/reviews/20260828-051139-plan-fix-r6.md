# Plan fix report — round 6

Source review: `_mill/reviews/20260827-192430-plan-review-r6.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 5 NIT).

This is the final round: the operator raised the holistic round cap from 4 to 6 and instructed a handoff after round 6 regardless of verdict. All seven findings are fixed below, and the plan is approved without a round-7 re-review — see "Unverified at handoff".

## Fixed

- r6 holistic, "[BLOCKING:scope] internal/quarryengine/doc.go is never edited by any card" — verified all four stale statements in that file: the "no call hierarchy, no implementation" bullet, the package-layout bullet naming only References/Definition/Symbol, the typed-error enumeration omitting `ErrBuildTagsUnsupported`, and the `daemon/daemontest` seam enumeration omitting the three `ConnKind` constants. Took the finding's second disposition — one owning card rather than four cards touching one file — adding card 30 to batch 7, which reconciles all four after the code they describe has landed. Added `internal/quarryengine/doc.go` to `## All Files Touched` and updated batch 7's card count and scope prose.
- r6 holistic, "[BLOCKING:scope] Live tier stays silently skippable for anyone following the README" — verified: the README's `## Testing` section claims the live tier requires gopls on `$PATH`, while the `gopls-lives-in-the-toolchain-cache-not-on-path` Shared Decision establishes it is not, and the `PATH` prefix that makes the tier actually run lives only in per-batch `verify:` lines that do not outlive the plan. Card 26 now requires updating that section with the toolchain-cache prefix and the silent-skip consequence.
- r6 holistic, "[NIT:consistency] Card 10 under-scopes its daemon/doc.go edit" — extended card 10's requirement from the `EnsureServer` signature line to also cover the native-strategy paragraph and the `ConnKindNative` bullet, both of which describe a shared daemon behind the proxy that a private spawn does not have. Both are to be qualified as describing the empty-tag-set case, with the non-empty behaviour stated.
- r6 holistic, "[NIT:consistency] query's package doc still claims three entry points and one LSP call" — card 15 now requires widening `internal/quarryengine/query/refs.go`'s package doc to name `Callers` and `runOnConnection` and to drop the "differ only in which single LSP call they make" claim, in the file that card already edits.
- r6 holistic, "[NIT:design] Card 15 does not say whether the pre-verification phases run under SkipVerification" — settled it explicitly: under `SkipVerification` only `textDocument/definition` and `textDocument/references` are issued. The definition call stays because its result is the returned `declaration` value the CLI's declaration exclusion depends on; `textDocument/implementation` and the directional-mode `documentSymbol` phase feed only the match set, which is unused when nothing is filtered. This also settles what card 16's `--no-verify` fake must answer.
- r6 holistic, "[NIT:consistency] filterWithin's own doc comment keeps the retracted rationale" — card 20's "do not modify" instruction now scopes to the three function bodies, with an explicit carve-out requiring `filterWithin`'s doc comment to lose the interface-conflation rationale card 21 retracts. Body stays byte-identical.
- r6 holistic, "[NIT:consistency] daemontest's package doc still names only two callers" — card 16 now updates that package comment alongside the `ConnKind` constants, naming all four callers or rewording the sentence so it stops enumerating callers it cannot keep current.

## Pushed Back

_None._

## Re-validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` pass, and `_plan_validate.run` returns zero findings with an empty `skip_checks`.

## Unverified at handoff

These fixes were not re-reviewed. Round 6's verdict line was "Design and sequencing are sound; four package-doc/README statements the plan itself falsifies are unowned" — every finding in it was a stale-documentation or ownership gap, and none was a correctness or sequencing defect, which is the basis on which handing off after this round is a reasonable risk rather than a blind one. The residual risk is concentrated in card 30 and the card 26 README requirement, both added this round and neither seen by a reviewer.
