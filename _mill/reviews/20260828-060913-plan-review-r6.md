MILL_REVIEW_BEGIN
# Review: Add file/dir toc verbs (Tree-sitter-backed) — holistic

```yaml
duration_s: 457.0
verdict: REQUEST_CHANGES
reviewer_model: opushigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-28
```

## Findings

### [NIT:consistency] Card 54 declares `Edits: none` but its own Requirements mandate edits
**Demoted-from:** BLOCKING
**Location:** batch 8 / card 54 (stale-prose sweep verification)
**Issue:** Card 54's Requirements explicitly instruct: "owned by an earlier batch — `quarry/facade_test.go` belongs to batch 6 card 35, and both guard-test files to batches 1 and 2 — ... make the fix here, in a commit of this card's own." That names three concrete edit targets (`quarry/facade_test.go`, `internal/quarryengine/layering_test.go`, `internal/quarryengine/seam_enforcement_test.go`), yet the card's own `Edits:` field is `none` and only lists these files under `Context:` (read-only). This is a direct self-contradiction between the card's file manifest and its prose instructions.
**Fix:** List `quarry/facade_test.go`, `internal/quarryengine/layering_test.go`, and `internal/quarryengine/seam_enforcement_test.go` (and any other file the sweep is authorized to correct outright, e.g. `README.md`) under a conditional `Edits:` for card 54, or state explicitly that this card's write scope is not fixed in advance and is exempted from the normal Edits contract.

### [BLOCKING:design] CGO_ENABLED=0 guard only fires under `go test`, not `go build`/`go vet`
**Location:** batch 1 / cards 2–3 (loud CGO_ENABLED=0 build guard)
**Issue:** The batch's own scope text promises "a loud build-time guard so a CGO_ENABLED=0 build fails with a sentence a human can act on rather than a linker dump," but the mechanism is `TestCGOEnabled_BuildLinksC`, a `_test.go` assertion that only runs under `go test`. A `CGO_ENABLED=0` invocation of `go build ./...`, `go vet ./...` (the plan's own top-level `verify: go vet ./...` in `00-overview.md`), or `go run` never executes any `_test.go` file, so it hits the raw cgo/compiler failure the guard exists to replace, with no custom message at all.
**Fix:** Either narrow the batch's stated goal to "fails loudly under `go test`" (matching card 3's own more precise "first package `go test ./...` reaches" language), or note in the plan that `go vet`/`go build` remain unguarded and that this is an accepted limitation of pure-Go build tooling.

## Verdict

REQUEST_CHANGES
Two contained fixes needed: card 54's Edits/Requirements contradiction and the CGO guard's stated-vs-actual scope.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
