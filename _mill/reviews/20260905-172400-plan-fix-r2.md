# Plan fix — round 2

Source review: `20260905-171504-plan-review-r2.md` (holistic, opushigh, REQUEST_CHANGES,
2 BLOCKING + 1 NIT). All three verified and fixed; nothing pushed back.

## Fixed

- `[BLOCKING:design]` "Depth-target probe names no --root and no binary" — verified against
  `internal/cli/cli.go`'s `Run` step 4 and `runTOC` step 1: without `--root` the candidates would
  resolve against the quarry repository, where none of them exists. Fixed by removing the probe
  entirely rather than by spelling it out: the probe was run during this review round instead, so
  the plan now names its result. See the next finding.
- `[BLOCKING:scope]` "Candidate list likely exhausts to the unbounded fallback" — verified, and
  worse than the reviewer could confirm from HEAD. Measured against a clone detached at the pin
  `72c23d9`, with the CLI built from this worktree: `internal/shedadapters` has no subdirectory at
  all; `internal/reedengine` has one but 616 symbols at `--depth 1`; `internal/fabricengine` is
  larger; and the ladder's own guaranteed fallback, `.` at `--depth 1`, yields **zero** symbols,
  because no Go file sits at the root or one level below it — a golden that would assert the exact
  opposite of what the row exists to assert. Exactly three directories in the repository at that pin
  have a subdirectory holding Go files. Card 14 now fixes the invocation as
  `toc --text --view glyphs --depth 1 internal/boardengine`, carries all four measurements and the
  reasoning, and instructs the implementer not to re-run the ladder. The `--text` format is the size
  bound doing its work: 172 symbols is 172 text lines against a ~200-line bound and a 254-line
  largest existing golden, where the same answer in JSON is roughly 1,200 lines. No JSON coverage is
  lost — two of the four other new goldens are JSON, and card 10's machine-independent
  `internal/cli/cli_test.go` cases already pin the JSON key set for a no-symbols-flag
  `toc --view glyphs` invocation. Card 15's inspection step and card 16's index-row instruction
  were updated to the named target and the stated numbers, and batch 3's Batch Scope now records
  why the probe is retired rather than confirmed.
- `[NIT:consistency]` "Two doc comments the plan makes false are not disposed" — verified at
  `quarry/doc.go:6` and `quarry/repo.go:1-2`. Card 5 now also rewrites the "The package adds no
  behaviour of its own" sentence, with an instruction not to weaken it into vagueness — the posture
  is a real property of the other four queries. Card 4 now also updates `quarry/repo.go`'s file
  header, which named only Repo, Open and the delegating TOC query. The
  `doc-comments-carry-the-reasoning` Shared Decision's enumeration was extended from four stale
  comments to six, adding both, plus `TestParseArgs_FourVerbGate` (already handled in card 9 in
  round 1 but missing from the enumeration).

## Pushed Back

_(none)_

## Note on artefacts produced while fixing

Settling the depth target required a real checkout and a built binary. Both were created under the
gitignored `.scratch/` — `.scratch/loomyard-pin` (a clone of `/home/knatte/Code/loomyard/wts/loomyard`
detached at `72c23d9`) and `.scratch/quarry` (built from `./cmd/quarry`). Nothing outside `.scratch/`
was touched, and the source repository was only read. Batch 2 card 6 already handles the
already-exists case for the checkout, so its presence is a head start rather than a conflict.
