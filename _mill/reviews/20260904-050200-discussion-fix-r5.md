# Discussion fix — round 5

Source review: `_mill/reviews/20260904-045403-discussion-review-r5.md` (APPROVE, 0 blocking, 5 nit;
2 demoted from BLOCKING by the stage's blocking-class ceiling).

Under `--orch` every finding is treated as FIX; there is no PUSH BACK path. All five were verified
against the source tree before being acted on.

## Fixed

- r5 "Scope puts the collision fixture in testdata/" — the Scope bullet said the `_test`-directory
  collision was committed under `testdata/`, contradicting D17 and D6. Reworded so it names the
  committed and run-time halves separately and points at D17's inventory as the authority.
- r5 "Two contract gaps or three" — Scope and the closing Q&A both still said two after D18 gained
  the intermediate-symlink gap in round 4. Both now say three, and "both gaps" in D18's rationale is
  now "each gap". The count is a deliverable count — one comment card per gap — so this was not
  cosmetic.
- r5 "\"Signature only, no body change\" is not achievable as written" — verified: `loomyardRepo`'s
  body calls `t.Helper`/`t.Skip`/`t.Skipf`/`t.Fatalf` seven times, so renaming the parameter to `tb`
  would rewrite it. The widening now keeps the parameter named `t` (`loomyardRepo(t testing.TB)`),
  which makes "one line, body untouched" true as stated, in all three places that describe it.
- r5 "\"D17's skip/fail rule\" names this document's D17" — T4's own D17 is the fixture inventory and
  carries no gate rule. Both citations now read "T3's D17", matching D16.
- r5 "D17's stated reason for the `.scratch/` collision tree is not the binding one" — the
  conclusion was right and the premise was wrong. Verified: no T3 test enumerates `testdata/`'s
  children, and `roundtrip_test.go`'s `tupleSetDiff` is a duplicate-tolerant multiset comparison.
  The assertion that actually forbids a committed `testdata/foo/` is
  `TestSpansOf_LiteralFirst`'s `collision == false` for `internal/engine/testdata/foo_test`. D6, D17's
  table and D17's rationale now cite that test by name, and the placement rule is restated as
  per-case named assertions rather than a blanket "do not disturb `testdata/`".

## Pushed Back

None. PUSH BACK is unavailable under `--orch`, and every finding was independently verified as
correct before being fixed.
