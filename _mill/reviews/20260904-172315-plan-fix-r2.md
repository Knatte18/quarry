# Plan fix — round 2

Source review: `_mill/reviews/20260904-171840-plan-review-r2.md` (holistic, REQUEST_CHANGES,
blocking_count 1, nit_count 4).

## Fixed

- r2 `[BLOCKING:design] Batch 4 verify can go red inside batch 1's TDD window` — batch 4 now declares
  `depends-on: [1, 2, 3]` in both the overview's Batch Index and its own frontmatter, and its Batch
  Scope states why the batch-1 edge exists separately from the batch-2/3 edges. The finding is
  correct: batch 4's `verify:` runs the whole `ladder` package, batch 1 deliberately lands failing
  `e2e_test.go` assertions in card 4 that stay red until card 5, and with both batches unordered
  batch 4 could have failed on batch 1's mid-TDD state rather than on its own diff. Took the
  dependency-edge option over the reviewer's alternative of narrowing batch 4's verify to a `-run`
  filter excluding `TestE2E`: the edge costs no extra serialisation (batch 5 already depends on
  both), while the filter would leave the package's e2e regressions unguarded at exactly the boundary
  before real budget is spent.
- r2 `[NIT:scope] Card 2 does not assert the rune-safe truncation card 1 mandates` — added a fifth
  case to card 2: an over-length detail built from multi-byte runes, asserting `utf8.ValidString` on
  the rendered value, plus the `unicode/utf8` import it needs. The finding is correct — the existing
  `len` and trailing-`...` assertions are both satisfied by a naive byte slice that splits a rune, so
  card 1's one non-obvious requirement was shipping unverified.
- r2 `[NIT:consistency] Batch 4 scope says "every control cell"; card 13 covers three of four` —
  added `a0-none` to card 13's id list, so the gate now covers every control the amended file
  declares rather than only the three this matrix runs, and the Batch Scope's wording becomes true as
  written. Chose widening the test over narrowing the prose: the assertion is a pure function of
  committed files and costs nothing, and `a0-none` passing is already established — T7 ran it as a
  control for five repetitions, which it could not have done had `CheckRenderedControlPrompt`
  returned a finding for it.
- r2 `[NIT:scope] Card 6 names ExplorationRule without score.go in Context` — added
  `bench/loomyard-eval/ladder/internal/ladder/score.go` to card 6's `Context:`. Kept the identifier
  in the rationale rather than dropping it: it is the reason the two schema blocks must stay
  identical, and an implementer who cannot check that claim is worse off than one who can.
- r2 `[NIT:decision] Card 7's subject-swap remedy leaves the task id undisposed` — card 7 now states
  that a swap keeps `02-shedadapters-exploration` as the task file name, fasit file name,
  `_meta.task` value, `tasks:` key and both `configs[].task` values, changing only the prompt body,
  the scope section and the notes; and that card 12's `droppedSubstrings` are then picked from the
  file as actually written, since `8.4k lines` goes stale. The id is an opaque key `validate` matches
  against, so renaming it would ripple through the ladder file, batch 4's cards and this plan's own
  `All Files Touched` for no gain.

## Pushed Back

_None. All five findings verified accurate against the files and line references they cite, and none
of the fixes trips a HARM CHECK category. The one structural change — batch 4's new dependency on
batch 1 — adds an edge to the DAG without changing any batch boundary, card numbering, or file
assignment, and `_plan_dag.validate` accepts the result._
