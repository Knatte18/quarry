# Discussion fix — round 1

Source review: `_mill/reviews/20260909-055824-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260909-055824-discussion-review-r1.md` — "[NIT:consistency] \"resolve is also called directly elsewhere\" overstates its callers": verified against the tree (`internal/engine/resolve.go:401` is the sole production caller; `internal/engine/resolve_test.go:651` the sole white-box one). Rewrote the Technical context rationale to name both call sites explicitly and to state that no other production sites exist, while keeping the verifier's placement in the unexported worker and grounding that placement in the white-box path plus future in-package callers.
- `20260909-055824-discussion-review-r1.md` — "[NIT:consistency] \"existing NameReasons facade coverage\" does not exist": verified — `grep NameReasons quarry/` matches only `quarry/quarry.go:231,234`, no test. Rewrote the Testing facade paragraph to say the assertion is written fresh, state the exact identity property to assert, and add the missing `quarry.NameReasons` twin assertion in the same test so the pattern claim becomes true.

## Pushed Back

_None._ Both findings were factually accurate and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope.
