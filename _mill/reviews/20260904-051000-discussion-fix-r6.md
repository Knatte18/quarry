# Discussion fix — round 6

Source review: `_mill/reviews/20260904-050128-discussion-review-r6.md` (APPROVE, 0 blocking, 4 nit;
2 demoted from BLOCKING by the stage's blocking-class ceiling).

Round 6 is the configured `rounds` cap. The convergence gate's demoted-predicate is not satisfied
(two findings demoted), so this round is an **implicit approval at the round cap**, recorded in the
commit message per the gate's rule.

Under `--orch` every finding is treated as FIX; there is no PUSH BACK path. All four were verified
against the source tree before being acted on.

## Fixed

- r6 "Testing 16 asserts Expand Members under a collision" — correct and self-contradictory as
  written: D14's collision row gives `Expand` no `Head` and no `Members` for any collision with a
  match, which is exactly what Testing 15c asserts on the same tree, so Testing 16 could never have
  produced the `Members` it required. Testing 16 now asserts `Candidates` on **both** verbs over the
  collision tree, and member-sort coverage moves to the separate `testdata/methods/` case it already
  named. D17's ordering row updated to match.
- r6 "D17's fixture inventory omits Testing 13, 14 and 15's kind cases" — D17 claimed to enumerate
  every fixture the Testing section consumes and did not. Added three rows sourcing them from
  `testdata/glyphs/` (`iface.go`'s `Iface`, `decls.go`'s `Weekday` and `UngroupedConst`, `inits.go`'s
  `init`), and stated the distinction the rejected-alternatives list had blurred: *adding a file* to
  that package perturbs T3's `glyph_test.go` symbol-list assertions, while *reading* declarations
  already in it cannot. The rejected alternative is now scoped to additions.
- r6 "D5/D6 precedence unstated for `init` under a collision" — D14 stated its row order for `Expand`
  and D5/D6 never did for `Resolve`, leaving several `init` under a collision undecided between
  `multipart` and `ambiguous`. D5 now states the order explicitly (zero-match, then collision, then
  the kind rows), D6 says the check runs after the zero row and before all others, and Testing 1's
  table gains the two rows that pin it.
- r6 "Two verifiable counts are wrong" — one was, one was not, and both are now at their verified
  values. `loomyardRepo`'s body makes **five** `t.*` calls (`Helper`, `Skip`, `Skipf` twice,
  `Fatalf`), not seven: corrected in both places. The reason count was re-verified two ways —
  `glyph/errors.go` declares **15** `Reason` constants and its `Reasons` slice holds 15 entries — so
  "the other fourteen" was already correct; the sentence now names the total explicitly so the
  arithmetic is checkable rather than implied. The finding's substantive claims (every `t.*` call is
  a `testing.TB` method; one grammar owns every rejection) verify as written and are unchanged.

## Pushed Back

None. PUSH BACK is unavailable under `--orch`. Recorded for accuracy rather than as a pushback: the
"16 declared reasons" premise in the fourth finding is off by one — the file declares 15 — but its
requested action (correct or drop the numbers) was carried out either way.
