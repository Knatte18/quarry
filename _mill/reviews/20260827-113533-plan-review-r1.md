MILL_REVIEW_BEGIN
# Review: Thin quarry/ facade over internal/quarryengine — holistic

```yaml
duration_s: 630.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-27
```

## Findings

### [NIT:consistency] Layering guard spec contradicts itself on `query` test imports
**Demoted-from:** BLOCKING
**Location:** Batch 3 / Card 11 **Issue:** The allowed-import table states `query` may import `daemon` with no test/prod split, yet the same paragraph requires the guard to fail on "a `query` test importing `daemon` directly instead of through `daemontest`" — an import-path check cannot fail an import the table already permits for that package. **Fix:** Give `query`'s own `_test.go` files a narrower allowed set (root/registry/lsp/daemontest, no direct `daemon`) distinct from `query` production files, and state that split explicitly in the table.

### [BLOCKING:scope] Card 7 edits a file absent from its own Edits: list
**Location:** Batch 2 / Card 7 **Issue:** Requirements instructs rewriting `toolchain_integration_test.go`'s line-28 `userCacheDir` assignment to use the `daemontest` helper, but that file is not in card 7's `Edits:` (only `toolchain_test.go` and `ensureserver_test.go` are listed) and appears nowhere else in batch 2 except as a card 6 Move-target. **Fix:** Add `internal/quarryengine/daemon/toolchain_integration_test.go` to card 7's `Edits:`.

### [BLOCKING:scope] Cards 8/9 retarget `DetectLanguage`/`LoadRegistry` without their files in Context
**Location:** Batch 2 / Card 8, Card 9 **Issue:** Card 9 needs `LoadRegistry`'s and `DetectLanguage`'s exact signatures to write byte-for-byte facade delegating wrappers, and card 8 retargets `DetectLanguage` call sites to `registry.DetectLanguage`; both cards' `Context:` lists only `internal/quarryengine/registry/registry.go`, omitting `registry/load.go` (declares `LoadRegistry`) and `registry/detect.go` (declares `DetectLanguage`). **Fix:** Add both files to `Context:` on cards 8 and 9.

### [BLOCKING:scope] Card 3 needs ensureserver.go's doc comment but it isn't in Context
**Location:** Batch 2 / Card 3 **Issue:** Requirements says to carry over `ensureserver.go`'s existing doc comment (lines 26–31) onto the new `Logger` var, but `Context:` lists only `quarry/lspclient.go` and `quarry/refs.go`, not `quarry/ensureserver.go`. **Fix:** Add `quarry/ensureserver.go` to card 3's `Context:`.

### [NIT:consistency] Card 7's "three call sites" undercounts its own line list
**Location:** Batch 2 / Card 7 **Issue:** Requirements says "rewrite its three call sites (lines 43, 50, 88, 93, 133, 140)" — that's six line numbers, one `withTempUserCacheDir` call plus one `withFakeInstaller` call in each of three tests, verified against `toolchain_test.go`. **Fix:** Say "six call sites across three tests" instead of "three call sites."

## Verdict

REQUEST_CHANGES
Layering-guard test spec self-contradicts on `query` test imports; four cards omit files their own Requirements name from Context/Edits.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 3._
MILL_REVIEW_END
