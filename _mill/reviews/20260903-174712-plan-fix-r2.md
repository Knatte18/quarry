# Plan fix — round 2

Source review: `20260903-174030-plan-review-r2.md` (holistic, REQUEST_CHANGES, 3 BLOCKING / 5 NIT).

## Fixed

- r2 `[BLOCKING:scope] Card 30 names ResolveQuarryRepoRoot without worktree.go in Context` — added
  `internal/ladder/worktree.go` to card 30's `Context:`.
- r2 `[BLOCKING:scope] Cards 28 and 31 read the score record without score.go in Context` — added
  `internal/ladder/score.go` to both cards' `Context:`.
- r2 `` [BLOCKING:design] `{target_dir}` is described as an existing ladder-file contract `` — the
  claim was false and is removed. Card 22 now states the placeholder is a **new contract this plan
  defines**, that no ladder file uses it today and that the migrated file ships with no argument
  list, and requires it documented in two places so the MCP-server task spells it identically: a doc
  comment on the config-document function, and one line in the migrated ladder file's header comment
  (added to card 5's requirements).
- r2 `[NIT:consistency] BuiltinTools declared in batch 6, used in batches 3 and 4` — the declaration
  moved from `run.go` (batch 6) to `config.go` (batch 1, card 3), the earliest file in the DAG, so
  every consumer is strictly downstream. Card 26 now says it uses that slice rather than declaring
  it. The overview decision was updated to name the new home and to state the one site that cannot
  reference the identifier — the fake binary, a separate `package main` — together with how it
  receives the set instead (an environment variable the test builds from the slice); card 33 states
  the same, so the fake still never spells the names.
- r2 `[NIT:decision] Card 5 leaves the annex/compact bullet undisposed` — card 5 now gives that
  bullet an explicit disposition: it **survives** as forward work, since annex delivery and the
  compact output form need engine features that do not exist and are out of this harness's scope
  rather than out of the project's, and the entry is the record of why the two deleted ladder files'
  subject matter has no successor here.
- r2 `[NIT:design] BuildServer has no working directory for a relative build target` — the signature
  is now `BuildServer(ctx, r Runner, quarryRepoRoot, buildTarget, outPath string)`, the command's
  working directory is the quarry repository root, and card 24 asserts that recorded directory.
- r2 `[NIT:scope] Summarize's behaviour with no provenance record is unspecified` — card 28 now fixes
  the disposition as an error naming the missing file, with the reason (the run subcommand writes
  the record before its first repetition, so a raw tree without one is hand-assembled or truncated,
  and an empty incomplete list would make an unknowably short root read as finished). Card 31 gained
  the matching test case.
- r2 `[NIT:consistency] Batch 8 claims to exercise both subcommands` — the scope paragraph now states
  the true dependency (the summariser and table renderer batch 7 adds) and says explicitly that the
  binary's flag wiring is deliberately untested, with the reason and the hand-run done-criterion that
  covers it instead.

## Pushed Back

_None._
