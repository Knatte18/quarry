# Discussion fix — round 3

Source review: `_mill/reviews/20260905-164618-discussion-review-r3.md` (APPROVE, 0 BLOCKING, 3 NIT,
1 demoted from BLOCKING by the stage's `blocking_classes` ceiling).

## Fixed

- r3 · "JSON wire type for the answer is undeclared" (`[NIT:consistency]`, demoted-from BLOCKING) —
  `glyphs-json-shadow-struct` now declares `glyphsEnvelope`, the one value `RenderGlyphsJSON` hands
  to `renderJSON`, with its `target`/`symbols`/`incomplete` tags; `GlyphsAnswer` is respelled with
  **no** JSON tags, and the reason is stated: tagged, its own `json.Marshal` would emit
  `"signature": ""` per symbol and disagree with the renderer under the same type's tags. The
  `"symbols": []` and `make(...)` rules moved onto the envelope, where they belong.
- r3 · "Depth golden's directory target is unnamed and unbounded" (`[NIT:scope]`) — the Testing
  section now states that the file inventory and all counts are unconditional (the golden is always
  committed; only the target string varies), gives an ordered four-candidate selection rule with a
  mechanical probe (`quarry toc --depth 1 <candidate>`, first with a `dirs` entry carrying `files`),
  ends it at `.` as a guaranteed fallback rather than a "no suitable directory" outcome, sets a
  ~200-line size bound, and requires the chosen target be recorded in `testdata/INDEX.md`. Also
  states why `internal/logger` cannot serve (its committed golden has no `dirs` key at the pin).
- r3 · "\"Absent line means absent symbol\" is unqualified under `--depth N`" (`[NIT:design]`) —
  `incomplete-is-explicit` gains a "Scope of the invariant" bullet: the promise holds for the frozen
  `--depth all` preset only; depth truncation contributes nothing to `Incomplete`, and cannot,
  because a depth-cut `DirAnswer` is indistinguishable from an empty leaf to a pure projection. The
  §5 documentation paragraph must carry the same scoping.

## Pushed Back

None. All three findings verified accurate against the source and the discussion text; under
`--orch` the PUSH BACK path is unavailable in any case.
