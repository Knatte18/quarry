# Plan fix — round 1

Source review: `_mill/reviews/20260904-171115-plan-review-r1.md` (holistic, REQUEST_CHANGES,
blocking_count 1, nit_count 5).

## Fixed

- r1 `[BLOCKING:scope] Card 1 Context omits run.go` — added
  `bench/loomyard-eval/ladder/internal/ladder/run.go` to batch 1 card 1's `Context:`. The finding is
  correct: card 1's Requirements tells the implementer to mirror `writeServerConnectFailure`'s error
  wrapping, and that function is in `run.go:586`, which the card did not allow it to read.
- r1 `[NIT:consistency] Decision exempts cards 7 and 9, but card 8 also reads the foreign checkout` —
  rewrote the overview decision, renaming it `cards-that-read-the-pinned-checkout-freely` and
  extending it to card 8 with its own rationale (a subject survey for a shape that names no package
  cannot be bounded). Card 8's Requirements already directed that survey, so the decision was the
  half that was wrong.
- r1 `[NIT:consistency] Card 3 calls the new fixture variant the "fifth"` — batch 1 card 3 now
  enumerates the five existing variants and calls `result_error` the sixth. Note that
  `_mill/discussion.md` carries the same off-by-one twice (`m2-cause-taxonomy-and-exit-code` and the
  Technical context's fixture paragraph both say "fifth"); the plan is corrected here and
  `discussion.md` is left as it is, since mill-plan does not own that file. The count has no bearing
  on the change itself — the variant is added to the `switch` either way.
- r1 `[NIT:consistency] Card 8 sources the schema block from a file batch 2 may not have written yet`
  — batch 3 card 8 now names `01-reed-geometry-exploration.md` as the single source to copy the
  schema block from, and states why: batch 3 declares `depends-on: []` and can run before batch 2's
  card 6 adds that section to task 02. `01-...md` is already in card 8's `Context:`.
- r1 `[NIT:design] sanitizeDetail's length bound is ambiguous in Go` — batch 1 card 1 now states the
  bound in **bytes** measured by `len`, specifies truncation to at most
  `invalidReasonDetailMaxLen - 3` bytes without splitting a UTF-8 rune followed by the three-byte
  ASCII `...`, and says why the ASCII form is chosen over `…`. Card 2's matching assertion now
  asserts against `len` explicitly. The card also now names the `strings` import `runstate.go` needs
  and does not have today.
- r1 `[NIT:consistency] Card 10's header pass says "update the --cells list" where none exists` —
  batch 4 card 10 now says to **add** a `--cells` line and states that the committed run command
  carries `--config` and `--results` only.

## Pushed Back

_None. All six findings verified accurate against the files they cite, and none of the fixes trips a
HARM CHECK category — every one is a wording or allowlist change inside `_mill/plan/`, with no effect
on the plan's DAG, batch boundaries, card numbering or scope._
