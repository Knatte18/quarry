# Plan fix — round 2

Source review: `_mill/reviews/20260827-114727-plan-review-r2.md` (verdict REQUEST_CHANGES, 2 BLOCKING, 2 NIT, reviewer sonnetmax).

## Fixed

- `20260827-114727-plan-review-r2.md` — "[BLOCKING:scope] `builtins()` is used well beyond load.go/detect.go": accurate. Confirmed the call sites: `ensureserver_integration_test.go` 44, 50, 68, 72, 132, 145, 176 plus a line-6 doc-comment mention; `toolchain_integration_test.go` 41; `supervised_integration_test.go` 46; `refs_integration_test.go` 71, 93, 207, 245 plus a line-180 mention. All four files are `//go:build lsp`, which is why the earlier production-only dependency analysis missed them. Corrected card 5's claim, and added the retarget to `registry.BuiltinRegistry()` — whose body is exactly `return builtins()`, so no new export is needed — to card 6 (daemon's three files) and card 8 (query's one).
- `20260827-114727-plan-review-r2.md` — "[BLOCKING:scope] `repoRoot(t)` test helper crosses the query→daemon boundary in the disallowed direction": accurate. `repoRoot` is defined once at `refs_integration_test.go:59` and called from `ensureserver_integration_test.go` 50, 71, 135 and `supervised_integration_test.go` 50. Resolved by duplication rather than by the suggested `daemontest` home — see the next entry for why `daemontest` cannot serve `daemon`'s own tests. `query` keeps the original definition; card 6 adds an independent copy to `ensureserver_integration_test.go`. Both copies must walk four `filepath.Dir` levels rather than the original two, since the files move from `quarry/` (one below the module root) to `internal/quarryengine/{daemon,query}/` (three below); keeping two levels silently yields a real but wrong directory, so both cards state the count explicitly. Recorded the duplication as a Shared Decision in `00-overview.md` and corrected `_mill/discussion.md`'s claim that only one test seam crosses a package boundary.
- **Beyond the finding, but forced by it:** investigating `repoRoot`'s disposition surfaced that card 7 as written did not compile at all. It instructed `toolchain_test.go`, `toolchain_integration_test.go`, and `ensureserver_test.go` — all in-package `package daemon` files — to import `daemontest`, which imports `daemon`. Go rejects that as `import cycle not allowed in test`. Verified against a scratch module rather than assumed. Card 7 is rewritten: the in-package helpers in `toolchain_test.go` stay exactly as they are, card 7 no longer edits any file (it only creates `daemontest`), and `daemontest` is documented as serving callers outside `package daemon` — today only `query`'s `refs_test.go`. `_mill/discussion.md`'s *toolchain test seam* Decision is corrected to match; only its `query` half survives.
- **Also beyond the finding:** `refs_integration_test.go` lines 75 and 193 build a fixture path with `filepath.Join(root, "quarry", "detect.go")` and feed it to `findFuncPosition`. Card 5 moves `detect.go` to `internal/quarryengine/registry/`, so both paths would resolve to a non-existent file. Added the retarget to card 8.
- `20260827-114727-plan-review-r2.md` — "[NIT:design] card 4's `documentSymbol`/`DocumentSymbol` \"collision\" is not a real Go conflict": accurate — method names live in the receiver's method set, not the package identifier scope, so the type and the method could both be named `DocumentSymbol`. Kept the pluralized `DocumentSymbols` as an explicit readability choice for a slice-returning method, and rewrote the rationale so it no longer claims to be avoiding a compile error.
- `20260827-114727-plan-review-r2.md` — "[NIT:consistency] `layering_test.go` has no package-count floor, unlike `seam_enforcement_test.go`": accurate — `discussion.md` promises both guards carry one and only card 10 specified it. Added the same at-least-six-package floor to card 11, plus an explicit note that `daemon`'s in-package tests never import `daemontest` and the table must not encode an expectation that they do.

## Pushed Back

None.
