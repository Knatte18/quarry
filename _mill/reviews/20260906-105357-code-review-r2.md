MILL_REVIEW_BEGIN
# Review: M4 matrix run: execute the descoped kick-start batch (cards 29-32) — holistic

```yaml
duration_s: 224.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-06
```

## Findings

No blocking or nit findings this round.

Verification performed: cards `07-e0-names.md`/`07-e1-pack.md`/`07-e2-files.md` `Uses:` lists are
byte-identical and match `pack_targets` in `ladder-kickstart.yaml` (9 entries, order preserved,
matching `prematrix_test.go`'s `TestPreMatrix_Kickstart*` invariants). `07-e1-pack.md`'s sentinel
block carries exactly the 9 resolved targets, two lines each, no docstring leakage, extractable via
`ExtractPackBlock`. `07-e2-files.md`'s `Files:` list is the correct 7-file dedup of those 9 targets.
No contingency substitution occurred (`pack-resolve.json`/`provenance.json.kickstart_pack.targets`
all 9 unchanged), so no cross-file substitution discipline was needed.

Recomputed both Mann-Whitney U statistics in `conclusion.md` independently from its own
per-repetition table: turns (`R1=55, R0=155, U1=0, U0=100`) and cost_usd (`R1=74, R0=136, U1=19,
U0=81`) both check out, both identities (`R1+R0=210`, `U1+U0=100`) hold, both reject the null at
U≤27 in the predicted direction — matches the conclusion's stated verdict.

`docs/roadmap.md`'s four required edits are all present and self-consistent: the standing-rule
paragraph names `results/2026-09-06-kickstart` as a separate sentence (not folded into the
three-root negative-finding list); point 1 (M4 matrix run) is removed and the remaining points
renumbered 1–3, then M4b re-inserted as point 2 (Loomyard=1, M4b=2, T8=3, More languages=4) per the
separating-result branch; the rewritten closing clause on point 1 refers to the conclusion rather
than to a deleted point; the `Updated` date line matches. No dangling "point 1" reference remains.
`provenance.go`/`pack.go` source confirms the mechanics `conclusion.md`'s coverage section describes
(dirty-check runs before any write in both `Pack` and `CollectInvocation`, `MergeProvenance` takes
the latest invocation's `quarry_dirty`), so those narrative claims are grounded, not fabricated.

The prior non-blocking item (self-referential framing of the two `quarry_dirty: true` pack
invocations at `conclusion.md:196-201`) stands: no new diff, reproducible failure, or convention
surfaced this round that would justify escalating it. `provenance.json` confirms both dirty
invocations (0 and 1) pre-date the measured `run` invocation (2, `quarry_dirty: false`), so the
Shared Decision's measurement-validity purpose is still met even though the preflight step was
literally missed twice on the pack side.

## Verdict

APPROVE
All four cards' artefacts, the statistics, and the roadmap edits are internally consistent and match the plan.
MILL_REVIEW_END
