# Plan fix — round 2

Source review: `_mill/reviews/20260905-115935-plan-review-r2.md` (holistic, REQUEST_CHANGES, 4
BLOCKING + 3 NIT, one of the NITs demoted from BLOCKING by the stage ceiling).

## Fixed

- r2 holistic, `[BLOCKING:scope] Card 17 misses text_test.go's own Dir: literals` — verified at
  `quarry/text_test.go:350` and `:355`; the `PathFound` and `PathFoundOneFileEntry` rows build a
  `Dir` composite-literal key, and card 17 named only the other two files. Card 17 now retargets both
  rows to their self-glyph spellings with the `Listing` key and an updated expected string, keeps and
  renames the `PathNotFound` and totality-guard rows as external-caller guards, and its closing
  sentence counts three files rather than two.
- r2 holistic, `[NIT:consistency] Card 15's branch numbering contradicts its own insertion order`
  (demoted from BLOCKING) — accurate and self-contradictory as written: the card inserts the arm at
  position 2 and then told the implementer to number it 3. `RenderResolveText`'s doc comment numbers
  the switch arms in code order, so the card now spells the whole renumbering: rejection 1, listing
  2, glyph 3, default 4.
- r2 holistic, `[BLOCKING:scope] ResolveResult's own doc comments left falsified after card 12` —
  verified at `internal/engine/answer.go:159-162` and on the `ID`, `Unit` and `Error` fields. Added
  `internal/engine/answer.go` to card 12's `Edits:` and named all four statements in its
  `Requirements:`. Card 12 rather than card 10 because card 12 is the card that deletes the path
  branch. Also told the implementer to leave the file's three recorded contract gaps alone, since
  none is this task's to close and they sit in neighbouring prose.
- r2 holistic, `[BLOCKING:scope] Card 19 leaves expand.go's header and worker comment stale` —
  verified: the file header calls `NotATypeError` the one typed failure the verb returns, and the
  worker's doc comment narrates parse, identifier, directory read with nothing between them. Card 19
  now names both, and its closing prohibition is narrowed so it forbids further *code* changes rather
  than reading as a ban on the comment edits the same card requires.
- r2 holistic, `[BLOCKING:scope] runTOC's new exit-2 disposition is documented nowhere` — verified
  at `internal/cli/cli.go:29-30` (the `exitUsage` constant enumerates three causes) and `:181-183`
  (`runTOC` step 1 documents exit 1 alone). Both added to card 28, whose count moves from eight
  paragraphs to ten, with a note that the constant's closing "TOC is never called on this path"
  sentence stays true because card 26's branch fires before the repository is opened. Card 26 now
  points at card 28 for the prose, so one file's comments are not edited from two cards.
- r2 holistic, `[NIT:consistency] Card 28's paragraph locator points at the wrong paragraph` —
  verified: the paragraph above the contract gap is the failure-envelope one, and the
  `strings.Contains` paragraph is two above. The positional clause is gone; the paragraph is now
  identified by its opening words. Also disambiguated "step 1", which after this round's other fix
  could name either `runTOC`'s list or `runResolve`'s.
- r2 holistic, `[NIT:consistency] Card 31 leaves target-echo-asymmetry's premise dead` — verified at
  `internal/cli/cli_test.go:563-588`; card 23 removes the relativisation the contrast rests on. Card
  31 now says the subtest is renamed to assert that the target field is always the argument verbatim,
  with its first half retargeted onto the absolute-path rejection and its glyph half kept.

## Pushed Back

_(none — every finding was accurate and every fix was applied.)_
