# Plan fix — round 3

Source review: `20260905-172532-plan-review-r3.md` (holistic, opushigh, REQUEST_CHANGES,
2 BLOCKING + 4 NIT, one of the NITs demoted from BLOCKING by the stage ceiling and handled as a
NIT). All six verified and fixed; nothing pushed back.

## Fixed

- `[BLOCKING:scope]` "usageText gains five entries with no test at all" — verified against
  `internal/cli/name_test.go:113-128`, this package's established `TestUsageText_*` pattern. Card 11
  now creates `internal/cli/usage_test.go` with `TestUsageText_GlyphsVerb`, pinning the `glyphs`
  usage line, the `--view <name>` row and the ASCII-only rule. It also carries the assertion the
  reviewer's real point demanded: `usageText` must contain `strings.Join(glyphsPreset, " ")`, read
  from the variable, so the help text's copy of the expansion cannot drift from the preset while the
  batch verify stays green. The card states why reading `glyphsPreset` is correct here and wrong in
  card 12 — this test asserts two spellings agree; card 12's asserts one matches a fixed documented
  value, and would pass by construction if it read the variable. `internal/cli/name_test.go` added
  to card 11's `Context:`; `internal/cli/usage_test.go` added to All Files Touched.
- `[BLOCKING:scope]` "Repo.Glyphs's error pass-through is asserted nowhere" — verified: `runTOC`
  calls `repo.TOC` and `quarry.GlyphView` directly and never calls `Repo.Glyphs`, so no CLI test
  reaches the branch either. Card 4 gains a third case: `Glyphs` on a target absent from the
  `writeScratchTree` fixture returns the zero `GlyphsAnswer` and an error satisfying
  `errors.Is(err, ErrTargetNotFound)`.
- `[NIT:consistency, demoted-from BLOCKING]` "Card 5 prescribes a doc.go claim card 1 falsifies" —
  verified against `quarry/doc.go:6` and `quarry/quarry.go:11-27`: every existing answer type is an
  alias, but `GlyphsAnswer` is a package-owned defined struct, so the replacement card 5 prescribed
  was stale as written. Card 5 now prescribes "the engine's types, plus this package's one projected
  answer type", and additionally disposes of the same paragraph's "the only code it owns beyond the
  queries themselves" clause, which `GlyphView` and the two shadow structs also falsify.
- `[NIT:consistency]` "quarry/render.go's file header left stale by card 2" — verified at
  `quarry/render.go:1-6`. `quarry/render.go` moved from card 2's `Context:` to its `Edits:`, with an
  explicit instruction to correct the four-renderer enumeration, say where the fifth lives, and
  change nothing else in the file. Added to All Files Touched.
- `[NIT:consistency]` "internal/cli/flags.go's file header left stale by card 9" — verified at
  `internal/cli/flags.go:1-4`. Card 9 now updates that header to name `glyphsPreset`, and says
  explicitly that card 4 applies the same rule to the facade's header so the two cards cannot
  diverge on whether a new package-level declaration obliges a header update.
- `[NIT:consistency]` "the-pinned-checkout-lives-in-scratch rests on a wrong mechanism" — verified:
  `os.Stat` accepts a relative path. The rationale now gives the real reason absolute is required —
  `go test` runs each package's binary with cwd set to that package's source directory, so a
  relative value would resolve against `internal/cli/` or `internal/engine/` and turn every
  checkout-gated case back into a skip — and says not to simplify the value.

## Pushed Back

_(none)_
