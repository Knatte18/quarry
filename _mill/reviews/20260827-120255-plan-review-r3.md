MILL_REVIEW_BEGIN
# Review: Thin quarry/ facade over internal/quarryengine — holistic

```yaml
duration_s: 707.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-27
```

## Findings

### [BLOCKING:scope] Card 8 omits `*lsp.Client` method-call retargeting
**Location:** 02-engine-repackage, card 8 (query package) **Issue:** Requirements lists type substitutions (`*lspClient`->`*lsp.Client`, `lspDocumentSymbol`->`lsp.DocumentSymbol`, etc.) but never states that the ~9 `lspClient` method calls used throughout refs.go/definition.go/symbol.go (`.references(`, `.definition(`, `.initialize(`, `.workspaceSymbol(`, `.documentSymbol(`, `.close()`, `.kill()`, `.supportsWorkspaceSymbol()`, `.supportsDocumentSymbol()`) must retarget to card 4's exported method names — unlike card 6, which explicitly says "and its methods to the exported names from card 4." **Fix:** Add the equivalent phrase to card 8, and call out that `documentSymbol` -> `DocumentSymbols` (plural per card 4's readability override), not a bare capitalization, since that one call site diverges from the pattern.

### [BLOCKING:scope] Card 8's refs_integration_test.go `builtins()` enumeration misses a call site
**Location:** 02-engine-repackage, card 8 **Issue:** Requirements states "five builtins() call sites (lines 71, 93, 207, 245, plus the line-180 doc-comment mention)," but the file (verified by direct read) has a sixth occurrence at line 188 — `t.Skip(builtins()["go"].InstallHint)` inside `TestReferences_InFile_Integration` — left unlisted. `builtins` is unexported and invisible from `package query`, so a missed site fails the `-tags lsp` compile pass. **Fix:** Add line 188 to the enumerated list (five code call sites total, not four, plus the doc mention).

### [BLOCKING:scope] Card 9's Context omits query/definition.go
**Location:** 02-engine-repackage, card 9 **Issue:** Requirements names `Definition` as one of the eight delegating functions `quarry/facade.go` must reproduce byte-for-byte, but `internal/quarryengine/query/definition.go` — where `Definition`'s signature lives — is absent from card 9's Context: list (only refs.go and symbol.go from `query` are listed). **Fix:** Add `internal/quarryengine/query/definition.go` to card 9's Context.

### [NIT:consistency] Card 14's Creates: none contradicts its own Requirements
**Demoted-from:** BLOCKING
**Location:** 04-doc-redistribution, card 14 **Issue:** Requirements text sanctions creating a new `internal/quarryengine/daemon/doc.go` if the relocated three-section comment dwarfs ensureserver.go, but the card's Creates: field reads "none," and that path also never appears in the overview's All Files Touched list. **Fix:** Either make the daemon doc.go split unconditional (Creates: internal/quarryengine/daemon/doc.go) or record it as an explicitly contingent Creates:/All-Files-Touched entry.

### [NIT:consistency] Card 6's "eight call sites" figure undercounts its own list
**Location:** 02-engine-repackage, card 6 **Issue:** Every individual `builtins()` location is correctly named (7 lines + 1 doc mention in ensureserver_integration_test.go, verified by grep, plus 1 each in toolchain_integration_test.go and supervised_integration_test.go = 10 named occurrences), but the lead-in says "eight." No site is actually missing here, unlike card 8's analogous list. **Fix:** Correct the summary count to match the enumeration.

## Verdict

REQUEST_CHANGES
Four scoped gaps (two missing rename/enumeration items, one Context omission, one Creates:/Requirements contradiction) need fixing before implementation.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 3._
MILL_REVIEW_END
