# Fixer report: plan-fix round 2

Source review: `20260830-114930-plan-review-r2.md` (holistic, opushigh).

## Fixed

- `20260830-114930-plan-review-r2.md` — "[BLOCKING:design] ExtractFencedJSON drops the block/inner distinction": verified against `ladder_config.py` — `extract_fenced_json` returns `(block_text, inner_text)` and `_first_fenced_json_block` takes the fenced half while the answer and scorer-reply parsers take the inner half. Card 7 now specifies `(block, inner string, err error)` and states why neither half is re-derivable from the other; cards 30 and 58 name the `inner` half, card 40 names the `block` half and says why the fences are part of the measured stimulus.
- `20260830-114930-plan-review-r2.md` — "[NIT:decision] No path carries gate observations into run.json": verified — `load_runs` and the cold-cell disposition both read the three observations off `run.json`, which the scoring session writes, while the observations are taken in the run session. Card 61 now requires `record-score` to read the ingest record and copy its observations into the run-marker payload verbatim, with a doc comment noting this is the only path by which they reach the marker. Card 23 states the payload's contents and where the caller assembles them from.
- `20260830-114930-plan-review-r2.md` — "[NIT:decision] \"exhausted attempt record\" is never defined": accurate; nothing wrote such a record. Card 25 now defines it concretely as `MaxAttempts` `<n>.invalid-<k>` siblings being present — the same on-disk siblings `NextAttempt` counts — and records that invalidation past the ceiling errors rather than writing a marker of its own.
- `20260830-114930-plan-review-r2.md` — "[NIT:decision] Cold not-run cause has no on-disk source": accurate; under the session split the live-daemon cause is an abort in a different process, and the Python held it in driver memory. Card 53 now writes a `<n>.cold-abort-<k>.json` sibling on a cold-before failure, using the same sibling convention as the invalidated directories so it survives a later successful attempt without polluting that run directory. Card 63 reads that sibling for the cause and keeps the both-causes reason text.
- `20260830-114930-plan-review-r2.md` — "[BLOCKING:design] Correlation glob under-specifies the subagent path": accurate. Card 57 now fixes the exact search space the way batch 10 fixes its own literals — projects root, the separator-mangled project directory, the `*/subagents/*.meta.json` glob, and the `agent-<id>.jsonl` sibling — and states why the session-id wildcard spanning prior sessions is correct rather than a hazard: each run session has its own scratch directory and the description embeds config, repetition, and attempt, so a second match is the genuine collision the multiple-match error exists to catch.
- `20260830-114930-plan-review-r2.md` — "[NIT:consistency] \"nine validation rules\" miscounts LoadLadder": verified — `load_ladder` has thirteen `_fail` sites. The count is dropped from batch 1's scope, card 2's title, its requirements, and its commit message; the enumeration is marked authoritative and explicitly not a count to match.
- `20260830-114930-plan-review-r2.md` — "[NIT:consistency] GateReport accessors attributed to the Python": verified — the Python `GateReport` exposes only `passed`. Card 13 now names that one as the port and marks the two subset accessors as additions this port introduces, with the doc comments required to say so.
- `20260830-114930-plan-review-r2.md` — "[NIT:scope] Card 26's Context omits settings.go": accurate. Added to card 26's `Context:`, and the requirement now states that `RunGates` derives the denied names through `DenyListFor` rather than accepting a precomputed list, keeping one derivation site.
- `20260830-114930-plan-review-r2.md` — "[NIT:design] Model-pinning gate's empty-transcript outcome unstated": accurate; the Python raised out of its own event lookup. Card 15 now requires a fatal finding naming the absence, never an error return and never a panic, and says the Python's uncaught exception is not behaviour to reproduce.

## Pushed Back

_None._
