# Discussion fix — round 1

Source review: `_mill/reviews/20260904-055611-discussion-review-r1.md` (verdict APPROVE, 0 blocking, 2 nits).

## Fixed

- r1 `[NIT:consistency]` "Plan §5 still spells the verb `toc <dir|file>...`" — added a "Known divergence this task deliberately leaves standing" bullet to the `single-target` decision in `discussion.md`, recording that `docs/rewrite-plan.md` §5's plural spelling stays on this task's Out list, that amending it is a follow-up owned outside T5a, and that the plan writer carries the single-target contract without attempting to reconcile the plan text. No T5a code change, per the finding's own suggested fix.
- r1 `[NIT:consistency]` "Cap-semantics claim overstates what mill-start does at the cap" — trimmed the Constraints bullet to "Review rounds are capped at 3 for the discussion and plan holistic loops (`.millhouse/config.local.yaml`, operator decision 2026-09-04)", dropping the never-blocks sentence. The same overstatement was echoed in the `.millhouse/config.local.yaml` comment the bullet was derived from, so that comment was reworded to say the key sets the round count only and that cap behaviour is the review loop's own convergence handling. (`config.local.yaml` is gitignored and is not part of this commit.)

## Pushed Back

None. Both findings verified accurate against source, and neither fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task.
