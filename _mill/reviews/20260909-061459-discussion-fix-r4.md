# Discussion fix — round 4

Source review: `_mill/reviews/20260909-061351-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, 0 demoted).

## Fixed

- `20260909-061351-discussion-review-r4.md` — "[NIT:consistency] Scope's test bullet lists fewer tests than Testing": verified — `TestCodeForResolveResult` (`internal/cli/cli_test.go:1139`) has exactly five rows and no bogus-value `default` row, and `quarry/text_test.go` lines ~305-316 carry the rejection-rendering cases. Rewrote the Scope test bullet as a full inventory matching the Testing section item for item: both verifiers including panic messages, the `Statuses` completeness test, the `Known()`/`Rejected()` truth tables, the facade `Statuses` assertion plus the added `quarry.NameReasons` twin, the `default`-arm row for `TestCodeForResolveResult`, and the `text_test.go` rejection-case confirmation.
- `20260909-061351-discussion-review-r4.md` — "[NIT:design] `Name` echo-mismatch message shape has no exemplar": accurate — only `resolve`'s message was spelled verbatim, leaving a `Target`-only divergence with an unlabelled got/want pair and the exact-string test nothing to target. Added the explicit rule that the message names the diverging field, plus two verbatim exemplars matching `resolve`'s (`engine: name result 2 echoes unit "a/b"; want "c/d"` and the `target` form), and stated why the field label and the `Unit`-first tie-break are each independently needed for determinism.
- `20260909-061351-discussion-review-r4.md` — "[NIT:design] §5 paragraph vs `resolve`'s whole-call failure path": accurate — the prescribed wording was unqualified while `internal/engine/resolve.go:417` returns `nil, err` on engine failure. Chose to state the exception rather than stay silent: qualified the contract paragraph with "on a call that returns answers at all", added a third §5 item spelling out that an engine failure returns no answers rather than a partial or padded slice while a malformed target taints only itself, noted `name` has no such path, and recorded the rationale (an unqualified rule would license exactly the fail-open reading this task closes) with the silent alternative as the rejected option.

## Pushed Back

_None._ All three findings were factually accurate and no fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope.
