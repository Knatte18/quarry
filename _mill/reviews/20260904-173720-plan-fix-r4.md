# Plan fix — round 4

Source review: `_mill/reviews/20260904-173408-plan-review-r4.md` (holistic, APPROVE,
blocking_count 0, nit_count 3).

## Fixed

- r4 `[NIT:decision] Card 10's --results example escapes date substitution` — took the reviewer's
  first option: card 10 now says to keep the header's existing `<date>` placeholder, so the example
  becomes `results/<date>-breadth` rather than carrying a literal date. Chose the placeholder over
  adding card 10 to the substitution list because `ladder-toc.yaml` is stimulus configuration rather
  than a record of one run — a literal date there would be stale for every future invocation of the
  file, not only this one — and because it keeps `results-root-date-substitution` scoped to the three
  sites that genuinely record this run.
- r4 `[NIT:scope] Card 13 does not say how to resolve task_file from the test directory` — card 13
  now states that `task_file` values are repository-relative, that `run.go` resolves them through the
  unexported `resolveRepoRelative`, and that the test joins the literal prefix `../../../../../` onto
  `task.TaskFile`. The finding is accurate: the card pinned the ladder and fasit paths but left the
  task file's resolution unstated, and passing a repository-relative value straight to `LoadTaskFile`
  from `internal/ladder/` fails.

  **Correction to the finding's suggested value.** The review proposed joining `../../../../`. That
  resolves to `<repo>/bench`, one level short — verified by resolving both candidates from
  `bench/loomyard-eval/ladder/internal/ladder/` and confirming
  `../../../../../bench/loomyard-eval/tasks/01-reed-geometry-exploration.md` exists while the
  four-level form does not. The card carries the five-level form and says explicitly that it is five,
  not four. The finding itself stands; only its example was off by one.
- r4 `[NIT:consistency] Task-02 swap contingency omits the blinding re-check` — added the matching
  sentence to card 7's swap paragraph: `c0-none` is a control, `CheckRenderedControlPrompt` is
  equally fatal for it, so a replacement prompt must again carry none of the bare tokens `quarry` or
  `toc` nor the server name. Card 13's gate already backstops it, so this is prose symmetry with card
  9 rather than a new mechanism.

## Pushed Back

_None. All three findings verified accurate. The one whose suggested remedy was itself wrong (the
`../../../../` prefix) was corrected rather than pushed back — the defect it names is real and the
card needed the fix, so rejecting the finding over an off-by-one in its example would have left the
gap standing._
