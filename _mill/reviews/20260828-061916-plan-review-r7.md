MILL_REVIEW_BEGIN
# Review: Add file/dir toc verbs (Tree-sitter-backed) — holistic

```yaml
duration_s: 375.0
verdict: APPROVE
reviewer_model: opushigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-28
```

## Findings

### [NIT:consistency] Batch `verify:` commands omit the mandatory `PYTHONPATH=` prefix
**Demoted-from:** BLOCKING
**Location:** every batch frontmatter (01-treesitter-backend.md through 08-docs-and-sweep.md), and 00-overview.md's own top-level `verify: go vet ./...`.
**Issue:** `mill-config.yaml`'s "verify command shape (canonical, enforced by `_plan_validate.verify-not-isolated`)" section states unconditionally: "Every non-null `verify:` in a per-batch plan file's frontmatter MUST start with the literal token `PYTHONPATH=` followed by a single space and the command." None of the eight batches' `verify:` lines (all `go test ...` / `go vet ...`) carry this prefix, and card 53's `pipeline.done_gate: go test ./...` doesn't either.
**Fix:** Prefix every batch's `verify:` (and the `done_gate` value card 53 sets) with `PYTHONPATH= `, e.g. `verify: PYTHONPATH= go test ./internal/quarryengine ./internal/quarryengine/treesitter ./internal/quarryengine/registry`, matching the canonical shape mill-config.yaml documents, even though the underlying risk (stale plugin-cache Python modules shadowing worktree code) is specific to Python test runners and cannot affect `go test`/`go vet` — the rule as written names no language carve-out and is stated as mechanically enforced.

I independently verified, by reading source rather than trusting the plan's prose, that everything else checked out: the Batch Index DAG (00-overview.md) is acyclic, every `depends-on` is backward-only, and every named `file:` exists at the stated path; the "All Files Touched" list is the exact union of every card's `Creates:`/`Edits:` targets across all 54 cards (verified by full enumeration); global card numbering 1–54 is sequential with no gaps or duplicates across the eight batches; every card's `Context:`/`Edits:` set contains every function/type/constant its own `Requirements:` names (spot-checked broadly, including the trickier batch-6/7 CLI cards); no `Moves:` is non-empty anywhere, so the absence of a `## Rename mechanic` section in every batch is correct, not a gap; the pinned tree-sitter versions in the "dependency set" Shared Decision match `.scratch/cgobench/go.mod` exactly; `errors.go`, `doc.go`, `layering_test.go`, `seam_enforcement_test.go`, `facade.go`, `facade_test.go`, `registry/{registry,detect,load}.go`, `output.go`, `cli.go`, `paths.go`, `exec.go`, `cwdcontext.go`, and the referenced test files all match the plan's claims about current-state stale prose (`"five-package DAG"`, `"29 identifiers"`, `"eight blank-identifier"`, `"seven re-exported"`, `"four verbs"`, `"The six internal..."`) and the card-54 sweep's grep alternation (`imports? all four`) correctly distinguishes the two "import all four" sites; `TestCommand_EveryCommandHasShort` and `TestInFileFlag_RegisteredOnRefsAndDefinitionOnlyNotSymbol` (named in cards 40/46) exist as described; `LoadRegistry`'s `KnownFields(true)`/`io.EOF` handling (cited by card 42) matches `load.go` exactly; `internal/output/output.go` has exactly three exported functions, matching card 32's integration-test claim precisely.

## Verdict

APPROVE
Every batch `verify:` line is missing the mill-config.yaml-mandated `PYTHONPATH=` prefix; everything else checked against source is sound.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
