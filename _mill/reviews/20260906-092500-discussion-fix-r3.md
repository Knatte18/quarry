# Discussion fix — round 3 (round cap)

Source review: `_mill/reviews/20260906-091944-discussion-review-r3.md` (REQUEST_CHANGES, 3 BLOCKING, 2 NIT, 1 of them demoted from BLOCKING by the stage's `blocking_classes` ceiling).

Round 3 is `roles.discussion-review.holistic.rounds` (3), the configured cap. Per operator instruction given during this session — approve and hand off at the cap regardless of open findings, rather than `_status.set_blocked` — every finding below was resolved and the phase advances to `discussed`. No finding was left open.

## Fixed

- `20260906-091944-discussion-review-r3.md` — "Whole-run abort has no disposition" ([BLOCKING:design]). Verified: `run.go:625` sets `abortRun` on a `ScanMemoryPaths` fatal finding (`provenance.go:517` — fatal on the bare token `quarry` in a memory file *or* on a missing memory path), `Run` then returns `nil`, and `summarizeAndReport` (`main.go:98,172`) still writes and prints `summary.json` and `table.txt`. The prior completion predicate read that as a normal completion. Replaced the launch command with one that appends `LADDER_EXIT=$?` to the log, replaced the two-outcome predicate with three (`=0` proceed, absent line = abnormal death, `=1` = classify before reading any number), and added an "Aborted invocation" disposition: detection signals, environment fix, deletion of the tainted repetition directory, resume, and a mandatory conclusion-coverage record of the deletion. Justified as not-optional-stopping by citing the harness's own comment at `run.go:615-618`.
- `20260906-091944-discussion-review-r3.md` — "Substitution consequence rests on a false premise" ([BLOCKING:design]). Verified: `Pack` renders e1's block from the same `pack_targets` slice e2's `Files:` list is derived from (`pack.go:287-292`), so the two cards name the identical file set by construction and a vacated file vacates from both. The round-2 fix's claim that e1 keeps seven while e2 drops to six was wrong. Removed the asymmetry claim; the conclusion now reports both non-control cards dropping to the same six and the vacated seventh being named by no card in any arm.
- `20260906-091944-discussion-review-r3.md` — "Reserve-glyph selection rule is unsatisfiable for two targets" ([BLOCKING:design]). Verified: the three reserves sit only in `internal/fabricengine` (2) and `internal/gitrepo` (1), while `pack_targets` also names `internal/fabriccli#addWeftVerbs` and `internal/mergeresolve#Resolver.Resolve`. Added a fixed per-package selection table with a deterministic within-package order, an explicit halt for the two uncovered packages, a halt when a chosen reserve itself does not resolve `found` and its package's reserves are exhausted, and a halt on any multi-glyph failure. Spelled out what halt forbids (no cross-package substitute, no gate weakening, no target-repo edit, no proceeding to card 30).
- `20260906-091944-discussion-review-r3.md` — "M4b follow-up condition disappears with roadmap point 1" ([NIT:decision], demoted from BLOCKING). Verified at `docs/roadmap.md:23-24`. Card 32 grew a fourth edit: on a non-separating result the condition is discharged in the conclusion's verdict section and deleted from the roadmap; on a separating result M4b is added as a new numbered point. The conclusion-contents section carries the matching one-sentence requirement.
- `20260906-091944-discussion-review-r3.md` — "Resume liveness test conflates two different pids" ([NIT:design]). Verified: `$!` is the `go run` parent shell while the lock file records `os.Getpid()` of the compiled ladder binary (`worktree.go:309`). The authoritative check is now the lock file's own `pid=` plus `pgrep -f`, with `$!` demoted to a convenience handle and the distinction stated.

## Pushed Back

None.
