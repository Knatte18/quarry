# Plan fix — round 3

Source review: `_mill/reviews/20260904-091653-plan-review-r3.md` (holistic, REQUEST_CHANGES,
2 BLOCKING + 6 NIT).

Round 3 is the configured round cap (`roles.plan-review.holistic.rounds: 3`), and the operator
instructed this run to approve and hand off on reaching it. Every finding below was verified and
fixed; none was carried into the approval unaddressed.

## Fixed

- `[BLOCKING:scope]` "stdout decision has no enforcement mechanism" — accurate, and the asymmetry was
  unjustified: the plan calls a stray stdout write the one way this binary fails catastrophically and
  silently, while giving the equally-weighted facade-only rule a mechanical guard on the stated
  ground that review is not a mechanism. Card 10 now adds a second check over the same two
  directories, failing on `os.Stdout`, the `fmt.Print`/`Println`/`Printf` family and the `print`
  builtins in production files, with the file header noting the check is deliberately blunt. Card
  title, header requirement and commit message updated to cover both rules.
- `[BLOCKING:design]` "card 8 leaves two identifiers unnamed, one used cross-batch" — accurate: card
  15 in a different batch requires calling a function no card named. Card 8 now names `tocResult` (the
  decision function) and `errorResult` (the failure-result helper) and says explicitly that batch 3
  cites the former; card 15, batch 3's scope paragraph and batch 2's scope note all now use the name.
- `[NIT:consistency]` "broken-symlink expectation departs from the discussion, unrecorded" — accurate.
  Added a Shared Decision recording that a broken symbolic link succeeds as a name-only entry, why the
  discussion's error-path expectation is wrong about the code, and why making it fail would break the
  never-follow rule the discussion actually wants protected — placed next to the `depth`-wording
  divergence, the only other departure of its kind.
- `[NIT:scope]` "layering test is direct-import only; card 10's mandated header would overclaim" —
  accurate. Card 10 now states the check is a direct-import scan, why a transitive check is impossible
  here (the facade the packages must depend on imports the engine itself), and names the residual gap
  an intermediate package would open — and requires the file header to say that rather than claim the
  rule is enforced outright.
- `[NIT:consistency]` "card 18's rooting rationale is refuted by D13" — accurate. Dropped the clause
  and replaced it with the opposite instruction: the shipped configuration does not test the rooting
  assumption, because discovery from this repository succeeds either way, which is exactly why D13
  puts the probe in a different repository.
- `[NIT:consistency]` "card 2 leaves a stale in-body comment in cli.go" — accurate. Card 2 now owns
  the inline comment on the root-resolution failure branch: its `usageError` contract claim becomes
  false after the card, and it cites a previous task's card number. The branch itself stays, for the
  reason its own comment gives.
- `[NIT:scope]` "no card pins mcpserver's no-repository-root wording" — accurate, and the asymmetry
  with batch 1 card 4 was an oversight. Card 6 now declares `rootErrorMessage` as a named formatter
  for the same reason batch 1 declares `rootUsageMessage`, and card 7 table-tests both startup
  sentences through it as exact strings.
- `[NIT:consistency]` "card 5 states \"direct requirements\" then \"expect indirect\"" — accurate.
  Dropped "as direct" from the opening sentence; the deferred-tidy paragraphs carry the real contract.

## Pushed Back

_(none)_
