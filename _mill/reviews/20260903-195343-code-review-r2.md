MILL_REVIEW_BEGIN
# Review: Ladder harness around headless claude -p (T2) — holistic

```yaml
duration_s: 186.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-03
```

## Findings

No findings. This holistic pass read all eight plan batches and every source, test and fixture file
listed in the manifest, cross-checking cards against `config.go`, `match.go`, `stream.go`,
`metrics.go`, `fenced.go`, `prompt.go`, `gates.go`, `score.go`, `worktree.go`, `mcp.go`,
`provenance.go`, `runstate.go`, `run.go`, `summarize.go`, `report.go`, `cmd/ladder/main.go`, both
fake test binaries, `live_test.go`, `e2e_test.go`, and the committed fixture root against its golden
summary/table.

Notable points verified rather than assumed:
- The prior non-blocking item (`run.go:657,663`, unused `cfg` parameter threaded through
  `redactionInputFor`/`redactedAnswerText`) no longer applies: neither helper takes a `cfg` parameter
  in this round's `run.go` — the signatures are `redactionInputFor(l *Ladder, quarryRepoRoot,
  taskWorktreePath string)` and `redactedAnswerText(l *Ladder, quarryRepoRoot, taskWorktreePath,
  answerText string)`. Resolved, nothing to escalate.
- Cross-batch contracts hold: `BuiltinTools` is declared once in `config.go` and referenced (never
  re-spelled) from `prompt.go`, `run.go`, `gates_test.go`, `live_test.go`, and the fake binary via an
  env var built from the slice; `MaxAttempts` is declared once in `runstate.go` and shared by both the
  invalid-rep and scorer-retry paths; `ProvenanceFile`/`SummaryFile`/`TableFile` are each declared once
  in their own writer file.
- `testdata/results/root`'s two repetitions and the golden summary/table are numerically
  self-consistent by hand-check (grouped usage, byte metrics, medians, exclusion counts all trace back
  to the two committed transcripts and score files).
- Gate 2's check (c) is structurally non-fatal (no fatal-setting branch exists in
  `checkTargetOriginQuarryMention`), matching the required unreachable-fatal-branch property, and
  `gates_test.go` covers the full antecedent x location matrix.
- The six-per-repetition write order (state file last) is enforced in `run.go` and asserted by
  modification time in `e2e_test.go`.
- `results/*/raw/` gitignore scoping, the retired-key rejection list, the migrated `ladder-toc.yaml`
  (four surviving cells, single `toc` tool, `{target_dir}` placeholder documented in both `mcp.go` and
  the yaml header), and the recovered exploration schema in task 01 all match their cards exactly.

## Verdict

APPROVE
All eight batches' cards are realised, cross-batch contracts hold, and no out-of-plan or duplicated logic was found.
MILL_REVIEW_END
