# Discussion fix — round 5

Source review: `_mill/reviews/20260903-162752-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 3 NIT — one demoted from BLOCKING — reviewer `opushigh`).

## Fixed

- r5 `[NIT:consistency, demoted-from BLOCKING]` "Detail-per-reason rule contradicts itself" — the eleven-versus-four grouping and the "a blank `Detail` means one of the four" invariant are both gone; they were false, since `unit_empty`, `unit_empty_segment` and `member_empty_component` all name a piece that is itself the empty string. `Detail` is now given per reason in its own column of the vocabulary table, and the file states explicitly that a blank `Detail` carries no meaning and `Reason` is the only discriminator. The unnameable "`unit_empty`'s sibling cases" clause is deleted. Two `Detail` values improved while the column was being written: `unsupported_language` carries the rejected `Language`, `member_too_deep` carries the whole member half.
- r5 `[NIT:design]` "Invalid UTF-8 input has no stated disposition" — a sixteenth `Reason`, `invalid_utf8`, checked in the language-free layer after the language check and before the split, with the reasoning recorded: both alphabets are defined over runes, so a bad byte would otherwise be silently accepted in the unit half and reported as U+FFFD in the member half. Two reject-table rows added (a bad byte on either side of the `#`), the precedence stated in `parse_test.go`'s list, and every "fifteen" count updated to sixteen. `unicode/utf8` added to the allowed-import list.
- r5 `[NIT:consistency]` "Zero-Glyph-on-error is pinned for one reason only" — the error decision now states that `Parse` returns the zero `Glyph` on every error for all sixteen reasons, with the reason why a partially populated struct is the wrong answer; the reject table asserts it on every row, not just the `unsupported_language` case.

## Pushed Back

None. Under `--orch` no operator is present, so PUSH BACK is unavailable; all three findings were verified accurate and none met a harm category.
