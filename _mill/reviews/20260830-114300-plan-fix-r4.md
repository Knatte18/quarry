# Fixer report: plan-fix round 4

Source review: `_mill/reviews/20260830-114055-plan-review-r4.md` (holistic, REQUEST_CHANGES,
1 BLOCKING / 2 NIT).

## Fixed

- r4 holistic — "[BLOCKING:scope] Stale `effectiveTargetDir` comment left in a file card 7 edits".
  Verified by a repository-wide grep: `bench/loomyard-eval/ladder/scripts/ladder_config.py:39`, in
  the comment above `DAEMON_BACKED_TOOLS`, carries the same "call effectiveTargetDir/tocPreflight
  directly and never resolveCall" claim card 4 rewords in `tools_toc.go` and card 5 rewords in
  `tools_toc_test.go`. It is the only reference outside `internal/mcpserver/`, and card 7's "change
  nothing else in the file" plus card 8's package-scoped grep meant nothing caught it. Both halves of
  the reviewer's suggested fix are applied rather than one: card 7 now dispositions the comment
  explicitly (reword the handler claim, leave the comment's actual daemon-warmth point and the
  `DAEMON_BACKED_TOOLS` expression untouched), and card 8's check two is widened from
  `internal/mcpserver/` to the whole repository, including non-Go files, with the reason stated.
- r4 holistic — "[NIT:consistency] Test-side grep whitelist blanket-allows the prose the production
  pass forbids". Verified: check one forbids per-call override phrasing in production prose, but
  check one-b listed "prose in test doc comments" unconditionally, so the same staleness in a
  `_test.go` comment would have passed silently. Check one-b's prose survivor now carries the same
  qualification — prose naming the server's target directory only — and states explicitly that a test
  doc comment describing a per-call override fails one-b exactly as it would fail one.
- r4 holistic — "[NIT:scope] Card 1 rests on a test function in a file absent from `Context:`".
  Verified: card 1's Requirements assert what `TestInputSchemaFor_CallWidePropertySurvives` pins
  without `internal/mcpserver/schema_test.go` being readable. Added to card 1's `Context:` as
  read-only; the existing single-line do-not-edit instruction is unchanged and still applies. The
  parenthetical same-shape case is fixed too: `internal/cli/toc.go`, which defines
  `cli.ResolveTOCPath`, is added to card 5's `Context:`.

## Pushed Back

_None._
