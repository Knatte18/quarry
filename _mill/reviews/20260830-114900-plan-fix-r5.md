# Fixer report: plan-fix round 5

Source review: `_mill/reviews/20260830-114603-plan-review-r5.md` (holistic, REQUEST_CHANGES,
2 BLOCKING / 1 NIT).

## Fixed

- r5 holistic — "[BLOCKING:design] Check two's repo-wide grep can never return zero hits". Verified:
  a bare `grep -rn 'effectiveTargetDir' .` from the repository root matches this task's own planning
  artefacts under `_mill/` — the discussion, all three plan files, the review files, and the briefs —
  every one of which names the deleted helper by design and goes on naming it after every card has
  landed. Round 4's widening of this check overshot: it fixed a real gap (the bench file) but made
  the stated expectation unsatisfiable. Check two is now scoped to the code, documentation, and bench
  surface, with `_mill/`, `.git/`, `.wiki/`, `.portals/`, and `.scratch/` excluded by name, and both
  card 8 and the `## Batch Tests` command state why those exclusions are what make the zero-hit
  expectation achievable rather than a loophole. The bench file remains in scope, so round 4's
  finding stays fixed.
- r5 holistic — "[BLOCKING:scope] Card 2 names cli.ResolveStateDir/ResolveBuildTags without listing
  paths.go". Verified: card 2's Requirements specify the new test's assertion in terms of
  `cli.ResolveStateDir` and `cli.ResolveBuildTags` and restate `resolveCall`'s helper ordering, but
  `internal/cli/paths.go` — where `ResolveStateDir` and `workspaceKey` live — was in neither
  `Context:` nor `Edits:`, while card 6 does list it. Added to card 2's `Context:`.
- r5 holistic — "[NIT:consistency] Card 6's doc content can trip card 8's check one". Verified: card
  6 requires stating that no tool accepts a target directory as an input property, and the natural
  phrasing spells the removed token, which check one greps `docs/mcp-setup.md` for and treats as a
  failure. Card 6 now requires the concept named in prose only, with the token never written in that
  file — not even to say it is gone — and gives the documentation reason as well as the gate reason.
  The requirement also states explicitly that the Go identifiers `ResolveLaunchTargetDir` and
  `Config.TargetDir` remain fine to name, since check one's whitelist sanctions both and the card
  itself asks for the first, so the instruction cannot be misread as forbidding them.

## Pushed Back

_None._
