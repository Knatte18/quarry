# Plan fix — round 5

Source review: `_mill/reviews/20260829-073718-plan-review-r5.md` (verdict APPROVE, 6 NITs)

## Fixed

- r5 "`go mod tidy` undoes card 6's own direct requirement" — accurate: nothing imports
  `jsonschema-go` until card 10, so tidy in card 6 would demote it back to `// indirect`. Card 6 now
  acquires the MCP SDK only and says explicitly why it does not acquire `jsonschema-go`; card 10
  owns `go get github.com/google/jsonschema-go@v0.4.3` plus `go mod tidy` and gains `go.mod` and
  `go.sum` in its `Edits:`, in the same card as the first direct import.
- r5 "Batch 1's edited engine test file is never compiled by any gate" — accurate: `go build ./...`
  does not compile `_test.go` files. Batch 1's `verify:` now chains
  `go test ./internal/quarryengine/query/...` (verified to pass in 0.2s at the worktree tip), in
  both the batch frontmatter and the overview's Batch Index, with the reason stated in
  `## Batch Tests`.
- r5 "Batch 2's external-interface list omits two consumed identifiers" — `dropEntryProperty` and
  the `docSentences` type added to batch 2's stated external interface.
- r5 "`ResolveLaunchTargetDir` spec is not expressible as written" — accurate:
  `filepath.Abs(os.Getwd())` is a two-value call in single-value position, and `os.Getwd` already
  returns an absolute path. Restated as `os.Getwd()` with its error wrapped.
- r5 "toc_file's `result` wrapper diverges from the CLI without a stated rationale" — batch 5 now
  carries the matching batch-local decision, recording the divergence as deliberate consistency
  with `impact`'s wrapper rather than a necessity, since `TOCFileResult` has no colliding key today.
- r5 "`.mcp.json` bare-map rationale is over-broad" — accurate: the reviewer verified both shapes
  occur at plugin roots, so plugin-root placement is not the discriminator. Rationale trimmed to
  the project-scoped rule; the `mcpServers` decision itself is unchanged.

## Pushed Back

_(none)_
