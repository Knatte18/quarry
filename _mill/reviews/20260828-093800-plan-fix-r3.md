# Plan fix — round 3

Source review: `_mill/reviews/20260828-093228-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 5 NIT).

## Fixed

- r3 holistic, "[BLOCKING:design] Live-tier query form is never pinned" — verified. Card 17 said to drive `impact` against `ApplyDiscount` without deciding between a bare name and a scanned position, and the precedent it cites takes the position route precisely because `workspace/symbol` resolution of a Go method is fragile (gopls reports it receiver-qualified). Card 17 now pins the position form explicitly, with the reason stated, and requires a **new** scan helper — the existing `findInterfaceMethodPosition` matches a bare interface-method line with no `func` keyword and no receiver, so it cannot match a receiver-method declaration. The neighbouring "do not add a second helper" rule was narrowed so it no longer reads as forbidding that new helper.
- r3 holistic, "[NIT:consistency] Card 5 cites a Shared Decision name that does not exist" — verified: the plan's heading is `parse-loop-cancellation-scope`; card 5 cited `discussion.md`'s longer name. Corrected to the plan's own heading.
- r3 holistic, "[NIT:consistency] `--within` help string copied verbatim misstates its scope on `impact`" — verified: `refsCommand`'s text says "restrict results to references", but on `impact` the flag filters the caller list only and leaves `target`/`definition` untouched, which the same card requires two paragraphs later. Card 10 now lists four flags whose help is copied verbatim and two exemptions (`--timeout`, already exempt, and `--within`, newly so), with the caller-only wording spelled out.
- r3 holistic, "[NIT:consistency] `impactCommand`'s file placement diverges from the discussion silently" — verified: `discussion.md`'s `cli-shape-mirrors-refs` places the command in `internal/cli/cli.go`; this plan creates `internal/cli/impact.go`. Added it to batch 3's `## Batch Scope` as an explicit recorded deviation, with the reason (cli.go is already 972 lines and card 11 makes three surgical doc edits to it; `toc.go` sets the precedent) and a statement that nothing else about that decision changes.
- r3 holistic, "[NIT:consistency] \"three-way error routing … verbatim\" contradicts the helper it names" — verified against `internal/cli/cli.go`: `emitLookupResult` has two error paths (ambiguous, then everything else — it deliberately does not special-case the not-found sentinel), while `classifyLookupError` has three, because batch mode must distinguish `not_found` from `error` where the single-argument shape collapses both onto exit 1. Card 10 now states each count separately and says why they differ, instead of asserting "three-way" for both.
- r3 holistic, "[NIT:consistency] `repoRoot` copy hazard across a different directory depth" — verified: the borrowed helper hard-codes three `filepath.Dir` calls for a file two directories below the root, while the impact test file sits three below and needs four. Card 6 now says the `runtime.Caller(0)` technique is what is borrowed, that the depth differs, and that the call count must be counted from the new file's own location rather than copied.

## Pushed Back

_None. Every finding passed VERIFY and none triggered a harm-check escape._
