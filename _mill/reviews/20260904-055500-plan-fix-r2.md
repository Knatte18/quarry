# Plan fix — round 2

Source review: `20260904-054210-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 4 NIT).

## Fixed

- `[BLOCKING:scope] Card 16 Context omits resolve.go and glyph/` — verified: card 16 tells the
  implementer to read the expected head span from a `SpansOf` lookup, and `SpansOf` lives in
  `internal/engine/resolve.go` and takes a `glyph.Glyph`. Added both `internal/engine/resolve.go` and
  `glyph/glyph.go` to card 16's `Context:`.
- `[NIT:consistency] not_found JSON assertion contradicts ID rule` — verified: card 7 sets `ID` the
  moment the parse succeeds, and card 2 documents it as present for any glyph target, so card 11's
  "`id` absent" assertion could not have passed. Resolved in favour of the implementation cards, which
  are what the discussion's D3 specifies (`ID` is "present only for a glyph target" — a miss is still a
  glyph target). Card 11 now asserts `target`, `id` and `status` present with `id` carrying the glyph's
  own string form, and only `symbols`, `candidates`, `dir`, `error` and `reason` absent, with a
  sentence stating that `id`'s `omitempty` exists for the path branch rather than for the miss.
- `[NIT:consistency] matchesFor leaves a third copy of the same filter` — verified: `SpansOf` keeps its
  own inline filter loop, so lifting `matchesFor` does leave two implementations in one file. Taken
  the finding's first offered resolution — record the duplication as deliberate — rather than its
  second. `SpansOf` calling `matchesFor` would be a third edit to code T3 wrote for its own purposes,
  and this task's scope names exactly two such exceptions, both elsewhere; `SpansOf` is also what T3's
  round trip is written against, so refactoring it would put a must-keep-passing test onto rewritten
  code. Card 7 now requires `matchesFor`'s doc comment to name `SpansOf`'s inline loop as the second
  copy and state both halves of that reason, and to leave the question to whichever later task next
  edits `SpansOf` for its own reasons.
- `[NIT:scope] Batch 5 prerequisite is work with no card` — verified: `loomyardRepo` reads
  `os.Getenv("LADDER_LOOMYARD_REPO")` and nothing else; no file in this repository reads
  `.scratch/ladder.env`, only the ladder harness does. The write instruction is dropped. The
  prerequisite is now stated as an environment step that resolves a path and exports it, explicitly
  writing and committing nothing, with the halt condition kept because it genuinely blocks all three
  cards.
- `[NIT:design] "expand needs no edit" claim for a subset-head language` — verified against
  docs/rewrite-plan.md's three-queries section, which says the whole class is the type symbol's own
  `start`–`end` and is available. The head substitution does make that span unrecoverable from an
  `ExpandAnswer` for a subset-head language. Recorded as a fourth contract gap at the substitution
  itself, per the finding's fix, with the decision left to that language's own task and an explicit
  instruction not to add a second span pair now. The overview's Shared Decision rationale is reworded
  to drop the "needs no edit" claim, its contract-gaps decision now lists four and states that the
  fourth surfaced during plan review, and that decision's `Applies to:` widened to include expand.

## Pushed Back

_None. All five findings verified accurate against the code and the documents. The one finding
offering two resolutions was fixed by the resolution that does not require a third scope exception;
that is a choice between the finding's own alternatives, not a pushback._
