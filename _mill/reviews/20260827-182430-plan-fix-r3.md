# Plan fix report — round 3

Source review: `_mill/reviews/20260827-182106-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 4 NIT).

## Fixed

- r3 holistic, "[BLOCKING:design] Directional branch fails open on documentSymbol" — verified: card 15 named only a `documentSymbol` error, with no `SupportsDocumentSymbol` gate and no empty-result case, and each of those makes `isInterfaceDeclaration` false for every implementation location, narrowing the match set and dropping references. Card 15's skip-verification enumeration now covers all three directional-mode cases — unadvertised `documentSymbolProvider`, an errored `documentSymbol` call, and an empty `documentSymbol` result — with an explicit statement that the match set is never narrowed on a degraded classification. Added the `SupportsDocumentSymbol` gate to the directional path, noting that it mirrors `resolvePosition`'s own gate but disposes differently on purpose: `resolvePosition` cannot resolve without the capability and raises `ErrResolverUnsupported`, whereas here a strictly safer answer exists.
- r3 holistic, "[NIT:consistency] Card 15's skip list reads as exhaustive but is not" — folded the `documentSymbol` cases into the enumerated list, marked the list exhaustive, and deleted the "classifies that file's locations as not-interface" clause that contradicted the same sentence's conclusion. The rationale now stands on its own bullet.
- r3 holistic, "[NIT:consistency] Card 18 breaks internal/cli's test build for 4 cards" — verified: `internal/cli/cli_test.go:585` is the sole `buildOptions` call site and card 18 left it to card 22, so the package's test build would not compile across cards 18–21 and card 19's written-first back-compat assertion would be unrunnable. Added `internal/cli/cli_test.go` to card 18's `Edits:` with a requirement to repair the call site in-card, matching card 19's existing in-card repair discipline, and narrowed card 22's own requirement to adding the `BuildTags` assertion.
- r3 holistic, "[NIT:consistency] Card 14's empty-match-set test bullet states a false property" — deleted the leading "keeps nothing and drops nothing" clause, which contradicted both the `filterVerifiedReferences` predicate defined three bullets earlier and the bullet's own parenthetical. The bullet now asserts only what the predicate produces.
- r3 holistic, "[NIT:scope] `filterUnexpectedCallers` named without internal/cli in Context" — took the first of the two offered dispositions rather than trimming the citations, since both citations carry real design rationale the implementer benefits from checking. Added `internal/cli/cli.go` and `internal/quarryengine/errors.go` to card 15's `Context:`, and `internal/cli/cli.go` to card 16's.

## Pushed Back

_None._

## Re-validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` pass, and `_plan_validate.run` returns zero findings with an empty `skip_checks`.
