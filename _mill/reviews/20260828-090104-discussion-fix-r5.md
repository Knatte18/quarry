# Discussion fix — round 5

Source review: `_mill/reviews/20260828-085941-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 6 NIT — 2 demoted from BLOCKING by the stage's blocking-class ceiling).

Operator note: the convergence gate did not fire on this round (two findings carried `demoted: true`). The operator elected to fix all findings and proceed to Handoff rather than run round 6 — see the Q&A log's final entry.

## Fixed

- `20260828-085941-discussion-review-r5.md` — "[NIT:consistency] `--within` counterpart drops refs' path normalization": stated that impact's CLI-side `--within` reproduces `filterWithin`'s three normalization steps (relative→target-dir join, `filepath.Abs`, `filepath.Clean`) *before* the per-entry `isWithinDir` call, and recorded the failure mode it prevents — an un-normalized relative value makes `filepath.Rel` error and silently filters every caller out, yielding an empty-but-successful answer. Added a CLI test requirement for a **relative** `--within` value specifically.
- `20260828-085941-discussion-review-r5.md` — "[NIT:scope] Doc/guard enumeration inventory is not exhaustive as claimed": replaced the phrase-grep inventory with an explicit derivation rule ("every comment, doc block, or hand-enumerated test table that lists the engine package set or the verb set"), and re-derived under it. `doc.go` went from two edits to four (adding the lines 29-30 enumeration that carries no "seven-package" phrase, and a new `impact` bullet in the lines 53-92 package-layout list). Added `internal/cli/cli_test.go`'s `TestBuildTagsFlag_RegisteredOnAllFourVerbs`, noting its second loop is the mechanical enforcement of the Scope "Out" no-`--no-verify` claim.
- `20260828-085941-discussion-review-r5.md` — "[NIT:consistency] `ok`/`resolution` listed as struct-tag-fixed keys": split the envelope bullet by owning layer — only `target`/`definition`/`callers` are struct-tag-fixed on `impact.Result`; `ok` comes from `output.Ok` and `resolution` is added CLI-side as `emitLookupResult` does for `refs`. Recorded that an engine-side `resolution` field would leak a CLI trust marker into `quarry.Impact`'s SDK contract and breach the engine/CLI seam.
- `20260828-085941-discussion-review-r5.md` — "[NIT:design] Greatest-`Start` rationale rests on the wrong overlap example": restated the tie-break's justification on Python/C# class↔method nesting (`pythonClassSymbols` emits a class Symbol spanning its method Symbols) and explicitly corrected the grouped-Go-type claim in both the decision and the Technical context gotcha — `goGroupedTypeSymbol` ranges each spec from the spec itself, so grouped specs are siblings, not nested. Added a Python fixture case asserting a line inside a method resolves to the method, not the class.
- `20260828-085941-discussion-review-r5.md` — "[NIT:design] Parse-cache assertion has no committed observable": committed to the counting seam as the assertion mechanism and deleted the identical-result proxy and the "if the design exposes one" hedge, recording that identical results cannot prove a single parse.
- `20260828-085941-discussion-review-r5.md` — "[NIT:consistency] `buildQuery` described as an existing shared seam": corrected to "the same `buildQuery` closure *shape*", stating it is a per-command local closure duplicated at `refsCommand` and `definitionCommand` that `impact` re-creates, not a helper to call.

## Pushed Back

_None._
