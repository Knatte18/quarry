# Plan fix — round 5

Source review: `20260904-060932-plan-review-r5.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 2 NIT).

## Fixed

- `[BLOCKING:scope] ExpandAnswer's JSON key set is never asserted` — verified: card 11 marshals a
  `ResolveResult` and nothing else, and no card in batches 4 or 5 marshalled an `ExpandAnswer`, so its
  six tags had no serialization coverage. The finding's point about `go vet` is the one that makes this
  BLOCKING rather than cosmetic: vet checks tag syntax, and a misspelled key or a missing `omitempty`
  is valid syntax. Split the coverage across the two cards that already build the relevant answers, so
  neither test is contrived: card 16's `TestExpand_TypeWithoutMembers` now marshals its member-less
  `found` answer — the only `found` case where `members` is legitimately absent, so one marshal pins a
  present key set and an omitted one at once — and card 17's `TestExpand_NotFound` marshals the
  `not_found` shape, which is where `unit` is present. Between them all six keys are pinned in both
  states. Batch 1's Batch Tests sentence, which claimed card 11 pinned "the JSON tags", now names all
  three marshalled assertions and states what vet does and does not catch.
- `[NIT:consistency] Batch 1's verify contradicts the verify Shared Decision` — verified: the Decision
  says every batch's `verify:` is scoped to `./internal/engine/`, and batch 1's opens with a
  module-wide `go build ./...`. Amended the Decision to name batch 1 as its one deliberate exception,
  with the reason rather than the bare carve-out: that batch adds exported types the rest of the module
  compiles against and has no test of its own, so a module-wide build is the only thing that can fail
  there and is what makes it verifiable at all. Scoping the build half instead was rejected — it would
  have removed the batch's only real gate to satisfy a sentence.
- `[NIT:consistency] Gap 1's recording site differs between Decision and card` — verified: the Decision
  said the gap is recorded "where the collision is promoted to `ambiguous`", which is
  `statusForMatches` in card 6, while card 7 records it where the flag is read from the memo. Aligned
  on card 7's site and reworded the Decision, with the reason the card is right and the Decision was
  wrong: `statusForMatches` takes a bare bool and knows nothing about unit directories, so a comment
  about the external test unit versus a real directory of the same name would have no context there.

## Pushed Back

_None. All three findings verified accurate against the plan's own text; no fix broke anything or
reached outside this task's scope._
