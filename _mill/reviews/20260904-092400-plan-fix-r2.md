# Plan fix — round 2

Source review: `20260904-091916-plan-review-r2.md` (holistic, opushigh, REQUEST_CHANGES, 2 BLOCKING + 5 NIT).

## Fixed

- r2 holistic, `[BLOCKING:design] RenderExpandText has no catch-all branch` — card 5 now specifies a fourth, fall-through branch covering the empty status and the multipart status the answer type does not admit, emitting the identifier and the status word on one line, with the same unreachable-default rationale card 10's mappings carry. The card's test table now drives that branch from a zero answer value, so the one-trailing-newline promise is pinned for every input rather than for the three the engine happens to produce.
- r2 holistic, `[BLOCKING:scope] Resolve on a file path target has no machine-independent test` — verified: the only file-path exercise was the evidence golden, which skips wherever the checkout variable is unset. Card 11's end-to-end list gains a file path target case asserting the enclosing-directory answer with exactly one file entry, and its text-flag case now covers the found file path alongside the found directory path; card 5's own text table gains the matching one-file-entry directory-form case.
- r2 holistic, `[NIT:scope] Expand + ambiguous is unreachable in every fixture the plan builds` — verified: card 14's build-tag duplicate was a function, which the kind gate routes to the not-a-type failure before any ambiguous answer exists. Card 14's fixture now also carries a build-tag-duplicated type, the reason is stated in the card, and the status table gains an expand-ambiguous row asserting candidates present with head and members absent.
- r2 holistic, `[NIT:consistency] Shared Decision's Applies-to omits the batch its own text binds` — the payload-error-text decision now lists facade-surface, which its own body already bound through the text view's rendering of that string.
- r2 holistic, `[NIT:consistency] Scratch-tree decision names the wrong directory for quarry` — verified against both helpers: the facade package writes under `.scratch/quarry-tests/` and the command-line package under `.scratch/cli-tests/`. The decision now names both and states that the two helpers are a deliberate per-package copy.
- r2 holistic, `[NIT:consistency] Plan verify runs none of the plan's new tests` — the done-gate decision gains an explicit statement of where each gate runs: per-batch verify per round, the plan-level build check at each batch boundary, and the linter and repository-wide test only once as the hub's configured done gate. It also tells an implementer that nothing in the per-round loop catches an unchecked write error, so the errcheck rule has to be checked by hand before then.
- r2 holistic, `[NIT:scope] Card 17 must read five files it is not given` — card 17's `Context:` now lists all eight files card 16 produces, which is what its own "drawn from reading the produced files" requirement needs.

## Pushed Back

_(none — every finding was factually accurate and no fix caused harm under the decision tree.)_
