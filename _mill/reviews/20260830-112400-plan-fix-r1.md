# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260830-112053-plan-review-r1.md` (holistic, REQUEST_CHANGES,
2 BLOCKING / 3 NIT).

## Fixed

- r1 holistic — "[BLOCKING:design] Card 6 mandates a false absolute about state-dir keying".
  Card 6's second requirement now scopes the automatic-isolation guarantee to `cli.ResolveStateDir`'s
  default user-cache tier and names `--state-dir` / `$QUARRY_STATE_DIR` as the exception where two
  servers pinned to one explicit state directory do share it. Notes that the file's own
  `## Launch-only flags` section documents `--state-dir`, so an unqualified absolute would
  contradict the same file two sections down.
- r1 holistic — "[BLOCKING:scope] Card 8's check-one whitelist omits real test-file survivors".
  Check one is now scoped to production `.go` files (`--exclude='*_test.go'`) plus
  `docs/mcp-setup.md`. A new check one-b runs the same grep over `_test.go` files with its own
  enumerated whitelist: `Config{TargetDir: ...}` construction, `cfg.TargetDir`/`opts.TargetDir`
  reads, `ResolveLaunchTargetDir`, the four existing test-function names carrying the token plus the
  two batch 1 adds, the `launchTargetDir`/`gotTargetDir`/`targetDir` test-local identifiers, and
  doc-comment prose — with the one sanctioned JSON-literal exception being the deliberately-rejected
  `targetDir` argument card 1 adds. The overview's `greps-are-necessary-but-not-sufficient` decision
  and the batch's `## Batch Tests` section were updated to match.
- r1 holistic — "[NIT:consistency] Card 8 declares no Edits and no Commit yet authorizes source
  edits". Card 8 is now explicitly diagnostic-only. The "fix the offending comment or string in the
  file it lives in" instruction is replaced by a requirement to report the failure — naming file,
  line, and offending text — so the batch fails visibly rather than absorbing an uncommitted,
  undeclared edit. `Edits: none` and `Commit: none` are now consistent with the card's own text.
- r1 holistic — "[NIT:consistency] mcp-setup.md's opening summary is not dispositioned". Card 6 now
  requires extending the document's opening `This document covers` clause list with the new
  scoping-contract section, and states why extending is the deliberate choice rather than leaving it
  alone (the summary already omits `## Launch-only flags` as reference material; the new section is
  a top-level one about the server's core contract).
- r1 holistic — "[NIT:consistency] Completeness gate's check three omits exceptSet". `exceptSet`'s
  doc comment is added to check three's mandatory re-read list in card 8, in the batch's
  `## Batch Tests` section, in the batch scope paragraph, and in the overview decision, with the
  reason stated: it paraphrases the deleted helper as "the effective absolute target directory"
  without naming it, so neither spelling of the token surfaces it.

## Pushed Back

_None. All five findings verified as factually accurate against the source files and
`_mill/discussion.md`, and none of the fixes breaks existing functionality, contradicts a documented
decision, or destabilizes work outside this task._
