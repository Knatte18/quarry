MILL_REVIEW_BEGIN
# Review: Port the capability-ladder bench harness to Go — holistic

```yaml
duration_s: 271.0
verdict: REQUEST_CHANGES
reviewer_model: opushigh
reviewer_self_id: claude-opus-5
reviewed_file: plan/
date: 2026-08-30
```

## Findings

### [NIT:consistency] Card 23 uses IngestRecord before card 24 defines it
**Demoted-from:** BLOCKING
**Location:** batch 05-run-state, cards 23 and 24
**Issue:** Card 23's `RunJSONPayload(rec IngestRecord, runModel string) map[string]any` names a type that card 24 is the one to introduce ("a new `ingest.json` marker whose `IngestRecord` carries..."), so card 23's commit cannot compile and the in-batch order is inverted.
**Fix:** Either move `IngestRecord`'s definition into card 23, or move `RunJSONPayload` into card 24 after the type exists.

### [BLOCKING:scope] ingest's Context omits transcript.go and worktree.go
**Location:** batch 12-cli-run-commands, card 58
**Issue:** The card's own Requirements need `ReadTranscript` (to extract usage, run gates, parse the answer) and the worktree dirtiness observation — `ObserveWorktreeDirtied`, whose result `RunGates` takes as its `dirtied GateFinding` argument — but `internal/ladder/transcript.go` and `internal/ladder/worktree.go` appear in neither `Context:` nor `Edits:`.
**Fix:** Add both files to card 58's `Context:`.

### [BLOCKING:scope] No card produces the granted-tool list reader
**Location:** batch 02 card 11 and batch 12 card 58
**Issue:** Card 11 populates `GrantedTools` from a `grantedTools` parameter and card 58 says the list is "read from the copied agent definition", but no card in any batch names a function that parses a definition's `tools:` frontmatter — `agentdef.go` only generates definitions, so the sole producer of that value is unspecified.
**Fix:** Name the parser (file, exported identifier, error behaviour on a malformed or missing frontmatter) in the batch that owns `agentdef.go`, and cite it from card 58.

### [BLOCKING:design] BuildServer's signature carries no seam its test can substitute
**Location:** batch 08-worktree-server-warm, card 36
**Issue:** The card fixes `BuildServer(repoRoot string) (string, error)` and then requires testing "the failure path against a builder function the test substitutes"; that signature has no injection point, unlike card 35, which spells the git-runner parameter out explicitly.
**Fix:** State the seam in the signature (a builder-func parameter, as card 35 does for git) or name the package-level variable the test replaces.

### [NIT:consistency] Done gate is weaker than every batch verify
**Demoted-from:** BLOCKING
**Location:** 00-overview.md frontmatter and Shared Decision "verification scope" / "repo-wide done gate is unchanged"
**Issue:** The plan's configured `verify:` is `go vet ./bench/loomyard-eval/ladder/...`, which runs no tests at all and is not repo-wide, contradicting both decisions' premise that "the repo-wide regression check is already the configured done gate, which runs once at the end" — nothing in the plan ever runs it.
**Fix:** Either point the overview `verify:` at the repo-wide check the decisions assume, or rewrite both decisions to state what the gate actually is.

### [NIT:consistency] Sibling resume filters take arguments in opposite orders
**Location:** batch 05-run-state, card 25
**Issue:** `PendingRuns(pairs []RunPair, resultsRoot string)` and `PendingScoring(resultsRoot string, pairs []RunPair)` are introduced in the same card with their two parameters transposed.
**Fix:** Fix one signature so both read `(resultsRoot, pairs)`.

### [NIT:consistency] `--run-model` outlives the smoke launch that justified it
**Location:** batch 11 card 52, batch 01 card 3
**Issue:** The discussion scopes `--run-model` as a smoke-launch-only override and card 3 justifies `RequireSessionPins`'s override parameter by it, but the overview's no-smoke-launch decision removes its only caller and no protocol step or skill card invokes it.
**Fix:** State the flag's remaining disposition (kept for the follow-up matrix task, or dropped) in card 52.

## Verdict

REQUEST_CHANGES
One in-batch forward reference, two context/scope gaps, an untestable seam, and a weak done gate.
_Note: 2 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 3._
MILL_REVIEW_END
