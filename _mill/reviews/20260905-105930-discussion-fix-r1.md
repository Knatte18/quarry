# Discussion fix — round 1

Source review: `_mill/reviews/20260905-105854-discussion-review-r1.md` (verdict APPROVE, reviewer `orchestrator`).

## Fixed

- `20260905-105854-discussion-review-r1.md` — "[NIT:consistency] D4's decided message text omits what Risks declares required": D4's quoted rendered line now carries the clause "to its repository-relative form", and D4 states explicitly that the clause is required rather than decorative, with the reason (`Detail` echoes the caller's argument verbatim, so a cwd-relative argument yields a non-working `logger.go#`). Testing item 2 now pins D4's sentence verbatim including that clause and gains a cwd-relative argument row so the test exercises the input the clause exists for. The Risks entry on cwd-relative targets is rewritten from an open requirement into a pointer at D4 as the resolved, authoritative wording.

## Pushed Back

None.
