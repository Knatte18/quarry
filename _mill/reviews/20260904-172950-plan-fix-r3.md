# Plan fix — round 3

Source review: `_mill/reviews/20260904-172616-plan-review-r3.md` (holistic, REQUEST_CHANGES,
blocking_count 1, nit_count 4).

## Fixed

- r3 `[BLOCKING:design] Copied schema block plants a reed path in both new prompts` — verified against
  the source and correct. Task 01's exploration schema block carries exactly one real path,
  `"internal/reedengine/geometry.go"`, in its `relevant_files` example (its `key_symbols` example
  already uses the placeholder `path/to/file.go`), and `RenderPrompt` assembles `SchemaBlock` into
  every rendered prompt. Copying it unchanged would have put a real Loomyard package path into task
  06's prompt, whose entire shape is "names no package and no file". Added the overview decision
  `neutral-schema-example-values-in-the-new-task-files`; cards 6 and 8 now make exactly that one
  substitution and state why; card 8's constraint (b) now says "anywhere in the **rendered** prompt —
  the task-text blockquote and the schema block alike". Took the reviewer's second option (neutralise
  the example) over its first (exclude `internal/reedengine` and `internal/reedcli` from card 8's
  pick): the exclusion fixes only the outright constraint violation and leaves the subtler problem
  standing, since a real package path shown as an example anchors an agent whose whole task is
  deciding where to look, regardless of which package the subject lands in.
- r3 `[NIT:consistency] Results-root date substitution stops at batch 5` — promoted the substitution
  rule out of batch 5's batch-local decisions into the overview as
  `results-root-date-substitution`, naming batch 5's cards 15 and 16, batch 6's cards 17 and 18, and
  the overview's `## All Files Touched` as its scope. Batch 5's own decision now points at it. The
  finding is correct: cards 17 and 18 write the path and the root name into two tracked documents, so
  a batch-scoped substitution would have left `docs/roadmap.md` citing a root that does not exist.
- r3 `[NIT:consistency] Card 14 has no commit despite the shared prefix decision` — qualified the
  `conventional-commit-prefixes` decision to cards that produce a diff, and named card 14 explicitly
  as this plan's one verification-only card carrying `Commit: none`.
- r3 `[NIT:scope] A few identifiers named outside their card's Context` — added the files: `run.go`
  and `summarize.go` to card 4's `Context:` (for `Run` and `summary.Incomplete`), `fenced.go` and
  `config.go` to card 5's (for `ExtractFencedJSON` and `cfg.ID`), and `config.go` to card 7's (for
  `validate` matching `configs[].task`). Added the files rather than dropping the identifiers: each
  identifier is the stable name the card's Requirements depends on, and the plan's own principle is
  that stable identifiers beat prose an implementer has to explore for.
- r3 `[NIT:consistency] Card 15 prescribes a report step the run subcommand already performs` —
  verified in `cmd/ladder/main.go:93`: `runCommand` calls `summarizeAndReport(*resultsRoot)` after
  `ladder.Run`. Card 15 now states that no separate report step is needed on a clean run, and scopes
  the `report` subcommand to re-derivation after a resume or a killed invocation.

## Pushed Back

_None. All five findings verified accurate against the files and line references they cite — the two
that turned on source behaviour (`runCommand`'s report call, and the schema block's one real example
path) were both checked directly in `cmd/ladder/main.go` and
`bench/loomyard-eval/tasks/01-reed-geometry-exploration.md` rather than taken on the reviewer's word.
None of the fixes trips a HARM CHECK category; the DAG, batch boundaries and card numbering are
unchanged by this round._
