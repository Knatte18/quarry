MILL_REVIEW_BEGIN
# Review: MCP, thin (T6) — holistic

```yaml
duration_s: 109.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-04
```

## Findings

### [NIT:consistency] scratchtree_test.go's own doc comment contradicts its file header
**Location:** `internal/repopath/scratchtree_test.go:3-4` vs `:27-29`
**Issue:** The file header (lines 1-8) correctly states this is "a deliberate per-package copy of `internal/cli/scratchtree_test.go`'s helper", matching card 3's requirement, but the function-level doc comment on `writeScratchTree` itself (lines 27-29) still says it is "a deliberate per-package copy of `internal/engine/scratchtree_test.go`'s helper" — the lineage CLI's own copy names, not the one true for this file. The two comments in the same file disagree about which file this was copied from.
**Fix:** Update the function doc comment's attribution from `internal/engine/scratchtree_test.go` to `internal/cli/scratchtree_test.go`, matching the file header.

## Verdict

APPROVE
End-to-end plan alignment, shared decisions, layering/stdout guards, goldens, and CLI-mirror tests all verified against source; one doc-comment nit only.
MILL_REVIEW_END
