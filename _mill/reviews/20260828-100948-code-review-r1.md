MILL_REVIEW_BEGIN
# Review: Add `impact` verb for caller-context lookup — holistic

```yaml
duration_s: 174.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-28
```

## Findings

None. Cross-checked all four batches against the overview, every Shared Decision, and every card's
Context/Edits/Creates list.

Verified in detail: `impact` package types (`internal/quarryengine/impact/types.go`) match the
`json-key-disposition` tag rules field-for-field; `enclosingSymbol` and `fileCache`
(`enclosing.go`) implement the greatest-Start rule and the per-call, non-package-level cache exactly
as pinned; `buildResult`/`Impact` (`impact.go`) implement declaration exclusion via a re-implemented
set-membership rule (not an import of `internal/cli`), the three-outcome degradation rule on both
the definition and caller sides, the caller-loop-only cancellation check placed immediately before a
cache-miss parse (confirmed against `TestBuildResult_CancellationStopsAtFirstCallerCacheMiss`'s parse
counts), and the file/line/character stable sort. The facade (`quarry/facade.go`,
`quarry/facade_test.go`) adds the five aliases, the one-line `Impact` delegation, and extends all
three enumerated guard blocks (26 alias pairs, 26 round-trips, 15 blank-identifier assignments) with
no options alias, matching batch 2's local decision. `internal/cli/impact.go` mirrors `refsCommand`'s
shape, registers exactly six flags with the `assertNoCallersCommand`-sourced `--timeout` wording and
the caller-only `--within` wording, and its `emitImpactResult`/`classifyImpactError`/
`filterImpactWithin` helpers reproduce the required two/three-branch error routing and the
`rewordImpactMarshalFailure` re-prefixing. Layering (`layering_test.go`) and seam
(`seam_enforcement_test.go`) guards, `internal/quarryengine/doc.go`'s five enumeration edits,
`internal/cli/toc.go`'s single sanctioned edit (with its companion `--lang` enumeration deliberately
left alone), the `cli.go` package-doc three-way edit, the README verb list and `--build-tags`
sentence, the build-tags verb-table rename to "AllFiveVerbs", and the live-tier test
(`impact_lsp_test.go`, reusing `repoRoot`/`killRecordedDaemonViaFacade` without duplication and
adding only the new `findMethodDeclarationPosition`/`docCommentStartLine` scan helpers) all match
their respective cards. The `impactfixture` tree's line numbers (docstring-before-`func`, the
package-level `var`, two call sites on distinct lines inside `ProcessRefund`) line up with both the
hermetic tests' hard-coded expectations and the live-tier test's scan-based assertions. No
out-of-plan files, no duplicated global helpers, no cross-batch signature drift.

## Verdict

APPROVE
Implementation matches the plan, Shared Decisions, and prior-round conventions end to end with no defects found.
MILL_REVIEW_END
