MILL_REVIEW_BEGIN
# Review: P2 — diff-to-symbols: changed file versions to symbol-table delta (roadmap 2c) — holistic

```yaml
duration_s: 219.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-05
```

## Findings

None. Every batch's cards are realised in the source provided, and the cross-batch contracts hold
end to end.

Specific points checked and confirmed clean:

- **Batch 1 (engine-symbol-seam):** `goDeclOffsets` is the single helper all six builders call
  (`internal/engine/golang.go`); `assertSymbolsEqual` zeroes the three new offset fields exactly as
  card 2 requires (`internal/engine/golang_test.go:307-314`); `offsets_test.go` covers all eight
  shapes with a non-zero-`DeclEnd` assertion.
- **Batch 2 (engine-unit-exports):** `PackageClause`, `UnitsForClauseMap` and `ClauseMapForFiles`
  are extracted rather than duplicated; `dirPackage`/`unitFor` are thin wrappers over them
  (`internal/engine/walk.go`, `internal/engine/units.go`); the UTF-8 check lives inside
  `PackageClause` as required.
- **Batch 3 (delta-types-and-tokens):** the type set, JSON key order and doc-comment requirements in
  `delta_answer.go` match the plan verbatim. `identicalModuloName`'s substitution rule is widened
  from a literal "identifier" kind to `isIdentifierNode` (any `*_identifier` suffix) — a deliberate,
  documented deviation from the card's summary wording, necessary because a Go method's own name node
  is `field_identifier`, not `identifier`; the receiver-hazard test
  (`TestIdenticalModuloName/ReceiverSharesPrefixWithOldName...`) exercises exactly this and passes,
  so the widening is correct and tested rather than a scope slip.
- **Batch 4 (delta-core):** table keying by `(id, kind)`, multiset tuple comparison, the seven
  exact-tier conditions (uniqueness checked before condition 7, per the card's own ordering note),
  the evidence-tier's three-condition gate with no threshold, and the full ordering pass all match
  `04-delta-core.md` card-by-card; `sameOwner` is reused from `resolve.go` rather than reimplemented.
- **Batch 5 (gitsrc):** sentinel + typed-error pairing with `Unwrap`, `-no-renames`, `-z` splitting,
  the `--exclude-standard` flags, and the immediate-children trim for both enumeration sides are all
  present with the required symmetry.
- **Batch 6 (facade-delta):** aliasing is exhaustive and alias-only (no copies); `GitDeltaAnswer`'s
  field order and pointer `To` match the card; `codeForDeltaError`-equivalent error propagation is
  unchanged through the layers; `layering_test.go` widens the import check exactly as card 37
  specifies (gitsrc forbidden over mcpserver/cmd/quarry-mcp/cli, engine only over the first two) and
  leaves the stdout check's scope untouched.
- **Batch 7 (delta-renderers):** `RenderDeltaJSON` delegates to the shared encoder; `RenderDeltaText`
  spells out the kind on every symbol/table-key line the shared `writeSymbolLine` doesn't, and the
  before-array's own location-line grammar is implemented as its own helper as required.
- **Batch 8 (cli-delta-verb):** flag parsing, verb-gate widening, usage text, pipeline, and all three
  doc-comment corrections (`cli.go`, `usage.go`, `doc.go`, `repopath/target.go`) are consistent and
  name four verbs / two path-taking verbs throughout.
- **Batch 9 (goldens-history-docs):** all seven golden cases exist as 14 files under
  `quarry/testdata/delta/`, internally consistent with the core's own rules (line-shift symbol
  absent, lossy flag present, evidence-tier candidates ordered by similarity then id);
  `TestDeltaRealHistory` skips cleanly on an unreachable pin and asserts the one genuine rename as a
  candidate, never asserted, exactly as the batch requires; `docs/rewrite-plan.md` carries the new
  delta paragraph, the corrected Loomyard mechanical-use bullet, and the four-queries opening count;
  `docs/roadmap.md` is untouched, per the plan's explicit instruction.
- No out-of-plan files, no duplicated helpers across batches, and every batch's `verify:` `-run`
  pattern matches test function names actually present in the corresponding files.

## Verdict

APPROVE
All nine batches match their plan cards; shared decisions hold consistently across the whole change.
MILL_REVIEW_END
