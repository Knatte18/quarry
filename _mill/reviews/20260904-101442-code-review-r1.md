MILL_REVIEW_BEGIN
# Review: Facade + CLI, resolve + expand (T5b) — holistic

```yaml
duration_s: 110.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-04
```

## Findings

None.

Verified end-to-end against all four batch files and the overview's Shared Decisions:

- `quarry/quarry.go`, `quarry/repo.go`, `quarry/render.go`, `quarry/text.go`, `quarry/doc.go` implement exactly the aliases, delegating methods, shared `renderJSON` encoder, symbol-line file prefix, and the two text renderers per batch 1's cards, with the regression golden (`TestRenderText_SymbolLine_EmptyFileRegressionGolden`) and totality-guard tests present as specified.
- `internal/cli/target.go`'s `repoRelPath`/`repoRelTarget` split is behaviour-preserving, matches the doc-comment contract, and `target_test.go` covers agreement, rebasing, and leading-`..` non-rejection as required.
- `internal/cli/flags.go` gates three verbs, enforces per-verb flag validity before value validation, spells arity and missing-separator messages exactly as specified, and `flags_test.go` updates the three invalidated rows (missing-verb, first-arg-is-flag, unknown-verb replaced with "bogus") rather than merely adding new ones.
- `internal/cli/usage.go`'s `usageText` matches the plan's literal block byte for byte.
- `internal/cli/cli.go` extracts `runTOC` unchanged, adds `runResolve`/`runExpand` and the three exit-code mapping functions exactly per cards 10–12, including the payload-before-code ordering, the no-stat rule for both new verbs, and the engine-error-field-untouched rule (pinned by `TestRun_Resolve/path-escapes-root`'s exact-string assertion).
- `internal/cli/glyph5_test.go` builds its own fixture (never the pinned checkout) and proves all four resolve statuses plus expand's not-a-type and ambiguous cases, asserting key presence/absence as required.
- `internal/cli/after_test.go` and `loomyard_test.go` widen `afterGoldenCase` with `verb`/`exitCode` fields, turn the blanket assertions into per-case ones, and the eight new evidence files under `docs/research/output-formats/after/` carry the correct invocation-line/blank-line/stdout-verbatim shape with no trailer and no absolute paths.
- `docs/research/output-formats/after/INDEX.md` is a total before-to-after mapping with an exit-code cell on every row, matching card 17's required table and prose exactly.
- No out-of-plan files, no duplicated helpers across batches (the two `writeScratchTree` copies and two `codeFor*` mapping families are the deliberately-documented per-package/per-verb duplication the plan calls for, not accidental drift).
- Cross-batch contracts hold: `repoRelPath`/`repoRelTarget` (batch 2) are consumed correctly by `runResolve`/`runTOC` (batch 3); `codeForResolveResult`/`codeForExpandAnswer`/`codeForExpandError` (batch 3, card 10) are the single source both pipelines and their tests route through; batch 4's golden table and status-gate consume batch 3's `Run` dispatch without assuming undocumented behaviour.

## Verdict

APPROVE
All four batches faithfully realize the plan with no blocking issues found.
MILL_REVIEW_END
