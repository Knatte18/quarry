# Discussion fix — round 5

Source review: `_mill/reviews/20260830-110358-discussion-review-r5.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, 1 demoted from BLOCKING).

## Fixed

- r5 `[NIT:consistency]` (demoted-from BLOCKING) "Comments naming deleted `effectiveTargetDir` undispositioned" — verified by `grep -rn effectiveTargetDir internal/mcpserver/`: seven production comment sites reference the deleted helper (`translate.go:23`, `callcontext.go:8`, `callcontext.go:62`, `tools_toc.go:5`, `tools_toc.go:162`, `tools_toc.go:185`) plus `nativeentry.go:119`'s paraphrase "the effective absolute target directory", none previously dispositioned. Added a bullet to `delete-effectivetargetdir` giving one disposition for all of them (reword to name `Config.TargetDir` as the single source), covering the two test sites, and requiring `grep -rn 'effectiveTargetDir' internal/mcpserver/` to return zero hits. Added that grep to Verification and explicitly excluded the identifier from the intentional-survivor whitelist, which previously would have waved every one of them through.
- r5 `[NIT:consistency]` "Toc input-struct comments mischaracterised" — verified: `tools_toc.go:22-24` and `tools_toc.go:40-43` read "plus the per-call overrides toc_file/toc_dir accepts", a back-reference like `symbolInput`'s, not an enumeration. Corrected the second enumeration criterion's characterisation and noted that the toc pair's "per-call overrides" wording is itself the stale phrasing this task removes.
- r5 `[NIT:consistency]` "`workspaceKey` line citation disagrees with itself" — verified: the function body is `paths.go:76-86`; `88-99` is `buildTagsSegment`. Corrected the Problem section's `76-97` to `76-86`, matching Technical context.

## Pushed Back

None.
