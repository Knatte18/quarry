# Discussion fix — round 1

Source review: `_mill/reviews/20260905-134302-discussion-review-r1.md` (holistic, APPROVE,
0 blocking, 1 nit).

## Fixed

- `20260905-134302-discussion-review-r1.md` — "[NIT:scope] Bare iota-continuation const specs
  unaddressed". Verified the behaviour empirically against tree-sitter-go before writing the fix
  rather than asserting it: a bare continuation spec's signature `const B` parses cleanly and
  yields exactly one symbol, so the round trip is not at risk. Technical-context gotcha 2 rewritten
  to state both the explicit (`const A Kind = iota`) and the bare-continuation (`const B`) forms
  and to name why the distinction matters (an iota enum is Loomyard's most common grouped-const
  shape). Added a "Verified against tree-sitter-go, not assumed" table recording the measurement
  for all seven fragments exercised, including the `type T struct` partial-parse result that is
  the completion retry's trigger and the observation that every emitted id carries the parameter
  unit `u/v` rather than the synthetic clause `q`. Added the reviewer's requested table-test case
  to the Testing section as its own named case, with the `var` counterpart, and stated the
  attribution argument for why it is not folded into the grouped-const case.

## Pushed Back

None.
