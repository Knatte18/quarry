# Plan fix — round 6

Source review: `20260903-181536-plan-review-r6.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 3 NIT).

## Fixed

- r6 `[BLOCKING:design] Fake claude's flag contract rejects the scorer invocation` — card 33 now
  states that the fake stands in for **two** invocations reaching it through one binary path within
  one harness process, so per-invocation environment cannot discriminate them, and names the
  argument-level discriminator card 26 already fixes: `--tools ""` together with `--max-turns 1` is
  the scorer, anything else is a measured cell. Each branch gets its own asserted flag set, and the
  scorer branch asserts a scorer model and effort that differ from the cell's so a mix-up fails
  rather than passing quietly.
- r6 `[BLOCKING:scope] Claude CLI flag spellings have no Context source` — card 26 now spells both
  argument vectors literally rather than describing them semantically: the measured cell's and the
  scorer's, each flag by name. The card records why the literal spelling is load-bearing — the turn
  ceiling flag is accepted by the CLI but absent from its own help output, so an implementer cannot
  recover it cold from the tool. This was preferred to adding `docs/rewrite-plan.md` to two
  `Context:` lists: the plan's own cards become the source, rather than pointing at a long document
  for ten tokens.
- r6 `[NIT:consistency] Resume test contradicts the reps-identical merge rule` — the finding is
  correct and the specified test could never have passed. Card 34's resume case is restated at a
  **constant** effective repetition count: a two-cell, two-repetition matrix run first with the cell
  selector restricted to one cell, then again unrestricted, asserting the first cell's repetitions
  are skipped while the second cell's are produced. That is also the case the merge rule's union of
  selected cells exists for, so one test now covers both.
- r6 `[NIT:consistency] "Exactly two gate entry points" then a third exported check` — card 16's
  opening count now reads four exported entry points and names all four.
- r6 `` [NIT:design] `WriteMCPConfig`'s `dir` parameter is undefined `` — the signature is now
  `WriteMCPConfig(quarryRepoRoot, name string, doc []byte)`, the caller passes the repository root
  and nothing pre-joined, and the function joins `.scratch/ladder/` and creates it itself, so the
  scratch location stays a single fact instead of one every caller re-spells.

## Pushed Back

_None._
