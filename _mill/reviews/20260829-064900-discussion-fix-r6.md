# Discussion fix — round 6

Source review: `_mill/reviews/20260829-064445-discussion-review-r6.md` (APPROVE, 0 BLOCKING, 5 NIT).
All five NITs fixed under the `mill-receiving-review` fix-everything default; none pushed back.

## Fixed

- r6 — "impact entry `target` key collision unresolved": named a `result` wrapper key for `impact`'s and `toc_file`'s marshalled struct results in result-shape, so the envelope's echoed `target` always survives; stated it as a deliberate divergence from the CLI's `runBatch` flattening and why the CLI never hits it (`symbol` identity key, no collision).
- r6 — "workspace_symbol bad entry: schema vs handler": corrected the Testing tier-1 claim to the per-entry rule; a position or file-scoped entry to `workspace_symbol` is now consistently `status: "error"` for that entry across entry-shape-lsp-mirrored, input-schema-strictness, and both test tiers.
- r6 — "toc entry arrays: objects or strings": scoped input-schema-strictness's element-type clause — objects for the five symbol/position tools, plain strings for `toc_file`/`toc_dir`.
- r6 — "`docSentences` JSON type unstated": pinned the schema type as `["integer", "string"]`, with integer→decimal-string conversion before `ParseDocSentences`, so `{"docSentences": 3}` is not a whole-call failure.
- r6 — "README and gitignore dispositions unstated": added both to Scope — a README section naming `cmd/quarry-mcp` with its build line and a link to `docs/mcp-setup.md`, and a `/quarry-mcp` `.gitignore` entry (no `!/quarry-mcp/` re-include needed, unlike `/quarry`, since there is no colliding package directory).

## Pushed Back

None.
