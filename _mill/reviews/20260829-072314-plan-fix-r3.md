# Plan fix — round 3

Source review: `_mill/reviews/20260829-072209-plan-review-r3.md`

## Fixed

- r3 "Card 2's call-site enumeration misses four sites" — verified by grep against
  `internal/cli/cli.go`: `filterWithin` has six call sites (lines 197, 210, 355, 372, 385, 689) and
  `absOrJoin` three (350, 861, 876), where the card named two contexts. Card 2 now enumerates every
  call site by enclosing function with counts and adds an explicit "grep for each old spelling and
  leave none behind" instruction.
- r3 "Shared Decision pins `For[T](nil)`, card 10 needs options" — the overview's
  `schema-derivation-and-patching` Decision now names `jsonschema.For[T]` called with the package's
  own `ForOptions`, never `nil`, and states why: the `docSentences` type schema is registered
  through `TypeSchemas`, and inference would otherwise reduce that type to a property-less object.
- r3 "Doc-comment prose updates enumerated unevenly" — verified all three sites. Card 1 now covers
  `resolveContext`'s doc comment in `internal/cli/cli.go`, card 3 covers `internal/cli/impact.go`'s
  file header comment, and card 5 covers `TOCDirEntries`' doc comment in `internal/cli/toc.go`.
- r3 "Card 28's `.claude-plugin/` claim is inaccurate" — rationale reworded to "at a plugin's own
  root, beside its `.claude-plugin/plugin.json`". The `mcpServers` decision itself is unchanged.

## Pushed Back

_(none)_
