# Fixer report: plan-fix round 4

Source review: `20260830-120549-plan-review-r4.md` (holistic, opushigh).

## Fixed

- `20260830-120549-plan-review-r4.md` — "[BLOCKING:design] e2e test cannot reach the handoffs it exists for": accurate and structural. Resolved by moving both drift-prone assemblers into the library rather than by relocating the test. Batch 5 now adds `NewIngestRecord` (gate report to ingest record), `ReadIngestRecord`, and `RunJSONPayload` (ingest record to run-marker payload), with a batch-local decision recording that a command-package home would put them out of reach of the only test whose purpose is catching drift across those seams. Cards 58 and 61 become thin callers; card 67 drives the assemblers directly and asserts the full gate-report to summary chain.
- `20260830-120549-plan-review-r4.md` — "[BLOCKING:scope] Card 53 Context omits runstate.go": accurate. Added to card 53's `Context:`, and the requirement now names `RunDirPath`, `Invalidate`, and `MaxAttempts` explicitly.
- `20260830-120549-plan-review-r4.md` — "[NIT:decision] cold_abort.json has one writer and an unspecified reader": accurate. Card 53 now fixes the file's schema in one place — `config_id`, `rep`, `attempt`, and `cause` — and card 63 cites the filename and the `cause` key it matches on, with `preparesession.go` added to its `Context:`.
- `20260830-120549-plan-review-r4.md` — "[NIT:consistency] Card 16 contradicts itself on the blinding gate's outcomes": verified against `gates.py` — after the dead literal check is dropped there are two unconditional fatal checks, then a short-circuit, then one conditional check with a fatal and a non-fatal outcome. Card 16 now describes exactly that structure, requires the short-circuit to be ported, and adds a test that a transcript tripping an unconditional check yields no bare-mention finding.
- `20260830-120549-plan-review-r4.md` — "[NIT:scope] RunGates' new signature is never given": accurate; its parameter list changes materially and only prose described it. Card 26 now states the full signature and lists the three changes from the Python — the dropped unused run-directory parameter, the added ceiling, and the injected dirtiness observation.
- `20260830-120549-plan-review-r4.md` — "[NIT:scope] ingest's description derivation is unstated": accurate. Card 58 now names `DispatchDescription` and `NextAttempt` as the source, matching what card 54 pins for the other command, so both provably build the same string.
- `20260830-120549-plan-review-r4.md` — "[NIT:scope] Card 49 Context omits server.go": accurate — the allowlist probe's session declares the server. Added.

## Pushed Back

_None._
