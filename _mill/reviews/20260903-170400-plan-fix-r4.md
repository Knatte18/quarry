# Plan fix — round 4

Source review: `20260903-170129-plan-review-r4.md` (holistic, REQUEST_CHANGES, 2 BLOCKING, 2 NIT).

## Fixed

- `20260903-170129-plan-review-r4.md` — "[BLOCKING:design] golangci-lint verify rests on a refuted
  premise": the hub config's comment is stale, not the plan's premise, and the Decision now says so
  and carries the evidence — `command -v golangci-lint` resolves to `/home/knatte/go/bin/golangci-lint`,
  `golangci-lint version` reports `v1.64.8 built with go1.26.0`, and `golangci-lint run` exits 0 at
  the parent tip. The Decision also records that correcting the hub config comment is deliberately
  left out of this task's scope.
- `20260903-170129-plan-review-r4.md` — "[BLOCKING:scope] \"green at parent tip\" unverified; no lint
  config exists": the observed run and the absence of any `.golangci.yml`/`.golangci.yaml` — so the
  default linter set — are both now recorded, and the second remedy is applied as well: the module-wide
  `verify:` lint half is scoped to `./glyph/...`, so this task never depends on the pre-existing cgo
  engine packages staying clean under a linter set this plan did not choose. `go vet` stays
  repository-wide.
- `20260903-170129-plan-review-r4.md` — "[NIT:consistency] Tie-break rule points at the aliasing bug
  card 1 forbids": Shared Decision 1 gains an explicit carve-out naming card 1's
  `make`-plus-two-`append` construction as the authoritative reading of the discussion's print-form
  line.
- `20260903-170129-plan-review-r4.md` — "[NIT:consistency] `section` column is written but never
  read": the Batch Scope decision now requires every table-driven test to compose its `t.Run` subtest
  name from the row's `name` and `section` fields, and names the `unused` report that would otherwise
  be plausible.

## Pushed Back

_None._

## Note on the first finding's premise

The finding's second clause — "the module-wide verify would therefore fail command-not-found at both
batch boundaries" — is factually wrong: the tool is installed and was run. The finding is still fixed
rather than pushed back, because its first clause is accurate (the plan asserted an exit-0 run
without recording the evidence, against a hub-config comment saying the opposite), and recording the
evidence is what resolves the contradiction for the next reader.
