MILL_REVIEW_BEGIN
# Review: Per-capability quarry-mcp benchmark suite — holistic

```yaml
duration_s: 224.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-29
```

## Prior non-blocking items — status

- **Fenced-json regex duplication** (`score_run.py` vs `run_ladder.py`): resolved. Both now import a single `extract_fenced_json(text, which="first"|"last")` from `ladder_config.py` (`bench/loomyard-eval/ladder/scripts/ladder_config.py:426-443`), used by `score_run.py:249-259` (`which="first"`) and `run_ladder.py:543-550,660-674` (`schema_for` uses `"first"`, `_extract_answer` uses `"last"`). One compiled pattern, one call site per selection semantics. Not escalating.
- **Cold-cell "not_run" cause conflation**: resolved. `run_ladder.py`'s `run_cold_cell` now tracks `not_run_causes` per repetition, distinguishing `"live_daemon_before_start"` (gate_cold_before found a live daemon before an attempt started) from `"native_fallback_exhausted"` (every attempt exhausted `MAX_ATTEMPTS` on the native-fallback branch), and `_not_run_reps_reason()` reports both when both occur (`run_ladder.py:923-1032`). Covered by `test_run_ladder.py`'s two "CARD 21/25" tests (`:419-486`), which also cross-check against `summarize.build_summary`. Not escalating.

## Findings

### [NIT:consistency] README Layout tree omits `cold_cell.json`
**Location:** `bench/loomyard-eval/ladder/README.md:371-401`
**Issue:** `summarize.build_summary` reads `<results_root>/cold_cell.json` (`summarize.py:363-365`) and it is a real, git-tracked artifact (not under the gitignored `raw/` subtree) central to the cold-cell disposition reported in `_meta`, but the Layout section's results-directory tree lists only `probe.json`, `summary.json`, `conclusion.md`, and `raw/`.
**Fix:** Add `cold_cell.json` (tracked) to the results-directory tree alongside `probe.json`.

## Verdict

APPROVE
Both prior non-blocking findings are now fixed; no new blocking issues found across all seven batches and their source/tests.
MILL_REVIEW_END
