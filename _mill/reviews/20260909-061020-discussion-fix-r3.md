# Discussion fix — round 3

Source review: `_mill/reviews/20260909-060929-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, 1 demoted).

## Fixed

- `20260909-060929-discussion-review-r3.md` — "[NIT:consistency] Files-that-change table omits internal/cli/cli.go" (demoted from BLOCKING): verified — the Technical context table listed six files while Scope and three Decisions require three edits in `internal/cli/cli.go`. Added the `internal/cli/cli.go` row naming all three edits (the `Rejected()` lift plus its doc comment, both arity-guard deletions, and the `strconv` import removal), so the work-inventory table and Scope now enumerate the same set.
- `20260909-060929-discussion-review-r3.md` — "[NIT:consistency] `quarry.Resolve` named as an existing facade symbol": verified — `grep '^func Resolve' quarry/*.go` matches nothing; the tree has only `(*quarry.Repo).Resolve` (`quarry/repo.go:87`) and package-level `quarry.Name` (`quarry/name.go:22`). Rewrote the decision to name the two entry points that exist and to state why there is no package-level `quarry.Resolve` (resolve needs a repository; `Name` does not). The decision itself — no facade change — is unaffected.
- `20260909-060929-discussion-review-r3.md` — "[NIT:design] `Name` echo-mismatch message under a two-field divergence": accurate — "names whichever field diverged" was undefined when both `Unit` and `Target` diverge at one index, while Testing asked for an exact message assertion. Fixed the tie-break in the Decisions panic-message spec: `Unit` is checked first and reported alone; `Target` is reported only when `Unit` matched. Updated the Testing paragraph to match (got/want for the diverging field, with the two-field case covered explicitly and asserted to report `Unit` alone).

## Pushed Back

_None._ All three findings were factually accurate and no fix breaks existing functionality, conflicts with a documented design decision, or destabilises work outside this task's scope.
