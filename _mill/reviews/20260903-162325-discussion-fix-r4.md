# Discussion fix — round 4

Source review: `_mill/reviews/20260903-162216-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 5 NIT, reviewer `opushigh`).

## Fixed

- r4 `[NIT:consistency]` "Whitespace-ban blast radius understated" — the Decision "The Go unit alphabet" now itemises the real cost of dropping the ban (one predicate, two reject rows deleted, two rows reworded, and the unit half of the "Input is not trimmed" decision retracted), and open question #2 points at that itemisation instead of repeating the "nothing else moves" claim.
- r4 `[NIT:design]` "`ParseError.Detail` undefined for four reasons; message untested" — the error decision now states what `Detail` holds for each reason and fixes it as the empty string for `no_separator`, `unsupported_language`, `member_empty` and `member_too_deep`; Testing gains two smoke assertions ranging over `Reasons` — every reason's `Error()` is non-empty, and the fifteen messages are pairwise distinct — so Scope's "names what was wrong" requirement is verified without freezing wording.
- r4 `[NIT:scope]` "§3's receiver-pair example is not in the accept table" — `internal/logger#dualHandler.Handle` and `internal/logger#durableHandler.Handle` added to the accept list, with the note that the pair must assert they differ only in `Owner`, which is the point §3 makes with it.
- r4 `[NIT:consistency]` "Unit segment bans `#`, but no reason covers it" — the `#` clause is removed from the segment rule, with an explicit note that it is unreachable after the first-`#` split and must not be implemented; the `/` clause is likewise restated as "the separator, so it cannot occur inside a segment by construction" rather than a checked ban.
- r4 `[NIT:scope]` "Test package placement stated for only one file" — Testing now opens by pinning all three test files to `package glyph`, with the three concrete reasons they must share one package (unexported split, shared accept table, cross-file reject rows).

## Pushed Back

None. Under `--orch` no operator is present, so PUSH BACK is unavailable; all five findings were verified accurate and none met a harm category.
