# Plan fix — round 3

Source review: `_mill/reviews/20260905-121000-plan-review-r3.md` (holistic, APPROVE, 0 BLOCKING +
4 NIT, one of the NITs demoted from BLOCKING by the stage ceiling).

## Fixed

- r3 holistic, `[NIT:consistency] _test self form: doc says none, code pins one` (demoted from
  BLOCKING) — accurate and the one substantive finding of the round. Card 33 told the implementer to
  write that the external test unit "has no self form of its own", while cards 6, 9, 16 and 31 all
  table its self glyph as well-formed and resolving `not_found` with the unit found. Discussion D10a
  says both things — "no self form *that resolves*" in its rationale and the looser "has no `unit#`
  of its own" in its D15 summary — and the resolving half is what the engine implements. Card 33 now
  states the precise disposition (well-formed, always `not_found`, unit reported found) and
  explicitly forbids the blanket denial, naming card 36 as the reason: that card turns §2's claims
  into test rows, so a denial would become a test asserting a rejection the parser does not perform.
- r3 holistic, `[NIT:consistency] Falsified-comment count disagrees with the overview` — accurate:
  the Shared Decision cited D7's seven while card 28 enumerated ten. Reconciled in the overview
  rather than by trimming the card: D7's seven plus D21's `usage.go` line are all carried, and the
  three sites planning found beyond them — the `exitUsage` constant's enumeration, `runTOC`'s step 1,
  and `Run`'s closing message-composition paragraph — are now named there. The decision records that
  card 28's ten is the authoritative per-card number and shows the arithmetic, since D7's own text
  says a site it did not name is a finding against its list.
- r3 holistic, `[NIT:consistency] Batch 2 claims parallelism the DAG forbids` — accurate; a leftover
  from restructuring batch 3's edge from `[1]` to `[2]` during planning. Batch 2's scope paragraph
  now says the same thing batch 3's already does: no data dependency, sequenced deliberately so each
  batch's `verify:` is a statement about that batch alone.
- r3 holistic, `[NIT:scope] parses == 0 does not prove "no directory was read"` — accurate; verified
  that `parses` is incremented only in `unitMemo.symbolsOf`, while the directory work happens in
  `dirsOf` through `unitDirs` and `dirExists` and is uncounted. Card 22 now requires both seams —
  `parses` zero and the `dirs` map empty — and says why neither alone is the claim, and it fixes the
  test's own doc-comment wording to "no unit was parsed and no unit-directory lookup was made". Card
  19's rationale carried the same overclaim and was corrected with it.

## Pushed Back

_(none — every finding was accurate and every fix was applied.)_
