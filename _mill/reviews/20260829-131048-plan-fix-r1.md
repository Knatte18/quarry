# Plan fix — round 1

Source review: `20260829-131302-plan-review-r1.md` (holistic, REQUEST_CHANGES, 7 BLOCKING / 4 NIT).

## Fixed

- `20260829-130302-plan-review-r1.md` — "[BLOCKING:design] `gate_cold_after`'s premise is false for toc-only runs". Verified against `internal/mcpserver/tools_toc.go`: `tocFileHandler` and `tocDirHandler` call `effectiveTargetDir` and `tocPreflight` directly and never `resolveCall`, so a toc call never reaches `EnsureServer` and writes no `daemon.json`. Added `DAEMON_BACKED_TOOLS` (batch 1 card 3), `used_daemon_backed_tool` and a three-outcome `gate_cold_after` (batch 3 card 9), the `cold_no_daemon_backed_call` observation through `run.json` and the summary (batch 3 card 10, batch 5 cards 13/15), the third cold-cell disposition (batch 6 card 21, batch 8 card 26), the conclusion's two-reason statement (batch 9 card 28), and a new overview Shared Decision recording the finding and its consequence for what the cold cell can deliver.
- `20260829-130302-plan-review-r1.md` — "[BLOCKING:scope] `warm_daemon` and the probe need an MCP client nobody plans". Named the mechanism in batch 6 card 17: `mcp_call`, a stdlib-only MCP stdio client, with the newline-delimited framing grounded in `cmd/quarry-mcp/main.go`'s `mcp.StdioTransport{}` and the SDK transport's own documentation. Pinned the warm-up to `WARM_UP_TOOL = "workspace_symbol"` with the rationale that a toc warm-up would start no daemon, and added a card 22 test asserting `WARM_UP_TOOL` is a member of `DAEMON_BACKED_TOOLS`.
- `20260829-130302-plan-review-r1.md` — "[NIT:consistency] Resume contradicts `build_worktree`'s existence check". Rewrote `ensure_task_worktrees` (batch 6 card 16) as the idempotent caller: build when absent, adopt after `restore_worktree` when present at the declared pin, raise naming both SHAs when present at a different one. Added the probe skip on an existing passing `probe.json` to card 22's CLI order, and tests for both in card 22.
- `20260829-130302-plan-review-r1.md` — "[BLOCKING:scope] `task_text_for` cannot get a schema from task 01". Confirmed task 01 has no schema section. Split `task_text_for` from a new `schema_for` (batch 6 card 19) that reads the impact schema from task 04's own `## Output schema` section and the exploration schema from the benchmark README's `## Output schemas` section, selected by the task's declared `schema` field, raising when the named section is absent. Added both task files to card 19's `Context:`.
- `20260829-130302-plan-review-r1.md` — "[BLOCKING:design] No field links the cold cell to its warm counterpart". Added `warm_counterpart: a5-bundle` to the cold entry's schema (batch 1 card 2), the `warm_counterpart` field on `LadderConfig`, `warm_counterpart_for`, and four validation raises (batch 1 card 3), and rewrote `compare_warm_cold` to resolve through it (batch 5 card 14) — no comparison in the suite is now built by parsing an id.
- `20260829-130302-plan-review-r1.md` — "[BLOCKING:scope] Batch 5 re-derives `is_complete` without depending on batch 3". Changed batch 5's `depends-on` to `[1, 3]` in both the overview Batch Index and the batch frontmatter, added `gates.py` to card 13's `Context:`, and stated that `load_runs` calls `gates.is_complete` as the single definition of run completeness.
- `20260829-130302-plan-review-r1.md` — "[NIT:consistency] `summary.json` promises fields its loader never reads". Added `denied_tool_attempts` to `METRICS` and stated that `load_runs` reads `run.json`'s observations alongside `usage.json` and `score.json` (batch 5 card 13), with the aggregate counts in card 15 sourced from them.
- `20260829-130302-plan-review-r1.md` — "[BLOCKING:design] Two client flags are asserted without the planning check the others got". Ran both checks: `--permission-mode dontAsk` was exercised in the two planning probes, and `--effort high` alongside an explicit `--model claude-opus-5` in a third, all against client 2.1.236. Replaced the inline note with a dedicated overview Shared Decision listing every flag the harness depends on and recording that each was exercised.
- `20260829-130302-plan-review-r1.md` — "[BLOCKING:scope] \"Wait for the previous run's daemon to exit\" names no mechanism". Confirmed `daemon.json` carries `pid` and that `daemonStale` reads it via `proc.IsAlive`, while only `daemon.sock` is removed on exit. Added `daemon_pid` and `wait_for_daemon_exit` with an `os.kill(pid, 0)` liveness predicate (batch 3 card 9) plus its three tests, and named `DAEMON_EXIT_TIMEOUT_S = 660` as the bound at the call site (batch 6 card 21).
- `20260829-130302-plan-review-r1.md` — "[NIT:consistency] `wall_clock_ms` is extracted but never summarised". Added `wall_clock_ms` to `METRICS` with a sentence on why both spans are reported (batch 5 card 13).
- `20260829-130302-plan-review-r1.md` — "[NIT:design] `load_ladder` does not reject two controls in one ladder". Added the "more than one config whose `allowed` is empty" raise, with the reason that it leaves `control_for` undefined (batch 1 card 3).

## Pushed Back

_(none — every finding verified as accurate, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task's scope.)_
