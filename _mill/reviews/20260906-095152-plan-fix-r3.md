# Plan fix — round 3

Source review: `_mill/reviews/20260906-095030-plan-review-r3.md` (holistic, APPROVE,
0 BLOCKING / 5 NIT; two of the five demoted from BLOCKING by the stage's blocking-class ceiling and
handled as NITs).

## Fixed

- r3 holistic, `[NIT:consistency] Decision says absolute root, card 29 writes relative` — the
  overview's `results-root-date-resolved-once` Decision said "absolute" while card 29 prescribed
  worktree-relative. Worktree-relative is made authoritative in both places, with the reason stated:
  it is what card 30's `--results <RESULTS_ROOT>` invocation needs, since every harness command runs
  from the worktree root.

- r3 holistic, `[NIT:decision] Void primary test has no M4b / roadmap disposition` — card 31 defined
  a void outcome that neither card's M4b disposition covered. Card 31's M4b sentence now has three
  branches (separating, non-separating, void). Card 32's edit 3 gains a void branch: carry the M4b
  conditional forward verbatim, attached to the surviving point, saying it awaits a future
  measurement — neither deleting it (asserting a negative the void run cannot support) nor promoting
  M4b to its own point (asserting a positive it cannot support). Card 32's edit 1 gains matching
  wording for the standing-rule sentence.

- r3 holistic, `[NIT:scope] .scratch/kickstart-run.log created but not declared` — the log is now
  declared under card 30's `Creates:` and added to the overview's All Files Touched, matching card
  29's treatment of the equally git-ignored `.scratch/kickstart-results-root.txt`.

- r3 holistic, `[NIT:consistency] resolve exit-code gloss is not the pack's own gate` — verified
  against `internal/cli/cli.go:85-96` (`codeForResolveResult` returns exit 0 for `StatusMultipart`
  as well as `StatusFound`) and `pack.go:47-64` (the renderer is fatal on any status other than
  `StatusFound` and on a found result carrying other than exactly one symbol). Card 29's enumeration
  is rekeyed onto the `--text` output — the first line's status word must be `found` and exactly one
  symbol line must follow — with the exit-code trap named explicitly, and a zero-failure branch added
  that halts (a pack that failed while every target resolves cleanly is a fault in the pack path, not
  in a glyph). The reserve-candidate cascade is bound to the same gate.

- r3 holistic, `[NIT:consistency] Batch Tests under-enumerates what -run 'TestPreMatrix' runs` —
  verified at `prematrix_test.go:41`. `## Batch Tests` now names `TestPreMatrix_ControlPromptsAreBlind`
  as the sixth selected test and states that it is keyed on `ladder-toc.yaml` rather than on this
  batch's artefacts, so the selector is knowingly broader than the checks the batch relies on.

## Pushed Back

_None._
