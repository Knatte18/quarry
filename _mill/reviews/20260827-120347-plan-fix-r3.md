# Plan fix — round 3

Source review: `_mill/reviews/20260827-120255-plan-review-r3.md` (verdict REQUEST_CHANGES, 3 BLOCKING, 2 NIT, reviewer sonnetmax).

## Fixed

- `20260827-120255-plan-review-r3.md` — "[BLOCKING:scope] Card 8 omits `*lsp.Client` method-call retargeting": accurate. Card 6 said "and its methods to the exported names from card 4"; card 8 listed only type substitutions, leaving the nine method invocations in `refs.go`/`definition.go`/`symbol.go` unaddressed. Enumerated all nine explicitly in card 8 and called out `.documentSymbol(` -> `.DocumentSymbols(` as the one that is not a plain capitalization, since a mechanical uppercase-the-first-letter pass would get exactly that one wrong.
- `20260827-120255-plan-review-r3.md` — "[BLOCKING:scope] Card 8's refs_integration_test.go `builtins()` enumeration misses a call site": accurate. Line 188 is `t.Skip(builtins()["go"].InstallHint)` inside `TestReferences_InFile_Integration`, structurally identical to line 71's skip guard, which is why it was missed. Added it, restated the total as six occurrences (five code call sites plus the line-180 doc mention), and noted why it is easy to overlook.
- `20260827-120255-plan-review-r3.md` — "[BLOCKING:scope] Card 9's Context omits query/definition.go": accurate. `Definition`'s signature lives in `query/definition.go`, and card 9 must reproduce it byte-for-byte in the facade wrapper; `Context:` is an allowlist, so the implementer could not have opened it. Added.
- `20260827-120255-plan-review-r3.md` — "[NIT:consistency] Card 14's Creates: none contradicts its own Requirements": accurate. Resolved by making the split unconditional rather than contingent: `internal/quarryengine/daemon/doc.go` is now a real `Creates:` entry, `daemon/ensureserver.go` moves from card 14's `Edits:` to its `Context:`, and the new path is added to the overview's All Files Touched. A contingent create would have left All Files Touched indeterminate, which is the deeper problem the finding points at.
- `20260827-120255-plan-review-r3.md` — "[NIT:consistency] Card 6's \"eight call sites\" figure undercounts its own list": accurate — the enumeration names ten occurrences (nine code call sites plus one doc mention). No site was missing; corrected the lead-in count and split it into "nine code call sites plus one doc-comment mention" so the two figures cannot drift apart again.

## Pushed Back

None.
