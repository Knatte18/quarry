# Plan fix report — round 5 (round cap)

Source review: `20260905-164008-plan-review-r5.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 3 NIT,
one NIT demoted from BLOCKING by the stage's blocking-class ceiling).

This is the last round under the operator's cap of 5. Every finding below was acted on before the
cap was reached; see the approve commit for the operator's standing waiver of the max-rounds halt.

## Fixed

- r5 BLOCKING:design "Card 17 states no rule that makes invalid UTF-8 an error entry" — verified
  accurate: the parse seam performs no UTF-8 validation and errors only for an unwired language, a
  failed language set, or a nil tree, so undecodable bytes would reach the grammar, yield a partial
  tree, and be reported as merely lossy — while card 22 asserts an error disposition for that exact
  input and card 48's `entry-error` golden pins it. The rule the tests assert had no stated
  implementation. Card 17 now rejects invalid UTF-8 per side before parsing, as an extraction failure
  for the entry, and states that this is the same rule card 6 gives the clause helper and the same
  one the existing walk already applies — the core is applying an existing engine rule, not inventing
  a condition.
- r5 BLOCKING:scope "internal/repopath/target.go's stale toc-only claims are unowned" — verified
  accurate: routing the new verb's target through that helper falsifies two of its doc comments — the
  unexported converter's claim that the table-of-contents verb is the only one reaching its exported
  form, and the exported wrapper's enumeration of its callers — and the file sat in a `Context:` list
  with no card able to edit it, while the plan corrects the equivalent claim in six other files.
  `internal/repopath/target.go` moves from card 44's `Context:` to its `Edits:`, the two comments are
  named in that card's Requirements, and the file is added to `## All Files Touched`.
- r5 NIT:decision (demoted) "Card 37 edits internal/mcpserver with no recorded departure" — verified
  accurate, and the finding this plan's own machinery earned: the departure rule added in round 2
  caught the card added in round 3. The discussion's Scope "Out" list states that nothing in that
  package or its command changes. Resolved by recording it rather than dropping the guard: a new
  Shared Decision names the overridden bullet, bounds the departure to one test file, states that no
  production file, tool, envelope or golden on that side moves — which is what the bullet exists to
  protect — and gives the reason the guard is worth it. Decision 1 now names two stated departures
  rather than one.
- r5 NIT:consistency "Card 6 rests its read-ordering gate on an environment-gated test" — verified
  accurate: that timing assertion gates on a pinned external checkout and skips both when it is
  absent, the normal state, and under the short-test flag. My round-4 wording presented the read
  ordering as mechanically guarded everywhere. Card 6 now states plainly that the test catches the
  regression only where that checkout exists, and that the ordering is a rule the implementer must
  honour by reading rather than one a failing test will report.
- r5 NIT:consistency "Card 48 miscounts the existing `-update` flag precedents" — verified accurate;
  three packages declare their own flag today, not two. Corrected, with the per-package-binary
  rationale now reading against the real set.

## Pushed Back

_None. All five findings verified accurate against the cited code or against `discussion.md`, and
none met any harm category of the receiving-review decision tree. No finding has been repeated across
any of the five rounds, and no round has produced a pushback._
