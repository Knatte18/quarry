# Plan fix — round 1

Source review: `20260909-062740-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING / 2 NIT).

## Fixed

- `20260909-062740-plan-review-r1.md` — "[BLOCKING:scope] Card 9 leaves `Run`'s doc comment
  describing deleted guards". Verified against `internal/cli/cli.go`: `Run`'s doc comment documents
  the `runResolve` guard as numbered step 4 (lines 275–277) and folds the identical sentence into
  `runName`'s step 1 (lines 339–343). Card 9's `Requirements:` now names both paragraphs, requires
  the `runResolve` step deleted with the numbering closed up, requires the sentence struck from
  `runName`'s first item, requires one replacement sentence citing `verifyResolveCoverage` /
  `verifyNameCoverage` as the reason the single result is now taken unconditionally, and requires
  every step-number cross-reference in the same comment — the `runExpand` paragraph's "continuing
  from step 4 above" named explicitly — to be re-checked after renumbering.
- `20260909-062740-plan-review-r1.md` — "[NIT:consistency] New test subjects, unextended test-file
  headers". Card 2 now requires `internal/engine/answer_test.go`'s header extended to name the
  vocabulary-completeness and the two fail-closed predicates; card 6 now requires both edited test
  files' headers extended to name that file's coverage verifier and its two panic message shapes.
- `20260909-062740-plan-review-r1.md` — "[NIT:consistency] `TestStatusCompleteness` breaks the
  file's own test-name shape". Renamed to `TestStatus_Completeness` in card 2, and card 2 now states
  that all three prescribed names follow the file's `TestSubject_Case` convention and are to be kept
  as written.

## Pushed Back

_None._
