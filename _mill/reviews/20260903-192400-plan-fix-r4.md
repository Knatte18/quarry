# Fixer report: plan-fix round 4 — Engine core (T3)

Source review: `_mill/reviews/20260903-192008-plan-review-r4.md` (holistic, APPROVE, 0 BLOCKING +
4 NIT, one of the NITs demoted from BLOCKING by the stage ceiling).

## Fixed

- `[NIT:consistency]` "Unspellable units error, they do not return []" — verified against
  `glyph/golang.go`'s `checkGoUnit`: the space-bearing fixture trips the bad-rune rejection and the
  empty-unit case trips the empty-unit rejection, and card 32 has `SpansOf` validate every argument
  through `glyph.Parse` before reading anything. The round-3 bullet I wrote asserting an empty slice
  was therefore unsatisfiable, and its "one statement rather than two" claim rested on it. Resolved
  in favour of card 32's validation, which is the task's own design decision, not a fixable
  accident: card 34's bullet now asserts the wrapped `*glyph.ParseError` via `errors.As` — the same
  rule as its hand-built-invalid-glyph bullet — and states that the walk's "listed but no symbols"
  and the lookup's "rejected outright" are two dispositions of one fact. Card 30's forwarded
  sentence is amended to say the same.
- `[NIT:consistency]` "Batch 3 Batch Scope names a seam no card declares" — verified: no card
  declares `fileUnitPackage`; it is a leftover from an earlier draft. The list now names the four
  seams card 17 actually declares — `dirPackage`, `dirDoc`, `fileEntry`, `walkDir` — and says unit
  derivation arrives in batch 4 as `unitFor`.
- `[NIT:consistency]` "Batch 6 verify deviates from the Shared Decision" — verified. Resolved by
  amending the Decision rather than by dropping `vet` from batch 6: card 40's own subject is making
  `go vet` clean, and a batch cannot check the thing it exists for by deferring to a gate that runs
  after it. The Decision now permits a batch inserting `vet` exactly when vet-cleanliness is that
  batch's own subject.
- `[NIT:design]` "File-target answer never names which seams build it" — verified: card 18 named
  the file target's answer shape without saying the four directory facts come from `dirPackage` and
  `dirDoc` over the whole enclosing directory. The card now names both seams, states that a file
  target therefore reads every `.go` file in its enclosing directory, and names the failure the
  rule prevents — emitting the target's own clause as the directory's `package` whenever the
  directory holds a clause deviation, which is the very case the tie-break exists for.

## Pushed Back

_(none — every finding verified accurate, and no fix broke existing functionality, conflicted with
a documented decision, or destabilised other work.)_

## Re-validation

`_plan_dag.validate` and `_plan_validate.run` re-run clean: DAG OK, zero validator findings.

## Convergence

Round 4 returned `APPROVE` with `blocking_count: 0`, and `min_rounds` is 1, so the review loop
converges here. The plan is approved.
