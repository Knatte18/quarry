MILL_REVIEW_BEGIN
# Review: Rethink quarry-mcp's per-call targetDir ergonomics — holistic

```yaml
duration_s: 242.0
verdict: REQUEST_CHANGES
reviewer_model: opushigh
reviewer_self_id: Claude Opus 5 (claude-opus-5)
reviewed_file: plan/
date: 2026-08-30
```

## Findings

### [BLOCKING:scope] check one-b whitelist omits real test-side survivors
**Location:** Batch 2 / card 8 (and `## Batch Tests` check 2)
**Issue:** The `_test.go` whitelist is written as exhaustive ("Its intentional survivors are: ...") but omits assertion-message strings and field-naming doc comments that already carry the token and that no card edits: `tools_assert_test.go:150` and `transport_errors_test.go:278` (`"...relative except resolved against targetDir must exempt wrapper.go"`), `callcontext_test.go:120` (`"opts.TargetDir = %q..."`), `nativeentry_test.go:146` (`"...resolved against targetDir %q"`), and the `TargetDir`-naming doc comments at `tools_lsp_test.go:54` and `transport_test.go:65`. None is a `Config{}` construction, a `cfg.TargetDir`/`opts.TargetDir` read, a test-function name, a listed test-local, or a doc comment naming the server's target directory.
**Fix:** Extend check one-b's enumeration with a sanctioned category for `t.Errorf`/`t.Fatalf` format strings and doc comments that name the `TargetDir` field or the `targetDir` parameter, so the gate's "expected to pass on arrival" claim holds and no unowned hit forces a card-8 failure report.

## Verdict

REQUEST_CHANGES
Card 8's test-side grep whitelist is under-enumerated; the gate fails on arrival as written.
MILL_REVIEW_END
