# Plan fix — round 3

Source review: `20260904-064358-plan-review-r3.md` (holistic, APPROVE, 0 BLOCKING + 3 NIT).
All three findings verified as accurate; none met a harm-check escape. All three fixed.

## Fixed

- `20260904-064358-plan-review-r3.md` — "[NIT:consistency] File comment may become a second package
  doc". Accurate: the overview required a file-level comment on every new file but never said it
  must be separated from the `package` clause by a blank line, and Go treats an abutting comment as
  a package doc. Since `quarry/` and `internal/cli` each hold several files beside a `doc.go`, the
  omission would have given both packages competing package docs. The
  `comment-discipline-matches-answer-go` decision now states the blank-line rule, names
  `answer.go`/`repo.go`/`toc.go` as the practice being followed, and records that the one comment
  which deliberately abuts its `package` clause is each package's own `doc.go`.
- `20260904-064358-plan-review-r3.md` — "[NIT:scope] CGO_ENABLED=0 glyph build check has no home".
  Accurate: `discussion.md`'s Testing block names a green `CGO_ENABLED=0 go build ./glyph/...` among
  its build checks, and the `cgo-stays-required-and-unguarded` decision covered only the "add no
  build tag" half — no card and no `verify:` ran the positive check. Added batch 5 card 24, a
  zero-diff verification card (`Commit: none`, every path field `none`) running three checks: the
  `./glyph/...` build green under `CGO_ENABLED=0`; the whole-module `CGO_ENABLED=0` build still
  failing through `internal/cgoguard`'s undefined identifier, which is the guard working; and
  `go build ./... && go test ./...` green with cgo. Both failure directions are reportable, and the
  card explicitly forbids "fixing" a red check by adding a build tag. The overview decision now
  points at the card, and batch 5's `## Batch Tests` records why the card sits outside the batch's
  `verify:` — its checks are about the environment they run under, which one `go test` invocation
  cannot express.
- `20260904-064358-plan-review-r3.md` — "[NIT:design] Step 4's non-usageError branch unstated".
  Accurate: steps 1, 5, 6 and 9 each state both dispositions and step 4 stated only the usage one,
  closed today only implicitly by card 11 returning `usageError` exclusively. Card 16 step 4 now
  spells the `exitInternal` branch and records that card 11's contract makes it unreachable today —
  stated anyway so a later change to that contract cannot fall through to a zero exit code.

## Pushed Back

None.
