# Plan fix report — round 4

Source review: `20260905-163054-plan-review-r4.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 2 NIT).
Both BLOCKINGs land on the same seam — the clause vote shared by batch 2 and batch 6 — and both were
introduced by my own card 6 wording rather than by the discussion.

## Fixed

- r4 BLOCKING:design "UTF-8 check dropped on the revision-side clause vote" — verified accurate, and
  it was a departure from `discussion.md`'s own contract for that function made without recording a
  Shared Decision, which the plan's own departure rule forbids. My card 6 had pushed the validity
  check out to the caller; the on-disk caller performed it and the revision-side caller in card 34
  could not, so a tracked file with invalid bytes would have voted on one side of a directory's
  comparison only — and a one-sided vote shifts the dominant clause, which shifts every glyph unit in
  the directory. Fixed by restoring the check inside `PackageClause`, where both sides pass through
  one rule, with the reason stated in the card. Cards 8 and 34 are updated to take the check from
  there rather than repeating it, card 9's table gains the invalid-UTF-8 row that pins the check's
  location, and card 34 now states the one-rule-both-sides property explicitly.
- r4 BLOCKING:design "Rewired dirPackage would read every file before the extension check" — verified
  accurate against `internal/engine/walk.go`, where the language and strategy lookups run before the
  read today. My card 6 removed the lookups while telling the function to keep its read "where it
  is", which read literally makes the walk's first pass read every entry in every directory —
  binaries, testdata and assets included — before consulting an extension. The output would have been
  identical, which is exactly why it needed catching here: the walk's own priced cost note and the
  timing assertion in `internal/engine/loomyard_timing_test.go` are what regress, and that assertion
  is selected by this batch's `verify:` as of round 2's fix. Card 6 now requires the extension guard
  ahead of the read, states that this is the order the existing code already runs in, and names the
  regression it prevents; card 8 applies the same guard.
- r4 NIT:decision "Card 37 leaves the stdout check's scope over internal/cli unstated" — verified
  accurate: the two checks share one directory-locating helper, and card 37 gave a disposition for
  only one of them. Card 37 now states that the import check alone widens and the standard-output
  check keeps its current scope, with the reason — the command-line package writes the rendered
  answer to standard output as its entire job, so carrying that rule onto it would fail immediately
  and for something this task never decided. The mechanism is stated too: each check builds its own
  directory list at its own call site, so the scopes diverge by construction rather than by accident.
- r4 NIT:scope "Run-level exit-3 case for a failed git command is not planned" — verified accurate
  against the discussion's own command-line testing list, which names it. Card 47 now includes it,
  constructed by making the fixture repository's object store unreadable after the top-level and
  revision checks have passed, following the permission technique and cleanup the existing
  unreadable-file case in `internal/engine/answer_test.go` already uses, and skipping when the
  process can read regardless. The card states why the case earns its place: it is the only one that
  pins the message shape, since the mapping-function table asserts the code alone.

## Pushed Back

_None. All four findings verified accurate against the cited code or against `discussion.md`, and
none met any harm category of the receiving-review decision tree._
