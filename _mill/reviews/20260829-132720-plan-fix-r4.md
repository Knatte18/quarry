# Plan fix — round 4

Source review: `20260829-132427-plan-review-r4.md` (holistic, REQUEST_CHANGES, 4 BLOCKING / 5 NIT).

## Fixed

- `20260829-132427-plan-review-r4.md` — "[BLOCKING:scope] METRICS drops every impact-only scoring field". Accurate: `decoy_admitted`, `lookalikes_matched`, and `summary_matches` were written by card 12 and never reached the summary, so card 28 — which may source numbers only from `summary.json` — could not have reported the decoy at all, the one finding task 04 exists to surface. Added `lookalikes_matched` to `METRICS` as an ordinary count, and gave the other two the per-cell treatment a median cannot provide: `decoy_admitted_count` as its own column never folded into precision, and `summary_matches` carried through verbatim as a list. Reconciled card 13's missing-metric sentence with the per-schema difference it was actually describing, and added the decoy column to card 28's required content (batch 5 cards 13 and 15, batch 9 card 28).
- `20260829-132427-plan-review-r4.md` — "[BLOCKING:design] No stage-selected harness entry point for cards 24-26". Accurate: batch 8 executes the probe, the main matrix, and the cold cell as three cards with their own commits, but the CLI ran all three in one invocation, so the only way to reach a card boundary was to interrupt a running matrix. Added a `--stage probe|main|cold|all` selector (default `all`), pointed cards 24, 25, and 26 at their own stage, and added a card 22 test asserting each stage runs only its own drivers.
- `20260829-132427-plan-review-r4.md` — "[NIT:consistency] `resolve_state_dir` halts on env the harness already scrubs". Accurate: `run_env` scrubs both variables from every subprocess, so a raise keyed on the harness's own ambient environment would halt on any machine that merely has one exported, for no reason. Changed the function to take the environment it resolves **for** as an explicit `env` argument rather than reading `os.environ`, passed `run_env()` in production, and restated the raise as a guard against being asked about an environment the runs were not launched with (batch 3 card 9).
- `20260829-132427-plan-review-r4.md` — "[NIT:decision] `max_turns` and the scorer pin carry no values and no gate". Accurate on both halves. Pinned `max_turns: 60` with its rationale, and `scorer.model: claude-opus-5` / `scorer.effort: high`, both confirmed accepted by the installed client; clarified that the prompt template is selected per task rather than pinned. Replaced `require_run_model` with `require_pins`, which raises on any of `run_model`, `max_turns`, `scorer.model`, or `scorer.effort` being unset, with `run_model` remaining the only one that ships null and the only operator prerequisite (batch 1 cards 2 and 3, batch 6 card 22).
- `20260829-132427-plan-review-r4.md` — "[BLOCKING:scope] Card 17 Context omits `gates.py` and the LSP handler source". Added `bench/loomyard-eval/ladder/scripts/gates.py` and `internal/mcpserver/tools_symbol.go` to card 17's `Context:` — the card's `warm_daemon` post-condition calls `gates.resolve_state_dir` and its whole premise rests on `workspace_symbol`'s handler calling `resolveCall` once per call, and only `tools_toc.go`, which documents the negative case, was listed.
- `20260829-132427-plan-review-r4.md` — "[BLOCKING:design] \"Cold cell not run\" has no durable record". Accurate and it broke three downstream cards: a not-run cold cell would have sat on disk as a cell with zero complete runs, indistinguishable from an interrupted one, so card 15's CLI would have exited non-zero, card 27's stated exception was unimplementable, and card 28 could not have said which reason applied. `run_cold_cell` now persists `<results_root>/cold_cell.json` recording the disposition, the confirmed-cold count, and the reason; `summarize.py` reads it into `_meta` and excludes a `not-run` cold cell from `incomplete` and from the non-zero exit; and cards 27 and 28 read the same record (batch 5 cards 13 and 15, batch 6 card 21, batch 8 cards 26 and 27, batch 9 card 28).
- `20260829-132427-plan-review-r4.md` — "[NIT:decision] Fasit free-text fields name quarry; only `_meta` is stripped". Accurate — the committed task-04 fasit says "quarry impact on singlellm.go:39:2" in three `evidence` entries and one `reason`. Stated the disposition and the reason it differs from the answer's: the fasit is one fixed file identical across all 45 runs of its task, so it cannot tell the scorer *which config* it is grading, which is the only thing the blinding claims; the answer varies per rung and is therefore redacted. Redacting the fasit would also damage the evidence fields the recall and precision rules match against. Added an assertion that the fasit's evidence text survives verbatim while `_meta` does not (batch 4 card 12).
- `20260829-132427-plan-review-r4.md` — "[NIT:scope] `extract_usage`'s error paths have no named test". Named one test per documented raise: a malformed line naming its line number, an absent `init` event, and an absent terminal `result` event — the last being the shape a run that died mid-way actually leaves behind (batch 2 card 7).
- `20260829-132427-plan-review-r4.md` — "[NIT:consistency] Scorer dispatch omits the permission mode every other dispatch carries". Added `--permission-mode dontAsk` to the default scorer `runner`, with the reason: a scoring call that reached for a tool would otherwise block on a prompt no one is there to answer, stalling the matrix mid-run (batch 4 card 12).

## Pushed Back

_(none — every finding verified as accurate against the committed fasit, the plan's own cross-batch contracts, and quarry's source, and no fix broke existing functionality, conflicted with a documented decision, or destabilised work outside this task's scope.)_
