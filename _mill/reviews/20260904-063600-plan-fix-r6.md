# Plan fix — round 6 (final round)

Source review: `20260904-061622-plan-review-r6.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 3 NIT).

All five findings were verified accurate and all five are fixed. The round is the last the configured
round budget allows; the operator instructed that the plan be approved and handed off after round 6
regardless of convergence, so no round 7 runs to re-review these fixes. See the note at the end.

## Fixed

- `[BLOCKING:scope] Marshal coverage claim for ExpandAnswer is false` — verified: round 5's fix added
  two marshals that both assert `members` and `candidates` **absent**, so neither key's present
  spelling was ever observed, and the claim that all six keys were covered "in both their present and
  their omitted state" was false. Added a marshal to `TestExpand_Struct` (asserting `head` and
  `members` present, `members` holding three entries) and to `TestExpand_AmbiguousBuildTags`
  (asserting `candidates` present with two entries). Card 17's closing sentence now enumerates all
  four marshals and which key each observes in which state, rather than asserting the coverage
  abstractly.
- `[BLOCKING:scope] ResolveResult's present-state tags unpinned by the same gap` — verified: card 11's
  only marshal was of a `not_found` result, leaving `symbols`, `candidates`, `dir`, `error` and
  `reason` observed only in their absent state. The finding correctly turns card 2's own reasoning
  back on the plan. Card 11 now additionally marshals a `found` result (`symbols` present) and an
  `ambiguous` one (`candidates` present); card 13 marshals the existing-file path result (`dir`
  present, `id` and `unit` absent) and one rejected-target entry (`error` and `reason` present,
  `status` absent). All nine of `ResolveResult`'s keys are now observed both ways.
- `[NIT:consistency] Gap 1 is recorded twice in one file` — verified: `unitDirs`'s existing doc comment
  already states the external-test-unit-versus-real-directory gap almost verbatim, and card 7 required
  it restated at the collision read while card 9 rewrote that very comment without noting the overlap.
  Card 9 now explicitly preserves `unitDirs`'s statement as the surviving copy, and card 7 writes one
  sentence pointing at it plus the only genuinely new half — that this verb promotes the reported
  collision to `ambiguous`, which `unitDirs` could not state because the status type did not exist when
  its comment was written.
- `[NIT:consistency] Card 2 miscounts card 9's comment edits` — verified: card 2 said "three", card 9
  says five clauses across four comments. Card 2's cross-reference now matches.
- `[NIT:consistency] Symbol.File's "batch 5 adds" clause left undispositioned` — verified: the sentence
  names a batch of T3's plan, which collides with this plan's own batch 5 and which no reader of
  `answer.go` can resolve. Card 2's item 3 now requires the whole sentence rewritten rather than just
  its "later verb" half, naming `symbolsOfUnit` as what fills `File` and `Resolve`/`Expand` as what
  emit it.

## Pushed Back

_None._

## Note on convergence

Rounds 1 through 6 each produced findings and each was fixed; round 6's own fixes are therefore
unreviewed. Six BLOCKING findings were raised across the six rounds and all six were fixed; one
finding across all rounds was pushed back as factually wrong (round 4's module-wide-verify claim). The
two BLOCKING findings this round were both of one kind — a coverage claim broader than the assertions
behind it — and both fixes are additive test requirements, not design changes, which is the lowest-risk
category of unreviewed change to hand to an implementer.
