# Plan fix report — round 3

Source review: `20260905-162006-plan-review-r3.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 5 NIT,
one NIT demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r3 BLOCKING:scope "Batch 1 breaks TestGoStrategy_Symbols; no card updates it" — verified accurate
  and the most serious finding of the round: `assertSymbolsEqual` compares whole Symbol values with
  a deep equality check, zeroing only the glyph field, so filling three new offsets fails every
  hand-written expectation in that file. My batch's own claim that "the broader suite has nothing new
  to say about it" was simply wrong. Fixed by zeroing the three offsets on the helper's own copy the
  way the glyph already is, rather than spelling byte offsets into roughly seventeen literals — those
  expectations pin the emitted shape, and the offsets are JSON-hidden internals card 5 pins directly.
  The edit is placed in card 2, the first card that fills any offset, so the suite is never red
  across a commit; `internal/engine/golang_test.go` is added to that card's `Edits:` and to
  `## All Files Touched`; and `TestGoStrategy` is added to batch 1's `verify:` in both the batch
  frontmatter and the overview's Batch Index, with the Batch Tests section rewritten to say why that
  half of the pattern is the regression gate.
- r3 BLOCKING:scope "Card 42's verb-list message change breaks TestParseArgs_UsageErrors" — verified
  accurate: two rows of that table hand-copy the parser's missing-verb sentence as literal bytes, and
  card 46 never mentioned them, so batch 8's own verify would have failed. Card 46 now names both
  rows as expectations to update, and states the contrast that makes card 43's opposite claim correct
  — every usage-text assertion compares the constant by reference, which is why that one needs no
  test edit and this one does.
- r3 NIT:consistency (demoted) "rewrite-plan.md §1's \"three queries\" left stale by card 50" —
  verified accurate; the opening section introduces quarry as three queries and names them. Card 50
  now makes that a third edit in its own commit, so the document this plan owns does not keep the
  count the plan corrects in four Go doc comments.
- r3 NIT:consistency "Two file header comments go stale unfixed" — both verified. Card 31 now
  corrects `quarry/quarry.go`'s header, which describes the file as aliasing the engine's identity
  alone while that card adds a second internal package's; card 43 now corrects `internal/cli/usage.go`'s
  header, whose explanation of the combined flags list counts three per-verb sections and three
  shared flags.
- r3 NIT:scope "Text view's rendering of a modified entry's `before` array is unspecified" — verified
  accurate: a before array element is a location block with no identifier, signature or docstring, so
  it is the one shape that cannot use the shared symbol-line writer, and it was the one shape card 39
  gave no grammar for. Card 39 now specifies the location line and requires it spelled in the
  renderer's own doc comment, since it has no existing precedent in the file.
- r3 NIT:scope "Existing all-verbs flag-validity test not extended to the fourth verb" — verified
  accurate; that table asserts three verbs while card 42 widens the parser's claim to four. Card 46
  now adds the two rows, so the widened claim is pinned rather than only documented.
- r3 NIT:design "gitsrc left outside the only mechanical layering gate" — verified accurate: card 37
  guarded a Shared Decision this plan invented with a one-time human inspection. Card 37 is now a
  real card that widens the existing layering test instead of adding a second mechanism: the git
  plumbing package joins the forbidden set, and the command-line package joins the scanned set
  checked against that path alone — the package the Shared Decision is actually about. The engine
  path is deliberately not added to the command-line package's forbidden set, since that would be a
  rule this task never decided. Batch 6's `verify:` now runs that test too, in both the batch
  frontmatter and the overview index, because a widened import gate that the widening batch does not
  run is not a gate; and batch 9's final sweep is corrected to expect this one test edit under the
  mcp server package rather than no change at all.

## Pushed Back

_None. All seven findings verified accurate against the cited code, and none met any harm category
of the receiving-review decision tree._
