MILL_REVIEW_BEGIN
# Review: Thin quarry/ facade over internal/quarryengine — holistic

```yaml
duration_s: 678.0
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-27
```

## Findings

### [BLOCKING:design] killRecordedDaemon/DaemonStateFile unreachable after daemon/query split
**Location:** batch 2 (cards 6, 7, 8), batch 3 (card 11)
**Issue:** `refs_integration_test.go` (→ `query`, card 8) calls `killRecordedDaemon(t, statePath)` at lines 87, 201, 239, an unexported helper card 6 relocates into `daemon`'s `supervised_integration_test.go` — cross-package and unreachable regardless of import. The same file's `DaemonStateFile(stateDir, "go")` calls (lines 86, 200, 238) need `daemon.DaemonStateFile`, but card 11's own layering table permits `query`'s `_test.go` files to import only `daemontest`, never `daemon` directly, so the one qualification that would compile is the one the plan's own guard rejects.
**Fix:** State the missing decision — e.g. add a daemon-state inspection/cleanup helper to `daemontest` (mirroring the toolchain-seam treatment) or an explicit layering carve-out for `DaemonStateFile`/`DaemonLock` — and thread it through cards 6, 7, 8 and 11's table, the same way `repoRoot` and the toolchain seams were resolved.

### [NIT:consistency] position_test.go's caller-facing Position literal not disambiguated
**Location:** batch 2, card 4
**Issue:** `position_test.go` (→ `lsp`) has two `Position{File: path, ...}` literals (lines 31, 51) meaning the caller-facing `quarryengine.Position`, which collides in name with the file's own newly-renamed wire type (`lspPosition` → `Position`, fields `Line`/`Character` only). Card 4's "update ... to the new names throughout" enumerates six old→new renames but never flags that these two pre-existing `Position{...}` literals need `quarryengine.Position{...}` qualification plus a new import — a mismatch a naive rename would leave as a "unknown field File" compile error.
**Fix:** Add a sentence to card 4 stating position_test.go's two caller-facing `Position{File:...}` literals become `quarryengine.Position{...}`, requiring the added `internal/quarryengine` import.

## Verdict

REQUEST_CHANGES
Card 8's daemon-cleanup calls in refs_integration_test.go have no compiling path under card 11's own layering rule.
MILL_REVIEW_END
