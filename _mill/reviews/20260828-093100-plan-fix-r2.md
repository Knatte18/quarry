# Plan fix — round 2

Source review: `_mill/reviews/20260828-092428-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 5 NIT).

## Fixed

- r2 holistic, "[NIT:consistency] Batch 4 verify differs between index and batch file" (demoted from BLOCKING) — verified and it was a real defect of my round-1 fix: I edited batch 4's frontmatter `verify:` but left the overview Batch Index entry on the old `go vet -tags lsp` command. Since the index is the authoritative DAG mill-go reads, the round-1 BLOCKING fix would not actually have taken effect. The index entry is now byte-identical to the batch file's `verify:`.
- r2 holistic, "[BLOCKING:scope] Doc-site ownership map omits internal/cli/toc.go" — verified against that file's header comment, which justifies `toc`'s bypass of `resolveContext`/`buildOptions` by enumerating "(refs/definition/symbol/assert-no-callers)". `impactCommand` uses both helpers, so the enumeration goes stale. Assigned the file to batch 3 in the `doc-site-ownership-by-touching-batch` Shared Decision (with a note on why a verb-set grep would never surface it — the file is named for a different verb group), added it to card 11's `Edits:` as a fourth doc edit, and added it to `## All Files Touched`.
- r2 holistic, "[BLOCKING:scope] Card 6 Context omits the fixture files it asserts against" — verified: card 6's `Requirements:` names `ApplyDiscount`, the package-level `var`, the Python `Shape`/`area` pair, and the TypeScript fixture, none of whose files were listed. Added all three fixture paths to card 6's `Context:`, matching card 17's already-correct practice.
- r2 holistic, "[NIT:design] Card 11's --help-ordering rationale is false" — verified: `EnableCommandSorting` appears nowhere in the repo and defaults to true, so `--help` sorts alphabetically regardless of `AddCommand` order. Kept the placement, restated the reason as source-order readability, and added an explicit instruction not to claim it controls `--help`.
- r2 holistic, "[NIT:consistency] --timeout help string copied from the wrong verb" — verified: `refsCommand`'s "(initialize, resolve, references)" is narrower than the phase set `query.Callers` actually runs, and `assertNoCallersCommand` already carries the correct wider wording. Card 10 now says to copy `refsCommand`'s help strings for five flags but `assertNoCallersCommand`'s for `--timeout`, with the reason stated and an explicit reminder not to name the parse phase.
- r2 holistic, "[NIT:design] Cancellation scope undefined for the definition-side parse" — verified the gap between card 5's caller-loop-only scoping and card 7's ambiguous "first cache-miss parse" wording. Card 5 now states explicitly that the definition-side lookup is deliberately not cancellation-checked (one bounded parse before the loop, versus the loop's unbounded file count, which is what the `parse-loop-cancellation-and-timeout-scope` Shared Decision scopes the check to). Card 7's case was rewritten to assert that ordering rather than merely observe an error.
- r2 holistic, "[NIT:scope] Card 5 names toc.TOCFile without toc/toc.go in Context" — verified. Added `internal/quarryengine/toc/toc.go` to card 5's `Context:`.

## Pushed Back

_None. Every finding passed VERIFY and none triggered a harm-check escape._
