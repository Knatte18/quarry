# Discussion fix — round 3

Source review: `_mill/reviews/20260904-083946-discussion-review-r3.md` (APPROVE, 0 BLOCKING, 6 NIT, 1 demoted).

## Fixed

- `20260904-083946-discussion-review-r3.md` — "[NIT:consistency] Three new goldens get no INDEX.md row or exit code": D15's table gains three `*(none)* | <after file> | new: …` rows for `resolve-not-found.txt`, `resolve-path.txt` and `expand-not-a-type.txt`, using the same device T5a applied to the two text-view goldens, plus an explicit statement that every after-side file therefore carries an exit-code cell — which is D14's rationale for writing no trailers into the goldens.
- `20260904-083946-discussion-review-r3.md` — "[NIT:consistency] D15 miscounts the rows it says stay unchanged": the preserved rows are now described as they actually are — two `toc` before→after rows, two compact-view "no successor" rows, two `*(none)*` text-view rows — and stated to gain only an exit-code cell.
- `20260904-083946-discussion-review-r3.md` — "[NIT:consistency] D2's envelope-only list predates D10a": D2's enumeration now carries the fourth payload-free path (`expand`'s exit-1 grammar rejection, D10a) and states that D3's table is the authority the list tracks.
- `20260904-083946-discussion-review-r3.md` — "[NIT:consistency] D13's exit-1 usage line omits the grammar rejection": the proposed `usageText` line now reads "not found, outside the repository, ambiguous, not a type, or not a well-formed glyph".
- `20260904-083946-discussion-review-r3.md` — "[NIT:design] Payload `target` for a path result is unstated": D8 gains an explicit bullet — a glyph target echoes argv verbatim, a path target echoes the repository-relative form the CLI hands the engine — grounded in `internal/cli/doc.go`'s existing "output is always repository-root relative" rule and plan §4. The cwd == repository root assumption behind every golden and every example, D10b's `resolve ../x` included, is now stated. A testing scenario pinning the asymmetry was added.
- `20260904-083946-discussion-review-r3.md` — "[NIT:scope] D3's exit-2 row and `repoRelPath`'s own error are unenumerated": D3's exit-2 row now names `discoverRoot`'s `no repository root found above …; pass --root`, and a new exit-1 row covers a `repoRelPath` failure with `toc`'s own message for the same condition, with the reasoning (no relative form exists, so there is no payload for D2's rule to apply to, and diverging from `toc` would make one argument exit differently under two verbs).

## Pushed Back

None. All six findings verified accurate against the discussion text and the merged code (`internal/cli/root.go`'s `discoverRoot` message, `internal/cli/target.go`'s `filepath.Rel` branch, `after/INDEX.md`'s actual six-row table, `internal/cli/doc.go`'s path-frame rule). No fix breaks existing functionality, conflicts with a documented decision, or destabilises work outside this task.
