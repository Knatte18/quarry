# Discussion fix — round 4

Source review: `_mill/reviews/20260828-085259-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 5 NIT — 1 demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- `20260828-085259-discussion-review-r4.md` — "[NIT:consistency] `definition.error` rule contradicts the caller rule": replaced the single-sentence "same degradation rules ... error when one did not" with three explicitly separated `definition` outcomes — resolved; parsed-but-no-enclosing-symbol (ranges omitted, `error` **absent**, `target` omitted, symmetric with the caller-side file-scope rule and reachable because `toc.Kind` is function/method/type only); and parse-or-language failure (`error` present). Added the middle shape to the assembly-step test list as a case that must be asserted separately from the third.
- `20260828-085259-discussion-review-r4.md` — "[NIT:scope] Doc-update inventory misattributes and under-covers": rewrote the Scope doc-update bullet as a per-site inventory. Corrected the third "seven-package DAG" attribution to `seam_enforcement_test.go`'s header (line 10, not the `minPackageDirs` comment), noted `TestEngineSeamInvariant_BannedImports`' doc comment needs no edit, and added the two previously-missing sites: `doc.go:41-46`'s second package enumeration and `README.md:17`'s "All four verbs accept `--build-tags`" (already wrong today at six verbs). Updated the Technical context gotcha to name all three "seven-package DAG" files.
- `20260828-085259-discussion-review-r4.md` — "[NIT:scope] Batch status classifier omitted from the reuse ledger": added `classifyLookupError` and `emitLookupResult` to the not-reusable bullet alongside `filterWithin`/`filterUnexpectedCallers`, clarified that "add nothing parallel" scopes to the reusable list only, and pinned impact's own error→status mapping to the same three branches in the same order (`ErrAmbiguousSymbol` → exit 2/`candidates`; `ErrSymbolNotFoundSentinel` → exit 1/`not_found`; anything else → exit 1/`error`).
- `20260828-085259-discussion-review-r4.md` — "[NIT:decision] New facade type names unspecified": named the engine result types (`impact.Result`, `Target`, `Definition`, `Caller`, `Range`) and their facade aliases following the repo's `TOC`-prefix convention (`ImpactResult`, `ImpactTarget`, `ImpactDefinition`, `ImpactCaller`, `ImpactRange`), noting these are the exact identifiers `facade_test.go`'s enumerated blocks must be extended with. `Target` is named for the JSON key and to avoid reading as a second `toc.Symbol`.
- `20260828-085259-discussion-review-r4.md` — "[NIT:design] Tree-sitter phase has no stated bound or cancellation": added a `parse-loop-cancellation-and-timeout-scope` decision — the loop checks `ctx.Done()` between files (not mid-parse), and `--timeout` deliberately does not cover the parse phase, keeping its documented per-LSP-phase meaning literally true. Rationale records that `toc.TOCFile` takes no `ctx`, so threading a deadline would change a shared engine signature; rejected alternatives cover that change, widening `--timeout`, and a separate `--parse-timeout`.

## Pushed Back

_None._
