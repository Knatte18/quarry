# Plan fix — round 4

Source review: `_mill/reviews/20260829-073041-plan-review-r4.md`

## Fixed

- r4 "`impact` entry schema advertises `except`" — verified: `impactInput.Targets` is
  `[]nativeEntry`, which declares `Except`, so inference would publish a property the handler
  rejects and the discussion's `per-entry-vs-call-wide` matrix forbids. Batch 2 card 10 gains a
  `dropEntryProperty(s, name)` helper (removing a property from the `targets` item schema's
  `Properties` and `PropertyOrder`), batch 4 card 18 calls it with `"except"` before assigning the
  input schema, and batch 6 card 25's matrix test now asserts `impact` declares no `except` while
  `assert_no_callers` does. The shared `nativeEntry` type is kept so both tools parse through one
  `nativeEntry.query`.
- r4 "`classifyLSPError`'s nil-error branch unspecified" — batch 2 card 9 now states the nil branch
  explicitly (`statusFound`, no candidates, no message), matching `classifyLookupError` and its own
  sibling `classifySymbolError`.
- r4 "Two cards name identifiers from files absent from `Context:`" — `quarry/facade.go` added to
  card 8's `Context:` and `internal/cli/toc.go` to card 11's.
- r4 "`outputSchemaFor` contradicts the schema-derivation decision" — card 10 now states that
  `outputSchemaFor` passes the same `ForOptions` value as `inputSchemaFor`, never `nil`.
- r4 "Per-entry timeout has no test anywhere in the plan" — card 11's test requirements now name
  the assertion: `options` carries `cfg.Timeout` through undivided for every entry of one call.

## Pushed Back

_(none)_
