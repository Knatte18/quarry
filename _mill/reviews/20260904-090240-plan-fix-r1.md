# Plan fix — round 1

Source review: `_mill/reviews/20260904-090016-plan-review-r1.md` (holistic, REQUEST_CHANGES,
3 BLOCKING + 3 NIT).

## Fixed

- `[BLOCKING:design]` "Negative-depth handler branch unreachable via protocol" — verified accurate
  against go-sdk v1.7.0: `toolForErr`'s wrapper calls `applySchema` → `resolved.Validate` before the
  typed handler, so `depth: -2` is rejected against the schema's `minimum` and never reaches the
  handler. Kept both layers (discussion D6 requires both) and fixed the disposition instead: batch 2
  card 8 step 1 now states the SDK validates server-side and why the handler branch is kept anyway;
  batch 3 card 15 now asserts the protocol rejection without pinning the SDK's private wording, and
  asserts our own wording by calling the handler's decision function directly. Batch 3's Batch Scope
  records this as the batch's one stated exception to protocol-only testing, and batch 2's scope now
  fixes every `internal/mcpserver` test as in-package so that call needs no export.
- `[BLOCKING:design]` "Broken-symlink case is a success, not a failure" — verified accurate against
  `internal/engine/repo.go`'s `resolveTarget` (`os.Lstat`, never `os.Stat`) and
  `internal/engine/toc.go`'s name-only entry path. Card 16 no longer demands a failure envelope for
  it; the case is restated as a success-shape assertion pinning the never-follow rule, and the card's
  title and commit message follow. The two genuine failure cases keep their exact-wording assertions.
- `[BLOCKING:scope]` "Card 16 Context omits files its Requirements name" — accurate. Added
  `internal/mcpserver/root_test.go` and `quarry/repo.go`, plus `internal/engine/repo.go` and
  `internal/engine/toc.go` which the restated symlink case now needs the implementer to read.
- `[NIT:consistency]` "`jsonschema-go v0.4.3` pin attributed to a decision that omits it" — accurate.
  Card 5 now attributes the pin to the SDK's own requirement rather than to D1, and tells the
  implementer to read that requirement rather than trust the number in the plan.
- `[NIT:decision]` "Stale README sentence left with no disposition" — accurate. Card 17 now owns the
  sentence explicitly and states what replaces it.
- `[NIT:consistency]` "No batch verify runs the repo's stated done gate" — accurate as an omission.
  Added a Shared Decision recording that the full gate runs once via `pipeline.done_gate` at the end
  of the task rather than at every batch boundary, with the cost rationale. The `verify:` fields are
  unchanged; what was missing was the disposition, not the coverage.

## Pushed Back

_(none)_
