# Fixer report: plan-fix round 2

Source review: `_mill/reviews/20260830-112919-plan-review-r2.md` (holistic, REQUEST_CHANGES,
3 BLOCKING / 1 NIT).

## Fixed

- r2 holistic — "[BLOCKING:scope] Completeness whitelists omit two real survivors". Verified against
  `internal/mcpserver/tools_toc.go`: `resolveTOCFileEntry` and `resolveTOCDirEntry` both take a live
  `targetDir` parameter that survives the change, and `internal/mcpserver/nativeentry_test.go` binds
  a `targetDir` local in `TestExceptSet_ResolvesAgainstTargetDirNotProcessCwd`. All three were
  missing. Both `resolveTOC*Entry` parameters are added to batch 1 card 4's leave-unchanged list and
  to card 8's check-one production whitelist; the `nativeentry_test.go` local is added to check
  one-b's test-side whitelist.
- r2 holistic — "[BLOCKING:scope] Card 6 Context omits the file defining ResolveStateDir/workspaceKey".
  Verified: card 6's Requirements dictate `cli.ResolveStateDir`'s tier precedence in detail, and no
  file in its `Context:` defines or calls either symbol. `internal/cli/paths.go` is added to card 6's
  `Context:`, and the requirement now tells the implementer to read it and confirm the tier
  precedence and the digest's inputs before writing the paragraph.
- r2 holistic — "[BLOCKING:design] Cwd-inheritance platform claim is asserted unsourced". Verified:
  the claim is client-side behaviour this repository neither controls nor can check —  `.mcp.json`
  carries only `command`/`args`, and `cmd/quarry-mcp/main.go` only reaches `os.Getwd` through
  `ResolveLaunchTargetDir` — and the existing `docs/mcp-setup.md` already hedges the same point. Card
  6 now requires the cwd claim scoped to clients that launch a project-scoped server from the project
  root, with the `quarry-mcp: resolved target directory <path>` stderr line named as the per-session
  confirmation, and states explicitly that it must not be written as a universal client guarantee.
  The card now separates the two claims in that paragraph and gives each its own distinct
  qualification, since they are qualified on different grounds.

## Pushed Back

- r2 holistic — "[NIT:consistency] Replacement prompt rationale diverges from the gate it mirrors".
  The finding is factually correct that the new second line does not restate
  `gate_no_target_override`'s docstring, but the fix is declined under HARM CHECK (b): it conflicts
  with a documented design decision. `_mill/discussion.md`'s Scope section prescribes the replacement
  text verbatim as the two lines `Never set buildTags on any of these calls -- the default build-tag
  set is` and `the one this run is scoped to.`, and this prompt is measured benchmark input, so
  altering its wording beyond what the discussion sanctioned changes the measured artifact. The two
  statements are not in conflict on the facts — both describe the run being scoped to one pinned
  build-tag set whose change moves the daemon key — they differ in audience: the prompt addresses the
  model deciding whether to set a parameter, the docstring addresses a bench maintainer reading a
  fatal gate. The underlying concern is addressed without changing the literal: card 7 now records
  that the divergence is deliberate, states why, and instructs the implementer not to import the
  gate's phrasing into the prompt.
