MILL_REVIEW_BEGIN
# Review: Add `impact` verb for caller-context lookup — holistic

```yaml
duration_s: 416.0
verdict: APPROVE
reviewer_model: opushigh
reviewer_self_id: Claude, Anthropic Opus-class model (system reports "Claude Opus 5"); best-effort self-assessment only
reviewed_file: plan/
date: 2026-08-28
```

## Findings

### [NIT:decision] classifyImpactError marshal-failure status unstated
**Demoted-from:** BLOCKING
**Location:** batch 3 / card 10 (`classifyImpactError`)
**Issue:** The card requires `classifyImpactError` to "re-word" a `structToFields` failure identically to `emitImpactResult`, then specifies the nil-error branch as `statusFound` carrying the marshalled result and forbids "a fourth branch" — so no `batchStatus` is ever named for a marshal failure in batch mode (`tocFileOne`'s precedent is `statusError`, but the plan never says).
**Fix:** State the disposition explicitly — which `batchStatus` a `structToFields` error yields inside the nil-error branch — or say the failure is impossible and drop the re-wording requirement for this helper.

### [NIT:consistency] "adding no branch of its own" contradicted two paragraphs later
**Location:** batch 3 / card 10 (`emitImpactResult`)
**Issue:** The card says `emitImpactResult` reproduces `emitLookupResult`'s two error paths "adding no branch of its own", then requires a third path for a re-worded `structToFields` failure on the success side.
**Fix:** Scope the "no new branch" clause to the incoming-error routing, so the marshal-failure path is not read as forbidden.

### [NIT:consistency] Parse-cache type, constructor, and predicate left unnamed
**Location:** batch 1 / cards 4–7
**Issue:** Card 4 specifies "an unexported struct type", "a constructor", and "a predicate reporting whether a path is already cached" without naming any of them, and card 5's otherwise-pinned signature reads `buildResult(..., resolver <the card 4 cache type>)` — a placeholder where every other identifier in the plan is a stable name.
**Fix:** Pin the type, constructor, and predicate names in card 4 the way `enclosingSymbol` and `buildResult` are pinned, and use them in card 5's signature.

### [NIT:scope] Second verb enumeration in toc.go has no disposition
**Location:** batch 3 / card 11 vs batch 4 / card 16
**Issue:** `internal/cli/toc.go:64` (the `toc file` Long help) enumerates the LSP-backed verbs a second time — "the servers.yaml-backed registry key --lang means on refs/definition/symbol" — which `impact` joins; card 11 edits only the file's header comment and says "Change nothing else in that file", while card 16's sweep is told to fix any uncovered stale site, so the two cards disagree about this line.
**Fix:** Name that line in card 11 (it already omits `assert-no-callers`, so "leave as non-exhaustive" is a legitimate disposition) rather than leaving the sweep and the "change nothing else" instruction in conflict.

## Verdict

APPROVE
One unstated batch-mode status disposition; the rest are wording and naming nits.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
