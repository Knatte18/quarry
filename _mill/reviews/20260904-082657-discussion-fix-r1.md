# Discussion fix — round 1

Source review: `_mill/reviews/20260904-082547-discussion-review-r1.md` (APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260904-082547-discussion-review-r1.md` — "[NIT:consistency] D10's \"no separator check\" purity is already conceded by D8": D10 rewritten. The `#`-less `expand` target is now rejected by the CLI's own `strings.Contains(target, "#")` check inside `parseArgs`, before root discovery or any facade call, keeping the recommended outcome (exit 2, CLI-spelled message) while removing the contradiction with `exitUsage`'s documented "TOC is never called on this path"; `internal/cli` no longer imports `glyph/`. A `#`-bearing target the grammar still rejects keeps D4's exit-1 envelope path, stated explicitly. Propagated to D3's exit-code table, D12's parser rules, the Testing scenario list, and the Q&A log entry.
- `20260904-082547-discussion-review-r1.md` — "[NIT:consistency] Plan §5's variadic `resolve` bullet stays unamended by this task": D1's tension paragraph now closes the inventory — the plan §5 amendment is an operator follow-up on `main`, the same move as `toc`'s own amendment (commit `55161a6`), and this task edits no line of `docs/rewrite-plan.md`. Added as an explicit Scope/Out entry.

## Pushed Back

None. Both findings verified accurate against the merged code (`internal/cli/cli.go`'s `exitUsage` comment; D8's own `#` check) and neither fix breaks existing functionality, conflicts with a documented decision, or destabilises work outside this task.
