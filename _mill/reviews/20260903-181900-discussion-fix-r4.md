# Discussion fix report — round 4

Source review: `_mill/reviews/20260903-181348-discussion-review-r4.md` (APPROVE, 0 BLOCKING, 6 NIT).

## Fixed

- r4 — "Ignore set never covers descended directories": D9 now extends the pattern set per directory
  entered and drops it on the way back up; `SpansOf` builds the same set along the root → unit chain.
  D22's "once per call, walking root-to-target" sentence is explicitly superseded, with "never
  cached" and "extended as the walk descends" named as independent properties that both hold.
- r4 — "`Symbol.File` has no producer in T3": `SpansOf` now returns `[]Symbol` with `File` set; the
  separate `Span` type is dropped from D16 and from D1's `answer.go` file list, and D3's `File`
  comment names `SpansOf` as its T3 writer. D6's round-trip comparison is restated over
  `(File, Start, SigEnd, End)` tuples.
- r4 — "D5's `const`/`var` `Start` contradicts §4's doc-inclusive start": D5's table gains a
  preamble defining "the node's span" as doc-block-first-line when `CommentBlockAbove` attaches one
  and the node's own first line otherwise, citing `goDeclSymbol` and §4's `placement` at line 16.
- r4 — "\"Most common package clause\" has no tie-break": D7 step 2 gains the lexicographically
  smallest clause as the tie-break, with the `//go:build ignore` `package main` case named.
- r4 — "`SpansOf` does not reject an unspellable unit": D16 gains argument validation by
  round-tripping through `glyph.Parse(g.Lang, g.String())`, which makes D7's "`SpansOf` never
  produces a root span" true rather than aspirational and reuses the one implementation of the
  grammar instead of restating its rules.
- r4 — "D22's freshness guarantee is untested": Testing gains 11g (mutate a fixture `.gitignore`
  between two calls on one `Repo`), plus 11h, 11i and 11j covering the descendant-`.gitignore`,
  `SpansOf`-validation and package-clause-tie-break rules the other five fixes introduced.

## Pushed Back

(none)
