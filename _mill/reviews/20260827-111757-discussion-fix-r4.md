# Discussion fix — round 4

Source review: `_mill/reviews/20260827-111720-discussion-review-r4.md` (verdict APPROVE, 0 BLOCKING, 1 NIT demoted-from-BLOCKING, reviewer sonnetmax).

## Fixed

- "[NIT:consistency] Layering guard's test-file walk is self-contradictory": accurate — "table-driven off the same walk the seam test uses" implied a non-test-only scan, which is incompatible with the `daemontest` carve-out for `_test.go` importers. Resolved by specifying the two walks separately: the seam test stays non-test-only (a banned cobra/output import matters when it ships), while `layering_test.go` scans production **and** `_test.go` files, since a cross-layer test import is the realistic rot path. Stated that `daemontest` is the single exemption and answered the reviewer's example explicitly — `registry`'s own test file importing `daemon` is a violation and must fail the guard, as is a `query` test reaching `daemon` directly instead of through `daemontest`. Updated the `daemontest` Decision's "Consequence for the layering guard" paragraph to match rather than restate the old wording.

## Pushed Back

None.
