# Fixer report: plan-fix round 2

Source review: `20260905-144813-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING / 4 NIT, one of them
demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r2 holistic, "[NIT:consistency] Batch 5's verify contradicts its own Shared Decision"
  (**Demoted-from:** BLOCKING) — accurate: the decision said "all batches" while batch 5 runs no
  `go test` at all and touches four packages plus two Markdown files, so one-package scoping is
  impossible for it by construction. The decision now carves the docs batch out in its own text and
  its "Applies to" line names both forms, so the decision and the batch index agree.
- r2 holistic, "[NIT:consistency] Card 1 step 6 describes a case step 3 makes unreachable" —
  accurate: step 3 collects symbols only on a clean parse, so "even if it happened to yield one
  surviving symbol" could never fire and misread as though a partial parse contributes symbols. Step
  6 is restated as the invariant it actually is — a still-partial retry returns none, so the count
  check is never reached — with the two-crisply-separate-conditions rationale kept.
- r2 holistic, "[NIT:consistency] Card 6's failure-line spelling can emit trailing whitespace" —
  accurate; verified against `quarry/text.go:242-246`, where `RenderResolveText`'s branch 1 writes
  `" error"` and then a conditional `" " + r.Reason` for exactly this reason. Card 6 now spells the
  segments the same way and says why, rather than describing a trailing-space ` error ` that
  contradicts the card's own no-trailing-whitespace invariant.
- r2 holistic, "[NIT:consistency] Batch 4 misstates who consumes `compareGolden`" — accurate;
  verified at `internal/engine/golden_test.go:34,48`, where `TestGolden_LoomyardRenderDir` and
  `TestGolden_LoomyardRenderLayoutFile` both call it and neither is reached by the `-run` pattern.
  The Batch Tests section now separates card 14's helper (three consumers, all reached) from card 15's
  (two further consumers, covered by compilation plus their own Loomyard gate).

## Pushed Back

_(none — every finding verified accurate against the cited code, and no fix broke existing behaviour,
contradicted a documented decision, or destabilised work outside this plan.)_
