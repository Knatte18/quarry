# Plan fix — round 2

Source review: `_mill/reviews/20260904-090815-plan-review-r2.md` (holistic, REQUEST_CHANGES,
1 BLOCKING + 5 NIT, two of the NITs demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- `[BLOCKING:design]` "Card 5's `go mod tidy` deletes the deps it just added" — accurate: at card 5
  nothing in the module imports either new module, so tidy prunes both back out and the card's commit
  is empty. Card 5 now runs `go get` only, states why tidy is deferred, and warns that its own
  requirement entries are correctly marked indirect until then. Card 8 — the first card whose file
  imports both — now carries `go.mod` and `go.sum` in its `Edits:` and owns the tidy run, which is
  also what promotes both requirements to direct.
- `[NIT:consistency]` (demoted from BLOCKING) "Card 10 prescribes a false claim about an engine
  layering test" — accurate: this tree carries no import check at all; V1's lives on the frozen
  branch. Card 10's header requirement now states that situation and explicitly forbids the claim the
  earlier wording prescribed.
- `[NIT:decision]` (demoted from BLOCKING) "D9's symbols caveat has no disposition in the plan" —
  accurate: the plan implemented D9's first half and named no home for its second. Added a Shared
  Decision recording that the caveat goes in the merge commit message alongside the live-probe
  outcome, which D13 already routes there, and forward into T7's own conclusion.
- `[NIT:consistency]` "Extraction leaves two stale doc comments no card owns" — accurate. Card 2 now
  edits `internal/cli/doc.go` for the one clause naming root discovery as this package's work; card 3
  now edits `internal/cli/scratchtree_test.go` for the one header sentence naming the two removed test
  files as the helper's chief users, with the helper body explicitly untouched. Both files added to
  `All Files Touched`, and batch 1's scope note restated to match.
- `[NIT:consistency]` "Scratch-tree Shared Decision omits the batch that adds a helper" — accurate.
  Added `mcp-server` to that decision's `Applies to:`.
- `[NIT:scope]` "Card 9 leaves `server.Run`'s error undisposed" — accurate. Card 9 now gives every
  failure one disposition regardless of which side of the transport's start it falls on: stderr,
  non-zero exit, never stdout, with a clean session end exiting zero.

## Pushed Back

_(none)_
