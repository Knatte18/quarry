MILL_REVIEW_BEGIN
# Review: Facade + CLI, resolve + expand (T5b) — holistic

```yaml
duration_s: 363.0
verdict: REQUEST_CHANGES
reviewer_model: opushigh
reviewer_self_id: Claude Opus 4.x-class model (Anthropic); exact build not self-observable
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [NIT:consistency] RenderExpandText found branch has no nil-Head guard
**Demoted-from:** BLOCKING
**Location:** batch 1 / card 5 **Issue:** `ExpandAnswer.Head` is `*Symbol` (internal/engine/answer.go:228), and card 5's found branch says "then the head's symbol line" with no nil guard and no test row, while the same card spells an explicit nil guard plus a test for the identically-shaped `ResolveResult.Dir` pointer and justifies it with "an external caller can construct a found result with no directory answer, and dereferencing nil there would panic". A caller-built `ExpandAnswer{Status: StatusFound}` therefore panics rather than honouring the one-trailing-newline promise the card makes "for every input". **Fix:** state the same guard and the same test row for `Head` on card 5's found branch, or state why the two pointers are treated differently.

### [BLOCKING:design] Card 11's subdirectory-relative target case is unreachable
**Location:** batch 3 / card 11, last test bullet **Issue:** the case "a path target given relative to a subdirectory, asserting the payload's target field echoes the repository-relative form" only differs from the argument when `base != root`, which requires the process working directory to be a subdirectory — forbidden by the overview's `.scratch/` decision ("no test changes the process working directory") and by `internal/cli/root_test.go`'s own header; with `--root` given, `Run` sets `base = root` and the assertion is vacuous. **Fix:** name the reachable form the card wants — e.g. an absolute target under the root, whose repository-relative echo is observably different from the argument — or state that this half of the asymmetry is pinned by `target_test.go`'s rebasing test instead.

### [NIT:consistency] Card 2's claim about the existing TOC fixtures is wrong
**Location:** batch 1 / card 2, fixture paragraph **Issue:** "The existing `TOC` fixtures put their Go files at the root" is false for two of the four — `TestRepoTOC_DirectoryTarget` and `TestRepoTOC_FileTarget` already write `sub/a.go` (quarry/repo_test.go:68, :91); only the two sentinel fixtures put a `.go` file at the root. The instruction the sentence justifies (nest every new fixture package) is correct regardless. **Fix:** restate the sentence against the two fixtures it is actually true of.

### [NIT:scope] Card 17 names before-side files its Context does not list
**Location:** batch 4 / card 17 **Issue:** the card requires "Reference each before-side file by its actual relative path in the table" for seven before-side outputs (`definition.txt`, `definition-ambiguous.txt`, `symbol.txt`, `impact.txt`, `impact-file-scope.txt`, `assert-no-callers.txt`, `refs.txt`); none is in `Context:`, which carries only the parent `INDEX.md` and the eight after-side files. The names are derivable from the parent index, so this is recoverable rather than a cold start. **Fix:** name the seven paths in the card, or add the parent index's file list as the stated source of them.

## Verdict

REQUEST_CHANGES
One unguarded pointer deref and one unreachable test case; everything else verified sound.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
