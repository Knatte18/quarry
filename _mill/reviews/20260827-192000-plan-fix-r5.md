# Plan fix report — round 5

Source review: `_mill/reviews/20260827-191453-plan-review-r5.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 3 NIT).

## Fixed

- r5 holistic, "[BLOCKING:design] Spike cannot produce the figure card 25 asserts on" — verified: card 3 issued `definition`, `implementation` and `references` at two positions only, never a per-reference `textDocument/definition`, which is exactly the input the verification filter consumes, so card 4's `references-verified:` and `callers-verified:` were underivable from the logged output. Took the first of the two offered dispositions, extending the spike rather than weakening the assertion, since the recorded figure is what makes card 25 a real gate. Card 3 now issues and logs one `textDocument/definition` per returned reference at the interface-method position, pairing each reference with the definition it resolved to; card 4's derivation now states the keep/drop rule over those logged pairs explicitly.
- r5 holistic, "[BLOCKING:scope] Card 16 is mode-blind about the documentSymbol phase" — verified: card 15 makes an unadvertised `documentSymbolProvider`, an errored or empty `documentSymbol` result, and a classification-phase deadline all skip-verification triggers under directional mode, so a fake server silent on `documentSymbol` would turn every drop assertion in card 16 into a keep. Card 16 now carries the same conditional cards 14 and 15 carry: under `mode: directional` every fake meant to let verification run must advertise `documentSymbolProvider` and answer with a hierarchy classifying the implementation locations; under `mode: symmetric` no such support is needed. Added `docs/implementation-widening-spike.md` to card 16's `Context:`, which the validator's `context-completeness` check flagged on the first re-run of this fix.
- r5 holistic, "[NIT:scope] Card 14 Context omits two files its Requirements name" — added `internal/cli/cli.go` and `internal/quarryengine/position.go` to card 14's `Context:` rather than dropping the citations, since both carry the coordinate-system rationale the card's positional-matching rule rests on.
- r5 holistic, "[NIT:consistency] Two lsp-tagged query test files may redeclare repoRoot" — verified: `repoRoot` and `findFuncPosition` are package-level declarations in `internal/quarryengine/query/refs_integration_test.go` under the same `lsp` tag, so a literal "resolve it the same way" reading yields a duplicate declaration. Cards 3 and 24 now say to call the existing helpers and state the build failure that follows from redeclaring them.
- r5 holistic, "[NIT:consistency] capabilities struct doc comment left stale by card 1" — card 1's field-adding bullet now also requires widening the `capabilities` struct's own doc comment, which names only workspace/symbol and documentSymbol support.

## Pushed Back

_None._

## Re-validation

`_plan_dag.extract_batch_index` + `_plan_dag.validate` pass, and `_plan_validate.run` returns zero findings with an empty `skip_checks`.
