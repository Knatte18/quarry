# Fixer report: plan-fix round 5

Source review: `20260830-121301-plan-review-r5.md` (holistic, opushigh).

## Fixed

- `20260830-121301-plan-review-r5.md` — "[NIT:consistency] Card 23 uses IngestRecord before card 24 defines it": accurate — an in-batch forward reference introduced by the round-4 fix. `RunJSONPayload` moves into card 24, after the record type exists; card 23 now says only that the payload is assembled by a helper the next card adds.
- `20260830-121301-plan-review-r5.md` — "[BLOCKING:scope] ingest's Context omits transcript.go and worktree.go": accurate. Both added to card 58's `Context:`, and the requirement now names `ReadTranscript` and `ObserveWorktreeDirtied` as the sources, with the latter feeding `RunGates`' injected observation.
- `20260830-121301-plan-review-r5.md` — "[BLOCKING:scope] No card produces the granted-tool list reader": accurate — the value had a consumer and a parameter but no producer anywhere in the plan. Card 43 now adds `GrantedToolsFromDefinition`, placed beside the generator so the two shapes cannot drift, and specifies its error behaviour: it errors on a missing file, absent or unparseable frontmatter, and a missing tools key, and never returns an empty list for any of them — an empty allowlist is a meaningful value a blinded config legitimately produces, and conflating it with a parse failure would record a false granted-tool set. Card 58 cites it as the sole source.
- `20260830-121301-plan-review-r5.md` — "[BLOCKING:design] BuildServer's signature carries no seam its test can substitute": accurate; the card required substituting a builder the signature had no room for. `BuildServer` now takes an explicit builder function, mirroring the git runner the sibling card takes.
- `20260830-121301-plan-review-r5.md` — "[NIT:consistency] Done gate is weaker than every batch verify": the wording was genuinely misleading, so both decisions are rewritten as three explicit tiers — batch verify runs the ladder tests, the module-wide check type-checks the whole module, and the repo-wide suite is the pipeline's done gate run once by the orchestrator, which no batch invokes. The module-wide check is also widened from the ladder subtree to `go vet ./...`, since a subtree-scoped vet could not answer the question that check exists to ask.
- `20260830-121301-plan-review-r5.md` — "[NIT:consistency] Sibling resume filters take arguments in opposite orders": accurate. `PendingRuns` now takes `(resultsRoot, pairs)` like its sibling, with a note that it deliberately departs from the Python's argument order to do so.
- `20260830-121301-plan-review-r5.md` — "[NIT:consistency] `--run-model` outlives the smoke launch that justified it": accurate — the no-smoke-launch decision removed its only caller. Card 52 now states the disposition explicitly: the flag is kept for the follow-up matrix task, whose probe dispatches and first real runs need a way to run against the committed file before the operator writes the pin, and its help text must say so, so a reader does not mistake an unused flag for dead code.

## Pushed Back

_None._
