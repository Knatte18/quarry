# Fixer report: plan-fix round 6 (round cap)

Source review: `20260830-122040-plan-review-r6.md` (holistic, opushigh).

This is the last allowed round. Every finding below was fixed; the operator directed that the plan hand
off at the cap rather than blocking, so `approved: true` is set on the operator's instruction, not on a
reviewer APPROVE verdict.

## Fixed

- `20260830-122040-plan-review-r6.md` — "[BLOCKING:design] run.json observation chain is ported broken": verified — the Python writes observations only as a structured gate/message list, while its summariser lifts them by looking for top-level keys named after each gate, keys nothing writes. The lift never fires and every metric downstream of it is dead. Card 24's `RunJSONPayload` now emits both shapes — the structured list the cold-cell disposition reads by gate name, and the top-level per-gate keys the summariser reads — recorded explicitly as a repair rather than a port. Card 31 states that its lift reads those new keys and warns a later reader not to "restore" the Python's dead behaviour.
- `20260830-122040-plan-review-r6.md` — "[BLOCKING:design] Repo-root-relative paths break under Go's per-package test cwd": accurate — the ladder file's task and answer-key paths and the benchmark README path are repo-root-relative, and pytest resolved them only by running from the repository root. `TaskTextFor` and `SchemaFor` now take an explicit `repoRoot`, with the reason recorded, and card 60 routes its answer-key and task resolution through the same root rather than the process working directory.
- `20260830-122040-plan-review-r6.md` — "[BLOCKING:design] ParseScorerReply rests on a validation that does not exist": verified — the Python only decodes. Card 30 now presents validation as an addition rather than a port, takes the task schema as a parameter since the required set is schema-dependent, derives that set from the two rule constants' own declared output shape so the rules stay the single source, and additionally requires every metric the summariser reads. `prompt_template` is given an explicit keep disposition, with its purpose recorded.
- `20260830-122040-plan-review-r6.md` — "[BLOCKING:scope] BuildServer loses the CGO_ENABLED=1 build environment": verified — the Python forces `CGO_ENABLED=1`, runs from the repository root, and names the C-toolchain requirement in its failure message. The round-5 seam had nowhere to put either value. The builder seam now carries a working directory and an environment, `BuildServer` supplies both, and the test asserts the substituted builder receives them.
- `20260830-122040-plan-review-r6.md` — "[BLOCKING:scope] Card 24's Context omits the file defining GateReport": accurate. Added.
- `20260830-122040-plan-review-r6.md` — "[NIT:scope] prepare-session's environment precondition could be checked vacuously": accurate and exactly the dead-check failure mode this plan guards against elsewhere. Card 52 now states the precondition receives the real process environment, never the scrubbed one, and says why.
- `20260830-122040-plan-review-r6.md` — "[NIT:decision] Scoring session's pin set contradicts the discussion": accurate — the discussion says both things in two places. Card 52 now records which statement it supersedes and why: the narrower set already covers the scorer model and effort the scoring session stamps, while the full check additionally demands the turn ceiling, which ships blank and which session preparation does not touch.

## Pushed Back

_None._
