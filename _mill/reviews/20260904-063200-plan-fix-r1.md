# Plan fix — round 1

Source review: `20260904-062525-plan-review-r1.md` (holistic, REQUEST_CHANGES, 4 BLOCKING + 3 NIT).
All seven findings verified as accurate; none met a harm-check escape. All seven fixed.

## Fixed

- `20260904-062525-plan-review-r1.md` — "[NIT:consistency] t.TempDir() contradicts the repo's
  .scratch convention". Verified against `internal/engine/scratchtree_test.go`, whose
  `writeScratchTree` doc states the ban verbatim. The finding is stronger than reported: the
  prohibition on writing tests or fixtures into a system temporary directory binds this repository
  generally, not only T3, so the disposition is adoption rather than a scoped carve-out. Added the
  overview Shared Decision `fixture-trees-live-under-scratch-never-t-tempdir`; batch 1 card 4 and
  batch 3 card 14 now each create a per-package `writeScratchTree` (`quarry/scratchtree_test.go`,
  `internal/cli/scratchtree_test.go`), and cards 4, 14, 15 and 18 build fixtures through it.
  Consequence handled while fixing: `.scratch/` lives inside this repository, so a root-discovery
  walk started there always finds this repository's own `.git` and the no-root-found branch is
  unreachable from it. Card 14 now exercises that branch by calling `discoverRoot` on the
  filesystem root, guarded by a `t.Skip` when a `.git` exists there.
- `20260904-062525-plan-review-r1.md` — "[BLOCKING:scope] Card 22 Context omits the files its
  assertions name". Added `quarry/quarry.go` and `internal/engine/answer.go` to card 22's
  `Context:`.
- `20260904-062525-plan-review-r1.md` — "[BLOCKING:scope] Exit 3 is asserted but no card says how
  to produce it". Card 18 now names two deliberate fixtures: a target whose parent component is a
  regular file, yielding an `ENOTDIR` stat that `os.IsNotExist` reports false for (skipped with a
  reason where the platform's errno differs), and a `failingWriter` passed as `stdout` to exercise
  the render-path write failure. The error-message-derivation assertion now excludes the
  failing-writer case from its stdout half, since that case cannot write stdout by construction.
- `20260904-062525-plan-review-r1.md` — "[BLOCKING:design] Single-dash tokens after the verb are
  routed to the target slot". Card 10 now defines a flag as a token beginning with `-`, single dash
  or double, so `-x` is `usageError("unknown flag: -x")` rather than a target; the bare `-` and the
  bare `--` are likewise unknown flags, each with a stated reason. Card 13 gains explicit rows for
  `-x`, `-`, `--`, and `-h` surviving via the help pre-scan.
- `20260904-062525-plan-review-r1.md` — "[BLOCKING:design] Card 20 leaves internal/cli red until
  card 21 runs". Took the reviewer's first offered fix and rejected the second: card 20 now states
  the red window explicitly — it opens at card 20, closes at card 21, and the batch's `verify:`
  runs only after every card lands, so the batch boundary is never reached red. The skip-when-absent
  alternative is explicitly forbidden in the card, because after card 21 a missing golden is a real
  regression and a skip would hide it. Batch 5's `## Batch Tests` records the same.
- `20260904-062525-plan-review-r1.md` — "[NIT:consistency] Cards cite discussion.md without listing
  it in Context". Added `_mill/discussion.md` to batch 2 cards 7 and 8 and batch 4 card 18,
  matching batch 3 card 9, which already listed it.
- `20260904-062525-plan-review-r1.md` — "[NIT:scope] Card 23 asserts before-side facts without the
  before-side file". Added `docs/research/output-formats/toc-dir.txt` and
  `docs/research/output-formats/toc-file.txt` to card 23's `Context:`.

## Pushed Back

None.
