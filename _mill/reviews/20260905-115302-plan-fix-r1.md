# Plan fix — round 1

Source review: `_mill/reviews/20260905-115114-plan-review-r1.md` (holistic, REQUEST_CHANGES, 4
BLOCKING + 2 NIT).

## Fixed

- r1 holistic, `[BLOCKING:scope] quarry/render_test.go never follows the Listing rename` — verified
  at `quarry/render_test.go:190`, `:219` and `:226`; the file was in no card and in no overview
  entry. Added to card 17's `Edits:` and to `## All Files Touched`, with requirements naming both
  composite literals and the pinned key list.
- r1 holistic, `[BLOCKING:scope] TestRepoResolve_PathTarget asserts the retired path contract` —
  verified at `quarry/repo_test.go:200-221`; it resolves the bare path `sub` and asserts
  `StatusFound` with a non-nil directory answer, which card 12 turns into an empty-status rejection.
  Card 17's "change no assertion there beyond the field name" is deleted and replaced with a
  retarget to the self glyph plus a rename off the word "PathTarget".
- r1 holistic, `[BLOCKING:design] codeForExpandError maps SelfGlyphError to exit 3, not exit 1` —
  verified at `internal/cli/cli.go:124-137`; the fallback is `exitInternal` and only
  `*quarry.NotATypeError` and `*glyph.ParseError` reach `exitNegative`. Card 27's false
  "confirm rather than assume it needs no edit" premise is deleted and replaced with an explicit
  requirement to add a `*quarry.SelfGlyphError` arm returning `exitNegative`.
- r1 holistic, `[BLOCKING:design] No module-wide compile gate enforces the green-build decision` —
  accurate: the overview's `verify:` was `null`, so the Shared Decision's own acceptance condition
  was unchecked. Set to `go vet ./...` rather than the suggested `go build ./...`, because `go build`
  skips `_test.go` files and every compile break this plan can cause is in a test file; `go vet`
  type-checks them, and it exits 0 on the tree as it stands, confirmed by running it. The Shared
  Decision records that reasoning, and the per-package-scope decision now names both wider gates.
- r1 holistic, `[NIT:scope] Card 27 names *quarry.SelfGlyphError without quarry/quarry.go in
  Context:` — accurate; cards 26 and 29 both list it. Added to card 27's `Context:`.
- r1 holistic, `[NIT:consistency] The new expand parse message leaks the glyph: prefix` — accurate
  that the prefix appears and that `internal/cli/cli.go`'s closing message-composition paragraph was
  not among card 28's named rewrites. The prefix is intended and stays: discussion D19 fixes the
  message as the grammar's full sentence, and the paragraph's rule bars *internal* package names,
  while `github.com/Knatte18/quarry/glyph` is a public package the contract already names. Card 27
  now states the intent, card 28 gains that paragraph as its eighth rewrite, and both say why the
  rule is not being relaxed.

## Pushed Back

_(none — every finding was accurate and every fix was applied.)_
