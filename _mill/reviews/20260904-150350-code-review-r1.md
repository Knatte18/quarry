MILL_REVIEW_BEGIN
# Review: Ladder, toc rerun (T7) — holistic

```yaml
duration_s: 263.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-04
```

## Findings

### [BLOCKING:consistency] Matrix's one invocation violated the unconditional clean-tree rule, and the root was not voided
**Location:** `bench/loomyard-eval/ladder/results/2026-09-04-toc/provenance.json:5-8` (`quarry_dirty: true`, `quarry_dirty_files: ["_mill/briefs/implement-matrix-run-r1.md"]`); `bench/loomyard-eval/ladder/results/2026-09-04-toc/conclusion.md:157-169`; `.scratch/ladder-toc-run.log:41-42`
**Issue:** `## Shared Decisions › clean-tree-and-no-edits-mid-matrix` states unconditionally: "every `_mill/` artifact and every source change is committed before the matrix's first invocation, and the run aborts if `git status --porcelain` is non-empty at that moment." The one stated carve-out is narrow and explicitly limited to a *resumed* invocation's own untracked results-root artifacts ("every listed path being inside that root is what makes the flag benign"). This matrix took exactly **one** invocation (no resume), yet its provenance record shows `quarry_dirty: true` with a dirty file *outside* the results root (`_mill/briefs/implement-matrix-run-r1.md`). Card 7's own requirements are explicit for exactly this case: "An entry outside the results root is a different matter and voids the root: stop and report it." The root was not voided, no restart into `-r2` happened, and the conclusion itself concedes the gap ("this was accepted per pre-matrix-gates precedent... the pre-matrix clean-tree check did not, in fact, catch it"). No such precedent exists in the provided materials — batch 1's card 2 only verifies a clean tree once, after card 1's own commit, well before card 7's invocation; it cannot license accepting a dirty tree at a later, unrelated invocation. This is a self-acknowledged deviation from a Shared Decision applying to "all batches," which the review criteria treat as BLOCKING regardless of class.
**Fix:** Per the plan's own restart rule, this root should either be voided (with `ABANDONED.md` naming the cause) and the matrix re-run in a fresh root once the clean-tree gate is fixed to actually catch a dirty `_mill/` file immediately before invocation, or the plan's clean-tree decision needs an explicit, reasoned carve-out for a live-updating implementer brief file — stated in the plan text, not invented after the fact in a conclusion.

## Verdict

REQUEST_CHANGES
The matrix's single invocation violated the plan's unconditional clean-tree gate and the root was accepted rather than voided per card 7's own rule.
MILL_REVIEW_END
