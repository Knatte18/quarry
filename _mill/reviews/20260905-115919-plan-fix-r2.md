# Plan fix — round 2

Source review: `20260905-115817-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 3 NIT).

## Fixed

- `[BLOCKING:design]` "Descriptive card's \"human-only comment\" has no mechanism" — verified against
  card 8's `LoadCardFile` (reads the file whole, no extraction) and card 17's own note that a
  markdown comment line stays literal in the prompt: the instruction would have injected an
  arm-only block describing the experiment into one measured prompt. Removed it from card 27, and
  replaced it with the general rule that a card holds prompt text and nothing else. Relocated both
  design notes — the treatment-versus-descriptive asymmetry and the inflated-recall caveat — into
  the ladder file's header comment via card 24, which no prompt ever reads.
- `[BLOCKING:scope]` "Card 5 names gates.go and runstate.go symbols not in Context" — added both
  paths to card 5's `Context:`; the card names three gate functions and the run-state struct.
- `[NIT:consistency]` "Card 5's call-site count is eleven, not ten" — corrected to eleven and added
  the `mcp.go` site to the breakdown, which the old arithmetic silently dropped.
- `[NIT:consistency]` "Card 11 misplaces the assertion it replaces" — named the subtest
  `ControlCell_CallerGatesNotTheCheckItself` and its enclosing `TestCheckServerConnected`; confirmed
  by reading `gates_test.go` that the block sits there and not under the granted-tool-used test.
- `[NIT:consistency]` "control/grants-tools decision's Applies-to omits two batches" — now lists
  config-surface and benchmark-content alongside control-sweep-and-card.

## Pushed Back

_None. Every finding verified accurate against the code and the plan's own text, and no fix breaks
existing functionality, conflicts with a documented decision, or destabilises work outside this task._
