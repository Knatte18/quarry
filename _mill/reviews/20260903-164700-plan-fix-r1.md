# Plan fix — round 1

Source review: `20260903-164422-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING, 4 NIT).

## Fixed

- `20260903-164422-plan-review-r1.md` — "[BLOCKING:scope] Completeness test cannot see card 5's
  reject rows": card 5 now declares a package-level `parseReject` slice with the same field shape as
  `goReject`, and card 6's completeness test ranges over both by name.
- `20260903-164422-plan-review-r1.md` — "[BLOCKING:scope] Card 8 must read three files absent from
  its Context": `glyph/parse_test.go`, `glyph/golang_test.go` and `glyph/string_test.go` added to
  card 8's `Context:`.
- `20260903-164422-plan-review-r1.md` — "[BLOCKING:design] Card 1 forward-references Language,
  leaving a non-compiling commit": batch 1's cards 1 and 2 swapped — card 1 now creates
  `glyph/doc.go` + `glyph/glyph.go`, card 2 creates `glyph/errors.go` — so every commit compiles.
  Batch Scope states the ordering reason.
- `20260903-164422-plan-review-r1.md` — "[NIT:consistency] Card 5's import ceiling excludes reflect
  but requires Glyph comparison": card 5's ceiling is now `errors`, `reflect`, `testing`, and its
  zero-`Glyph` assertion names `reflect.DeepEqual`.
- `20260903-164422-plan-review-r1.md` — "[NIT:consistency] Const block drops the Reason type on
  fifteen of sixteen constants": all sixteen constants now carry the explicit `Reason` type, citing
  the `Kind` block as the precedent.
- `20260903-164422-plan-review-r1.md` — "[NIT:consistency] Shared Decision's test-import enumeration
  disagrees with the cards": the Shared Decision now reads `errors`, `reflect` and `testing`;
  `slices` is dropped from it and from the `reflect.DeepEqual` Decision, and `strings` is dropped
  from card 6's ceiling since no case needs it.
- `20260903-164422-plan-review-r1.md` — "[NIT:consistency] Batch 1 scope miscounts its files":
  corrected to four.

## Pushed Back

_None._
