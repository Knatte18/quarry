# Plan fix — round 3

Source review: `20260903-174814-plan-review-r3.md` (holistic, REQUEST_CHANGES, 4 BLOCKING / 5 NIT).

## Fixed

- r3 `[BLOCKING:design] Golden files cannot match a nondeterministic header` and
  r3 `[NIT:consistency] Results root in summary.json/table.txt contradicts a Shared Decision` — one
  fix settles both, at the source rather than by normalising at comparison time. The overview's
  no-machine-paths-in-tracked-output decision now states that the summary and the table carry the
  results root's **base name only**, never the operator-supplied path, and that neither carries a
  wall-clock write time (that stays in `provenance.json`, which is not golden-compared). Cards 28 and
  29 were changed to match, and card 35 now requires the fixture to be copied under the same
  base-name directory and compares with **no** normalisation and **no** exclusions, stating why the
  exact comparison is possible. Removing the two nondeterministic values also removes the machine
  path from two tracked files, which is what the second finding asked for.
- r3 `[BLOCKING:design] Gate-2 fatal disposition never states the blinding-failed flag is set` —
  card 26's disposition table now says a fatal gate-2 finding (check (a) or (b), exactly like check
  (d) before dispatch) writes the repetition complete **with the blinding-failed flag true**, and
  spells out the four consequences that depend on it, including that a finding written without the
  flag would satisfy resume and be silently skipped forever.
- r3 `[BLOCKING:scope] Test cards name identifiers from files absent from Context:` — added
  `internal/ladder/config.go` and `internal/ladder/match.go` to card 15's `Context:`, and
  `internal/ladder/config.go` to card 19's.
- r3 `[BLOCKING:scope] Summariser never names the score-record keys it reads` — card 28 now names the
  exact keys `recall` and `precision`, states that both scoring rules declare them identically so no
  per-schema mapping is needed, states that a record lacking them is excluded by the existing
  exclusion rules rather than read as zero, and states that the schema-specific remaining fields are
  left unaggregated with the reason.
- r3 `[NIT:consistency] CLI flag spellings exist only in a card the CLI card cannot read` — card 30
  now fixes all five run-subcommand flag spellings and the report subcommand's single flag
  literally, says why the spellings are load-bearing (the ladder file's header and the hand-run
  done-criterion both invoke them verbatim), and lists `ladder-toc.yaml` in its `Context:`.
- r3 `[NIT:consistency] Batch 4 scope names the wrong consumer of gate 1` — the scope paragraph now
  splits the interface: the run loop consumes the blinding checks, the redactor and the scorer
  helpers, while `CheckGrantedToolUsed` is consumed by batch 7's summariser, with the reason.
- r3 `` [NIT:scope] `ladder.env` location is spelled two different ways `` — card 20 now spells
  `.scratch/ladder.env` literally and states it is directly inside the scratch directory rather than
  inside the harness's own `.scratch/ladder/` subdirectory; the file was also added to card 20's
  `Context:`.
- r3 `[NIT:consistency] Scorer retry ceiling and the fasit meta key are spelled as prose` — card 26
  now references card 25's `MaxAttempts` constant explicitly and says it is the same constant the
  invalid-repetition path uses, not a second hand-written three; cards 17 and 19 now name the literal
  `_meta` key, leading underscore included.

## Pushed Back

_None._
