MILL_REVIEW_BEGIN
# Review: Add file/dir toc verbs (Tree-sitter-backed) — holistic

```yaml
duration_s: 433.0
verdict: REQUEST_CHANGES
reviewer_model: opushigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-28
```

## Findings

### [BLOCKING:scope] Card 43 names `SetExit` but omits its defining file from Context
**Location:** Batch 7 (doc-sentences-config) / Card 43. **Issue:** Requirements state every resolution-chain error "is emitted as `SetExit(ctx, output.Err(out, msg))`", but `internal/cli/exec.go` — where `SetExit` is declared — is absent from Card 43's `Context:`/`Edits:` (only `tocconfig.go`, `paths.go`, `output.go`, `facade.go`, `cwdcontext.go` are listed). Cards 36 and 38, which introduce the identical `SetExit`/`output.Err` shape, both include `exec.go` in their own Context lists — Card 43 breaks that established precedent. **Fix:** Add `internal/cli/exec.go` to Card 43's `Context:`.

### [BLOCKING:scope] Card 37 cites `DirEntry.Name`'s tag from a file not in its Context
**Location:** Batch 6 (facade-and-cli) / Card 37. **Issue:** Requirements justify the `tocDirEntries` zip-by-index composition by name: "`DirEntry.Name` is `json:"-"`" — that field and tag are declared in `internal/quarryengine/toc/types.go` (Card 9), which is not listed in Card 37's `Context:` (`cli.go`, `cwdcontext.go`, `output.go`, `facade.go`) or `Edits:` (`toc.go`). **Fix:** Add `internal/quarryengine/toc/types.go` to Card 37's `Context:`.

## Verdict

REQUEST_CHANGES
Two Context-completeness gaps (Cards 37, 43) name identifiers from files absent from Context/Edits; plan is otherwise sound.
MILL_REVIEW_END
