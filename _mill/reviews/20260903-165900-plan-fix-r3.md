# Plan fix — round 3

Source review: `20260903-165527-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 3 NIT).

## Fixed

- `20260903-165527-plan-review-r3.md` — "[BLOCKING:scope] ParseError.Detail is prescribed but never
  asserted": `rejectCase` gains a `detail` field; card 6's reject table gains an expected-`Detail`
  column filled in for all thirty-three rows, and its test asserts `Detail` string-for-string on
  every one; card 5's `parseReject` rows carry and assert `detail` too. Card 6's `ParseError` field
  test is rescoped to `Lang` and `Input`, with a line saying why `Detail` needs no separate test.
- `20260903-165527-plan-review-r3.md` — "[NIT:consistency] Shared Decision's blank-line rule
  contradicts card 1 for doc.go": the Decision now names `glyph/doc.go` as the sole file whose
  comment abuts the package clause, because there the comment *is* the package doc.
- `20260903-165527-plan-review-r3.md` — "[NIT:consistency] No file-level comment required on the
  three test files": the Decision now covers test files, and cards 3, 5 and 6 each require an opening
  file-level comment naming what the file covers, citing the toc test file as the precedent.
- `20260903-165527-plan-review-r3.md` — "[NIT:consistency] Card 8 is a zero-diff card with
  `Commit: none`": both remedies applied — card 8 is reduced to the three checks no verify command
  covers (`go list -deps ./glyph`, the test-import read, the `go.mod` check), with the overlap
  explicitly named as already covered elsewhere; and the Batch Scope declares the zero-diff card
  shape as supported and says what it is reserved for.

## Pushed Back

_None._
