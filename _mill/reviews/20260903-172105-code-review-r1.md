MILL_REVIEW_BEGIN
# Review: The glyph package (T1) — holistic

```yaml
duration_s: 186.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-03
```

## Findings

No findings. The implementation was checked file-by-file and rule-by-rule against both batch files
and the overview's Shared Decisions.

Verified in detail:
- Card order / compilability: `glyph.go` → `errors.go` → `parse.go`/`golang.go` each compile standalone
  as required; `Language` lands before `errors.go` references it.
- Import allowlists match exactly per file: `glyph.go` (`strings`), `doc.go` (none), `errors.go`
  (`fmt`), `parse.go` (`strings`, `unicode/utf8`), `golang.go` (`fmt`, `strings`, `unicode`),
  `parse_test.go`/`golang_test.go` (`errors`, `reflect`, `testing`), `string_test.go`
  (`testing`, plus `reflect` added by card 7). `go.mod` unchanged, no new `require`.
- `String()` builds `parts` via `make`+two `append`, never `append(g.Owner, g.Name)`; a dedicated test
  (`TestGlyph_String_DoesNotMutateOwner`) proves no backing-array corruption.
- All 16 `Reason` constants, `Reasons` slice order, and `reasonText` phrases (pairwise distinct)
  present exactly as specified; `Error()` composes from `Reason`/`Lang`/`Input` with `Detail` appended
  only when non-empty, falling back to the raw `Reason` string.
- `Parse`'s four-step order (language → UTF-8 → split → dispatch) and `parseGo`'s unit-before-member,
  first-failure-wins ordering (including 7a bad-rune before 7b/7c) match `02-parser-and-go-alphabet.md`
  verbatim, including the doubly-invalid-unit precedence and the pointer-over-parens precedence for
  `(*dualHandler).Handle`.
- All 33 reject-table rows (`Reason` + `Detail`, quoted via `%q`) and all ≥15 accept-table rows from
  card 6's minimums are present and correctly asserted with `reflect.DeepEqual`, including the
  `Owner`-differs-alone assertion for `dualHandler.Handle`/`durableHandler.Handle`.
- `rejectCase`/`acceptCase` are package-level named types shared across `parse_test.go`/
  `golang_test.go` as required, with `section` genuinely read in every `t.Run` name (no unused-field
  lint risk). `TestReasons_Completeness` ranges over both `goReject` and `parseReject`.
- Round-trip tests (card 7) are driven from `goAccept`, total, in `string_test.go`, both directions,
  `reflect.DeepEqual` for the reverse direction.
- File-comment convention (blank line before `package glyph`, `doc.go` alone abutting) matches
  `internal/quarryengine/toc/{doc.go,types.go,toc_test.go}` in every one of the eight new files.
- No out-of-plan files, no dependency reimplementation, no deviation from any Shared Decision found.

## Verdict

APPROVE
Every batch's cards, tables, and Shared Decisions are faithfully and consistently implemented.
MILL_REVIEW_END
