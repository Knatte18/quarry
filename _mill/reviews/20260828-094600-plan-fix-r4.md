# Plan fix — round 4

Source review: `_mill/reviews/20260828-094136-plan-review-r4.md` (holistic, APPROVE, 0 BLOCKING + 4 NIT).

## Fixed

- r4 holistic, "[NIT:decision] classifyImpactError marshal-failure status unstated" (demoted from BLOCKING) — verified: card 10 required the re-wording but named no `batchStatus` for a `structToFields` failure in batch mode, while also forbidding "a fourth branch". Card 10 now states the disposition explicitly — `statusError` with the re-worded message under an `error` key, returned from inside the nil-error branch — and records that this is the same disposition `tocFileOne` already uses for exactly this failure, so batch mode matches the existing verbs rather than inventing behaviour. Also clarified that this is not a fourth *error* branch, since it lives inside the nil-error branch.
- r4 holistic, "[NIT:consistency] \"adding no branch of its own\" contradicted two paragraphs later" — verified: the clause read as forbidding the success-side marshal-failure path the same card requires. Scoped it to the routing of the incoming `err` argument, with the exemption stated in the same sentence.
- r4 holistic, "[NIT:consistency] Parse-cache type, constructor, and predicate left unnamed" — verified: card 4 described the type, constructor, and predicate without naming any, and card 5's signature carried a `<the card 4 cache type>` placeholder where every other identifier in the plan is pinned. Pinned all five names in card 4 as an explicit list — `parseFunc`, `fileCache`, `newFileCache`, `(*fileCache).resolve`, `(*fileCache).cached` — and rewrote card 5's `buildResult` signature to use `*fileCache` by name. Card 4's counting-seam sentence now names `parseFunc` and both consuming cards.
- r4 holistic, "[NIT:scope] Second verb enumeration in toc.go has no disposition" — verified against `internal/cli/toc.go`'s `toc file` Long help, which describes `--lang` as "never the servers.yaml-backed registry key `--lang` means on refs/definition/symbol". Card 11's "change nothing else in that file" and card 16's "fix any uncovered stale site" genuinely disagreed about it. Adjudicated in card 11: leave it unchanged, because it already omits `assert-no-callers` and so reads as examples rather than an enumeration, which `impact` does not make less accurate. Card 16's sweep now carries that decision as a named exception with an instruction to record having applied it, and `internal/cli/toc.go` was added to card 16's `Context:` so the sweep can actually read the line it is told not to change.

## Pushed Back

_None. Every finding passed VERIFY and none triggered a harm-check escape._
