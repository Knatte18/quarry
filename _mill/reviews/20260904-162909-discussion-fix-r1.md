# Discussion fix — round 1

Source review: `_mill/reviews/20260904-162827-discussion-review-r1.md` (verdict APPROVE, 0 blocking, 2 nits).

## Fixed

- r1, "[NIT:decision] Smoke run rejected outright, not offered to the operator" — the Testing
  section's closed rejection is now a recorded option: not taken on this task's own authority, but
  available with the operator's word, into a throwaway results root outside
  `bench/loomyard-eval/ladder/results/` that is deleted afterwards.
- r1, "[NIT:design] Attempt-number mechanism named as needed, not decided" — added the
  `m2-attempt-numbering` decision (loop-local counter incremented per rejection, never
  `InvalidateRep`'s post-rename return; agreement guaranteed by assertion), pointed the Technical
  context line at it, and extended the attempt-equals-suffix assertion to every e2e case rather than
  the `runner_error` one alone.

## Pushed Back

None. Both findings verified accurate against `run.go`'s attempt loop and the task's own cost
discipline, and neither fix breaks existing functionality, contradicts a documented decision, or
touches work outside this task.
