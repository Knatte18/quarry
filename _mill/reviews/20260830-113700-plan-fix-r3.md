# Fixer report: plan-fix round 3

Source review: `_mill/reviews/20260830-113544-plan-review-r3.md` (holistic, REQUEST_CHANGES,
1 BLOCKING / 2 NIT).

## Fixed

- r3 holistic — "[BLOCKING:scope] Test-helper source files missing from Context". Verified: card 1's
  Requirements name `withStubbedFacade` (defined in `internal/mcpserver/tools_lsp_test.go`) and
  `definitionFn` (a seam variable in `internal/mcpserver/facade.go`), and card 5's name `tocFileFn`
  (`facade.go`) and `statusFound` (`internal/mcpserver/result.go`) — none of the three files was in
  either card's `Context:`, and card 5 already listed `tools_lsp_test.go` for exactly this reason, so
  the omission was inconsistent within the batch. Added `internal/mcpserver/facade.go` and
  `internal/mcpserver/tools_lsp_test.go` to card 1's `Context:`, and `internal/mcpserver/facade.go`
  and `internal/mcpserver/result.go` to card 5's.
- r3 holistic — "[NIT:consistency] Card 8's provenance sentence omits card 2". Verified: card 2 is
  the only card that deletes the six `TargetDir` fields, their `jsonschema` tags, and
  `effectiveTargetDir`, so it is what makes checks one, one-b, and two pass. Card 2 is now named
  first in that sentence, with the reason stated.
- r3 holistic — "[NIT:consistency] New resolveCall test duplicates an existing assertion". Verified:
  the prescribed `StateDir` assertion does restate what `TestResolveCall_StateDirMatchesCLIResolution`
  already asserts and card 2 retains. Kept both, and card 2 now states why the restatement is
  deliberate: `TargetDir == cfg.TargetDir` alone does not show the state directory was derived from
  that same value, and the existing test alone does not show which target directory it derived from —
  the pairing is the invariant, and the card now requires the new test's doc comment to say so.

## Pushed Back

_None._
