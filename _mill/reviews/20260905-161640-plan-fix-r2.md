# Plan fix report — round 2

Source review: `20260905-161118-plan-review-r2.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 5 NIT,
two of the NITs demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r2 BLOCKING:scope "Card 7's \"every call site\" reaches an unlisted file" — verified accurate:
  `symbolsOfDir` in `internal/engine/resolve.go` calls both `unitFor` and `dirPackage`, and that file
  was in no card's `Edits:`. Resolved by removing the option rather than widening the batch: card 7
  now requires the thin-wrapper form only, with the reason stated — a wrapper keeps one
  implementation of the rule without pulling the resolve path into this batch — and
  `internal/engine/resolve.go` is added to that card's `Context:` as a file it must read.
- r2 BLOCKING:scope "Batch 2 verify misses the suites the refactor can break" — verified accurate;
  the previous pattern selected nothing in `resolve_test.go` or `expand_test.go` although
  `symbolsOfDir` consumes both rewritten functions. Batch 2's `verify:` now also selects
  `TestResolve`, `TestExpand`, `TestSpansOf` and `TestRoundTrip` — prefixes confirmed against the
  actual test-function names — in both the batch frontmatter and the overview's Batch Index entry.
  Card 10 and the Batch Tests section now name the resolve-side suites explicitly as the non-obvious
  half, and `internal/engine/resolve.go` is added to card 10's `Context:`.
- r2 NIT:consistency (demoted) "Card 28 drops --exclude-standard the spec requires" — verified
  accurate against `discussion.md`, whose working-tree directory listing carries that flag and whose
  Rejected list names its absence as sweeping in every ignored artefact. My blanket phrasing "neither
  applies any ignore filtering of its own" was wrong; the discussion's symmetry claim is specifically
  about the engine's own ignore set. Card 28 now requires the flag and restates the symmetry in its
  two real halves: tracked-inclusive on both sides, ignored-untracked excluded on the only side it
  can exist.
- r2 NIT:decision (demoted) "The \"--to was given\" flag has no stated consumer" — verified accurate:
  the git delta method takes two plain strings, so a present-but-empty revision has no way to reach
  it and no defined behaviour. Resolved by stating the disposition rather than carrying an
  unreachable state: card 42 drops the third field and rejects an explicitly empty value for either
  revision flag as a usage error, so the empty string means the working tree and nothing else. Card
  46's table is updated to match, including the new empty-value rejection case.
- r2 NIT:consistency "Three more doc claims go stale unnamed" — all three verified in place. Card 32
  now corrects the facade repository type's "holds only the engine handle" claim and the
  concurrency-safety argument drawn from it; card 7 now corrects `walk.go`'s header comment, which
  enumerates the vote and the derivation as living there; card 44 now corrects the dispatch switch's
  inline three-verb comment alongside the doc comments it already named.
- r2 NIT:consistency "Decision 1 does not carve out the plan's own departures" — verified accurate:
  applied literally, decision 1 would send an implementer to write the goldens where the discussion
  says, contradicting card 48. Decision 1 now names the stated-departure exception, points at the one
  departure that exists, and requires any future one to be recorded the same way.
- r2 NIT:consistency "Card 5 says \"six builder shapes\" then lists eight" — verified accurate; six
  builders, eight shapes. Reworded to "all six builders, across these eight shapes".

## Pushed Back

_None. All seven findings verified accurate against the cited code or against `discussion.md`, and
none met any harm category of the receiving-review decision tree._
