# Fixer report: plan-fix round 1 — Engine core (T3)

Source review: `_mill/reviews/20260903-185043-plan-review-r1.md` (holistic, REQUEST_CHANGES,
3 BLOCKING + 7 NIT, two of the NITs demoted from BLOCKING by the stage ceiling).

## Fixed

- `[BLOCKING:scope]` "Card 5 omits the moved tests' import retargets" — verified against
  `toc_test.go:22-23,306` and `golang_test.go:13`; card 5's Requirements now name both import
  retargets and the unqualified `ErrLanguageUnsupported` reference.
- `[BLOCKING:scope]` "fakeStrategy is never updated for the new Strategy interface" — verified
  against `classify_test.go:97-104`; card 20 now adds `PackageDoc` and the one-value
  `Generated`/`TestFile` to `fakeStrategy`, and card 29 adds `classify_test.go` to its `Edits:` and
  the `unit` parameter to `fakeStrategy.Symbols`.
- `[BLOCKING:design]` "Round trip compares a File the walk never sets" — card 38 now states that the
  walk-side tuple's `File` is composed by forward-slash joining `DirAnswer.Dir` with
  `FileEntry.Name`, the root's `"."` contributing no prefix, and says why the comparison is
  otherwise unsatisfiable rather than merely failing.
- `[NIT:decision]` "Ported toc_test.go keeps t.TempDir, against a Shared Decision" — verified: 19
  call sites including the `writeTempFile` helper. Card 20 now rebuilds every fixture through
  `writeScratchTree`, deletes `writeTempFile`, and lists `scratchtree_test.go` in its `Context:`.
- `[NIT:consistency]` "Card 10 forward-depends on card 11" — resolved by re-splitting rather than by
  swapping, because a plain swap only reverses the same cycle (`HeaderForFile` needs the tables).
  Card 10 now declares `headerRule` and the five extractors only; card 11 adds both tables to
  `extension.go` and `HeaderForFile` to `headers.go`, editing both files.
- `[NIT:scope]` "Card 3 cannot read the comment it must merge" — `internal/quarryengine/toc/doc.go`
  is now card 3's `Context:` entry, replacing the unused `sentences.go`.
- `[NIT:scope]` "Round-trip cards omit ignore.go from Context" — `internal/engine/ignore.go` added
  to cards 38 and 39.
- `[NIT:scope]` "writeScratchTree cannot build the trees three cards require" — card 9 now states
  that the helper writes regular files only and that symlink, permission and creation-order
  fixtures are made by the test on the path it returns, with the reason for not parameterising it.
- `[NIT:consistency]` "D-numbers are unresolvable from the plan" — every `D1`–`D22` citation is
  removed from all six batch files; each card already restates the rule it needs, so nothing is
  lost and no card now points at a document it cannot read.
- `[NIT:design]` "The target directory's .gitignore is extended twice" — card 18 now extends the set
  from the root down to the target's **parent** only, and card 17 states that `walkDir` owns the
  extend/trim for its own directory at every level including the first.

## Pushed Back

_(none — every finding verified accurate against the code, and no fix broke existing functionality,
conflicted with a documented decision, or destabilised work outside this task.)_

## Re-validation

`_plan_dag.validate` and `_plan_validate.run` both re-run clean after the fixes: DAG OK, zero
validator findings.
