# Discussion fix — round 3

Source review: `_mill/reviews/20260904-043807-discussion-review-r3.md` (APPROVE, 0 blocking, 6 nit;
3 demoted from BLOCKING by the stage's blocking-class ceiling).

Under `--orch` every finding is treated as FIX; there is no PUSH BACK path. All six were verified
against the source tree before being acted on.

## Fixed

- r3 "Provenance and Technical context contradict on batch 5" — deleted the superseded Technical
  context paragraph calling batch 5 "design only"; corrected "six-item" to "eight-item"; normalised
  every reference to one wording, "committed on `engine-core`, unmerged" (D2, D12, Q&A).
- r3 "The first of Scope's \"two permitted exceptions\" is never defined" — Scope now names both
  exceptions concretely and closes the list: `golang.go`'s stale comment, and the `loomyard_test.go`
  helper signature widening D16 needs. Both are their own cards.
- r3 "D17's fixture inventory does not cover the Testing section" — D17 rewritten as a complete
  table mapping every fixture to its location, the Testing rows that consume it, and why it lives
  there. Confirmed against the committed tree that `testdata/tree/pkg` holds no method, so the new
  `testdata/methods/` package (a type with methods across two files) covers Testing 3 and 12; added
  the non-lexicographic and unreadable-directory `.scratch/` trees; recorded that T3's
  `TestSpansOf_IgnoreFilter` already covers the ignore case and T4 does not rebuild it. Testing 3,
  12 and 16 updated to name their fixtures.
- r3 "D9's memo API names an undefined type and an impossible error" — `unitDirsResult` is now
  defined inline, and `dirsOf` drops its `error` return, since `unitDirs` is two `os.Lstat` tests
  and cannot fail.
- r3 "The benchmark's Loomyard gate has no stated disposition" — D16 decides it: widen T3's
  `loomyardRepo` to `testing.TB` rather than duplicate the pin check, with the reason (two
  implementations of "is this the right Loomyard" is what makes a done-criterion silently
  unverifiable). Added to Scope's exception list and to the Files table.
- r3 "Misattributed measurement in D16" — the 65 ms figure now cites §5's 35-file/317 KB row, with a
  note that §4's 67-file figure is a different package and a different measurement.

## Pushed Back

None. PUSH BACK is unavailable under `--orch`, and every finding was independently verified as
correct before being fixed.
