# Plan fix — round 5

Source review: `20260903-180738-plan-review-r5.md` (holistic, REQUEST_CHANGES, 2 BLOCKING / 4 NIT).

## Fixed

- r5 `[BLOCKING:design] worktree_dirtied observation lands after run.json` — card 26's write order now
  reads the worktree's porcelain status and appends the dirtied observation to the repetition's
  observations list **before** the state file is written, so it rides out in that file's single
  write, with the worktree restore following it. The card states both failure modes the previous
  ordering had: the observation would have had no carrier at all, or it would have forced a second
  write of the file the write-last rule exists to keep atomic.
- r5 `[BLOCKING:scope] card 36 cannot locate the transcript it asserts on` — added
  `internal/ladder/runstate.go` to card 36's `Context:`, which is where the repetition-directory
  helper and the transcript filename constant are declared.
- r5 `[NIT:consistency] migrated ladder file keeps a pointer to a deleted file` — card 5 now names
  the inline comment above `source_repo` explicitly among the lines to rewrite: it points at a file
  card 6 deletes and describes resolution as happening at prepare-session time, which is retired V1
  dispatch vocabulary. It is replaced with a self-contained sentence, and the card adds the general
  rule that no line in the file may point at a file this batch deletes.
- r5 `[NIT:consistency] card 1 claims go.sum will be updated; it will not` — verified against the
  tree: `go.sum` already carries both `gopkg.in/yaml.v3 v3.0.1` lines. Card 1 now says to expect
  `go.mod` to gain the direct require and `go.sum` to be unchanged, that an unchanged `go.sum` is the
  correct outcome rather than a failed command, and that the file stays in `Edits:` only so a
  toolchain-driven change to it would be authorised rather than an unlisted file in the commit.
- r5 `[NIT:scope] report subcommand's summary.json write is never stated` — card 30 now states that
  the report subcommand writes both the summary and the table at the results root through the same
  two calls the run subcommand uses, and prints the table, with the reason: re-deriving without
  rewriting would leave a stale summary beside a fresh table.
- r5 `[NIT:consistency] fixture root called "complete" but omits the redacted answer` — the file was
  added rather than the omission excused: `answer.redacted.json` for repetition 1 is now in card 35's
  `Creates:` and in the overview's All Files Touched. The card now states that repetition 1 carries
  both answer files, that repetition 2 carries neither because that is what a ceiling repetition
  looks like, and that the metrics file is the single deliberate absence, with its existing reason.

## Pushed Back

_None._
