MILL_REVIEW_BEGIN
# Review: Kick-start pack bench: pre-resolved glyph spans in the prompt (M4) — holistic

```yaml
duration_s: 302.0
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude Opus 4.x-class model (Anthropic); the exact "Opus 5" build id is not self-verifiable from inside the session
reviewed_file: plan/
date: 2026-09-05
```

## Findings

### [BLOCKING:scope] Card 29 writes four files it declares read-only
**Location:** batch 7 / card 29 **Issue:** the glyph-substitution branch prescribes editing `ladder-kickstart.yaml`, `cards/07-e0-names.md`, `cards/07-e2-files.md` and `tasks/07-fabric-merge-state-tracing.md` ("edit the glyph list in the ladder file; hand-edit the `Uses:` list in all three cards; re-derive the descriptive card's file list; record the substitution … in the task file's notes section"), but `Edits:` lists only `cards/07-e1-pack.md` and all four appear under `Context:` instead. **Fix:** move those four paths from `Context:` to `Edits:` on card 29 (they are already in `## All Files Touched`, so the overview needs no change).

### [NIT:consistency] mcp.go's other two stale control comments are unswept
**Location:** batch 2 / card 6 **Issue:** card 6 rewords only `MCPConfigDocument`'s doc comment, but `mcp.go`'s file header ("A control cell's document declares no server at all", lines 2–4) and the `mcpConfigDoc` type comment ("A control cell's document has an empty map", line 34) assert the same superseded control-equals-tool-less equation that card 7 sweeps out of `gates.go`. **Fix:** add both to card 6's Requirements, worded as "a cell that grants no tools", with no behaviour change.

### [NIT:consistency] Recall-inflation rationale names only the treatment arm
**Location:** Shared Decision `recall-and-precision-are-descriptive-only-in-this-matrix`; batch 6 / card 24; batch 7 / card 31 **Issue:** the stated reason is "`e1-pack`'s card names the seven files verbatim", but card 26 derives the descriptive card's `Files:` list from the fasit's own `relevant_files` ("that list is also what one card's own file list is derived from, and the two must agree"), so `e2-files`' file recall is inflated at least as directly — a conclusion stating only the e1 reason understates the design property. **Fix:** state the reason as covering both non-control arms in the decision and in card 31's prescribed sentence.

### [NIT:scope] Card 14 cites `Run` with an empty Context
**Location:** batch 3 / card 14 **Issue:** `Context:` is `none`, yet Requirements name `Run` ("`Run` rewrites the whole file through `MergeProvenance` at startup") as the rationale the implementer must write into a comment; `run.go` is in neither `Context:` nor `Edits:`. **Fix:** add `bench/loomyard-eval/ladder/internal/ladder/run.go` to card 14's `Context:`, or mark the sentence "mentioned, not read".

## Verdict

REQUEST_CHANGES
One card's Edits list omits four files its own contingency branch writes.
MILL_REVIEW_END
