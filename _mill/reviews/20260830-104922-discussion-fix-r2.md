# Discussion fix — round 2

Source review: `_mill/reviews/20260830-104816-discussion-review-r2.md` (verdict APPROVE, 0 BLOCKING, 4 NIT, 1 demoted from BLOCKING).

## Fixed

- r2 `[NIT:scope]` (demoted-from BLOCKING) "Bench prompt edit breaks an unlisted pytest" — verified: `bench/loomyard-eval/ladder/tests/test_ladder_config.py:311` asserts the literal `"Never set targetDir or buildTags" in prompt`, which the `ladder_config.py:383` edit breaks. Added the test file to Scope "In" with an explicit disposition (narrow the literal to match the reworded prompt, keep it a literal assertion), and added a Testing paragraph covering the ladder pytest suite, including that `test_gates.py:89` is unaffected because the gate keeps both arms.
- r2 `[NIT:consistency]` "\"five input structs\" enumerates six" — verified by grep: six `TargetDir string \`json:"targetDir...\`` fields across `tools_lsp.go`, `tools_symbol.go`, `tools_impact.go`, `tools_assert.go`, and two in `tools_toc.go`. Corrected "five" to "six"; the enumeration was already complete.
- r2 `[NIT:design]` "Gate-retention rationale rests on a false premise" — verified: `gate_no_target_override` (`gates.py:117-136`) iterates transcript `tool_input` maps and never inspects the published schema, so it cannot assert the property is gone. Rewrote the rationale to "costs nothing and still guards the pinned-worktree constraint", noted `buildTags` remains fully reachable so the gate keeps real work to do, answered the `fatal=True` question explicitly (retained for both keys — a firing `targetDir` arm means the run diverged from the pinned-worktree design and its numbers are not matrix-comparable), and added a "What the gate is not" bullet separating it from the schema-level regression test.
- r2 `[NIT:scope]` "Docs bullet omits the toc absolute-path asymmetry" — verified: `cli.ResolveTOCPath` (`internal/cli/toc.go:253-258`) returns `filepath.Clean(arg)` for an absolute arg, so the toc tools keep an escape the LSP-backed tools lack. Added the asymmetry to the `docs/mcp-setup.md` Scope bullet so a plan writer reading Scope alone cannot drop it.

## Pushed Back

None.
