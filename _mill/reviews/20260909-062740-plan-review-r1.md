MILL_REVIEW_BEGIN
# Review: Batch-answer contract: per-target coverage + fail-closed Status helpers — holistic

```yaml
duration_s: 274.0
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: Claude Opus 5 (claude-opus-5)
reviewed_file: plan/
date: 2026-09-09
```

## Findings

### [BLOCKING:scope] Card 9 leaves `Run`'s doc comment describing deleted guards
**Location:** batch 3 / card 9
**Issue:** `Run`'s doc comment in `internal/cli/cli.go` documents both guards as numbered pipeline steps — runResolve step 4 ("A returned slice whose length is not exactly one is exit 3, named with the count — the facade contracts a positional one-to-one mapping…", lines 275–277) and the identical runName step 1 (lines 340–343) — and after the deletion those steps no longer exist, leaving runResolve's list numbered 1,2,3,4(gone),5,6.
**Fix:** Name both paragraphs of `Run`'s doc comment in card 9's `Requirements:` and state they are rewritten to cite the producer verifiers (`verifyResolveCoverage` / `verifyNameCoverage`) instead of a consumer-side arity step, with runResolve's numbering closed up.

### [NIT:consistency] New test subjects, unextended test-file headers
**Location:** batch 1 / card 2; batch 2 / card 6
**Issue:** Every test file here opens with a header enumerating what it covers — `internal/engine/answer_test.go` lines 1–4 ("pins the emitted JSON shape … the Depth and Symbols knobs, the failure entries…"), `name_test.go` lines 1–4, `resolve_test.go` lines 1–7 — and neither card asks for that enumeration to be extended, while card 1 requires exactly this for `answer.go`'s header and card 3 requires one on the new `quarry/quarry_test.go`.
**Fix:** Add a clause to cards 2 and 6 requiring each edited test file's header comment to name the new subjects (vocabulary completeness / `Known` / `Rejected`; the two coverage verifiers and their panic messages).

### [NIT:consistency] `TestStatusCompleteness` breaks the file's own test-name shape
**Location:** batch 1 / card 2, requirement 1
**Issue:** `internal/engine/answer_test.go` names every test `TestSubject_Case` (`TestAnswerJSON_DirsAbsentWhenEmpty`, `TestAnswerKnobs_Depth`), and the mirrored precedent is `TestName_ReasonCompleteness`; the card's other two names (`TestStatus_Known`, `TestResolveResult_Rejected`) follow it while this one does not.
**Fix:** Prescribe `TestStatus_Completeness` (or `TestStatuses_Completeness`) so all three new names carry the file's convention.

## Verdict

REQUEST_CHANGES
Card 9 must also dispose of `Run`'s doc comment for the two deleted guards.
MILL_REVIEW_END
