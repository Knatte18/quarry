# Discussion fix — round 1

Source review: `_mill/reviews/20260904-110823-discussion-review-r1.md` (verdict APPROVE, 1 NIT,
demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- `20260904-110823-discussion-review-r1.md` — "probe-before-matrix overrides the task's stop-and-ask
  constraint on a false premise". VERIFY: accurate, and it supplies a fact the worker did not hold —
  the operator's §9a probe was run on 2026-09-04 against the merged `cmd/quarry-mcp` and both halves
  were green. HARM CHECK: none. Applied in full: the `probe-before-matrix` decision is replaced by
  `probe-is-reported-done` (the probe is not re-run; `probe.md` records the operator's report); the
  Constraints bullet is restored to the task's own wording, with the answer appended rather than the
  constraint rewritten; the harness's guarded `TestLive` is kept as the worker's own pre-matrix smoke
  test and relabelled as such in Scope and Testing; the Q&A entry is rewritten to record that the
  orchestrator answered the question and that the draft's inference from an absent artifact was
  wrong; and the reviewer's `--setting-sources ""` note is added to Technical context as a gotcha for
  any hand-run probe.

## Pushed Back

None.
