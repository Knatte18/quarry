# Plan fix — round 5

Source review: `20260903-170706-plan-review-r5.md` (holistic, APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- `20260903-170706-plan-review-r5.md` — "[NIT:decision] TDD ordering from the discussion has no
  disposition": the second remedy is taken. A new Shared Decision — "per-commit compilability is
  chosen over card-level test-first ordering" — names the discussion's TDD instruction, records that
  it is knowingly not followed at card granularity, and gives the reason: each card is one commit, so
  a test card placed first would push a commit whose package does not compile. It also states where
  the TDD value is banked instead — the accept table, the thirty-three-row reject table with its
  expected `Detail` column, and the round-trip property were written into the plan from the spec
  before any implementation card was authored — and that within a card the implementer may still
  write the test body first.
- `20260903-170706-plan-review-r5.md` — "[NIT:consistency] Module-wide verify quoted unscoped in
  three places": all three prose restatements now read
  `go vet ./... && golangci-lint run ./glyph/...`, matching the overview's `verify:` field and the
  scoping Shared Decision — batch 1's Batch Tests, batch 2's Batch Tests, and card 8's requirement
  text.

## Pushed Back

_None._
