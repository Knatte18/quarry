# Plan fix — round 4

Source review: `_mill/reviews/20260827-121540-plan-review-r4.md` (verdict REQUEST_CHANGES, 1 BLOCKING, 1 NIT, reviewer sonnetmax).

## Fixed

- `20260827-121540-plan-review-r4.md` — "[BLOCKING:design] killRecordedDaemon/DaemonStateFile unreachable after daemon/query split": accurate on both halves. Verified: `killRecordedDaemon` is defined once at `supervised_integration_test.go:29` (moving to `daemon`) and called from `refs_integration_test.go` 87, 201, 239 (moving to `query`); `DaemonStateFile(stateDir, "go")` is called at `refs_integration_test.go` 86, 200, 238, and card 11's own table forbids `query`'s test files from importing `daemon`. Resolved by extending `daemontest` rather than relaxing the layering rule, mirroring how the toolchain seams were handled: card 7 gains `StateFile(stateDir, lang)` (a one-line delegation to `daemon.DaemonStateFile`) and `KillRecordedDaemon(t, statePath)` (ported from the original and rewritten against exported names); card 6 exports `daemonState` -> `State` and `readDaemonState` -> `ReadState` so that port stands on real API instead of re-deriving the on-disk JSON schema; card 8 retargets all six call sites; card 11 records that this is the rule's purpose, so a future reader does not "fix" the guard by widening it. `daemon`'s own `killRecordedDaemon` stays in `supervised_integration_test.go`, duplicated for the same import-cycle-in-test reason that already forced duplicating `withFakeInstaller` and `repoRoot`. Overview Shared Decision extended to cover it.
- `20260827-121540-plan-review-r4.md` — "[NIT:consistency] position_test.go's caller-facing Position literal not disambiguated": accurate. `position_test.go` lines 31 and 51 pass `Position{File: path, Line: 3, Character: N}` to `toLSPPosition`; after card 3 that type is `quarryengine.Position`, while the file's other `Position` references become the wire type. Added a sentence to card 4 naming both lines, the required `quarryengine.Position{...}` qualification and the added import, and the exact symptom of getting it wrong (`unknown field File in struct literal`) — this is the one rename in the batch that a find-and-replace pass silently corrupts.

## Pushed Back

None.
