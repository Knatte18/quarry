# Plan fix — round 1

Source review: `_mill/reviews/20260906-093342-plan-review-r1.md` (holistic, REQUEST_CHANGES,
2 BLOCKING / 3 NIT).

## Fixed

- r1 holistic, `[BLOCKING:design] LADDER_EXIT=1 triage rests on a false premise` — verified against
  `run.go:220-221`, `run.go:583`, `run.go:650` and `main.go:195`: an attempt-exhausted repetition
  returns `incomplete: true` and a post-run blinding failure returns `blindingFailed: true`, both
  without `abortRun`, and both set the non-zero-exit flag; the summariser additionally exits non-zero
  on any non-empty `incomplete`/`invalid` list. Card 30's third completion outcome is rewritten to
  say a non-zero exit is not evidence of an abort, and the aborted-invocation section is replaced by
  a single discriminator — a fatal finding whose gate is `memory_path_scan` in some repetition's own
  `run.json` — with two explicit branches: abort (fix environment, delete that repetition, resume)
  and completed-but-short (read results, no resume, no deletion, card 31 dispositions the realised
  n). Added the explicit statement that `blinding_failed_count > 0` and a short `n` column never
  establish an abort on their own.
- r1 holistic, `[BLOCKING:design] "exactly one offending target" is not observable` — verified
  against `pack.go:47-64` (the renderer returns on the first bad result, so its message names one
  target regardless of how many failed) and `pack.go:287-318` (the resolve record is written after
  the render, so a failed pack leaves no `pack-resolve.json`). Card 29's contingency branch now
  enumerates the full resolve set independently first, running
  `go run ./cmd/quarry resolve <glyph> --root <pinned worktree> --text` over all nine `pack_targets`
  against the pinned worktree at `<worktree-root>/worktrees/07-fabric-merge-state-tracing`, and
  states that the one-failure/multi-failure halt test applies to that enumeration and never to the
  pack command's message. The table's cascade ("if that itself does not resolve `found`") is bound to
  the same invocation.
- r1 holistic, `[NIT:consistency] Lock path asserted as ~/.cache/ladder-eval` — verified against
  `worktree.go:163` and `AcquireRunLock`. Card 29 preflight step 2 now resolves the worktree root by
  the harness's own three-alternative rule, records it, and locates the lock at
  `<worktree-root>/.ladder.lock`; card 29 preflight step 3 writes the resolved value into
  `.scratch/kickstart-results-root.txt` alongside the results root, and card 30's poll and resume
  precondition 2 both read it rather than restating a path.
- r1 holistic, `[NIT:consistency] Card 30 declares Deletes: none but authorises a deletion` — card 30
  now states that `Deletes:` is the tracked-file inventory and that the raw tree under
  `$RESULTS_ROOT/raw/` is git-ignored and outside it, which is where the one conditional
  repetition-directory removal happens. No tracked file is deleted by the card. Naming a
  `raw/<cell>/<rep>/` placeholder in `Deletes:` was rejected because `Deletes:` tokens must resolve
  on disk or be a plan-wide `Creates:` target, which a runtime-produced ignored directory is not.
- r1 holistic, `[NIT:scope] Cross-check command does not name its repository` — verified: `72c23d9`
  is the loomyard `pinned_sha` and does not exist in this quarry worktree. Contingency step 6 now
  spells the cross-check as `git -C <loomyard checkout> show 72c23d9:<file>` against the checkout
  named by `LADDER_LOOMYARD_REPO` in `.scratch/ladder.env`, explicitly not a bare `git show`.

## Pushed Back

_None._
