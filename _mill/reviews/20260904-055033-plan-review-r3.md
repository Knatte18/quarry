MILL_REVIEW_BEGIN
# Review: resolve + expand (T4) — holistic

```yaml
duration_s: 325.0
verdict: REQUEST_CHANGES
reviewer_model: opus
reviewer_self_id: claude-opus-5 (1M context) — self-assessment, matches the environment's stated model ID
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [BLOCKING:scope] Card 17 has no function fixture in Context
**Location:** batch 4 / card 17 (`TestExpand_NotAType`)
**Issue:** the card requires "a package-level function glyph" returning `*NotATypeError`, but its `Context:` holds only `decls.go` (const/var/type), `inits.go` (`init` only, used for the separate multipart row) and `tags/*.go` (whose `Dup` is a two-match ambiguous set, not the single-match function case the row is testing); the only clean single-match package-level functions are in `testdata/glyphs/iface.go` / `generic.go` / `tree/pkg/alpha.go`, none of them listed.
**Fix:** add `internal/engine/testdata/glyphs/iface.go` to card 17's `Context:` and name the function glyph it should use, as card 16 already does for its own fixtures.

### [NIT:consistency] Stale "later resolve verb" comments left in resolve.go
**Location:** batch 3 / card 9 (and card 7's "do not change `SpansOf` at all")
**Issue:** card 9 rewrites `resolve.go`'s header but forbids touching `unitDirs`' doc comment (`resolve.go:39-43`: "the later resolve verb promotes collision … a status type that is not its to design") and `SpansOf`'s (`resolve.go:253`: "unlike the later resolve verb's …"); both describe the file wrongly once `Resolve` lands, which is the exact condition cards 2 and 23 invoke to justify correcting two other stale comments. `answer.go`'s `Symbol.File` comment ("The span lookup batch 5 adds, and the later resolve and expand verbs, fill File") is the same case, and card 2 rewords only `HeadStart`.
**Fix:** either extend card 9 to re-tense those two `resolve.go` clauses (and card 2 to cover `Symbol.File`), or state in the Shared Decision why "later verb" phrasing is left standing where `HeadStart`'s and `golang.go`'s were not.

### [NIT:consistency] Loomyard cards open the checkout with `Open`, not `openRepo`
**Location:** batch 5 / cards 20, 21, 22
**Issue:** all three say "opens the checkout with `Open`", while every existing Loomyard-gated test in this package uses the `openRepo(t, root)` helper (`toc_test.go:19`, used by `roundtrip_test.go:202` and `golden_test.go`), which is already in each card's `Context:`.
**Fix:** name `openRepo` instead of `Open` in cards 20 and 22 (card 21's benchmark needs `Open` or a `testing.TB` variant — say which).

## Verdict

REQUEST_CHANGES
One context gap in card 17; DAG, numbering, decisions and file inventory all check out.
MILL_REVIEW_END
