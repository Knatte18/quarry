# Discussion fix — round 2

Source review: `_mill/reviews/20260904-060152-discussion-review-r2.md` (verdict APPROVE, 0 blocking, 6 nits; 2 demoted from BLOCKING by the stage's blocking-class ceiling).

## Fixed

- r2 `[NIT:consistency]` "Golden test contradicts cwd-relative targets" (demoted from BLOCKING) — verified: as written, `cli.Run("toc", "internal/logger", "--root", <loomyard>)` would resolve the target against the quarry test process's own cwd, land outside the Loomyard root, and return exit 1 rather than a golden. Resolved by stating one rule in `root-discovery-and-target-interpretation`: the base directory for a relative target is `--root` when given, and the process cwd otherwise, so `--root` rebases target interpretation along with discovery. `golden-tests-run-the-cli-in-process` now spells the invocation with `--root` plus a repository-relative target, asserts exit 0, and records that the two decisions are load-bearing on each other. A dedicated case for the rebasing rule was added to the Testing section's target-conversion bullet.
- r2 `[NIT:consistency]` "`(exit code: 0)` line is not the before side's layout" (demoted from BLOCKING) — verified against source: `docs/research/output-formats/toc-dir.txt` ends at its closing `}` (line 96), `toc-file.txt` likewise (line 226), and both `toc-*-compact.txt` end at their last text line; only `impact*`, `definition*` and `assert-no-callers` carry an exit-code line, so the before `INDEX.md`'s blanket claim is itself wrong. Resolved by dropping the trailer: each `after/*.txt` is the invocation line, a blank line, and the output verbatim, with the false "mirrors the before side" premise replaced by the actual per-file evidence and an explicit note not to propagate the before `INDEX.md`'s claim. The exit code is asserted by the test instead of appearing in the fixture.
- r2 `[NIT:design]` "`--text` failure output unstated" — added to `failure-envelope-and-exit-codes`: the failure envelope is JSON on stdout regardless of `--text`, there is no text failure form, and the rationale (a failure payload is two fields with no prose, so a text spelling would be a second envelope for zero gain). The Testing section's failure-envelope bullet now repeats its assertion under `--text`.
- r2 `[NIT:design]` "File-form path when the answer's `dir` is `.`" — verified `walk.go:47` `joinRel` returns the bare child when the parent is `"."`. The text-view file form's template changed from `<dir>/<name>` to `<path>`, defined as the engine's own `joinRel`, with the root-level `README.md` vs `./README.md` case stated. A test case was added.
- r2 `[NIT:design]` "`[error …]` tag outside prose normalisation" — extended the collapse rule to cover `error` alongside `doc`, `header` and `signature`, with the reason stated (it is an arbitrary `os`/UTF-8 message quarry does not author, emitted inside a bracketed tag, so a multi-line value would break the one-record-per-line property). A multi-line-error test case was added.
- r2 `[NIT:decision]` "`--help` has no stated disposition" — added to `flag-semantics`: `--help` and `-h`, at any position and with or without a verb, print usage to stdout and exit 0, with the distinction from the exit-2 usage-error path (usage on stderr) stated. A test case was added.

## Pushed Back

None. All six findings verified accurate against the discussion text and, where they made claims about the repository, against source (`internal/engine/walk.go`, `docs/research/output-formats/toc-*.txt`). No fix breaks existing functionality, conflicts with a documented design decision, or destabilizes work outside this task.
