# Plan fix — round 1

Source review: `20260904-091121-plan-review-r1.md` (holistic, opushigh, REQUEST_CHANGES, 2 BLOCKING + 3 NIT).

## Fixed

- r1 holistic, `[BLOCKING:scope] Card 8 invalidates three existing parser test rows` — verified against `internal/cli/flags_test.go:156-158`: all three rows are real and two of them assert the old no-verb wording while the third uses `resolve` as its unknown verb. Card 8's `Requirements:` now names those three rows explicitly and states what each becomes, rather than only saying "extend the existing pure table".
- r1 holistic, `[BLOCKING:design] Card 11's fixture spec contradicts card 11's own expected result` — verified against `internal/engine/resolve.go`'s `statusForMatches`: two declarations of one name resolve to the ambiguous status, so the build-tag duplicate would have made card 11's own found case fail. The duplicate is dropped from card 11's fixture, with the reason stated in the card, and the ambiguous status stays covered by card 14's self-built tree.
- r1 holistic, `[NIT:consistency] Card 11 calls runExpand before card 12 declares it` — card 11's dispatch now carries the table-of-contents and resolve arms only, so its commit compiles on its own; card 12 now explicitly adds the expand arm together with that function's own shorter argument list.
- r1 holistic, `[NIT:decision] The payload-carries-engine-text rule is not in Shared Decisions` — added as its own `### Decision:` in the overview, stating that the no-engine-prefix rule binds quarry-authored sentences and not a payload's error field, and instructing the implementer not to rewrite that field anywhere in the plan.
- r1 holistic, `[NIT:consistency] Card 6 says five renderers, then lists seven` — the count is now seven, matching the enumeration.

## Pushed Back

_(none — every finding was factually accurate and no fix caused harm under the decision tree.)_
