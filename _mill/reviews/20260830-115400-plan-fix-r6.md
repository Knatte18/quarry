# Fixer report: plan-fix round 6

Source review: `_mill/reviews/20260830-115146-plan-review-r6.md` (holistic, REQUEST_CHANGES,
1 BLOCKING / 0 NIT).

This is the final round under `roles.plan-review.holistic.rounds: 6`.

## Fixed

- r6 holistic — "[BLOCKING:scope] check one-b whitelist omits real test-side survivors". Verified
  against the files: check one-b's enumeration is written as exhaustive, and five real categories of
  hit fall outside it — the assertion format strings and failure messages in
  `internal/mcpserver/callcontext_test.go`, `internal/mcpserver/nativeentry_test.go`,
  `internal/mcpserver/tools_assert_test.go`, and `internal/mcpserver/transport_errors_test.go`, and
  the helper doc comments naming the `TargetDir` field in `internal/mcpserver/tools_lsp_test.go` and
  `internal/mcpserver/transport_test.go`. None is a `Config{}` construction, a `cfg.TargetDir` or
  `opts.TargetDir` read, a test-function name, one of the listed test-locals, or prose naming the
  server's target directory, and no card edits any of them — so the gate would have failed on arrival
  and forced a card-8 failure report against hits nothing owns. Check one-b now sanctions two further
  categories: `t.Errorf`/`t.Fatalf` format strings and failure messages naming the `TargetDir` field
  or the `targetDir` parameter, and test doc comments naming the `TargetDir` field a helper
  populates, each with the files that carry them today named. Both new categories inherit the same
  qualification the existing prose survivor carries — a message or comment describing a per-call
  override still fails — and the card now states the distinguishing rule outright: a survivor reports
  on or names a live Go field, parameter, or local, never a tool input property.

## Pushed Back

_None._

## Round-cap disposition

Round 6 is the configured cap. The round's single BLOCKING finding was verified, accepted, and fixed
above; nothing was pushed back and no finding remains unaddressed. Per the operator's explicit
instruction for this run — hand off at the cap rather than block — the plan is approved here instead
of halting via `_status.set_blocked`. The fix has not been re-reviewed by a further round, which is
the one thing the cap costs, and the approve commit records that.
