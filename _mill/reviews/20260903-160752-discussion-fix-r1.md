# Discussion fix — round 1

Source review: `_mill/reviews/20260903-160640-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 3 NIT, reviewer `orchestrator`).

## Fixed

- r1 `[NIT:consistency]` "Reserved-names citation points at the wrong section" — Scope → Out now cites §6 and quotes it ("the names reserved for the alphabets below") instead of §1.
- r1 `[NIT:design]` "Whitespace ban is presented as required by §6, but §6 tolerates quote-needing glyphs" — the Go-unit-alphabet rationale and open question #2 both now state the ban is this task's own proposal, note that §6 explicitly tolerates glyphs needing quotes, give the narrower argument that actually supports it, and say the hub may accept or drop it. The stale claim in the Q&A log entry on unit characters was corrected in place for the same reason.
- r1 `[NIT:design]` "Reject precedence between language check and structural split is unstated" — the split decision now fixes the order (language check first, then split, then unit half, then member half), says why, and adds "validating `s` before `lang`" to its rejected alternatives; `parse_test.go`'s test list gains a doubly-invalid case (`Parse(Language("python"), "no-hash")` → `unsupported_language`) pinning it.

## Pushed Back

None. Under `--orch` no operator is present, so PUSH BACK is unavailable; all three findings were verified accurate and none met a harm category.
