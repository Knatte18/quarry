# Plan fix — round 1

Source review: `20260903-172944-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING / 9 NIT).

## Fixed

- r1 `[BLOCKING:scope] Batch 5 depends on gates.go but not on batch 4` — added `4` to batch 5's
  `depends-on` in both the overview Batch Index and the batch file's own frontmatter, added
  `internal/ladder/gates.go` to cards 23 and 24 `Context:`, and stated the reason in batch 5's
  scope paragraph.
- r1 `[BLOCKING:scope] Provenance reader, writer and field collection are unassigned` — card 23 now
  names `ReadProvenance` (nil record and no error on a fresh root, error naming the file on a
  malformed one), `WriteProvenance` and `CollectInvocation`, the latter as the single producer of an
  invocation's own facts and enumerating every field it gathers and through which seam. Card 26's
  startup sequence now names which of them the loop calls and when, including the merge-and-write
  before the first repetition. Card 24 gained the matching test cases.
- r1 `[BLOCKING:scope] Card 14 names no origin/v1-final path for the recovered schema` — card 14 now
  names `origin/v1-final:bench/loomyard-eval/README.md`, section "Output schemas", and requires a
  byte-for-byte copy of its exploration block rather than a reproduction from prose.
- r1 `[NIT:decision] Scorer invocation is never specified by any card` — card 26 now specifies
  `RunScorer`, its full flag set, that model and effort come from the ladder file's scorer block and
  never the cell's, that it runs from the harness scratch directory rather than the pinned worktree,
  and how its prompt and reply are assembled and parsed. Card 34's assertion was tightened to match.
- r1 `[NIT:consistency] Runner signature cannot carry the environment BuildServer needs` — the
  overview's seam is now `Run(ctx, c Cmd) error` with a `Cmd` struct carrying `Env` and a separate
  `Stderr` writer. Mirrored in card 20 (declaration), card 22 (the cgo variable set in the command's
  environment, not the harness process's), card 24 (assert both, including that the harness's own
  environment is unchanged) and card 33 (the fake's error output is readable because the seam
  carries an error writer).
- r1 `[NIT:consistency] ResolveWorktreeRoot takes no repo-root argument` — signature is now
  `ResolveWorktreeRoot(quarryRepoRoot string) (string, error)` and the first invariant reads
  "the supplied quarry repository root".
- r1 `[NIT:decision] "the four built-ins" is never enumerated` — new overview Shared Decision
  `the-four-built-in-tools` fixes the set, its passed spelling and its reported sorted order, and
  card 26 declares the package-level `BuiltinTools` slice. Cards 13, 19, 26, 33 and 36 now reference
  that decision instead of the phrase.
- r1 `[NIT:decision] Two of the six per-rep filenames are never fixed` — new overview Shared Decision
  `the-six-per-repetition-filenames` fixes all six names, the write order and the diagnostic status
  of the metrics file; card 25 declares them as package-level constants; card 26's write step names
  them; card 28 now states that recall and precision are read from the score record while every cost
  metric is recomputed, and that the metrics file is never read.
- r1 `[NIT:scope] Quarry repo-root resolution has no named producer` — card 20 now creates
  `ResolveQuarryRepoRoot`, and card 30 calls it by name.
- r1 `[NIT:decision] The target-directory placeholder token is unnamed` — card 22 now fixes the
  literal token `{target_dir}`.
- r1 `[NIT:consistency] Card 5 leaves deleted ids and tools in the header comment` — card 5 now
  requires the design block to list exactly the four surviving ids and requires each entry of the
  "deliberately not here" list to be rewritten against the migrated file's contents, with no line
  describing something the file no longer has.
- r1 `[NIT:consistency] Overview verify omits the task's own test half` — **fixed by a different
  mechanism than the one proposed.** The proposed `verify: go build ./... && go test ./...` is
  refused by the plan validator's own `verify-full-suite` check, which flags `go test ./...` without
  a `-run` filter and directs the planner to scope it or justify it in `## Batch Tests`; an
  unscoped repository-wide suite as a per-batch-boundary gate is exactly what that check exists to
  prevent. The underlying gap the finding names is real and is now closed: a new overview Shared
  Decision, `where-the-done-criterion's-test-half-is-enforced`, states that the build half is the
  module-wide `verify:`, that each batch's own `verify:` runs every test this task writes, and that
  the repository-wide test half runs once at the end via the hub's configured `pipeline.done_gate`
  (`go test ./...`, run by mill-go from the repository root before the task is marked done). The
  done-criterion is now accounted for by name rather than left implicit.

## Pushed Back

_None._
