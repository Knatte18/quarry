# Plan fix report — round 1

Source review: `20260905-160105-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 2 NIT).

## Fixed

- r1 BLOCKING:scope "DeltaGit has no seam to the repository root" — verified accurate against
  `quarry/repo.go` (the facade type holds only the engine handle) and `internal/engine/repo.go`
  (`root` is unexported with no accessor). Card 32 now adds a root field to the facade's repository
  type, captured from the constructor's already-validated argument, with `quarry/repo.go` added to
  its `Edits:` and its commit message updated; card 33 now names that field as what the git layer is
  opened against. Capturing on the facade rather than exporting an engine accessor is stated with
  its reason, so the choice is not re-litigated at implementation time.
- r1 BLOCKING:design "Delta text losslessness contradicts the reused symbol-line writer" — verified
  accurate: `writeSymbolLine` in `quarry/text.go` emits no kind, and created, deleted and a renamed
  pair's symbols all carry one in the JSON view with no other carrier. Resolved in favour of the
  lossless contract rather than by weakening the assertion, since `docs/rewrite-plan.md` §4 fixes the
  text view as lossless: card 39 now requires the delta renderer to spell the kind itself at every
  symbol and every table key, and explicitly forbids widening the shared writer, which is byte-pinned
  by the other three verbs' goldens. Card 41 gains the const-replaced-by-var case that makes the kind
  load-bearing.
- r1 BLOCKING:scope "Stale `internal/cli` doc comments left uncorrected" — verified accurate for all
  three: the parser's doc claims a three-verb gate and three-verb flag validity, the entry point's
  doc names three pipeline functions and a default unreachable for all but three verbs, and the
  usage-code constant scopes the separator rejection to a table-of-contents target. Cards 42 and 44
  now require each correction in the same commit as the code change that falsifies it, matching what
  card 45 already does for the package doc. Card 44 additionally requires the new verb's numbered
  pipeline description, since that comment is where each pipeline's step order is stated.
- r1 NIT:consistency "Mapping-function count disagrees within batch 8" — verified accurate;
  `internal/cli/cli.go` declares four. Card 44 now says four, matching its own Batch Tests section.
- r1 NIT:consistency "Card 10's toc-suite check is not in batch 2's verify" — verified accurate; the
  toc tests are named `TestRepoTOC_*`, which the old pattern did not select. Fixed by widening the
  selection rather than narrowing the claim, since the toc surface genuinely exercises the unit
  derivation this batch rewrites: batch 2's `verify:` gains `TestRepoTOC` in both the batch file's
  frontmatter and the overview's Batch Index entry, and the Batch Tests section now names both
  suites.

## Pushed Back

_None. All five findings verified accurate against the cited code, and none met any harm category of
the receiving-review decision tree._
