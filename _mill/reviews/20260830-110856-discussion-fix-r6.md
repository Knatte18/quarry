# Discussion fix — round 6

Source review: `_mill/reviews/20260830-110822-discussion-review-r6.md` (verdict APPROVE, 0 BLOCKING, 2 NIT, 0 demoted).

## Fixed

- r6 `[NIT:scope]` "Per-tool \"override cases\" to remove do not exist" — verified: across `tools_lsp_test.go`, `tools_impact_test.go`, `tools_assert_test.go`, and `tools_toc_test.go`, every `targetDir`/`TargetDir` occurrence is either `Config{TargetDir: ...}` at construction (`tools_lsp_test.go:58`, `tools_toc_test.go:225`) or comment prose; no test sets an input struct's `TargetDir`. Rewrote the "Per-tool tests" paragraph to state that `callcontext_test.go:12-39` is the sole file with override cases, that these four files are expected to need no deletions or conversions, and that the toc absolute-path case is the group's only new work.
- r6 `[NIT:consistency]` "Undispositioned per-call phrasing in a test comment" — verified: `tools_toc_test.go:212` reads "never the call's targetDir", which the Verification grep rule ("never per-call phrasing") would flag, but the Scope reword list covered production files only and the `effectiveTargetDir` disposition named `tools_toc_test.go:370` alone. Added `tools_toc_test.go:212` to the per-call-phrasing reword bullet, so the grep rule is no longer self-contradicting.

## Pushed Back

None.
