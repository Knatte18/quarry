# Discussion fix — round 2

Source review: `_mill/reviews/20260905-110544-discussion-review-r2.md` (verdict REQUEST_CHANGES, reviewer `opushigh`, 2 BLOCKING / 4 NIT, 2 demoted from BLOCKING).

All six findings verified accurate against the worktree before fixing. Under `--orch` the decision tree's PUSH BACK path is unavailable; every finding was treated as FIX.

## Fixed

- `20260905-110544-discussion-review-r2.md` — "[NIT:scope] A third `#`-containment classifier is unenumerated": verified `internal/cli/flags.go:153` holds `verb == "expand" && !strings.Contains(req.target, "#")` → usage error, exit 2. Added **D19** deciding the gate is deleted, so `expand <bare-path>` reaches `glyph.Parse` and exits 1 like `resolve` does; corrected the Problem paragraph from "implemented twice" to three classifiers with the verb-disagreement consequence spelled out; corrected D4's rationale to note the claim is false today and holds only once D19 lands, with the dependency stated; added the acceptance condition to D7 that all three classifiers must go or its mandated doc.go sentence is false the day it is written. Scope-In, the module map (`internal/cli/flags.go`, `usage.go`), Testing (both-verbs-agree row, `flags_test.go` grep), the scenario list and D18's squash-message list all updated.

- `20260905-110544-discussion-review-r2.md` — "[BLOCKING:design] D8's \"MCP needs no other edit\" rests on a false premise": verified `internal/mcpserver/toc.go:107-112` is one `errors.Is(ErrTargetOutsideRepo)` branch plus an `"internal error: " + err.Error()` else-arm. D8 now requires an explicit `tocResult` branch emitting the same sentence the CLI emits, fixes the sentinel's own text (`engine: target contains the glyph separator "#"`), and states the failure mode the missing branch produces. The Technical-context entry for `internal/mcpserver/` is rewritten from "needs no other edit" to naming the edit. Scope-In gains the file. The `toc_errors_test.go` Testing item now requires asserting the exact sentence and its byte-identity with the CLI's, because a shape-only assertion would pass against the wrong else-arm.

- `20260905-110544-discussion-review-r2.md` — "[BLOCKING:design] The self form is not language-free as stated": verified `docs/glyph.md:52-54` defines the unit as the Go package directory, the Python dotted module, the C# namespace. Added `docs/glyph.md` §2 to D15's edit inventory (now six sites, was five) with a per-language statement of what `unit#` means and the fact that a file self glyph is Go-only (Python's file *is* its module; C# has no file-level unit) — plus the deeper point the finding implies: §2 as written does not admit a file as a left half at all, so the new paragraph states that in the self form the left half is the thing's own repository-relative path or unit name, whichever the language spells. D15's §3 sentence is now explicitly scoped to Go. D1's rationale and D2's rejected-alternative are narrowed from "the self form is language-free" to the true claim (only the empty-member representation is shared). The Scope-Out bullet is corrected: Python/C# *extractors* are out, their *documented* self form is in.

- `20260905-110544-discussion-review-r2.md` — "[NIT:consistency] `docs/rewrite-plan.md` §4's round-trip sentence is left standing": verified line 81 reads "Glyphs as keys in every output, under `id`. What `toc` lists is what `resolve` takes", and that `toc`'s file/directory entries carry no `id`. D16 restructured from one §4 edit to three, with (b) rewriting that bullet, and **D20** added: a new `glyph.Self(lang, path)` constructor so a consumer completes the listing→glyph round trip inside the one implementation instead of concatenating `dir + "/" + name + "#"`. `glyph/doc.go`'s "exports no constructor" sentence is named for update. Scope-In, Testing (a `Self` case set plus an end-to-end no-concatenation round trip) and the scenario list updated.

- `20260905-110544-discussion-review-r2.md` — "[NIT:consistency] Module map misplaces two sentinels": verified `ErrTargetOutsideRepo` and `ErrTargetNotFound` are at `internal/engine/repo.go:46,50` and that `errors.go`'s header reads "the one error sentinel the engine's subpackages share". D8 now declares `ErrTargetHasSeparator` in `repo.go` beside its two neighbours and says why not `errors.go`; the module map gains a `repo.go` entry and marks `errors.go` read-but-do-not-edit. No header rewrite is needed anywhere.

- `20260905-110544-discussion-review-r2.md` — "[NIT:consistency] \"Delete the `RepoRelPath` tests\" deletes tests of retained code": verified `TestRepoRelPath_LeadingDotDotNotRejected` (line 120) and `TestRepoRelPath_AgreesWithRepoRelTarget` (line 146) call the unexported `repoRelPath`, which D6 retains. Testing now deletes only tests of the exported wrapper, keeps and renames those two, and amends the agreement claim to "agree on every input that neither escapes the root nor contains a `#`", with the separator divergence asserted as its own row.

## Pushed Back

None.
