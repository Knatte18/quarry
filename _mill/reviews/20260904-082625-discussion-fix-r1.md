# Discussion fix — round 1

Source review: `_mill/reviews/20260904-082529-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- r1 `[NIT:consistency]` "Testing cites an internal/cli testdata/ that does not exist" — the golden-test section now names `internal/mcpserver/testdata/` as a committed fixture in `internal/engine/testdata/`'s style, and states explicitly why the CLI's `writeScratchTree` precedent was not followed (unexported test helper in `package cli`, unreachable from another package's tests; a committed tree also makes golden bytes stable).
- r1 `[NIT:decision]` "Repo open-once vs per-call left as a preference with a sanctioned fallback" — the two-designs sentence is removed from Technical context and replaced by a binding decision, D17 "Open the repository once, at startup", with per-call open recorded as a stop-and-raise contingency rather than an open choice. D2 and the Scope bullet for `cmd/quarry-mcp` updated to name the single `quarry.Open` call.

## Pushed Back

None.
