# Plan fix — round 3

Source review: `20260909-064127-plan-review-r3.md` (holistic, APPROVE, 0 BLOCKING / 3 NIT).

## Fixed

- `20260909-064127-plan-review-r3.md` — "[NIT:consistency] `./glyph/` does not actually cover card
  10". Verified: `glyph/docs_test.go`'s `docsAccept`/`docsReject` tables are hand-written Go
  literals and the file reads no markdown. Batch 3's Batch Tests bullet now says the suite must keep
  passing but does not verify the documentation edit, and names card 10's no-new-glyph-string rule
  as what keeps that acceptable.

- `20260909-064127-plan-review-r3.md` — "[NIT:scope] Conditional edit to an undeclared file".
  Verified: the three rejection-rendering rows exist at `quarry/text_test.go:306`, `:311` and `:316`
  (`RejectionWithReasonAndError`, `RejectionEmptyReason`, `RejectionEmptyError_TotalityGuard`), so
  the conditional branch was dead while the instruction to edit an undeclared file was live. The
  bullet now states the rows as verified present and says explicitly that no card edits that file;
  the conditional is gone. `quarry/text_test.go` stays out of every `Edits:` list and out of
  `## All Files Touched`.

- `20260909-064127-plan-review-r3.md` — "[NIT:consistency] \"return from the panic site\" invites
  unreachable code". Card 5's `Unit`-first clause now reads that the panic ends the check for that
  index, states that no `return` follows it because `panic` does not return and a trailing `return`
  would be unreachable code the overview's `go vet ./...` gate flags, and puts the `Target`
  comparison in the else branch.

## Pushed Back

_None._
