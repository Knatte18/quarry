# Plan fix — round 1

Source review: `_mill/reviews/20260828-091622-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 5 NIT).

## Fixed

- r1 holistic, "[BLOCKING:scope] cli.go batch status-vocabulary doc site missed" — verified against `internal/cli/cli.go` lines 30-34, which scope `"ambiguous"` to "refs/definition only". `classifyImpactError` makes `impact` a third producer, so the parenthetical goes false. Card 11's doc work is now an explicit three-edit enumeration (verb list, identity key, status vocabulary) instead of two edits plus a "change nothing else" clause.
- r1 holistic, "[BLOCKING:design] The live-tier test is never executed by any gate" — verified: `go vet -tags lsp` type-checks but never runs, and `pipeline.done_gate` is untagged. Batch 4's `verify:` now runs `go test -tags lsp ./internal/cli/`, which is a strict superset of the untagged run for that package and so replaces it. Recorded that gopls is absent in this worktree, so the assertions skip here while the compile check still binds unconditionally.
- r1 holistic, "[BLOCKING:scope] Requirements name identifiers from files absent from `Context:`" — verified all three sites. Added `internal/cli/cwdcontext.go` and `internal/cli/exec.go` to card 10's `Context:` (`CwdFrom`, `SetExit`), `internal/cli/cli.go` to card 5's (`filterUnexpectedCallers`, with an explicit "read for the rule, do not import" note so the seam ban stays unambiguous), and `internal/quarryengine/toc/types.go` to card 1's (`toc.Kind`, `toc.Symbol.Start`).
- r1 holistic, "[NIT:consistency] Card 15's minPackageDirs comment instruction is self-contradictory" — verified the arithmetic: after batch 1 the two-tree walk visits nine engine directories plus `quarry/`, so a floor of 8 is two below, not one, and "keep the reasoning intact" could not coexist with "correct the counts". Card 15 now states one disposition — restate the slack with no count at all, correct only the enumeration's factual totals, and leave the constant alone per `discussion.md`'s explicit decision. The card's own stale opening premise ("the walk actually visits 9") was corrected too.
- r1 holistic, "[NIT:consistency] Card 14 announces four edits and prescribes five" — verified. Restated as five and folded the opening-paragraph edit into the enumeration as "Fifth".
- r1 holistic, "[NIT:consistency] Reused `structToFields` emits a \"toc:\"-prefixed error for impact" — verified against `internal/cli/toc.go`, which wraps both failure modes with a literal `toc: ` prefix. Card 10 now requires `emitImpactResult` and `classifyImpactError` to re-word the failure as an `impact` marshalling error before emitting, keeping the shared helper unchanged per the reuse decision while stopping the verb's envelope from naming a verb the caller never invoked.
- r1 holistic, "[NIT:consistency] `definition` flattens the range that `callers` nests" — verified the asymmetry is real. Took the finding's first offered option (record why) rather than its second (reuse `*Range` on `Definition`): the flat shape is what `discussion.md`'s `json-key-set` decision fixes verbatim, so nesting would contradict a documented design decision. The `json-key-disposition` Shared Decision now carries a "Rationale for the two range shapes" paragraph explaining that a caller's range is present-or-absent as a unit (a documented file-scope signal) while `definition`'s three fields are individually omittable by the three-outcome rule, so there is no unit to nest.
- r1 holistic, "[NIT:consistency] Doc-audit sweep assigned only in Batch Tests, not in the owning card" — verified both halves. The sweep is now card 16's own last `Requirements:` step, with its outcome recorded in that card's commit body and an explicit instruction for what to do if it finds an uncovered stale site. Batch 4's `## Batch Tests` was rewritten to match the new `verify:` and to attribute card 16 correctly to `README.md` (a documentation file with no runnable surface) rather than to the CLI package.

## Pushed Back

_None. Every finding passed VERIFY and none triggered a harm-check escape._

Note on the `definition`/`callers` range-shape finding: the alternative remedy it offered (reuse `*Range` on `Definition`) was not taken, but the finding itself was fixed via its own first option. This is a choice between two offered remedies, not a pushback.
