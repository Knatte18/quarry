# Plan fix — round 3

Source review: `20260905-120459-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 3 NIT).

## Fixed

- `[BLOCKING:scope]` "Card 29 writes four files it declares read-only" — moved
  `ladder-kickstart.yaml`, `cards/07-e0-names.md`, `cards/07-e2-files.md` and
  `tasks/07-fabric-merge-state-tracing.md` from card 29's `Context:` to its `Edits:`, since the
  glyph-substitution branch writes all four. Added a sentence saying the ordinary path writes only
  the treatment card and that the four are declared as edits because the contingency branch writes
  them. No change to `## All Files Touched` — all four were already in it as earlier batches'
  `Creates:` targets. Also added the fasit to card 29's `Context:`, which the substitution branch's
  cross-check step reads.
- `[NIT:consistency]` "mcp.go's other two stale control comments are unswept" — card 6 now rewords
  the file header and the config-document type comment alongside the function's own doc comment, so
  the whole file stops asserting the control-equals-tool-less equation its code no longer implements.
- `[NIT:consistency]` "Recall-inflation rationale names only the treatment arm" — corrected in the
  Shared Decision, in card 24's ladder-header note and in card 31's prescribed sentence. The
  descriptive arm's file list is derived from the fasit's own relevant-files list, so its file recall
  is inflated at least as directly as the treatment's; the control is the only arm whose file recall
  is earned.
- `[NIT:scope]` "Card 14 cites `Run` with an empty Context" — added `run.go` to card 14's `Context:`
  rather than taking the mentioned-not-read escape, since the implementer must write that rationale
  into a comment and should be able to check it.

## Pushed Back

_None. Every finding verified accurate against the plan's own text and the code it describes, and no
fix breaks existing functionality, conflicts with a documented decision, or destabilises work outside
this task._
