# Plan fix — round 5

Source review: `20260904-094243-plan-review-r5.md` (holistic, opushigh, REQUEST_CHANGES, 2 BLOCKING + 1 NIT).

This is the final round under the operator-set round cap of 5. The operator's standing directive for this run is to approve and hand off after round 5 regardless of remaining findings; every finding this round raised was nevertheless fixed in full before that approval, and none was pushed back.

## Fixed

- r5 holistic, `[BLOCKING:design] Card 16's -update run destroys the no-regression proof` — verified against `compareAfterGolden`: the regeneration flag rewrites every row's file unconditionally, the four already-committed table-of-contents goldens included. As previously ordered, the card regenerated first and compared afterwards, so the comparison would have been regenerated bytes against themselves and the task's stated proof that the shared-encoder and symbol-line refactors changed nothing could never have fired — a real byte change would have landed silently, since those four files are in no card's `Creates:` and not in `## All Files Touched`. Card 16 now specifies an ordered three-step procedure: the unregenerated comparison first, where the four table-of-contents rows pass on their committed bytes while the eight new rows fail on a missing golden, and where the proof is actually taken; then the regeneration; then a `git diff --exit-code` scoped to those four paths, with an explicit stop-and-report on a non-empty diff. Batch 1's Batch Tests and batch 4's Batch Tests are both updated to point at that first step as the place the proof fires.
- r5 holistic, `[BLOCKING:scope] Cards 11 and 12 assert on answer.go's key set without listing it` — verified: both cards' requirements pin payload keys and their omitempty behaviour, and both types are declared in `internal/engine/answer.go`, which neither card listed. Card 12's `internal/engine/expand.go` declares only the not-a-type error and the methods, not the answer struct. Added `internal/engine/answer.go` to both cards' `Context:`, matching cards 3, 5, 10 and 14.
- r5 holistic, `[NIT:consistency] RenderResolveText branch 1 breaks the no-trailing-whitespace rule` — verified: with an empty error field the specified form emitted a trailing space, violating the invariant the same card asserts for every input. Branch 1 now emits the colon, space and message only when the normalised error string is non-empty, and branch 2's not-found clause is guarded the same way for an empty unit field. Card 5's test table now enumerates five totality guards rather than three, and asserts both invariants — no trailing whitespace, exactly one trailing newline — on each.

## Pushed Back

_(none — every finding was factually accurate and no fix caused harm under the decision tree.)_
