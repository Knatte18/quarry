# Plan fix — round 3

Source review: `20260904-092728-plan-review-r3.md` (holistic, opushigh, REQUEST_CHANGES, 1 BLOCKING + 5 NIT).

## Fixed

- r3 holistic, `[BLOCKING:design] RenderResolveText's path branch has no nil-Dir disposition` — verified: the directory answer is a pointer field, so an externally constructed found path result with a nil pointer would panic rather than honour the renderer's own one-trailing-newline promise. Branch 3 now spells the guard — line 1 alone, nothing else — with the same external-caller rationale the expand renderer's fall-through carries, and card 5's test table now drives it from a found path result whose pointer is nil.
- r3 holistic, `[NIT:consistency] Card 14 fixture makes its own "found" row impossible` — verified against `statusForMatches`: the fixture declared one free function and then duplicated that same name, so the found row and the ambiguous row queried one glyph with two incompatible expectations. The fixture is respelled as an explicit list: singly-declared function and type for the two found rows, separately named duplicated function and duplicated type for the two ambiguous rows, with the distinctness requirement stated in the card and repeated in the ambiguous row itself.
- r3 holistic, `[NIT:consistency] Card 5 says three branches, then lists four` — the lead-in now says four, matching the enumeration the previous round added.
- r3 holistic, `[NIT:consistency] Shared Decision over-claims the evidence goldens` — verified: none of card 15's eight rows escapes the pinned checkout's root, so no golden carries a payload error field. The decision now claims only the end-to-end tests, and card 11's own bullet is tightened from "emitted verbatim" to an exact byte-for-byte assertion, naming itself as the one place that string is pinned.
- r3 holistic, `[NIT:scope] Card 2's fixture guidance points at an unspellable unit` — verified: the existing facade fixtures write Go files at the fixture root, and a root-level file has the empty unit no glyph can spell. Card 2 now requires a nested package directory and states why.
- r3 holistic, `[NIT:scope] Card 11 does not say where the shared fixture grows` — verified: an existing flag-pass-through case asserts the shared fixture's nested directory holds exactly one file entry, so growing that directory would break an assertion card 11 itself requires to keep passing. The card now requires a new sibling directory, and rules out the fixture root for the unspellable-unit reason.

## Pushed Back

_(none — every finding was factually accurate and no fix caused harm under the decision tree.)_
