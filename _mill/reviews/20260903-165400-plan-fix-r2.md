# Plan fix — round 2

Source review: `20260903-165002-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING, 4 NIT).

## Fixed

- `20260903-165002-plan-review-r2.md` — "[BLOCKING:design] ParseError.Lang is never populated on Go
  rejects": card 4 now states that every `*ParseError` sets all four fields — `Parse` sets `Lang` to
  the `lang` argument and `Input` to the original `s`; `parseGo` sets `Lang: Go` and `Input: input`,
  the whole original string and never a half — and the batch's own decision list repeats it. Card 6
  gains a `ParseError` field test asserting `Lang` and `Input` on one unit reject, one member reject
  and one `unsupported_language` case.
- `20260903-165002-plan-review-r2.md` — "[NIT:consistency] `parseReject`/`goReject` field shape is
  unpinned and contradicted": card 5 now declares a named package-level `rejectCase` struct with
  explicit fields including `section`, and card 6 reuses that type for `goReject` and declares its
  own named `acceptCase` for `goAccept`. The Batch Scope decision is reworded to name the two types
  and to say why an anonymous literal cannot work here.
- `20260903-165002-plan-review-r2.md` — "[NIT:consistency] Card 5's internal cross-references are off
  by one": the three references now read items 4, 5 and 6.
- `20260903-165002-plan-review-r2.md` — "[NIT:consistency] Unreachable dispatch default contradicts
  the plan's own rule": card 4 step 4 now says the `default:` arm exists only to satisfy Go's
  terminating-statement rule, fixes its `Detail` to `string(lang)` to match step 1, and requires a
  comment on it saying it can never fire.
- `20260903-165002-plan-review-r2.md` — "[NIT:consistency] File-level comments risk becoming rival
  package doc comments": the Shared Decision now requires a blank line between the file-level comment
  and the `package glyph` clause, citing the toc precedent and naming `glyph/doc.go` as the only
  owner of the package comment.

## Pushed Back

_None._
