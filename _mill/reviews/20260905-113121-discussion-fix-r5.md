# Discussion fix — round 5

Source review: `_mill/reviews/20260905-113025-discussion-review-r5.md` (verdict APPROVE, reviewer `opushigh`, 0 BLOCKING / 3 NIT, all three demoted from BLOCKING).

All three findings verified against the worktree before fixing. Under `--orch` the decision tree's PUSH BACK path is unavailable; every finding was treated as FIX.

## Fixed

- "[NIT:consistency] D10a's `Unit` reversal not propagated to D14/Testing/Q&A": verified `quarry/text.go:243-252` appends `" (unit found)"` / `" (unit not_found)"` whenever `r.Unit != ""`. D10a (round 4) reversed D10's `Unit` suppression but three sites still asserted the old rule — D14's parenthetical, the `quarry/text_test.go` row, and the round-2 Q&A entry — while the engine Testing row already pinned `unit: not_found`, so the two Testing rows demanded contradictory bytes and one had to fail. D14 now states the suffix is always present on a self-glyph `not_found` and notes it supersedes the earlier draft; the `text_test.go` row expects `<path># not_found (unit found)` for the `_test` case and `(unit not_found)` otherwise, with an explicit requirement that it agree with the engine rows; a new Q&A entry records the reversal and marks the earlier answer superseded.

- "[NIT:scope] D7's falsified-paragraph inventory is wrong in count and in location": verified `cli.go:200-206` (Run step 1, the `strings.Contains` narration) and `cli.go:219-221` (the `runExpand` preamble, "the parser has already guaranteed the target contains a `#`") are two separate paragraphs, not one. D7 (iv) split into (iv) and (iv-b) with their real line ranges and an explicit note that editing only "step 1" leaves the sentence D19 falsifies standing; the count raised from six to seven; the round-3 Q&A entry, which still answered "four", reconciled to seven with all sites named. Every entry now carries a line range, because two sit in the same doc comment.

- "[NIT:scope] `glyph/` doc-comment inventory omits two falsified statements": verified `glyph/doc.go:1-2` ("names one symbol in a repository … one name for one source symbol" — false for a directory or file self glyph), `glyph/errors.go:18` (`ReasonNoSeparator`'s constant comment, "a glyph needs a unit and a member" — false once the member is optional, and a different comment from the `reasonText` map entry D4 rewrites), and `glyph/errors.go:108-109` (the `Detail` sentence duplicated on the field itself, outside the previously cited 99-100 range). The `glyph/` Scope-In bullet is now a five-entry enumeration with line ranges and required new wording, including the note that both `Detail` copies must change or they contradict each other. A Q&A entry records the count.

## Pushed Back

None.
