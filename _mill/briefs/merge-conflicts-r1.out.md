Summary of the resolution work:

- `/home/knatte/Code/quarry/wts/toc-verbs/README.md` — two conflicts, both non-overlapping additions (this branch's `--doc-sentences` config section and Testing-tier note vs. origin/main's `--build-tags` config section and gopls-`$PATH` Testing note, plus origin/main's new `## Verification` section). Combined both sides in full.
- `/home/knatte/Code/quarry/wts/toc-verbs/internal/quarryengine/doc.go` — one conflict where this branch inserted a new `toc` package-layout bullet ahead of the `query` bullet, and origin/main updated the `query` bullet in place (added `callers.go`/`verify.go`, `Callers`). Kept the `toc` bullet and merged the `query` bullet's updated file list/entry points, replacing the now-inaccurate "It imports all four packages above" (stale after the DAG grew from 4 to 6 bullets above `query`) with an explicit `root, lsp, registry, and daemon` list, matching the style of every other bullet in the file.
- `/home/knatte/Code/quarry/wts/toc-verbs/quarry/facade.go` — two conflicts: (1) the file-header comment, where this branch replaced a numeric re-export count with a property-based statement (immune to staleness) and origin/main updated the count to 33; kept this branch's property-based version since it already covers origin/main's new exports without needing further edits — discarded. (2) non-overlapping new declarations (this branch's `TOC*` types/functions vs. origin/main's `Callers`/`NormalizeBuildTags`); combined both, placing `Callers`/`NormalizeBuildTags` before the `TOC*` block.
- `/home/knatte/Code/quarry/wts/toc-verbs/quarry/facade_test.go` — three conflicts, all non-overlapping additions (this branch's TOC alias-check vars/round-trips/blank-identifier entries vs. origin/main's `ErrBuildTagsUnsupported` alias-check/round-trip/blank-identifier entry, and both sides' new sentinel-identity test-table rows). Combined all, and corrected the three stale counts in surrounding comments that only made sense before the merge: "twenty" → "twenty-one" aliased types, "twelve"/"ten" → "fourteen" blank-identifier assignments, "eight" → "nine" sentinel values.

All four files build (`go build ./quarry/... ./internal/quarryengine/...`), vet cleanly, and their test suites pass (`go test ./quarry/... ./internal/quarryengine/...`). All four are staged; `git status` confirms "All conflicts fixed but you are still merging." No commit or `git merge --continue` was run, per instructions.

{"status":"success","discarded":["quarry/facade.go: dropped origin/main's exact-count doc comment ('It re-exports exactly 33 identifiers... no more, no less.') in favor of this branch's property-based no-count description, which already covers origin/main's added exports (Callers, NormalizeBuildTags, ErrBuildTagsUnsupported, ErrBuildTagsUnsupportedSentinel) without needing future edits"]}
