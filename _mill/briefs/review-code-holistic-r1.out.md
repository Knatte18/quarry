MILL_REVIEW_BEGIN
# Review: Add file/dir toc verbs (Tree-sitter-backed) — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-08-28
```

## Verification performed

Read all nine plan files (overview + eight batches) and every source file in the manifest end to
end: the tree-sitter backend and its build-guard pair, the extension map, the whole `toc` package
(types, strategy registry, comment/sentence/classify helpers, node helpers, the three language
strategies, the two entry points), the facade, the full CLI toc surface (command group, batch
driver, config resolver/loader), `internal/output`, the layering/seam guard tests, `mill-config.yaml`,
`README.md`, and `docs/toc-docstring-association.md`.

Checked, concretely rather than by trusting prose:
- Every JSON tag in `types.go` against the "emitted key set is closed" Shared Decision — exact match.
- `SigEnd`'s per-language flags (Go `true`; Python `false`; C# read off `body.Kind()`) and the clamp,
  including the grouped-type-spec `SigEnd(spec, ...)` vs `decl` distinction card 19 calls out.
- The grouped/ungrouped Go `type` split is by literal `(` child, not spec count, as required.
- Python's `unwrapDecorated`, in-body docstring (no `start` adjustment), and `StripLineComment(text,
  "")` reuse for PEP 257 normalization.
- C#'s closed member-emission set, the two-level namespace/type descent, `arrow_expression_clause`
  vs `block`/`declaration_list` `SigEnd` flag, and the `Header`-checks-before-XML-strip ordering
  (verified against the literal `<auto-generated` tag text it must still match).
- `TOCFile`/`TOCDir`'s language resolution, `ErrLanguageUnsupported` wrapping via `StrategyFor`, the
  `Error`/`Partial` mutual exclusion, and that `applyDocSentences` never touches `Start`/`SigEnd`/`End`.
- Facade: all twelve delegating functions, twenty type aliases, and eight sentinels in
  `facade_test.go` — counted by hand against the file, all three prose counts (twelve/twenty/eight)
  are accurate.
- CLI: `classifyTOCError`'s `errors.Is`+`TOCImplemented()` branch, `runPathBatch`'s exact mirroring of
  `runBatch`, and `tocDirEntries`' index-zip re-injection of `path` after the `structToFields`
  re-marshal — the one place a subtle off-by-one would silently corrupt `files[]` in batch mode.
- The `--doc-sentences` four-tier chain (`resolveDocSentences`/`resolveTOCConfigPath`/
  `loadTOCConfig`/`parseDocSentences`), including the no-upward-search invariant and its test.
- Guard tests: `layering_test.go`/`seam_enforcement_test.go` both correctly raised to
  `minPackageDirs = 8` with the two different rationales the plan requires them to state
  independently; both files' package enumerations and comments are current.
- `mill-config.yaml`'s `pipeline.done_gate: go test ./...` (card 53) and no lint command appended.
- Ran the card 54 sweep greps myself: only the two authorized `docs/scout-vs-grep.md` hits and the
  legitimately-contrastive "LSP-backed" mentions in README/cli.go remain; no stale
  `five-package`/`four verbs`/count-drift hits anywhere in source. No "per-symbol" batch-entry prose
  remains.
- `registry/registry.go` and `registry/detect.go` carry no extension-map duplication — the map has
  exactly one definition, in `extension.go`, as required.

No out-of-plan files, no reimplemented shared helper, no cross-batch signature mismatch found. Every
batch's external interface (treesitter → toc-scaffolding → go-strategy → python/csharp →
toc-entry-points → facade/cli → doc-sentences-config → docs-and-sweep) is consumed exactly as each
batch's own "external interface" section specifies.

## Findings

None.

## Verdict

APPROVE
Faithful, fully cross-checked realization of the plan across all eight batches; no blocking issues found.
MILL_REVIEW_END
