// doc.go declares the sub fixture package. This header is deliberately NOT prefixed "Package sub"
// immediately above the package clause, so it is a file header, not package documentation, and
// this directory's Doc must come back empty.
package sub
