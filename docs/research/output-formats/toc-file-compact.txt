$ quarry toc file --compact internal/logger/logger.go

# internal/logger/logger.go (package logger): logger.go provides a thin log/slog wrapper for lyx: a package-level level threshold, an injectable io.Writer stderr…
155-163: func stderrHandlerSnapshot() slog.Handler -- stderrHandlerSnapshot returns the current stderr-half handler under a narrow critical section; caller must not hold…
173-195: type dualHandler struct -- dualHandler is the composite slog.Handler discussion.md's dual-handler-fan-out decision requires: Enabled is the OR of…
197-201: func newDualHandler() dualHandler -- newDualHandler builds the root dualHandler with no accumulated WithAttrs/WithGroup transform.
203-208: func (d dualHandler) stderr() slog.Handler -- stderr resolves this dualHandler's stderr-half handler by reading the current package-level inner handler and applying…
210-214: func (d dualHandler) Enabled(ctx context.Context, level slog.Level) bool -- Enabled implements slog.Handler as the OR of the stderr and durable halves' own Enabled -- see the dualHandler doc…
216-239: func (d dualHandler) Handle(ctx context.Context, record slog.Record) error -- Handle implements slog.Handler by independently re-checking each half's own Enabled and delegating only to the halves…
241-250: func (d dualHandler) WithAttrs(attrs []slog.Attr) slog.Handler -- WithAttrs implements slog.Handler by extending both halves' accumulated transform with the same attrs, so a logger…
252-260: func (d dualHandler) WithGroup(name string) slog.Handler -- WithGroup implements slog.Handler by extending both halves' accumulated transform with the same group, mirroring…
262-267: type durableWriter struct -- durableWriter adapts the durable sink's ensureDurableSink/writeDurable pair (sink.go) to an io.Writer so durableHandler…
269-279: func (durableWriter) Write(p []byte) (int, error) -- Write opens the durable sink on first use (a no-op on every call after the first, via sinkOnce) and writes p to it.
281-288: type durableHandler struct -- durableHandler is dualHandler's durable-sink half.
290-294: func newDurableHandler() durableHandler -- newDurableHandler builds the durable half over durableWriter, formatting exactly as slog.NewTextHandler would.
296-300: func (d durableHandler) Enabled(_ context.Context, level slog.Level) bool -- Enabled reports whether level is Info or above, unconditionally of levelVar -- see the durableHandler doc comment.
302-306: func (d durableHandler) Handle(ctx context.Context, record slog.Record) error -- Handle formats and writes record via the wrapped text handler, which in turn writes through durableWriter into the…
308-314: func (d durableHandler) withAttrs(attrs []slog.Attr) durableHandler -- withAttrs returns a copy of d with attrs bound into the wrapped handler.
316-320: func (d durableHandler) withGroup(name string) durableHandler -- withGroup returns a copy of d with name bound into the wrapped handler, mirroring withAttrs.
322-325: func init()
327-349: func configureFromEnv() -- configureFromEnv applies LYX_LOG_LEVEL and LYX_LOG_FILE, if set, on top of the just-initialised Warn/stderr defaults.
351-360: func Debug(msg string, args ...any) -- Debug logs msg at debug level with the given key/value args, stamping a trace key with TraceID()'s current value (batch…
362-367: func Info(msg string, args ...any) -- Info logs msg at info level with the given key/value args, stamping trace= as Debug does.
369-374: func Warn(msg string, args ...any) -- Warn logs msg at warn level with the given key/value args, stamping trace= as Debug does.
376-388: func SetVerbosity(count int) -- SetVerbosity maps a -v repeat count to a log level: count<=0 keeps the default Warn threshold (silent normal run),…
390-413: func SetOutput(w io.Writer) -- SetOutput rebinds the stderr half of the log sink to w and rebuilds the composite handler's stderr-half inner handler…
